# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Banco SQLite (dados locais — nunca versionar)
*.db
*.db-wal
*.db-shm

# Configuração com segredos (chave OpenAI etc.)
.env

# Pasta de dados local (CAS + banco)
data/
