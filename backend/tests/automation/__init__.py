"""Suíte de testes das automações de arquivo (Fase 6).

Scaffold Wave 0 (RED): os arquivos de teste aqui importam de `app.automation.*`
ainda inexistentes (criados nas waves de lógica seguintes). `pytest.importorskip`
no topo de cada módulo evita ImportError fatal na COLETA — os testes ficam
pendentes/skipped até a wave correspondente, então passam a falhar/passar de
verdade. Provê o alvo Nyquist das waves 2–4.
"""
