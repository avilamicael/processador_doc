---
phase: 04-templates-sub-templates-e-classificacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/models/template.py
  - backend/app/models/classification.py
  - backend/app/models/document.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/0004_templates_classification.py
  - backend/tests/test_migrations.py
  - backend/tests/classification/__init__.py
  - backend/tests/classification/conftest.py
  - backend/tests/validation/__init__.py
autonomous: true
requirements: [TPL-01, EXT-04]
must_haves:
  truths:
    - "python-dateutil consta nas dependencies do pyproject.toml (build reprodutível)"
    - "Existem 4 tabelas novas (templates, template_fields, classification_results, filled_fields) criadas pela migração 0004"
    - "ClassificationResult tem UNIQUE(document_id) = 1 classificação por bloco (rede contra double-charge)"
    - "Os novos tunables de classificação são lidos da config sem deploy"
  artifacts:
    - path: "backend/app/models/template.py"
      provides: "Modelos Template + TemplateField (campos tipados + sinais + validações)"
      contains: "class Template"
    - path: "backend/app/models/classification.py"
      provides: "Modelos ClassificationResult + FilledField (resultado por documento×template)"
      contains: "class ClassificationResult"
    - path: "backend/alembic/versions/0004_templates_classification.py"
      provides: "Migração 0004 que cria as 4 tabelas"
      contains: "down_revision"
    - path: "backend/app/config.py"
      provides: "Tunables de limiar de classificação + modelo OpenAI de classify"
      contains: "classify"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "Template, TemplateField, ClassificationResult, FilledField"
      via: "import + __all__"
      pattern: "Template|ClassificationResult"
    - from: "backend/alembic/versions/0004_templates_classification.py"
      to: "0003"
      via: "down_revision"
      pattern: "down_revision.*0003"
---

<objective>
Fundação da Fase 4: adicionar a dependência ausente, declarar os tunables de classificação, criar os 4 modelos novos (template/campo/resultado/campo-preenchido) e a migração 0004 que materializa as tabelas, e preparar os diretórios de teste (Wave 0) que os planos seguintes vão preencher.

Purpose: Todos os planos seguintes (validação, classificação, stage, API, frontend) dependem destas tabelas e tunables existirem. Esta é a base goal-backward de TPL-01 (declarar campos/tipos/validações/sinais persistidos) e a rede de idempotência de EXT-04 (UNIQUE(document_id) no ClassificationResult contra double-charge).
Output: pyproject.toml com python-dateutil; config.py estendido; models/template.py + models/classification.py; document.py com relationships reversos; models/__init__.py registrando os 4 modelos; alembic 0004; scaffolds de teste vazios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-CONTEXT.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md

<interfaces>
<!-- Forma exata a espelhar. Extraída de backend/app/models/extraction.py. -->

Modelo análogo (models/extraction.py linhas 29-56) — Mapped/mapped_column, FK ondelete CASCADE, UNIQUE para idempotência, relationship back_populates, created_at server_default=func.now():
- `id: Mapped[int] = mapped_column(primary_key=True)`
- `document_id: Mapped[int] = mapped_column(ForeignKey("documents.id", ondelete="CASCADE"), index=True, unique=True, nullable=False)`
- `fields_json: Mapped[str] = mapped_column(Text, nullable=False)`
- `created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)`
- `document: Mapped["Document"] = relationship(back_populates="extraction")`

Em document.py (linhas 92-104) o reverso 1:1 é:
- `extraction: Mapped["Extraction | None"] = relationship(back_populates="document", uselist=False, cascade="all, delete-orphan")`

Migração análoga (alembic/versions/0003_extractions.py): `revision='0003'`, `down_revision='0002'`, `op.create_table(...)` + `with op.batch_alter_table(...) as batch_op: batch_op.create_index(..., unique=True)`. A 0003 NÃO recria o trigger `trg_documents_updated_at` porque não toca `documents`.

Config análogo (config.py bloco `openai_extract_*`): `Field(default=..., validation_alias=AliasChoices("OPENAI_EXTRACT_MODEL", "openai_extract_model"))`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar python-dateutil + tunables de classificação na config</name>
  <files>backend/pyproject.toml, backend/app/config.py</files>
  <read_first>
    - backend/pyproject.toml (seção [project] dependencies — onde openai/pydantic/sqlalchemy estão fixados)
    - backend/app/config.py (bloco `openai_extract_*`, ~linhas 88-132 — padrão Field + AliasChoices a espelhar; e o bloco `queue_*`)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md (Standard Stack: python-dateutil 2.9.0.post0; "Live service config": limiar global + openai_classify_model)
  </read_first>
  <action>
    Rodar `cd backend && uv add python-dateutil==2.9.0.post0` (adiciona às dependencies do pyproject.toml — está instalado no ambiente mas ausente do arquivo; necessário para build reprodutível no cliente — D-11 normalização de data). Em backend/app/config.py adicionar ao `Settings` os tunables, espelhando EXATAMENTE o padrão `Field(default=..., validation_alias=AliasChoices(...))` do bloco `openai_extract_*`:
    - `classify_match_threshold: float` (default 0.5, alias `CLASSIFY_MATCH_THRESHOLD`) — limiar GLOBAL de confiança do matcher local (discretion D-03; por-template é v2/INT2-05). Acima → casa custo-zero; zona cinzenta → IA desempata; zero sinais → quarentena.
    - `openai_classify_model: str` (default = o mesmo valor de `openai_extract_model`, alias `OPENAI_CLASSIFY_MODEL`) — modelo das chamadas pagas de classify (D-01/D-06). Modelos giram rápido (CLAUDE.md) → tunável por env, default reusa o de extract.
    - `openai_classify_temperature: float` e `openai_classify_max_output_tokens: int` espelhando os equivalentes de extract.
    Documentar cada um com docstring/comentário curto como os tunables existentes. NÃO criar novo SecretStr (a chave OPENAI_API_KEY já existe).
  </action>
  <verify>
    <automated>cd backend && grep -q "python-dateutil" pyproject.toml && python -c "from app.config import get_settings; s=get_settings(); print(s.classify_match_threshold, s.openai_classify_model)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^# ' não aplicável`; `grep "python-dateutil" backend/pyproject.toml` retorna a linha de dependência com `2.9.0.post0`.
    - `python -c "from app.config import get_settings; s=get_settings(); assert 0.0 <= s.classify_match_threshold <= 1.0; assert s.openai_classify_model"` sai sem erro.
    - Definir `CLASSIFY_MATCH_THRESHOLD=0.7` no ambiente e reler a config reflete 0.7 (env override funciona como nos `openai_extract_*`).
  </acceptance_criteria>
  <done>python-dateutil consta no pyproject.toml; 4 tunables de classify lidos da config com defaults sensatos e override por env.</done>
</task>

<task type="auto">
  <name>Task 2: Criar os 4 modelos novos + relationships reversos + registro</name>
  <files>backend/app/models/template.py, backend/app/models/classification.py, backend/app/models/document.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/app/models/extraction.py (forma exata a espelhar: Mapped/mapped_column, FK CASCADE, UNIQUE, relationship back_populates, created_at)
    - backend/app/models/usage.py (modelo com FK não-única — análogo para FilledField que é 1:N por classification_result)
    - backend/app/models/document.py (linhas ~92-104: relationship reverso 1:1 `extraction`, a espelhar para classification_result)
    - backend/app/models/__init__.py (imports + __all__ — registrar os novos modelos senão autogenerate/test_migrations não os veem)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md (seção "models/template.py + classification.py": sinais D-02 e validações D-09 como JSON Text no TemplateField; ClassificationResult UNIQUE(document_id) + FK nullable template_id)
  </read_first>
  <action>
    Criar backend/app/models/template.py com `Template` (tabela `templates`: id, name str unique, doc_type str nullable, signals_json Text — lista de termos/chaves dos sinais identificadores D-02 serializada, created_at, updated_at) e `TemplateField` (tabela `template_fields`: id, template_id FK→templates ondelete CASCADE index, name str, field_type str default "texto" — conjunto texto/numero/data/moeda/cpf_cnpj/booleano D-08, required bool default False, regex str nullable D-09, hint str nullable). `Template.fields: Mapped[list["TemplateField"]] = relationship(back_populates="template", cascade="all, delete-orphan")`.
    Criar backend/app/models/classification.py com `ClassificationResult` (tabela `classification_results`: id, document_id FK→documents ondelete CASCADE **index + unique** — Pitfall 2 rede contra double-charge; template_id FK→templates ondelete SET NULL **nullable** — null = quarentena/não-casou D-03; confidence Float nullable; created_at) e `FilledField` (tabela `filled_fields`: id, classification_result_id FK→classification_results ondelete CASCADE index, field_name str, raw_value Text nullable D-11 bruto, normalized_value Text nullable D-11 normalizado, valid bool D-10 marca, invalid_reason str nullable). `ClassificationResult.filled_fields: relationship(back_populates="classification_result", cascade="all, delete-orphan")`.
    Em document.py adicionar o reverso `classification: Mapped["ClassificationResult | None"] = relationship(back_populates="document", uselist=False, cascade="all, delete-orphan")` espelhando o `extraction` existente (NÃO criar relação reversa de Template→Document — template não pertence a um documento).
    Registrar Template, TemplateField, ClassificationResult, FilledField em models/__init__.py (import + __all__). NÃO usar create_all — schema só via Alembic (D-10).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.models import Template, TemplateField, ClassificationResult, FilledField; from app.models.document import Document; assert ClassificationResult.__table__.c.document_id.unique; assert ClassificationResult.__table__.c.template_id.nullable; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `ClassificationResult.__table__.c.document_id.unique is True` (idempotência Pitfall 2).
    - `ClassificationResult.__table__.c.template_id.nullable is True` (null = quarentena, D-03).
    - `TemplateField.__table__.c.field_type` existe e o default é "texto" (D-08); `TemplateField.__table__.c.regex.nullable is True` (D-09 opcional).
    - `FilledField` tem colunas `raw_value`, `normalized_value`, `valid`, `invalid_reason` (D-10/D-11).
    - Os 4 nomes estão em `app.models.__all__`.
  </acceptance_criteria>
  <done>4 modelos importáveis via app.models; UNIQUE(document_id) e template_id nullable corretos; reverso `Document.classification` presente; tudo no __all__.</done>
</task>

<task type="auto">
  <name>Task 3: Migração 0004 + scaffolds de teste (Wave 0)</name>
  <files>backend/alembic/versions/0004_templates_classification.py, backend/tests/test_migrations.py, backend/tests/classification/__init__.py, backend/tests/classification/conftest.py, backend/tests/validation/__init__.py</files>
  <read_first>
    - backend/alembic/versions/0003_extractions.py (forma exata: revision/down_revision, create_table, batch_alter_table create_index unique; e o caveat de NÃO recriar trg_documents_updated_at quando não toca documents)
    - backend/tests/test_migrations.py (estender: asserir que 0004 cria as 4 tabelas)
    - backend/tests/extraction/conftest.py (cabeçalho + fixtures mock_openai/openai_success_payload/openai_refusal_payload — análogo a espelhar em tests/classification/conftest.py)
    - backend/tests/conftest.py (schema_engine / sqlite_url / engine — infra de DB em teste)
  </read_first>
  <action>
    Criar backend/alembic/versions/0004_templates_classification.py com `revision='0004'`, `down_revision='0003'`. No `upgrade()`: `op.create_table` para as 4 tabelas (templates, template_fields, classification_results, filled_fields) com as mesmas colunas/FKs dos modelos da Task 2; criar o índice UNIQUE de `classification_results.document_id` via `with op.batch_alter_table('classification_results') as batch_op: batch_op.create_index(batch_op.f('ix_classification_results_document_id'), ['document_id'], unique=True)` (espelha 0003 linhas ~33-55) e o índice UNIQUE de `templates.name`. `downgrade()` faz `op.drop_table` na ordem inversa (filhas antes das pais). NÃO recriar o trigger `trg_documents_updated_at` (a 0004 não toca `documents` — mesmo caveat resolvido da 0003).
    Estender backend/tests/test_migrations.py com um teste que aplica todas as migrações (até head/0004) e asserta que as 4 tabelas existem no schema resultante (inspector ou metadata), seguindo o padrão já presente no arquivo para 0003.
    Criar os scaffolds Wave 0 VAZIOS (apenas estrutura, sem testes reais ainda — os planos seguintes os preenchem):
    - backend/tests/classification/__init__.py (vazio)
    - backend/tests/validation/__init__.py (vazio)
    - backend/tests/classification/conftest.py: fixtures respx para classify, ESPELHANDO tests/extraction/conftest.py — `mock_openai_classify` interceptando `POST /v1/responses` e devolvendo payloads de desempate (matched_template_id/confidence/reason) e de campos faltantes (fields list-of-pairs); chave fictícia "sk-test"; reusar/importar os helpers de tests/extraction/conftest.py onde fizer sentido (sem gastar token).
    Confirmar que a config do pytest descobre os novos diretórios (`testpaths=["tests"]` já cobre).
  </action>
  <verify>
    <automated>cd backend && uv run alembic upgrade head && uv run pytest tests/test_migrations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` aplica 0004 sem erro; `alembic current` reporta 0004 (ou head apontando 0004).
    - O teste de migração em test_migrations.py asserta presença de `templates`, `template_fields`, `classification_results`, `filled_fields` e passa.
    - `grep "down_revision" backend/alembic/versions/0004_templates_classification.py` mostra `'0003'`.
    - Os arquivos `tests/classification/conftest.py`, `tests/classification/__init__.py`, `tests/validation/__init__.py` existem; `uv run pytest tests/classification tests/validation -q` coleta 0 testes sem erro de import.
  </acceptance_criteria>
  <done>Migração 0004 aplica e cria as 4 tabelas com UNIQUE(document_id); test_migrations verde; conftest de classify e diretórios de teste prontos para os planos seguintes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency install (uv add) | Pacote externo entra no build do cliente |
| migração de schema | Altera o banco do cliente (preservar dados existentes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | migração 0004 toca dados existentes | mitigate | 0004 só CRIA tabelas novas (como 0003); não altera `documents`/`extractions`; downgrade dropa só as 4 tabelas novas; sem perda de dado |
| T-04-02 | DoS / Integrity | ausência de UNIQUE permite re-cobrança | mitigate | `ClassificationResult.document_id` UNIQUE é a rede de banco contra double-charge (consumida pelo stage no Plan 05) |
| T-04-SC | Tampering | `uv add python-dateutil` | mitigate | Pacote canônico do PyPI (~20 anos, top-10 downloads, prescrito em CLAUDE.md, já instalado no ambiente). Audit em 04-RESEARCH.md "Package Legitimacy Audit" = Approved. Versão fixada `==2.9.0.post0` |
</threat_model>

<verification>
- `cd backend && uv run alembic upgrade head` aplica 0004.
- `cd backend && uv run pytest tests/test_migrations.py tests/test_config.py -q` verde.
- `cd backend && python -c "from app.models import Template, ClassificationResult"` importa sem erro.
</verification>

<success_criteria>
- python-dateutil 2.9.0.post0 nas dependencies do pyproject.toml.
- 4 modelos novos importáveis e registrados; ClassificationResult com UNIQUE(document_id), template_id nullable.
- Migração 0004 (down_revision 0003) cria as 4 tabelas e passa em test_migrations.
- Tunables `classify_match_threshold` + `openai_classify_*` lidos da config.
- Diretórios e conftest de teste (classification/validation) prontos para Wave 0.
</success_criteria>

<output>
Create `.planning/phases/04-templates-sub-templates-e-classifica-o/04-01-SUMMARY.md` when done
</output>
