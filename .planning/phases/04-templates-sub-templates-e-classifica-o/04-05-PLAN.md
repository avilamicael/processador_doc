---
phase: 04-templates-sub-templates-e-classificacao
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - backend/app/classification/stage.py
  - backend/app/queue/worker.py
  - backend/tests/classification/test_stage.py
  - backend/tests/queue/test_worker.py
autonomous: true
requirements: [TPL-03, TPL-04, EXT-04]
must_haves:
  truths:
    - "classify_stage casa o documento, mapeia/valida/normaliza os campos e persiste num único commit"
    - "Nenhum template casa → o documento vai para QUARENTENA via transition (não some)"
    - "Documento que casa fica PROCESSANDO com last_completed_step='classificado' vinculado ao template (nunca CONCLUIDO)"
    - "Re-executar o stage NÃO faz nova chamada paga à IA (idempotência por ClassificationResult existente)"
    - "step='classify' é enfileirado após 'extraido' e despachado pelo worker; sweep cobre legados"
    - "Cada chamada paga grava Usage(step='classify') no mesmo commit atômico"
  artifacts:
    - path: "backend/app/classification/stage.py"
      provides: "classify_stage async idempotente atômico (espelha extract_stage)"
      contains: "async def classify_stage"
    - path: "backend/app/queue/worker.py"
      provides: "dispatch step=classify + enqueue_pending_classifications sweep"
      contains: "CLASSIFY_STEP"
  key_links:
    - from: "backend/app/classification/stage.py"
      to: "ClassificationResult (UNIQUE document_id)"
      via: "checagem ANTES de chamada paga"
      pattern: "ClassificationResult"
    - from: "backend/app/queue/worker.py"
      to: "classify_stage"
      via: "await no _dispatch"
      pattern: "await classify_stage"
    - from: "backend/app/classification/stage.py"
      to: "QUARENTENA"
      via: "transition"
      pattern: "transition.*QUARENTENA"
---

<objective>
O coração da Fase 4: `classify_stage` async idempotente atômico (espelha `extract_stage`) que compõe matcher + (IA desempate) + filler + (IA faltantes) + validação/normalização num único commit, manda não-casados para QUARENTENA; e a fiação na fila (novo `step="classify"` no worker + sweep idempotente para legados).

Purpose: Materializa TPL-03 (classificação híbrida), TPL-04 (quarentena), EXT-04 (preenchimento+validação) e USE-02 (Usage step=classify) num pipeline end-to-end, com a rede dura contra double-charge (checar ClassificationResult ANTES de qualquer chamada paga — risco #1 da pesquisa).
Output: classification/stage.py + worker.py estendido + testes de stage (casa/quarentena/idempotência/desempate/faltantes/usage) e de worker (step classify + sweep).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-CONTEXT.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md

<interfaces>
<!-- Padrões exatos a espelhar (já lidos no repositório). -->

extract_stage (extraction/stage.py 74-185) — forma e garantias:
- `async def extract_stage(session: Session, *, content_hash: str) -> ExtractStageResult`
- (1) `doc = session.scalar(select(Document).where(Document.content_hash == content_hash))`; None → raise ValueError
- (2) IDEMPOTÊNCIA: `existing = session.scalar(select(Extraction).where(Extraction.document_id == doc.id))`; if existing → `return ...(called_ai=False)` SEM chamar IA
- (6) PERSISTÊNCIA ATÔMICA: `session.add(...)` + `session.add(Usage(...step=USAGE_STEP...))` + `doc.last_completed_step = EXTRACTED_STEP` + UM `session.commit()`
- EXTRACTED_STEP="extraido"; USAGE_STEP="extract"; marcador em memória (NÃO mark_step, NÃO transition PROCESSANDO→PROCESSANDO)

worker.py — já tem: INGEST_STEP/EXTRACT_STEP; `_dispatch(engine, *, step, original_hash, payload)` (extract → `with get_session: await extract_stage(session, content_hash=original_hash)`; ingest → to_thread); `_fail_for_step` (extract → `_fail_document_for_content_hash`); `enqueue_pending_extractions(session)` (Documents PROCESSANDO + last_completed_step==AWAITING_EXTRACTION_STEP sem Extraction → enqueue (content_hash, "extract")); chamado no `run_worker` startup. AuthenticationError → dead-letter imediato.

states.py linha 28: aresta PROCESSANDO→QUARENTENA JÁ na allowlist. `transition(session, doc, DocState.QUARENTENA)`.

repo.enqueue(session, *, original_hash, step, payload) — aceita step arbitrário (NÃO mudar repo.py); UNIQUE(content_hash, step) → no-op se já existe.

Do Plan 02/03: validation.fields.validate_field; classification.matcher.match_templates + política de limiar; classification.filler.map_fields; classification.openai_client.disambiguate/fill_missing_fields; classification.schema.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify_stage async idempotente atômico</name>
  <files>backend/app/classification/stage.py, backend/tests/classification/test_stage.py</files>
  <read_first>
    - backend/app/extraction/stage.py (ARQUIVO INTEIRO, 1-191 — forma/garantias a espelhar 1-para-1)
    - backend/app/pipeline/state_machine.py (linhas 24-63 — transition) + backend/app/pipeline/states.py (linha 28 — aresta PROCESSANDO→QUARENTENA)
    - backend/app/models/classification.py, template.py, extraction.py, usage.py (do Plan 01)
    - backend/app/classification/matcher.py, filler.py, openai_client.py, schema.py (Plan 03)
    - backend/app/validation/fields.py (Plan 02 — validate_field)
    - backend/app/config.py (classify_match_threshold)
    - backend/tests/classification/conftest.py (mock respx de classify — Plan 01)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md (diagrama linhas 163-175 — fluxo do stage; Pitfall 2 double-charge; Open Question 1 stage atômico)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md (Shared Pattern "Stage atômico idempotente" + "Quarentena via state machine")
  </read_first>
  <behavior>
    - casa: doc com sinais fortes p/ um template → ClassificationResult(template_id setado, confidence), FilledFields preenchidos/validados, last_completed_step="classificado", state PROCESSANDO; sem chamada IA (matcher resolveu).
    - quarentena: nenhum template casa → transition(QUARENTENA); ClassificationResult com template_id=None persistido (motivo para Fase 5).
    - idempotência: 2ª execução com ClassificationResult já existente → no-op, called_ai=False, ZERO novas chamadas respx (call_count inalterado).
    - desempate: zona cinzenta → disambiguate() (respx) decide; Usage(step="classify") gravado.
    - faltantes: campos obrigatórios sem par → fill_missing_fields() (respx) só p/ faltantes; Usage gravado.
    - usage: cada chamada paga adiciona exatamente 1 Usage(step="classify") no commit.
    - campo inválido (DV CNPJ falho/data não parseável): FilledField.valid=False + invalid_reason; documento SEGUE (D-10, não bloqueia, não quarentena).
  </behavior>
  <action>
    Criar backend/app/classification/stage.py espelhando extraction/stage.py em forma e garantias. `async def classify_stage(session, *, content_hash) -> ClassifyStageResult` (dataclass: matched: bool, template_id: int|None, called_ai: bool). Fluxo (04-RESEARCH.md linhas 163-175):
    1. Localiza Document por content_hash (None → raise ValueError).
    2. IDEMPOTÊNCIA (Pitfall 2): `existing = session.scalar(select(ClassificationResult).where(...document_id==doc.id))`; existe → `return ClassifyStageResult(matched=existing.template_id is not None, template_id=existing.template_id, called_ai=False)` ANTES de QUALQUER chamada paga.
    3. Lê a `Extraction` do bloco (fields_json/full_text/doc_type_guess) — INTACTA (D-07). Sem Extraction → tratar como erro de orquestração (raise) ou quarentena (decidir; preferir raise para o worker re-tentar, alinhado ao extract_stage).
    4. Carrega templates (session.scalars(select(Template)...) com fields). `matcher.match_templates(...)` → confianças.
    5. Política (config `classify_match_threshold`): top ≥ limiar e sem ambiguidade → casa direto (called_ai permanece False). Ambíguo/zona cinzenta → `await openai_client.disambiguate(...)` (PAGO) → matched_template_id. Nenhum sinal/abaixo do piso → quarentena.
    6. Nenhum casa → `transition(session, doc, DocState.QUARENTENA)`; persistir ClassificationResult(template_id=None, confidence) + Usage (se houve desempate) num commit; NÃO avançar marcador "classificado". (transition já comita o estado — garantir que o ClassificationResult/Usage estejam no mesmo escopo de sessão; se transition comitar antes, adicionar o ClassificationResult na MESMA sessão e cuidar para não deixar parcial — seguir o padrão de state_machine.transition e ajustar a ordem para manter consistência.)
    7. Casou → `filler.map_fields(...)`; se `missing_required` não vazio → `await openai_client.fill_missing_fields(...)` (PAGO) só p/ faltantes; mesclar.
    8. Para cada campo do template: `validate_field(field_type=..., raw=..., required=..., regex=...)` (Plan 02) → FilledField(raw_value, normalized_value, valid, invalid_reason) (D-10/D-11).
    9. COMMIT ATÔMICO ÚNICO: ClassificationResult(template_id, confidence) + FilledFields + Usage(step="classify") por chamada paga + `doc.last_completed_step = CLASSIFIED_STEP` EM MEMÓRIA. NUNCA mark_step (comita sozinho), NUNCA transition(PROCESSANDO→PROCESSANDO). state permanece PROCESSANDO; NUNCA CONCLUIDO (D-04).
    Constantes: `CLASSIFIED_STEP="classificado"`, `USAGE_STEP="classify"`. Recusa/erro PROPAGAM ao worker (sem try/catch no stage). Logar só metadados (document_id/template_id/route — V7/V8), nunca full_text/fields/chave.
    Criar backend/tests/classification/test_stage.py cobrindo os 7 comportamentos acima (usar o mock respx de classify; provar call_count para idempotência e contagem de Usage).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/classification/test_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/classification/test_stage.py -k idempot` prova que a 2ª execução NÃO incrementa o call_count do respx (não re-cobra) e retorna called_ai=False.
    - `pytest -k quarentena` prova doc.state==QUARENTENA via transition (grep `transition` + `QUARENTENA` no stage) e ClassificationResult com template_id=None persistido.
    - `pytest -k desempate` e `-k faltantes` provam as chamadas IA pagas só nos cenários certos; matcher resolve o caso "sinais fortes" sem IA (called_ai=False).
    - `pytest -k usage` prova exatamente 1 Usage(step="classify") por chamada paga; caso custo-zero → 0 Usage.
    - Caso "casou" termina com state PROCESSANDO + last_completed_step=="classificado" + template_id setado; NUNCA CONCLUIDO.
    - `grep -n "mark_step" backend/app/classification/stage.py` vazio; o stage tem exatamente um `session.commit()` no caminho de sucesso de casamento.
    - Campo inválido vira FilledField.valid=False sem mudar o estado para quarentena (D-10).
  </acceptance_criteria>
  <done>classify_stage espelha extract_stage: idempotente (sem double-charge), atômico, quarentena via transition, marcador "classificado" em memória, Usage(step=classify); test_stage verde em todos os cenários.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fiação na fila — step="classify" no worker + sweep de legados</name>
  <files>backend/app/queue/worker.py, backend/tests/queue/test_worker.py</files>
  <read_first>
    - backend/app/queue/worker.py (ARQUIVO INTEIRO — EXTRACT_STEP, _dispatch, _fail_for_step, enqueue_pending_extractions, run_worker startup; replicar para classify)
    - backend/app/classification/stage.py (Task 1 — classify_stage, CLASSIFIED_STEP) + extraction/stage.py (EXTRACTED_STEP="extraido")
    - backend/app/queue/repo.py (enqueue aceita step arbitrário — NÃO modificar; UNIQUE(content_hash, step))
    - backend/app/models/classification.py (ClassificationResult — gate do sweep)
    - backend/tests/queue/test_worker.py (estender com cenário classify + sweep)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md (Pitfall 4 não enfileirar dentro do stage; sweep idempotente no startup)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md (worker.py: ramo classify coroutine await NÃO to_thread; enqueue_pending_classifications)
  </read_first>
  <behavior>
    - Após um bloco ficar last_completed_step="extraido", um job (content_hash, "classify") é enfileirado (via gatilho pós-extract OU sweep) e o worker o despacha chamando classify_stage como coroutine (await direto, NÃO to_thread).
    - _fail_for_step roteia "classify" por content_hash (igual a extract).
    - enqueue_pending_classifications: Documents PROCESSANDO + last_completed_step=="extraido" SEM ClassificationResult → enfileira classify; idempotente (UNIQUE content_hash+step); cobre legados; rodar 2x não duplica.
    - O sweep roda no startup do run_worker (como enqueue_pending_extractions).
    - AuthenticationError no classify → dead-letter imediato (herdado do _run_once existente).
  </behavior>
  <action>
    Estender backend/app/queue/worker.py: adicionar `CLASSIFY_STEP = "classify"`. Em `_dispatch` adicionar `elif step == CLASSIFY_STEP:` com `from app.classification.stage import classify_stage` e `with get_session(engine) as session: await classify_stage(session, content_hash=original_hash)` (coroutine await direto — NUNCA to_thread, igual ao extract; Pitfall 1). Em `_fail_for_step` rotear `CLASSIFY_STEP` para `_fail_document_for_content_hash` (igual extract — Pitfall 2). Criar `enqueue_pending_classifications(session) -> int` espelhando `enqueue_pending_extractions`: seleciona Documents com `state==PROCESSANDO`, `last_completed_step==EXTRACTED_STEP` ("extraido") e SEM ClassificationResult (`~Document.content_hash.in_(select(Document.content_hash).join(ClassificationResult))`), enfileira `(content_hash, "classify")` via `repo.enqueue`; idempotente. Chamá-lo no `run_worker` startup logo após `enqueue_pending_extractions` (mesmo padrão de sweep único). Para o ENFILEIRAMENTO PÓS-EXTRACT por gatilho: NÃO enfileirar dentro do extract_stage (quebra o commit único — Pitfall 4); o sweep no startup + a re-execução do sweep cobrem o fluxo; (opcional, se necessário p/ latência) enfileirar fora do commit no worker após mark_done de um job extract — manter idempotente. `repo.py` NÃO muda.
    Estender backend/tests/queue/test_worker.py: cenário que enfileira/despacha classify (com classify_stage mockado ou via respx) e cenário do sweep cobrindo um Document legado "extraido" sem ClassificationResult (assert job classify criado, rodar 2x não duplica).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/queue/test_worker.py -k classify -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/queue/test_worker.py -k classify` verde: o worker despacha classify como coroutine (grep `await classify_stage` em worker.py) e NÃO usa to_thread para classify.
    - `enqueue_pending_classifications` existe, seleciona blocos "extraido" sem ClassificationResult e é idempotente (rodar 2x → mesmo nº de jobs); chamado no run_worker startup (grep no run_worker).
    - `_fail_for_step` roteia CLASSIFY_STEP por content_hash.
    - `grep -n "enqueue" backend/app/queue/repo.py` inalterado (nenhuma edição em repo.py).
    - `grep -n "to_thread" backend/app/queue/worker.py` não aparece no ramo classify do _dispatch.
  </acceptance_criteria>
  <done>worker despacha step=classify (coroutine await), FALHA roteada por content_hash, sweep idempotente enqueue_pending_classifications cobre legados, repo.py intacto; teste de worker verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extraction (conteúdo do doc) → matcher/IA | Conteúdo não confiável dirige classificação e prompt |
| fila → chamadas pagas OpenAI | Retry/crash pode disparar re-cobrança |
| chave OpenAI → openai_client | Segredo provisionado na Fase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | DoS / Custo (risco #1) | double-charge das 2 chamadas pagas (D-01 desempate + D-06 faltantes) | mitigate | Checar `ClassificationResult` (UNIQUE document_id) ANTES de qualquer chamada paga; stage atômico (commit único) — crash antes do commit = nada persistido; retry re-executa do zero, no-op se já completou (Pitfall 2 / Open Question 1) |
| T-04-14 | Integrity | setar document.state direto / auto-laço PROCESSANDO→PROCESSANDO | mitigate | Quarentena só via `transition` (aresta na allowlist); "classificado" avança só o marcador em memória + commit (NUNCA mark_step/transition) — espelha extract_stage |
| T-04-15 | DoS | enfileirar dentro do stage quebra o commit atômico | mitigate | NÃO enfileirar no stage (`repo.enqueue` comita por si — Pitfall 4); sweep idempotente no startup + UNIQUE(content_hash, step) |
| T-04-16 | Information Disclosure | vazar full_text/fields/chave em log do stage | mitigate | Logar só metadados (document_id/template_id/route); `get_secret_value()` só na criação do cliente (V7/V8 — disciplina da Fase 3) |
| T-04-17 | Tampering | injeção via conteúdo do doc no prompt de desempate/faltantes | mitigate | Structured Outputs (schema fixo); SYSTEM_INSTRUCTIONS fixas; recusa tratada |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/classification/test_stage.py tests/queue/test_worker.py -q` verde.
- `cd backend && uv run pytest` (suíte completa) verde após este plano.
- `cd backend && grep -n "await classify_stage" app/queue/worker.py` presente; `grep -n "ClassificationResult" app/classification/stage.py` presente (checagem prévia).
</verification>

<success_criteria>
- classify_stage idempotente (sem double-charge), atômico (commit único), quarentena via transition, marcador "classificado", Usage(step=classify).
- Campos preenchidos/validados/normalizados (bruto+normalizado, marca sem bloquear).
- worker despacha step=classify (coroutine) + sweep idempotente cobre legados; repo.py intacto.
- Pipeline ingest→extract→classify completo end-to-end; suíte de backend verde.
</success_criteria>

<output>
Create `.planning/phases/04-templates-sub-templates-e-classifica-o/04-05-SUMMARY.md` when done
</output>
