---
phase: 04-templates-sub-templates-e-classificacao
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/app/classification/__init__.py
  - backend/app/classification/schema.py
  - backend/app/classification/matcher.py
  - backend/app/classification/filler.py
  - backend/app/classification/openai_client.py
  - backend/tests/classification/test_matcher.py
  - backend/tests/classification/test_filler.py
autonomous: true
requirements: [TPL-03, EXT-04]
must_haves:
  truths:
    - "O matcher local pontua templates por sinais sobre fields_json/full_text/doc_type_guess (custo 0)"
    - "Acima do limiar global → casa direto; zona cinzenta → sinaliza necessidade de desempate por IA"
    - "Os pares já extraídos (Fase 3) são mapeados nos campos do template sem IA (D-05)"
    - "Os schemas da IA são list-of-pairs / nullable — nunca dict aberto com chaves variáveis"
  artifacts:
    - path: "backend/app/classification/matcher.py"
      provides: "match_templates(extraction, templates) → confiança por template (puro)"
      contains: "def match"
    - path: "backend/app/classification/filler.py"
      provides: "map_fields(template, extraction) → pares→campos + faltantes obrigatórios"
      contains: "def "
    - path: "backend/app/classification/schema.py"
      provides: "DisambiguationResult + MissingFieldsResult (Structured Outputs strict-safe)"
      contains: "class DisambiguationResult"
  key_links:
    - from: "backend/app/classification/schema.py"
      to: "ExtractedField (extraction.schema)"
      via: "reuso list-of-pairs"
      pattern: "ExtractedField|list\\["
    - from: "backend/app/classification/openai_client.py"
      to: "Responses API"
      via: "responses.parse text_format"
      pattern: "responses\\.parse|text_format"
---

<objective>
Blocos puros da classificação: o schema Structured Outputs (desempate D-01 + campos faltantes D-06), o matcher local por sinais (D-02, custo 0), o filler que mapeia pares extraídos → campos do template (D-05), e o cliente OpenAI das chamadas de classify (espelhando extraction/openai_client).

Purpose: São as peças que o classify_stage (Plan 05) compõe. Materializam o motor custo-zero de TPL-03 (regras locais resolvem a maioria sem IA) e a base de EXT-04 (mapeamento local + chamada dirigida só para faltantes). Mantêm list-of-pairs/nullable (Pitfall 1 strict mode).
Output: pacote classification/ com schema/matcher/filler/openai_client + testes de matcher e filler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-CONTEXT.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md

<interfaces>
<!-- Padrões exatos a espelhar. -->

extraction/schema.py (linhas 19-33) — list-of-pairs strict-safe:
- `class ExtractedField(BaseModel): key: str = Field(description=...); value: str = Field(...); confidence: float = Field(...)`

Schema de classify (04-RESEARCH.md Pattern 2):
- `DisambiguationResult`: `matched_template_id: int | None` (null = nenhum casa → quarentena) + `confidence: float` + `reason: str`
- `MissingFieldsResult`: `fields: list[ExtractedField]`

extraction/openai_client.py (linhas 68-133) — _client() lê SecretStr só na criação; _map_usage(response) mapeia input_tokens→prompt, output_tokens→completion; `await client.responses.parse(model=..., instructions=SYSTEM_INSTRUCTIONS, input=[...], text_format=<PydanticModel>, temperature=..., max_output_tokens=...)`; `output_parsed is None` → recusa (levanta).

extraction/router.py (linhas 22-42) — choose(blob): função pura de módulo, decide e retorna, sem efeitos. Estilo do matcher.

Extraction (Fase 3, INTACTA): `fields_json` (JSON list-of-pairs key/value/confidence), `full_text`, `doc_type_guess`, `doc_type_confidence`, `route`.
TemplateField: `name`, `field_type`, `required`, `regex`, `hint`. Template: `name`, `doc_type`, `signals_json` (lista de termos).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema de classify (list-of-pairs/nullable) + cliente OpenAI</name>
  <files>backend/app/classification/__init__.py, backend/app/classification/schema.py, backend/app/classification/openai_client.py</files>
  <read_first>
    - backend/app/extraction/schema.py (linhas 19-60 — ExtractedField/ExtractionResult, padrão list-of-pairs strict-safe a espelhar)
    - backend/app/extraction/openai_client.py (linhas 68-133 — _client SecretStr, _map_usage, responses.parse text_format, tratamento de recusa output_parsed is None)
    - backend/app/config.py (tunables openai_classify_* criados no Plan 01)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md (Pitfall 1 strict mode rejeita dict aberto; Pattern 2 schemas)
  </read_first>
  <behavior>
    - DisambiguationResult valida com matched_template_id=None (nenhum casa) e com int (casou) + confidence float + reason str.
    - MissingFieldsResult.fields é list[ExtractedField] (reuso do tipo da Fase 3).
    - As funções do cliente chamam `client.responses.parse(..., text_format=DisambiguationResult/MissingFieldsResult)` e mapeiam usage input→prompt/output→completion; recusa (output_parsed is None) levanta exceção (sem retry — backoff é da fila).
  </behavior>
  <action>
    Criar backend/app/classification/__init__.py. Criar backend/app/classification/schema.py com `DisambiguationResult` (matched_template_id: int | None nullable = quarentena; confidence: float; reason: str — metadado curto, não sensível) e `MissingFieldsResult` (fields: list[ExtractedField] — REUSAR `ExtractedField` de app.extraction.schema, NÃO redeclarar; NUNCA dict com chaves dinâmicas — Pitfall 1). `description` em cada Field guiando o modelo.
    Criar backend/app/classification/openai_client.py espelhando extraction/openai_client.py: `disambiguate(candidates_summary: str, doc_context: str) -> tuple[DisambiguationResult, ClassifyUsage]` e `fill_missing_fields(missing_field_specs: str, doc_text: str) -> tuple[MissingFieldsResult, ClassifyUsage]`. Usar `_client()` que lê `settings.openai_api_key.get_secret_value()` SÓ na criação (CFM 5 — segredo nunca logado), `responses.parse` com `model=settings.openai_classify_model`, `text_format` o schema correspondente, instructions FIXAS (sem few-shot do conteúdo). Mapear usage como `_map_usage`. `output_parsed is None` → levanta `ClassificationRefused` (análogo a ExtractionRefused). Logar SÓ metadados (nunca full_text/fields/chave — V7/V8). Reusar `ClassifyUsage` espelhando `ExtractionUsage` (prompt_tokens/completion_tokens).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.classification.schema import DisambiguationResult, MissingFieldsResult; d=DisambiguationResult(matched_template_id=None, confidence=0.0, reason='x'); print(d.matched_template_id)" && python -c "from app.classification import openai_client; print(hasattr(openai_client,'disambiguate'), hasattr(openai_client,'fill_missing_fields'))"</automated>
  </verify>
  <acceptance_criteria>
    - `DisambiguationResult(matched_template_id=None, confidence=0.0, reason='x')` valida (nullable).
    - `MissingFieldsResult` usa `ExtractedField` importado de app.extraction.schema (grep confirma o import; sem redeclaração de ExtractedField em classification/schema.py).
    - openai_client expõe `disambiguate` e `fill_missing_fields`; ambas usam `responses.parse` com `text_format` (grep confirma `text_format`).
    - `grep -n "get_secret_value" backend/app/classification/openai_client.py` aparece só no ponto de criação do cliente; nenhum log de `full_text`/`fields`/chave.
  </acceptance_criteria>
  <done>schema.py strict-safe (nullable/list-of-pairs, reuso ExtractedField); openai_client com disambiguate/fill_missing_fields espelhando a Fase 3, usage mapeado, recusa tratada, segredo não logado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Matcher local por sinais (D-02) + filler de campos (D-05)</name>
  <files>backend/app/classification/matcher.py, backend/app/classification/filler.py, backend/tests/classification/test_matcher.py, backend/tests/classification/test_filler.py</files>
  <read_first>
    - backend/app/extraction/router.py (linhas 22-42 — estilo função pura de módulo, sem classe/DB/IA)
    - backend/app/extraction/stage.py (linhas 188-191 — _fields_to_json, padrão de serialização list-of-pairs; e como `result.fields` é lido)
    - backend/app/models/extraction.py (fields_json/full_text/doc_type_guess — fonte do matcher e do filler)
    - backend/app/models/template.py (Template.signals_json, TemplateField — do Plan 01)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-RESEARCH.md (Pitfall 5 matcher sem critério; Open Question 2 formato do sinal = lista de termos, confiança = fração presente)
    - .planning/phases/04-templates-sub-templates-e-classifica-o/04-PATTERNS.md (matcher.py role-match: presença em fields_json.key OU full_text case-insensitive pontua)
  </read_first>
  <behavior>
    - match_templates: template com sinais ["linha digitável","cnpj","valor"] e extração contendo esses termos → confiança alta (fração presente ~1.0); termos ausentes → confiança baixa.
    - doc_type_guess que bate com Template.doc_type adiciona pontuação (atalho D-01).
    - Resultado é ordenável: maior confiança vence (D-03); empate/zona cinzenta sinalizado para desempate.
    - map_fields: para cada TemplateField, casa o par extraído (por nome/key, case-insensitive) → preenche; campos obrigatórios sem par viram lista de "faltantes" (D-06 input).
    - map_fields NÃO chama IA (custo 0) e não valida (validação é o Plan 02 / no stage).
  </behavior>
  <action>
    Criar backend/app/classification/matcher.py com `match_templates(*, fields_json: str, full_text: str, doc_type_guess: str, templates: list[Template]) -> list[TemplateMatch]` (dataclass `TemplateMatch(template_id, confidence)`), função PURA de módulo (estilo router.choose — sem DB/IA dentro; recebe os templates já carregados). Sinal (Open Question 2): parsear `Template.signals_json` como lista de termos; confiança = fração de termos presentes (case-insensitive) em alguma `key` de fields_json OU em full_text; somar bônus se `doc_type_guess` casar com `Template.doc_type`. Expor helper para a política de desempate: maior confiança ≥ `classify_match_threshold` → casa direto; nenhum sinal/abaixo do piso → não casa (quarentena); diferença pequena entre top-2 → "ambíguo" (precisa IA). NÃO embutir isso em extraction/router.choose (mata o seam D-03 — Anti-Pattern).
    Criar backend/app/classification/filler.py com `map_fields(*, template_fields: list[TemplateField], fields_json: str) -> FillResult` (dataclass com `filled: list[(field_name, raw_value)]` e `missing_required: list[field_name]`). Casa cada TemplateField com o par extraído por nome (case-insensitive, normalizando espaços/acentos de forma simples). Campos obrigatórios sem correspondência → `missing_required` (alimenta a chamada D-06 no stage). Sem IA, sem validação aqui.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/classification/test_matcher.py tests/classification/test_filler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/classification/test_matcher.py` passa: sinais presentes → confiança alta; ausentes → baixa; doc_type_guess casando soma pontos.
    - Há um teste que prova maior-confiança-vence e outro que prova zona-cinzenta marcada como ambígua (desempate por IA).
    - `pytest tests/classification/test_filler.py` passa: pares mapeados sem IA; obrigatório ausente vira missing_required.
    - `grep -rn "openai\|responses.parse\|session\|select(" backend/app/classification/matcher.py backend/app/classification/filler.py` vazio (puros — sem IA nem DB).
  </acceptance_criteria>
  <done>matcher pontua por sinais + doc_type_guess com política de limiar/desempate; filler mapeia pares→campos e lista faltantes obrigatórios; ambos puros; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full_text do documento → prompt da IA | Conteúdo do documento vai à OpenAI no desempate/faltantes |
| chave OpenAI → cliente | Segredo provisionado na Fase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | injeção via conteúdo do documento no prompt (desempate/faltantes) | mitigate | Structured Outputs limita a saída ao schema (DisambiguationResult/MissingFieldsResult); SYSTEM_INSTRUCTIONS fixas, sem few-shot do conteúdo; recusa (`output_parsed is None`) tratada como na Fase 3 |
| T-04-07 | Information Disclosure | vazamento de full_text/fields/chave em log | mitigate | openai_client e matcher/filler logam só metadados (template_id/doc_type_guess/route); `get_secret_value()` só na criação do cliente (V7/V8) |
| T-04-08 | DoS / Custo | matcher fraco joga tudo na IA | mitigate | confiança = fração de sinais presentes + limiar global; regras locais resolvem a maioria custo-zero (Pitfall 5) |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/classification/test_matcher.py tests/classification/test_filler.py -q` verde.
- `cd backend && python -c "from app.classification import schema, matcher, filler, openai_client"` importa sem erro.
- matcher/filler sem imports de openai/sqlalchemy.
</verification>

<success_criteria>
- schema.py strict-safe (nullable + list-of-pairs reusando ExtractedField).
- openai_client com disambiguate/fill_missing_fields espelhando a Fase 3 (usage, recusa, segredo seguro).
- matcher por sinais + doc_type_guess com política de limiar/desempate (custo-zero motor).
- filler mapeia pares→campos sem IA e lista faltantes obrigatórios.
- Testes de matcher e filler verdes.
</success_criteria>

<output>
Create `.planning/phases/04-templates-sub-templates-e-classifica-o/04-03-SUMMARY.md` when done
</output>
