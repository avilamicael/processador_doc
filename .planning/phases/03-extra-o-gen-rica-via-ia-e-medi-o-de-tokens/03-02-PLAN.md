---
phase: 03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/app/extraction/pdf_io.py
  - backend/app/extraction/router.py
  - backend/app/extraction/openai_client.py
  - backend/tests/extraction/test_pdf_io.py
  - backend/tests/extraction/test_router.py
  - backend/tests/extraction/test_openai_client.py
autonomous: true
requirements: [EXT-01, EXT-02, USE-02]
must_haves:
  truths:
    - "PDF com texto nativo é lido localmente sem custo de IA (PyMuPDF get_text)"
    - "A heurística escolhe native_text para PDF com texto e vision para escaneado/imagem"
    - "Blob de imagem (JPG/PNG) é detectado por magic bytes e roteado direto à visão (não tenta abrir como PDF)"
    - "openai_client chama Responses API, devolve ExtractionResult tipado e mapeia tokens input→prompt/output→completion"
    - "Recusa (output_parsed is None) levanta ExtractionRefused; a chave nunca é logada"
  artifacts:
    - path: "backend/app/extraction/pdf_io.py"
      provides: "PyMuPDF: get_text + heurística texto-vs-visão + render PNG + magic-byte detection"
      contains: "import fitz"
    - path: "backend/app/extraction/router.py"
      provides: "Seam de extração D-03 (choose: native_text|vision)"
      contains: "def choose"
    - path: "backend/app/extraction/openai_client.py"
      provides: "AsyncOpenAI wrapper: responses.parse + _unwrap + leitura de usage"
      contains: "AsyncOpenAI"
  key_links:
    - from: "backend/app/extraction/openai_client.py"
      to: "ExtractionResult (text_format)"
      via: "client.responses.parse(text_format=ExtractionResult)"
      pattern: "text_format=ExtractionResult"
    - from: "backend/app/extraction/openai_client.py"
      to: "openai_api_key"
      via: ".get_secret_value() só no ponto de criação"
      pattern: "get_secret_value"
---

<objective>
Implementar as três primitivas de extração atrás de interface (funções de módulo, sem DB nem HTTP): `pdf_io.py` (PyMuPDF — texto nativo, heurística texto-vs-visão, render página→PNG, detecção PDF-vs-imagem por magic bytes), `router.py` (o seam D-03 que decide o caminho — ponto de costura que Fases 4/7 estendem) e `openai_client.py` (wrapper `AsyncOpenAI` sobre a Responses API + Structured Outputs, `_unwrap` de recusa, leitura/mapeamento de tokens).

Purpose: Isolar a integração externa (OpenAI) e a leitura de PDF em módulos puros e testáveis com OpenAI mockado, prontos para o `extract_stage` (Plan 03) orquestrar. O `router.choose()` é o seam arquitetural mais importante da fase (D-03) — mantê-lo mínimo e plugável.
Output: `backend/app/extraction/{pdf_io,router,openai_client}.py` + testes unitários com respx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-CONTEXT.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-AI-SPEC.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-PATTERNS.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-RESEARCH.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-01-SUMMARY.md

<interfaces>
From Plan 03-01 (já existe):
- `app.extraction.schema.ExtractionResult` (fields: list[ExtractedField], full_text, doc_type_guess, doc_type_confidence) — é o `text_format`
- `app.config.get_settings()` → openai_api_key (SecretStr), openai_extract_model/temperature/max_output_tokens/image_detail/min_chars_per_page
- `tests/extraction/conftest.py` → fixture respx de AsyncOpenAI (sucesso + recusa), fixtures de PDF com/sem texto e imagem

Análogos de forma (funções de módulo atrás de interface, sem classe):
- backend/app/storage/cas.py:24 (funções de módulo) — copiar o estilo para pdf_io/router/openai_client
- backend/app/config.py:11,55 (segredo SecretStr; "nada loga nem retorna o valor")

Sintaxe Responses API (AI-SPEC §3 Entry Point Pattern — completa):
- `await client.responses.parse(model=..., instructions=SYSTEM_INSTRUCTIONS, input=[{role:"user", content:[...]}], text_format=ExtractionResult, temperature=..., max_output_tokens=...)`
- content texto: `{"type":"input_text","text": native_text}`
- content visão: `{"type":"input_image","image_url": "data:image/png;base64,...","detail": settings.openai_extract_image_detail}`
- `response.output_parsed` → ExtractionResult ou None (recusa); `response.usage.input_tokens`/`output_tokens`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pdf_io.py — texto nativo, heurística, render, magic bytes</name>
  <files>backend/app/extraction/pdf_io.py, backend/tests/extraction/test_pdf_io.py</files>
  <read_first>
    - PATTERNS.md §"backend/app/extraction/pdf_io.py" (esqueleto extract_text_and_decide; import fitz; magic bytes)
    - RESEARCH §Pattern 2 (heurística texto-vs-visão) e §Pitfall 5 (blob de imagem tratado como PDF)
    - backend/app/storage/cas.py:24,125 (estilo de funções de módulo; read_bytes que o stage usará)
    - tests/extraction/conftest.py (fixtures de PDF com/sem texto e imagem — de Plan 03-01)
  </read_first>
  <behavior>
    - detect_blob_type(b): `%PDF-` → "pdf"; `\xFF\xD8` → "jpeg"; `\x89PNG` → "png"; outro → erro/"unknown"
    - extract_text_and_decide(pdf_bytes, min_chars_per_page): PDF com texto → ("...texto...", "native_text"); PDF escaneado (sem texto) → (texto vazio/curto, "vision")
    - render_pages_png(pdf_bytes): devolve uma imagem PNG por página do bloco
    - imagem (jpeg/png) NÃO é aberta como PDF (não chama fitz.open(filetype="pdf") nela) — vai direto ao caminho visão (a imagem já é a página)
  </behavior>
  <action>
    Criar `backend/app/extraction/pdf_io.py` como módulo de funções puras (estilo cas.py), `import fitz`. Implementar: `detect_blob_type(blob: bytes) -> str` por magic bytes (`%PDF-`→pdf, `\xFF\xD8`→jpeg, `\x89PNG`→png) — resolve Open Question 2 / Pitfall 5 (CAS guarda só o hash, sem extensão); `extract_text_and_decide(pdf_bytes, min_chars_per_page) -> tuple[str, str]` que abre via `fitz.open(stream=pdf_bytes, filetype="pdf")`, soma o texto extraível por página (`page.get_text()`) e decide `"native_text"` se `total >= min_chars_per_page * page_count`, senão `"vision"` (EXT-01/D-04); `render_pages_png(pdf_bytes) -> list[bytes]` via `page.get_pixmap().tobytes("png")` (uma imagem por página, todas as páginas do bloco). Para blob de imagem, o caminho visão usa os próprios bytes como a única "página" (não abrir como PDF). Funções síncronas (CPU-bound) — o Plan 03 as envolve em `to_thread` de dentro do stage async. Criar `test_pdf_io.py` cobrindo o `<behavior>` usando as fixtures sintéticas.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_pdf_io.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `detect_blob_type` distingue pdf/jpeg/png por magic bytes
    - `extract_text_and_decide` devolve "native_text" p/ PDF com texto, "vision" p/ escaneado
    - `render_pages_png` produz 1 PNG por página; imagem nunca aberta como PDF
    - test_pdf_io verde
  </acceptance_criteria>
  <done>Leitura local de PDF (EXT-01) e detecção de tipo prontas, sem custo de IA na leitura.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: router.py — seam de extração D-03</name>
  <files>backend/app/extraction/router.py, backend/tests/extraction/test_router.py</files>
  <read_first>
    - PATTERNS.md §"backend/app/extraction/router.py" (função de módulo choose; D-03 é o ponto de costura mais importante da fase)
    - CONTEXT D-03/D-05 (seam: nunca "sempre chama IA" cravado; Fases 4/7 plugam atalho local sem reescrever o motor)
    - backend/app/extraction/pdf_io.py (de Task 1) e backend/app/queue/repo.py:20 ("funções de módulo, estilo cas.py, sem classe")
  </read_first>
  <behavior>
    - choose(blob): PDF com texto → "native_text"; PDF escaneado → "vision"; imagem (jpeg/png) → "vision"
    - choose é mínimo e plugável: usa pdf_io (detect_blob_type + extract_text_and_decide), não embute lógica de OpenAI
  </behavior>
  <action>
    Criar `backend/app/extraction/router.py` como função de módulo (estilo repo.py:20 / cas.py — sem classe). `choose(blob: bytes) -> str` retorna `"native_text" | "vision"`: usa `pdf_io.detect_blob_type` — se imagem → `"vision"` direto; se PDF → delega a `pdf_io.extract_text_and_decide(blob, settings.openai_extract_min_chars_per_page)` e retorna a rota. Este é o seam D-03: default v1 = "decide texto-vs-visão, sempre IA"; documentar no docstring que Fases 4 (template casado) e 7 (determinístico→nativo→IA) plugam aqui o atalho local de custo zero SEM reescrever o motor (D-03/D-05). Manter mínimo — nenhuma lógica de OpenAI nem de DB. Criar `test_router.py` cobrindo o `<behavior>`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_router.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `choose` retorna "native_text"/"vision" corretamente para PDF-com-texto / PDF-escaneado / imagem
    - Docstring documenta o seam D-03 (Fases 4/7 estendem)
    - test_router verde
  </acceptance_criteria>
  <done>Seam de extração (D-03) pronto e plugável — não bloqueia o atalho local das Fases 4/7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: openai_client.py — Responses API + _unwrap + tokens</name>
  <files>backend/app/extraction/openai_client.py, backend/tests/extraction/test_openai_client.py</files>
  <read_first>
    - AI-SPEC §3 Entry Point Pattern (responses.parse, extract_from_text, extract_from_image_pages, _unwrap) + §4b Async-First + Retry logic
    - PATTERNS.md §"backend/app/extraction/openai_client.py" (padrão de segredo a copiar; mapeamento de tokens)
    - RESEARCH §Code Examples linhas 314-327 (mapeamento input→prompt / output→completion) + §Pitfall 4 (output_parsed is None = recusa, não exceção)
    - backend/app/config.py:11,55 (SecretStr; nada loga a chave); tests/extraction/conftest.py (fixture respx sucesso+recusa)
  </read_first>
  <behavior>
    - extract_from_text(native_text): chama responses.parse com 1 bloco input_text; devolve (ExtractionResult, usage) — mockado via respx
    - extract_from_image_pages(png_list): chama responses.parse com input_text + N input_image (data URL base64, detail do tunable); devolve (ExtractionResult, usage)
    - recusa: `output_parsed is None` → levanta ExtractionRefused (não AttributeError, não loop)
    - usage mapeado: input_tokens→prompt, output_tokens→completion (retornado para o caller gravar em Usage)
    - a chave NUNCA aparece em log/exceção (asserção no teste: nenhum log contém o valor da chave)
  </behavior>
  <action>
    Criar `backend/app/extraction/openai_client.py` (funções de módulo, estilo cas.py). Cliente: `AsyncOpenAI(api_key=get_settings().openai_api_key.get_secret_value())` — `.get_secret_value()` SÓ aqui, no ponto de criação; nunca logar a chave nem o conteúdo do documento (Critical Failure Mode 5 / V7). Definir `SYSTEM_INSTRUCTIONS` (system prompt fixo via `instructions=`: "extraia TODOS os pares dado→valor que encontrar, o texto integral e um palpite de tipo com confiança; não invente campos — só o que está no documento" — AI-SPEC §4b prompt discipline; SEM few-shot inline, anti-padrão p/ motor genérico). Implementar `extract_from_text(native_text) -> tuple[ExtractionResult, usage]` (1 bloco `input_text`) e `extract_from_image_pages(png_bytes_per_page) -> tuple[ExtractionResult, usage]` (1 `input_text` + N `input_image` com data URL base64 e `detail=settings.openai_extract_image_detail`, todas as páginas do bloco numa só chamada — AI-SPEC §3/§4). Ambas `await client.responses.parse(..., text_format=ExtractionResult, temperature=settings.openai_extract_temperature, max_output_tokens=settings.openai_extract_max_output_tokens)` (max_output_tokens SEMPRE explícito — Pitfall 3/6). `_unwrap(response)`: se `response.output_parsed is None` → `raise ExtractionRefused(document_id-aware mais tarde)` logando só o motivo do bloco refusal — NUNCA a chave nem o conteúdo (Pitfall 4 / D-08). Devolver também o usage com mapeamento documentado **input_tokens→prompt_tokens, output_tokens→completion_tokens** (a Responses API usa input/output; o modelo Usage usa prompt/completion — Open Question 3). NÃO implementar retry aqui — o backoff é da fila (Plan 04); a recusa apenas levanta. Definir a exceção `ExtractionRefused` neste módulo (ou em um errors.py do pacote extraction). Criar `test_openai_client.py` cobrindo o `<behavior>` com a fixture respx (sucesso + recusa) — sem gastar token.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_openai_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `extract_from_text` e `extract_from_image_pages` chamam responses.parse com `text_format=ExtractionResult` e devolvem (resultado, usage)
    - `output_parsed is None` → `ExtractionRefused` (testado); sem AttributeError
    - usage mapeia input_tokens→prompt / output_tokens→completion (comentado)
    - teste assere que o valor da chave OpenAI não aparece em logs/mensagens de erro
    - test_openai_client verde (OpenAI mockado, 0 token)
  </acceptance_criteria>
  <done>Cliente OpenAI (Responses API + Structured Outputs) isolado, com recusa tratada e tokens lidos — pronto para o extract_stage orquestrar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → OpenAI | texto nativo OU imagem da página (dado pessoal: CPF/CNPJ/salário) cruza para fora da máquina (LGPD) |
| openai_api_key → cliente | segredo lido da config no ponto de criação do AsyncOpenAI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | `openai_client.py` (log da chave) | mitigate | `.get_secret_value()` só no ponto de criação; nunca logar a chave. Teste assere ausência do valor da chave em logs/erros. |
| T-03-04 | Information Disclosure | `openai_client.py` (log de conteúdo sensível) | mitigate | Logar só metadados (motivo de recusa, caminho). Nunca logar `full_text`/`fields`/conteúdo do documento (V7/V8 LGPD). |
| T-03-05 | Information Disclosure | envio à OpenAI (LGPD minimização) | accept (v1) | Caminho texto envia texto nativo (mais barato/menos cru que imagem); router prefere native_text quando há texto. Controle por-documento explícito é v2 (INT2-03). Postura conservadora mantida (premissa "uso interno" não confirmada). |
| T-03-06 | Denial of Service (custo) | malformed/malicious PDF → PyMuPDF | mitigate | `pdf_io` abre via fitz com stream; PDF inválido levanta exceção controlada que o stage (Plan 03) transforma em FALHA — NÃO derruba o worker. Coberto no teste de erro do Plan 03. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/extraction -x -q` verde (schema/persistence de Plan 01 + pdf_io/router/openai_client deste plan)
- Nenhum teste gasta token (respx mocka tudo)
</verification>

<success_criteria>
- `pdf_io` lê texto nativo (EXT-01), decide texto-vs-visão (D-04) e detecta PDF-vs-imagem por magic bytes
- `router.choose` é o seam D-03 plugável (não crava "sempre IA")
- `openai_client` chama a Responses API com Structured Outputs, trata recusa e lê/mapeia tokens
- Chave OpenAI nunca logada (testado)
</success_criteria>

<output>
Create `.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-02-SUMMARY.md` when done
</output>
