---
phase: 03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/extraction/__init__.py
  - backend/app/extraction/schema.py
  - backend/app/models/extraction.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/0003_extractions.py
  - backend/tests/extraction/__init__.py
  - backend/tests/extraction/conftest.py
  - backend/tests/extraction/test_schema.py
  - backend/tests/extraction/test_persistence.py
autonomous: true
requirements: [EXT-02, USE-02]
must_haves:
  truths:
    - "O schema genérico ExtractionResult valida como list-of-pairs (não dict aberto), com full_text + doc_type_guess + confiança"
    - "A tabela extractions existe via Alembic 0003 com UNIQUE(document_id) = 1 extração por bloco"
    - "Os tunables OPENAI_EXTRACT_* são lidos de env como os queue_*"
    - "tests/extraction/ existe com fixture respx de AsyncOpenAI mockado"
  artifacts:
    - path: "backend/app/extraction/schema.py"
      provides: "ExtractionResult / ExtractedField (Pydantic, strict-safe)"
      contains: "class ExtractionResult"
    - path: "backend/app/models/extraction.py"
      provides: "Modelo SQLAlchemy Extraction"
      contains: "class Extraction"
    - path: "backend/alembic/versions/0003_extractions.py"
      provides: "Migração que cria a tabela extractions"
      contains: "create_table"
    - path: "backend/tests/extraction/conftest.py"
      provides: "Fixture respx de OpenAI mockado + fixtures de PDF sintético"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "Extraction"
      via: "import + __all__ (autogenerate Alembic depende disso)"
      pattern: "Extraction"
---

<objective>
Estabelecer os contratos e a fundação da extração (Wave 0 + skeleton): dependências, tunables de config, o schema genérico Pydantic (`ExtractionResult`), o modelo de banco `Extraction` via Alembic 0003, e o scaffold de testes `tests/extraction/` com OpenAI mockado por respx.

Purpose: As demais plans (pdf_io/router/openai_client, extract_stage, worker wiring) consomem estes contratos diretamente — sem este plan elas fariam scavenger hunt. Este é o "blueprint": tipos e tabela primeiro, implementação depois.
Output: `backend/app/extraction/schema.py`, `backend/app/models/extraction.py`, migração 0003, tunables em `config.py`, deps em `pyproject.toml`, e `tests/extraction/` (conftest com respx).

Nota de escopo: **EXT-04 (JSON Schema derivado de template + validações de campo configuráveis) foi re-escopado para a Fase 4** (CONTEXT `<domain>`/`<deferred>`). O schema desta fase é **genérico** (D-02), não derivado de template. Não modelar campos por tipo aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-CONTEXT.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-AI-SPEC.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-PATTERNS.md

<interfaces>
<!-- Contratos existentes que este plan estende. Extraídos do código real. Use diretamente. -->

backend/app/models/usage.py:20-33 (análogo EXACT do novo modelo):
- class Usage(Base): __tablename__="usage"; id PK; document_id FK->documents.id; step:str; prompt_tokens:int default 0; completion_tokens:int default 0; created_at DateTime(timezone=True) server_default=func.now()

backend/app/config.py:71-86 (padrão EXACT dos tunables):
- queue_poll_interval_seconds/queue_max_attempts/... = Field(default=..., validation_alias=AliasChoices("UPPER","lower"))
- openai_api_key: SecretStr | None = None (config.py:55) — já existe, NÃO recriar

backend/app/models/__init__.py:8-25:
- imports + __all__ — TODO modelo precisa estar aqui (autogenerate Alembic depende disso)

backend/alembic/versions/0002_ingestion.py (análogo EXACT da migração):
- revision/down_revision encadeados; op.create_table + op.batch_alter_table para índices; ForeignKeyConstraint(..., ondelete=...)
- CAVEAT trigger trg_documents_updated_at: SÓ se a migração tocar a tabela `documents`. 0003 só CRIA `extractions` → NÃO toca `documents` → NÃO precisa recriar trigger.

backend/tests/conftest.py + pyproject.toml:43-45:
- pytest-asyncio asyncio_mode="auto", testpaths=["tests"]; fixtures de engine/session já existem em tests/conftest.py — reusar para test_persistence
</interfaces>

ExtractionResult / ExtractedField: definição completa em AI-SPEC §4b.1 (Structured Outputs with Pydantic). Esquema do modelo Extraction: PATTERNS.md "backend/app/models/extraction.py" (file:line anchors).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependências + tunables de extração em config</name>
  <files>backend/pyproject.toml, backend/app/config.py</files>
  <read_first>
    - backend/pyproject.toml (linhas 7-24: [project.dependencies] já lista pikepdf==10.8.0 com pin; [dependency-groups].dev)
    - backend/app/config.py (linhas 18, 35, 55, 71-86: imports AliasChoices/Field/SecretStr; class Settings; openai_api_key existente; bloco queue_* a espelhar)
    - RESEARCH §Standard Stack (versões: openai 2.41.*, PyMuPDF 1.27.*, respx) + AI-SPEC §4 Model Configuration (defaults dos tunables)
  </read_first>
  <action>
    Adicionar a `backend/pyproject.toml` `[project.dependencies]`: `"openai==2.41.*"` e `"PyMuPDF==1.27.*"` (pin como pikepdf). Adicionar a `[dependency-groups].dev`: `"respx"`. Rodar `uv add "openai==2.41.*" "PyMuPDF==1.27.*"` e `uv add --group dev respx` no diretório backend (NÃO editar lockfile à mão). Lembrar: o import de PyMuPDF é `import fitz`, não `import pymupdf`.

    Em `backend/app/config.py`, na `class Settings`, junto aos `queue_*` (config.py:71-86), adicionar os tunables de extração seguindo o MESMO padrão `Field(default=..., validation_alias=AliasChoices("UPPER","lower"))`: `openai_extract_model` (default `"gpt-4o-2024-08-06"`; comentar que o implementador deve CONFIRMAR o modelo vigente na conta — precisa de visão + Structured Outputs, D-04), `openai_extract_temperature` (default `0.0` — extração determinística, não geração), `openai_extract_max_output_tokens` (default `4096` — teto explícito, Pitfall 3/6; saída inclui full_text), `openai_extract_image_detail` (default `"high"` — Pitfall 4, alavanca de custo de visão D-04), `openai_extract_min_chars_per_page` (default escolhido por você — limiar da heurística texto-vs-visão; ~16 é um ponto de partida razoável). NÃO recriar `openai_api_key` (já existe em config.py:55 como SecretStr; nunca logar — Critical Failure Mode 5).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.config import Settings; s=Settings(); print(s.openai_extract_model, s.openai_extract_temperature, s.openai_extract_max_output_tokens, s.openai_extract_image_detail, s.openai_extract_min_chars_per_page)" && uv run python -c "import fitz, openai; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `openai`, `PyMuPDF` em [project.dependencies]; `respx` em dev; `import fitz` e `import openai` funcionam no venv
    - Os 5 tunables `openai_extract_*` existem em Settings com defaults e validation_alias (lêem de env)
    - `openai_api_key` permanece SecretStr e NÃO foi alterado
  </acceptance_criteria>
  <done>Deps instaladas; tunables de extração lidos de env como os queue_*.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema genérico ExtractionResult (Pydantic, strict-safe)</name>
  <files>backend/app/extraction/__init__.py, backend/app/extraction/schema.py, backend/tests/extraction/__init__.py, backend/tests/extraction/test_schema.py</files>
  <read_first>
    - AI-SPEC §4b.1 (definição completa de ExtractedField/ExtractionResult + restrição strict mode)
    - AI-SPEC §3 Common Pitfalls #1 (strict NÃO aceita dict de chaves abertas → usar list-of-pairs)
    - PATTERNS.md §"No Analog Found" (schema.py: não há Pydantic-de-IA no repo; usar AI-SPEC)
  </read_first>
  <behavior>
    - test_schema: `ExtractionResult(fields=[{key,value,confidence}], full_text, doc_type_guess, doc_type_confidence)` valida
    - test_schema: `fields` é `list[ExtractedField]` (list-of-pairs), NÃO um dict aberto — validar que um dict bruto em `fields` é rejeitado
    - test_schema: o JSON Schema gerado por `ExtractionResult.model_json_schema()` não contém `additionalProperties: true` em nenhum objeto (compatível com strict mode)
  </behavior>
  <action>
    Criar `backend/app/extraction/__init__.py` (pacote vazio) e `backend/app/extraction/schema.py` com exatamente os modelos da AI-SPEC §4b.1: `ExtractedField(key:str, value:str, confidence:float)` e `ExtractionResult(fields: list[ExtractedField], full_text:str, doc_type_guess:str, doc_type_confidence:float)`, com `description` em cada Field (as descriptions são embutidas no JSON Schema e guiam o modelo — disciplina de prompt, AI-SPEC §4b). PONTO TÉCNICO Nº 1 DA FASE: `dado→valor` é modelado como **list-of-pairs**, NUNCA `dict[str,str]` de chaves abertas (strict mode exige todo campo required + additionalProperties:false; dict aberto é rejeitado pela API). Não adicionar tipagem por campo nem validações de domínio — isso é Fase 4 (EXT-04 re-escopado). Criar `backend/tests/extraction/__init__.py` e `test_schema.py` cobrindo o `<behavior>`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `from app.extraction.schema import ExtractionResult, ExtractedField` importa
    - `ExtractionResult` usa `fields: list[ExtractedField]` (não dict aberto)
    - test_schema verde, incluindo a checagem de que `fields` rejeita dict bruto
  </acceptance_criteria>
  <done>Schema genérico strict-safe definido e testado; é o contrato `text_format` que o openai_client (Plan 02) usará.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Modelo Extraction + migração 0003 + scaffold respx</name>
  <files>backend/app/models/extraction.py, backend/app/models/__init__.py, backend/alembic/versions/0003_extractions.py, backend/tests/extraction/conftest.py, backend/tests/extraction/test_persistence.py, backend/tests/test_migrations.py</files>
  <read_first>
    - backend/app/models/usage.py:20-33 (análogo EXACT: FK->documents ondelete=CASCADE, index, created_at server_default, docstring de papel)
    - backend/app/models/__init__.py:8-25 (imports + __all__ — registrar Extraction)
    - backend/alembic/versions/0002_ingestion.py (padrão: revision/down_revision, create_table, batch_alter_table p/ índice, ForeignKeyConstraint ondelete; trigger CAVEAT só se tocar `documents`)
    - backend/tests/conftest.py (fixtures de engine/session existentes para reusar) + backend/tests/test_migrations.py (padrão de teste de upgrade/downgrade já no repo)
    - PATTERNS.md §"backend/app/models/extraction.py" e §"0003_*.py" (esqueleto com file:line anchors)
  </read_first>
  <behavior>
    - test_persistence: criar um Document + Extraction(document_id, fields_json, full_text, doc_type_guess, doc_type_confidence, route) e ler de volta
    - test_persistence: inserir 2 Extractions com o MESMO document_id viola a UNIQUE (1 extração por bloco = idempotência / não re-cobrar)
    - test_migrations: upgrade até head cria a tabela `extractions`; downgrade -1 a remove (limpo)
  </behavior>
  <action>
    Criar `backend/app/models/extraction.py` espelhando `usage.py` (PATTERNS.md tem o esqueleto exato): `Extraction(Base)`, `__tablename__="extractions"`, `id` PK, `document_id` FK→documents.id ondelete=CASCADE com **`index=True, unique=True`** (UNIQUE = 1 extração por bloco = idempotência, evita re-chamar IA / cobrança dupla — Pitfall 3), `fields_json:Text` (list[ExtractedField] serializado em JSON — D-02/D-06), `full_text:Text` (texto nativo persistido — D-06, base para Fases 4/7), `doc_type_guess:String`, `doc_type_confidence:Float`, `route:String` ("native_text"|"vision" — métrica D-04), `created_at` DateTime(timezone=True) server_default=func.now(). Docstring explicando o papel na fase. OBRIGATÓRIO: registrar em `app/models/__init__.py` (import + adicionar `"Extraction"` ao `__all__`) — o autogenerate do Alembic depende disso.

    Criar `backend/alembic/versions/0003_extractions.py` com `revision='0003'`, `down_revision='0002'`, `op.create_table('extractions', ...)` + `op.batch_alter_table` criando o índice UNIQUE em `document_id` (PATTERNS.md tem o DDL). NÃO tocar a tabela `documents` → NÃO replicar o bloco de trigger trg_documents_updated_at do 0002 (o caveat só se aplica a alterações em `documents`). `downgrade` faz `op.drop_table('extractions')`.

    Criar `backend/tests/extraction/conftest.py` com: (a) fixture respx de `AsyncOpenAI` mockado que devolve um `ExtractionResult` válido em `output_parsed` (e uma variante de **recusa** → `output_parsed is None` para o Plan 03/04 reusarem), (b) fixtures de bytes de PDF sintético COM texto nativo e SEM texto (escaneado, força caminho visão) usando `fitz` para gerar, e (c) bytes de uma imagem PNG/JPEG mínima (para o caminho magic-bytes do Plan 02/03). Reusar as fixtures de engine/session de `tests/conftest.py`. Criar `test_persistence.py` cobrindo o `<behavior>` de persistência; estender `tests/test_migrations.py` com o caso 0003 (ou criar o teste de migração 0003 seguindo o padrão existente).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_persistence.py tests/test_migrations.py -x -q && uv run python -c "from app.models import Extraction; print('registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.models import Extraction` funciona (registrado em __init__.py + __all__)
    - Alembic 0003 upgrade cria `extractions`; downgrade a remove; teste de migração verde
    - UNIQUE em `document_id` rejeita 2ª Extraction para o mesmo bloco (teste verde)
    - `tests/extraction/conftest.py` fornece fixture respx (sucesso + recusa) e PDFs sintéticos (com/sem texto) + imagem
  </acceptance_criteria>
  <done>Tabela extractions + modelo registrado + scaffold de testes respx prontos; demais plans podem persistir e mockar a IA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → OpenAI | (preparado aqui; chamada real no Plan 02/03) chave OpenAI cruza para fora da máquina |
| env → config | tunables OPENAI_EXTRACT_* e a chave entram via env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `config.py` openai_api_key | mitigate | Manter `SecretStr`; `.get_secret_value()` só no ponto de criação do cliente (Plan 02). Este plan NÃO loga a chave; tunables não-secretos. |
| T-03-02 | Tampering | migração 0003 / schema | mitigate | Schema só via Alembic versionado (D-10), nunca `create_all`; downgrade testado para upgrade seguro entre versões. |
| T-03-SC | Tampering | `uv add openai/PyMuPDF/respx` | mitigate | Pacotes oficiais/estabelecidos já prescritos no CLAUDE.md e auditados em RESEARCH §Package Legitimacy Audit (Approved, [VERIFIED: PyPI]). Sem [ASSUMED]/[SUS] → sem checkpoint humano. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/extraction tests/test_migrations.py -x -q` verde
- `cd backend && uv run python -c "import fitz, openai; from app.models import Extraction; from app.extraction.schema import ExtractionResult"` sem erro
- Os tunables `openai_extract_*` lêem de env (config.py)
</verification>

<success_criteria>
- Schema genérico `ExtractionResult` (list-of-pairs, strict-safe) definido e testado
- Tabela `extractions` criada via Alembic 0003 (UNIQUE por document_id), modelo registrado
- Deps openai/PyMuPDF/respx instaladas; tunables de extração em config
- `tests/extraction/` com fixture respx (sucesso+recusa) e PDFs/imagem sintéticos — base de Wave 0 para os Plans 02-04
</success_criteria>

<output>
Create `.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-01-SUMMARY.md` when done
</output>
