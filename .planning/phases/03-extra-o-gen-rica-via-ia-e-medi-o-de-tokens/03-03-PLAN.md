---
phase: 03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - backend/app/extraction/stage.py
  - backend/tests/extraction/test_stage.py
  - backend/tests/extraction/test_usage.py
  - backend/tests/extraction/test_state.py
  - backend/tests/extraction/test_idempotency.py
autonomous: true
requirements: [EXT-01, EXT-02, USE-02]
must_haves:
  truths:
    - "extract_stage produz e persiste Extraction (fields + full_text + doc_type_guess + route) para o bloco"
    - "Cada extração grava Usage(step='extract') com input→prompt/output→completion tokens, exatamente uma vez"
    - "Sucesso avança o marcador para 'extraido' via mark_step, mantendo state=PROCESSANDO (NUNCA transition PROCESSANDO→PROCESSANDO, NUNCA CONCLUIDO)"
    - "Re-execução do mesmo bloco é no-op: Extraction já existente → não re-chama a IA (sem cobrança dupla)"
    - "Recusa/PDF malformado levanta para o worker tratar (não corrompe estado, não derruba o worker)"
  artifacts:
    - path: "backend/app/extraction/stage.py"
      provides: "extract_stage async isolável, idempotente, commit atômico"
      contains: "async def extract_stage"
  key_links:
    - from: "backend/app/extraction/stage.py"
      to: "Extraction + Usage (mesmo commit, ANTES de mark_done)"
      via: "session.add + commit único"
      pattern: "session.add"
    - from: "backend/app/extraction/stage.py"
      to: "mark_step('extraido')"
      via: "marcador interno, state permanece PROCESSANDO (D-07)"
      pattern: "extraido"
---

<objective>
Implementar o `extract_stage` — o estágio de pipeline async, isolável (sem HTTP), idempotente e com commit atômico, espelhando `ingest_stage`. Lê o bloco do CAS por `content_hash`, decide o caminho pelo `router` (D-03), extrai via `openai_client`, persiste `Extraction` + texto nativo + `Usage(step="extract")` no MESMO commit e avança o marcador para `"extraido"` (D-07) — tudo ANTES do worker marcar o job done (idempotência = não cobrar duas vezes).

Purpose: É o coração funcional da fase (EXT-01 + EXT-02 + USE-02 juntos num fluxo atômico). A correção crítica do estado (mark_step, não transition) e a idempotência (checar Extraction existente + commit único) vivem aqui.
Output: `backend/app/extraction/stage.py` + a suíte de testes de comportamento (stage/usage/state/idempotency) com OpenAI mockado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-CONTEXT.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-AI-SPEC.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-PATTERNS.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-RESEARCH.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-01-SUMMARY.md
@.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-02-SUMMARY.md

<interfaces>
Análogo EXACT — backend/app/pipeline/ingest_stage.py:
- docstring rico (garantias: idempotência/atomicidade/estado terminal); assinatura keyword-only; @dataclass(frozen=True) p/ resultado
- ingest_stage.py:115/177 — `session.commit()` ÚNICO ao final ("crash antes daqui = rollback total")
- ingest_stage.py:153-160 — checagem de existência ANTES de recriar (select(Document).where(content_hash==...))
- ingest_stage.py:162-177 — seta doc.state/last_completed_step EM MEMÓRIA e comita no fim (NÃO via transition)
- ingest_stage.py:53 — AWAITING_EXTRACTION_STEP = "aguardando_extracao" (estado onde a Fase 3 começa)

state_machine.py:
- mark_step(session, doc, step) [state_machine.py:66] — mantém state, atualiza last_completed_step, FAZ session.commit() (state_machine.py:74)
- transition(...) [state_machine.py:24] — valida allowlist; PROCESSANDO→PROCESSANDO NÃO está na allowlist (states.py:25-30) → levantaria InvalidTransition. NÃO usar p/ sucesso.

De Plan 01/02 (já existem):
- app.models.Extraction (UNIQUE document_id); app.models.Usage (document_id, step, prompt_tokens, completion_tokens)
- app.extraction.router.choose(blob) → "native_text"|"vision"
- app.extraction.pdf_io.{detect_blob_type, extract_text_and_decide, render_pages_png}
- app.extraction.openai_client.{extract_from_text, extract_from_image_pages, ExtractionRefused} → (ExtractionResult, usage)
- app.storage.cas.read_bytes(content_hash) [cas.py:125]
- tests/extraction/conftest.py → fixture respx (sucesso+recusa), PDFs com/sem texto, imagem
</interfaces>

CORREÇÕES CRÍTICAS aplicáveis a este plan (do contexto de planejamento, confirmadas no código real):
1. Sucesso usa `mark_step(session, doc, "extraido")` — NÃO `transition(...PROCESSANDO...)` (auto-loop proibido, states.py).
2. Só a parte PyMuPDF (get_text/render) vai em `await asyncio.to_thread(...)` de DENTRO do stage; a chamada OpenAI é `await` direto (nunca to_thread, nunca asyncio.run).
3. Atomicidade: Extraction + Usage + marcador "extraido" no MESMO commit, ANTES de o worker (Plan 04) chamar mark_done. Checar Extraction existente no início → no-op (não re-chama IA).
4. Token mapping: input_tokens→prompt_tokens, output_tokens→completion_tokens (comentar).
5. PDF malformado / recusa → levantar (o worker captura e roteia p/ retry/FALHA). NÃO derrubar o worker.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract_stage — orquestração async idempotente + commit atômico</name>
  <files>backend/app/extraction/stage.py, backend/tests/extraction/test_stage.py</files>
  <read_first>
    - backend/app/pipeline/ingest_stage.py (análogo EXACT: docstring, dataclass resultado, commit único, checagem de existência, set em memória)
    - PATTERNS.md §"backend/app/pipeline/stage.py → extract_stage" (CORREÇÃO CRÍTICA de estado mark_step vs transition; sub-armadilha CPU-bound em to_thread)
    - backend/app/pipeline/state_machine.py:24,66 (transition vs mark_step) + states.py:25-30 (allowlist; PROCESSANDO→PROCESSANDO ausente)
    - backend/app/storage/cas.py:125 (read_bytes)
    - tests/extraction/conftest.py (fixtures de Plan 01)
  </read_first>
  <behavior>
    - happy path (PDF com texto, OpenAI mockado): persiste Extraction com fields/full_text/doc_type_guess/route="native_text"; não chama o caminho visão
    - happy path (PDF escaneado): route="vision"; envia render à IA; persiste full_text com o que houver de texto nativo (D-06)
    - happy path (blob de imagem): detectado por magic bytes → caminho visão direto (imagem é a página)
    - test_refusal: openai_client levanta ExtractionRefused → extract_stage propaga (não engole), estado NÃO corrompido (nenhuma Extraction parcial persistida)
    - PDF malformado: fitz levanta → extract_stage propaga exceção controlada (worker tratará); worker não cai
  </behavior>
  <action>
    Criar `backend/app/extraction/stage.py` com `async def extract_stage(session, *, content_hash: str) -> ExtractStageResult` (assinatura keyword-only; `@dataclass(frozen=True)` p/ o resultado), espelhando `ingest_stage.process_ingest` em forma/docstring/atomicidade. Fluxo:
    1. Localizar o `Document` do bloco por `content_hash` (`select(Document).where(Document.content_hash==content_hash)`).
    2. IDEMPOTÊNCIA (Pitfall 3): `select(Extraction).where(Extraction.document_id==doc.id)` — se já existe → `return` no-op (NÃO re-chamar a IA; evita cobrança dupla).
    3. `blob = cas.read_bytes(content_hash)`.
    4. `route = router.choose(blob)` (D-03).
    5. Parte PyMuPDF (CPU-bound) — extrair texto e/ou renderizar — dentro de `await asyncio.to_thread(...)` para não bloquear o loop; persistir o texto nativo disponível mesmo no caminho visão (D-06).
    6. Chamada OpenAI: `await openai_client.extract_from_text(...)` ou `extract_from_image_pages(...)` (NÃO em to_thread; é o ponto async) → `(result, usage)`.
    7. PERSISTÊNCIA ATÔMICA: `session.add(Extraction(document_id=doc.id, fields_json=<result.fields serializado JSON>, full_text=result.full_text, doc_type_guess=result.doc_type_guess, doc_type_confidence=result.doc_type_confidence, route=route))`; `session.add(Usage(document_id=doc.id, step="extract", prompt_tokens=usage.input_tokens, completion_tokens=usage.output_tokens))` (mapeamento input→prompt/output→completion — comentar); avançar o marcador setando `doc.last_completed_step="extraido"` EM MEMÓRIA (mesma técnica de ingest_stage.py:162-177) e fazer UM ÚNICO `session.commit()` ao final. **NÃO usar `mark_step` aqui se ele comita sozinho** — preferir o set-em-memória + commit único para manter Extraction+Usage+marcador no MESMO commit (atomicidade CR-02). state permanece `PROCESSANDO`; NUNCA `transition(PROCESSANDO→PROCESSANDO)` (quebra) nem `CONCLUIDO` (D-07).
    8. Recusa/erro: NÃO capturar aqui — deixar a exceção (`ExtractionRefused`, erro de PyMuPDF, erro transitório de rede) propagar para o worker (Plan 04), que faz schedule_retry/FALHA (D-08). Garantir que nada parcial foi comitado antes da exceção (a exceção ocorre antes do commit único).
    Logar só `document_id`/route/doc_type_guess/motivo de recusa — NUNCA a chave nem full_text/fields (V7/V8). Criar `test_stage.py` cobrindo o `<behavior>` com a fixture respx.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `extract_stage` persiste Extraction (fields/full_text/doc_type_guess/route) e Usage num commit único
    - Caminho texto NÃO renderiza imagem; caminho visão/imagem usa render/bytes
    - `test_stage.py::test_refusal` verde: ExtractionRefused propaga, estado não corrompido
    - PDF malformado propaga exceção controlada (não derruba o processo)
    - Só PyMuPDF em to_thread; chamada OpenAI é await direto (sem asyncio.run)
  </acceptance_criteria>
  <done>Núcleo da extração (EXT-01+EXT-02) funcional, atômico e idempotente, com estado correto via marcador.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes de tokens, estado e idempotência</name>
  <files>backend/tests/extraction/test_usage.py, backend/tests/extraction/test_state.py, backend/tests/extraction/test_idempotency.py</files>
  <read_first>
    - backend/app/extraction/stage.py (de Task 1)
    - backend/app/models/usage.py:20-33 (campos prompt/completion) + backend/app/models/document.py (last_completed_step, state)
    - RESEARCH §Phase Requirements → Test Map (USE-02, Estado, Idempotência) + VALIDATION.md Per-Task Verification Map
    - tests/extraction/conftest.py (fixture respx com usage sintético: input_tokens/output_tokens conhecidos)
  </read_first>
  <behavior>
    - test_usage: após extração, existe exatamente 1 Usage(document_id, step="extract") com prompt_tokens==usage.input_tokens e completion_tokens==usage.output_tokens (SC4/USE-02)
    - test_state: após sucesso, doc.state==PROCESSANDO e doc.last_completed_step=="extraido" (D-07); doc NUNCA vira CONCLUIDO; nenhuma chamada a transition(PROCESSANDO→PROCESSANDO)
    - test_idempotency: chamar extract_stage 2x para o mesmo content_hash → a IA é chamada só 1x (respx route call_count==1), e há só 1 Extraction e 1 Usage (sem cobrança dupla)
  </behavior>
  <action>
    Criar os três testes de comportamento usando a fixture respx (usage sintético com input_tokens/output_tokens conhecidos). `test_usage.py`: assere 1 Usage(step="extract") por extração com o mapeamento input→prompt/output→completion correto (USE-02/SC4 — base da cobrança). `test_state.py`: assere `state==PROCESSANDO` + `last_completed_step=="extraido"` após sucesso e que o documento nunca atinge CONCLUIDO; opcionalmente espionar que `transition` não foi chamado com PROCESSANDO→PROCESSANDO. `test_idempotency.py`: rodar `extract_stage` duas vezes para o mesmo bloco e verificar via respx que a Responses API foi chamada exatamente UMA vez e que Extraction/Usage não duplicaram (Failure Mode 3 — não cobrar duas vezes). Estes testes são a maior alavanca de confiança da fase (VALIDATION.md / deep_work_rules).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/extraction/test_usage.py tests/extraction/test_state.py tests/extraction/test_idempotency.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_usage verde: 1 Usage(step="extract") por extração, tokens mapeados corretamente
    - test_state verde: PROCESSANDO + "extraido", nunca CONCLUIDO
    - test_idempotency verde: 2ª execução não re-chama a IA (call_count==1), sem Extraction/Usage duplicados
  </acceptance_criteria>
  <done>Medição de tokens (USE-02/SC4), transição de estado (D-07) e não-cobrança-dupla provadas por teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fila/worker → extract_stage | job de extract dispara trabalho pago sobre o bloco |
| extract_stage → OpenAI | conteúdo do documento (texto/imagem) sai da máquina (LGPD) |
| extract_stage → DB | Extraction + Usage persistidos (base de cobrança) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | (custo, não-STRIDE) | reprocessamento → cobrança dupla | mitigate | Checar Extraction existente no início → no-op; UNIQUE(document_id); commit único ANTES de mark_done. test_idempotency prova call_count==1. |
| T-03-08 | Repudiation/Integrity | medição de tokens perdida/dupla | mitigate | Usage(step="extract") gravado no MESMO commit da Extraction; test_usage assere exatamente 1 por extração. |
| T-03-09 | Denial of Service (custo) | PDF malformado/malicioso → PyMuPDF crash | mitigate | fitz abre via stream; erro propaga como exceção controlada → worker faz FALHA (Plan 04); o worker NÃO cai (cada coroutine tem sessão própria e try/except no worker). |
| T-03-10 | Information Disclosure | log de full_text/fields/chave | mitigate | Logar só document_id/route/doc_type_guess/motivo de recusa; nunca conteúdo nem chave (V7/V8). |
| T-03-11 | Tampering (do dado) | alucinação de campo passa como verdade | accept | Sem gate de qualidade na Fase 3 (D-09); mitigado a jusante (evals offline + Fase 5). Documentado, não resolvido aqui. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/extraction -x -q` verde (todos os comportamentos da fase, OpenAI mockado, 0 token)
- Nenhum teste usa `transition(PROCESSANDO→PROCESSANDO)`; sucesso via marcador "extraido"
</verification>

<success_criteria>
- `extract_stage` extrai (texto nativo ou visão), persiste Extraction + full_text + Usage e avança o marcador "extraido" num commit atômico
- Idempotente: 2ª execução não re-chama a IA nem duplica cobrança
- Recusa/PDF malformado propaga para o worker sem corromper estado nem derrubar o processo
</success_criteria>

<output>
Create `.planning/phases/03-extra-o-gen-rica-via-ia-e-medi-o-de-tokens/03-03-SUMMARY.md` when done
</output>
