---
phase: 10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/classification/matcher.py
  - backend/tests/classification/test_matcher_norm.py
autonomous: true
requirements: [BL-05]
nyquist_compliant: true

must_haves:
  truths:
    - "Um sinal `texto` 'nota fiscal' casa um texto que contém 'NOTA FISCAL', 'Nota\\nFiscal' e 'Notá Fiscál' (D-02: lowercase + sem acento + quebra de linha colapsada)"
    - "Um sinal `texto` com pontuação (ex.: CNPJ '12.345.678/0001-99') casa o mesmo CNPJ no texto após normalização simétrica de pontuação (D-02)"
    - "Um sinal `texto` com palavra trocada (D-02: 'NATUREZA DA OPERAÇÃO' vs texto 'NATUREZA DE OPERAÇÃO') continua NÃO casando — normalização não resolve isso (D-04, tradeoff aceito)"
    - "Os sinais em modo `regex` continuam rodando contra o haystack lowercase-só (NÃO normalizado): `\\d{44}` casa, ReDoS estoura timeout e falha-fechada, tetos intactos (D-03)"
    - "Existe um helper público `evaluate_groups(groups, full_text)` que devolve, por-grupo e por-condição, se casou — usando a MESMA preparação de haystack do `match_templates` (D-09, base do preview do Plano 02)"
  artifacts:
    - path: "backend/app/classification/matcher.py"
      provides: "Função pura `_normalize_text`, bifurcação por modo em `_condition_matches` (texto normalizado vs regex lowercase-só), helper público `evaluate_groups` + dataclasses de relatório"
      contains: "def _normalize_text"
    - path: "backend/tests/classification/test_matcher_norm.py"
      provides: "Cobertura RED→GREEN de D-02/D-03/D-04 (normalização simétrica, regex intacto, palavra trocada não casa)"
      contains: "def test_"
  key_links:
    - from: "matcher.match_templates"
      to: "matcher.evaluate_groups"
      via: "match_templates reusa evaluate_groups (ou a mesma preparação de haystack) — D-09 garante resultado idêntico ao preview"
      pattern: "evaluate_groups"
    - from: "matcher._condition_matches (ramo texto)"
      to: "matcher._normalize_text"
      via: "value e haystack passam pela MESMA _normalize_text (simetria D-02)"
      pattern: "_normalize_text"
---

<objective>
Tornar o matcher de sinais tolerante a diferenças mecânicas via NORMALIZAÇÃO (D-01/D-02),
SEM mexer no modo `regex` (D-03), SEM adotar N-de-M (D-01), e SEM resolver palavra
trocada (D-04, deferido à ferramenta de testar sinais). Expor um helper público
`evaluate_groups` que devolve o detalhamento por-grupo/por-condição reusando a MESMA
preparação de haystack do `match_templates` — base do preview de sinais (Plano 02, D-09).

Purpose: Reduzir quarentenas falsas por acento/quebra/pontuação/espaço, preservando os
invariantes de segurança do matcher (ReDoS/timeout, falha-fechada, seam `decide` puro, LGPD).
Output: `matcher.py` com `_normalize_text`, bifurcação por modo, `evaluate_groups` + suíte nova.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova/10-CONTEXT.md
@.planning/phases/10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova/10-RESEARCH.md
@.planning/phases/10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova/10-PATTERNS.md

<interfaces>
<!-- Contratos atuais do matcher (já em contexto). O executor NÃO precisa explorar o código. -->

backend/app/classification/matcher.py (estado atual a refatorar):
- `_MAX_SIGNAL_REGEX_LEN = 512`, `_MAX_HAYSTACK_LEN = 200_000`, `_REGEX_TIMEOUT_S = 0.25`, `_AMBIGUITY_MARGIN = 0.1` — PRESERVAR todos.
- `_parse_groups(raw: str | None) -> list[list[dict]]` — forward-compatible; NÃO alterar.
- `_condition_matches(cond: dict, haystack: str) -> bool` — hoje recebe UM haystack (lowercase). Ramo regex: tetos + `regex.compile(value, regex.IGNORECASE)` + `.search(haystack[:_MAX_HAYSTACK_LEN], timeout=_REGEX_TIMEOUT_S)` + `except (regex.error, TimeoutError)`. Ramo texto/default: `value.strip().lower() in haystack`.
- `_group_matches(group, haystack) -> bool` — E (all), grupo vazio não casa.
- `_template_matches(groups, haystack) -> bool` — OU (any).
- `match_templates(*, fields_json, full_text, doc_type_guess, templates) -> list[TemplateMatch]` — hoje faz `haystack = (full_text or "").lower()`; confiança booleana 1.0/0.0; ordena desc.
- `decide(matches, *, threshold) -> MatchDecision` — seam puro D-03; NÃO tocar.
- dataclasses `TemplateMatch(template_id, confidence)`, `MatchDecision(status, template_id)`.
- `import regex` (lib com timeout) — para ReDoS. NÃO confundir com `re` stdlib.

backend/app/automation/naming.py:146-154 (padrão a COPIAR, não importar — Pitfall 8):
- `_strip_accents(s)` = `"".join(c for c in unicodedata.normalize("NFKD", s) if not unicodedata.combining(c))`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — suíte RED de normalização e não-regressão do regex</name>
  <files>backend/tests/classification/test_matcher_norm.py</files>
  <read_first>
    - backend/tests/classification/test_matcher_groups.py (análogo de estilo: funções puras, sem DB/IA; padrões de teste de regex e ReDoS já existentes)
    - backend/app/classification/matcher.py (contratos atuais — em <interfaces>)
    - .planning/phases/10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova/10-RESEARCH.md (Pitfalls 1 e 2; seção Normalização)
  </read_first>
  <behavior>
    - Test D-02 acento: sinal texto "nota fiscal" casa haystack contendo "Notá Fiscál".
    - Test D-02 quebra de linha: sinal "nota fiscal" casa haystack contendo "Nota\nFiscal".
    - Test D-02 caixa: sinal "nota fiscal" casa "NOTA FISCAL".
    - Test D-02 pontuação simétrica: sinal "12.345.678/0001-99" casa haystack com o mesmo CNPJ (ambos viram "12 345 678 0001 99").
    - Test D-02 simetria: normalizar SÓ haystack (sem value) falharia — provar que value também é normalizado (sinal acentuado casa haystack sem acento e vice-versa).
    - Test D-04 (tradeoff aceito): sinal "natureza da operacao" NÃO casa haystack "natureza de operacao" (palavra trocada não é resolvida por normalização).
    - Test D-03 regex intacto: sinal regex "\\d{44}" casa um haystack com 44 dígitos; um pattern com `$`/`\\.` ancorado em quebra de linha mantém a semântica lowercase-só (NÃO normalizado).
    - Test D-03 ReDoS/tetos: pattern catastrófico estoura `_REGEX_TIMEOUT_S` e retorna False (falha-fechada); pattern acima de `_MAX_SIGNAL_REGEX_LEN` retorna False.
    - Test `evaluate_groups`: para um template com 2 grupos (um casa, um falha), o relatório lista cada grupo com `matched` e cada condição com `mode`/`value`/`matched`; o agregado é idêntico ao `match_templates` (D-09).
  </behavior>
  <action>
    Criar `backend/tests/classification/test_matcher_norm.py` cobrindo os casos do bloco <behavior>.
    Importar de `app.classification.matcher` as funções públicas (`match_templates`, `decide`, e o novo `evaluate_groups`) e — onde necessário para o teste unitário da normalização — a função `_normalize_text`.
    Para os casos de regex/ReDoS, REPLICAR o estilo já presente em `test_matcher_groups.py` (não reinventar fixtures). Os testes que dependem de `evaluate_groups`/`_normalize_text` ficam RED até a Task 2 (import inexistente). Rodar para confirmar o estado RED (coleta falha ou asserts falham).
    NÃO logar `full_text` nem valores nos testes (V7) além do necessário ao assert.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/classification/test_matcher_norm.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe e é coletado pelo pytest.
    - Os testes de normalização e `evaluate_groups` estão RED (falham por símbolo/comportamento ainda inexistente) — comprovando que medem algo real antes da implementação.
  </acceptance_criteria>
  <done>Suíte RED criada; falha esperada por `_normalize_text`/`evaluate_groups` ausentes ou comportamento não implementado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — `_normalize_text`, bifurcação por modo e helper `evaluate_groups`</name>
  <files>backend/app/classification/matcher.py</files>
  <read_first>
    - backend/app/classification/matcher.py (arquivo a alterar — contratos em <interfaces>)
    - backend/app/automation/naming.py (linhas 146-154: `_strip_accents` — COPIAR o corpo, NÃO importar; Pitfall 8)
    - backend/tests/classification/test_matcher_norm.py (a suíte RED da Task 1 que deve passar)
    - backend/tests/classification/test_matcher_groups.py (testes legados de regex/ReDoS que DEVEM continuar verdes)
    - .planning/phases/10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova/10-RESEARCH.md (Code Examples: bifurcação por modo; Pattern 1)
  </read_first>
  <action>
    Em `matcher.py`, adicionar `import re as _re` (stdlib, SÓ para a regex de normalização — NÃO a lib `regex` de ReDoS) e reusar o `import unicodedata`. Definir, no topo do módulo, `_PUNCT_RE = _re.compile(r"[^\w\s]", _re.UNICODE)` e `_WS_RE = _re.compile(r"\s+")`.
    Criar a função pura `_normalize_text(s: str) -> str` que: NFKD-decompõe (`unicodedata.normalize("NFKD", s or "")`), remove combinantes (`unicodedata.combining`), aplica `.lower()`, substitui `_PUNCT_RE`→espaço, colapsa `_WS_RE`→espaço único e `.strip()`. Docstring: PURA, não loga (V7).
    Refatorar `_condition_matches` para a assinatura `(cond, haystack_norm: str, haystack_lower: str) -> bool` com bifurcação CEDO (Pitfall 1): `if mode == "regex":` mantém o corpo ATUAL byte-a-byte usando `haystack_lower` (tetos `_MAX_SIGNAL_REGEX_LEN`/`_MAX_HAYSTACK_LEN` + `timeout=_REGEX_TIMEOUT_S` + `except (regex.error, TimeoutError)`); senão (texto/default) usa `needle = _normalize_text(value)` (simetria D-02), `if not needle: return False`, `return needle in haystack_norm`.
    Propagar a nova assinatura em `_group_matches` e `_template_matches` (recebem e repassam ambos os haystacks).
    Em `match_templates`, preparar UMA vez `haystack_lower = (full_text or "").lower()` e `haystack_norm = _normalize_text(full_text or "")` e passá-los adiante; manter a confiança booleana 1.0/0.0 e a ordenação desc (sem mudança semântica de grupos que já casavam).
    Adicionar dataclasses `@dataclass(frozen=True) ConditionReport(mode: str, value: str, matched: bool)` e `GroupReport(matched: bool, conditions: list[ConditionReport])`, espelhando o estilo de `TemplateMatch`/`MatchDecision`.
    Criar o helper PÚBLICO `evaluate_groups(groups: list[list[dict]], full_text: str) -> list[GroupReport]` que prepara os dois haystacks da MESMA forma que `match_templates` e, por grupo, avalia cada condição via `_condition_matches`, montando o relatório (`matched` do grupo = E das condições). Este helper é a fonte-única consumida pelo preview (D-09) — `match_templates` pode reusá-lo internamente OU compartilhar a preparação de haystack, mas o resultado agregado precisa bater.
    NÃO tocar em `decide` (seam D-03) nem em `_parse_groups`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/classification/test_matcher_norm.py tests/classification/test_matcher_groups.py tests/classification/test_matcher.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `tests/classification/test_matcher_norm.py` passa (D-02/D-04/D-09 verdes).
    - `tests/classification/test_matcher_groups.py` e `test_matcher.py` permanecem 100% verdes (não-regressão D-03: regex/ReDoS/tetos intactos).
    - `evaluate_groups` é público e devolve relatório por-grupo/condição cujo agregado bate com `match_templates`.
    - O ramo `regex` de `_condition_matches` usa `haystack_lower` (não normalizado); o ramo `texto` usa `haystack_norm` com `value` também normalizado.
  </acceptance_criteria>
  <done>Normalização simétrica implementada só no ramo texto; regex intacto; `evaluate_groups` público; suíte de classificação verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → matcher (signals_json) | regex/texto colados pelo operador (single-tenant) atravessam até `_condition_matches` |
| documento → matcher (full_text) | texto extraído (não confiável quanto a tamanho/conteúdo) vira haystack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Denial of Service | `_condition_matches` ramo regex | mitigate | NÃO regredir: manter `regex.compile` + `.search(timeout=_REGEX_TIMEOUT_S)` + tetos `_MAX_SIGNAL_REGEX_LEN`/`_MAX_HAYSTACK_LEN` + `except (regex.error, TimeoutError)`→False. Bifurcar CEDO para a normalização NÃO tocar este ramo (Pitfall 1) |
| T-10-01 | Tampering | `_normalize_text` / ramo texto | mitigate | Dispatch explícito por etiqueta (nunca `eval`); normalização simétrica value↔haystack (Pitfall 2) evita casamento assimétrico; falha-fechada para needle vazio |
| T-10-IL | Information Disclosure | logs do matcher | accept | Matcher não loga `full_text` nem valores (já é invariante; `_normalize_text` documentada como não-logante). Sem novo log de conteúdo introduzido |
| T-10-SC | Tampering | npm/pip installs | mitigate | Nenhuma dependência nova neste plano (só `re`/`unicodedata` stdlib + `regex`/`unicodedata` já presentes). Sem checkpoint de instalação |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/classification -x -q` verde (normalização + legados).
- Não-regressão dos testes de regex/ReDoS de `test_matcher_groups.py`.
</verification>

<success_criteria>
- D-01/D-02/D-03/D-04 cobertos por testes verdes; `evaluate_groups` público pronto para o Plano 02.
- Zero dependência nova; invariantes de ReDoS/LGPD preservados.
</success_criteria>

<output>
Create `.planning/phases/10-robustez-de-ingestao-e-classificacao-varredura-de-pasta-nova/10-01-SUMMARY.md` when done
</output>
