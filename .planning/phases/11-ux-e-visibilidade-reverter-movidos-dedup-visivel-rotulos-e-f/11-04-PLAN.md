---
phase: 11-ux-e-visibilidade
plan: 04
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-03"
files_modified:
  - frontend/src/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useDocuments.ts
  - frontend/src/pages/DocumentsPage.tsx
autonomous: false
requirements:
  - "Backlog item 1 (D-01)"
  - "Backlog item 3 (D-05)"

must_haves:
  truths:
    - "No detalhe de um documento concluído, o usuário vê origem→destino e um botão 'Reverter para a origem'"
    - "Reverter chama POST /automations/undo com document_id e a lista/detalhe atualizam (doc reabre)"
    - "Após 'Forçar varredura', um toast informa quantos foram enfileirados e quantos pulados por duplicata"
  artifacts:
    - path: "frontend/src/pages/DocumentsPage.tsx"
      provides: "Seção origem→destino + botão Reverter no DocumentDetailModal + toast pós-rescan"
      contains: "Reverter"
    - path: "frontend/src/lib/api.ts"
      provides: "getDocumentAudit + postRescan retornando skipped_duplicates"
      contains: "getDocumentAudit"
  key_links:
    - from: "DocumentDetailModal"
      to: "GET /documents/{id}/audit"
      via: "useQuery getDocumentAudit"
      pattern: "getDocumentAudit"
    - from: "botão Reverter"
      to: "POST /automations/undo"
      via: "useUndoDocument → postUndo({document_id})"
      pattern: "postUndo"
    - from: "toast pós-rescan"
      to: "rescan.data.skipped_duplicates"
      via: "onSuccess da mutation useRescan"
      pattern: "skipped_duplicates"
---

<objective>
Itens 1 e 3 — frontend. (Item 1, D-01) No detalhe do documento, mostrar origem→destino (lido do novo GET /documents/{id}/audit) e um botão "Reverter para a origem" que chama o undo por document_id já existente. (Item 3, D-05) Após "Forçar varredura", mostrar um toast "X novos enfileirados, Y pulados por já existirem" usando o novo `skipped_duplicates` do /rescan.

Purpose: O backend já tem CAS+audit+undo por document_id (item 1) e agora retorna skipped_duplicates (item 3, do 11-01); falta a UI. Mata "não consigo reverter pela tela" e "o /rescan parece não fazer nada".
Output: types.ts (DocumentAudit/AuditEntry), api.ts (getDocumentAudit + postRescan novo shape), useDocuments.ts (useUndoDocument), DocumentsPage.tsx (seção reverter + toast).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-CONTEXT.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-PATTERNS.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-01-SUMMARY.md

<interfaces>
<!-- Contratos do backend (11-01) e do frontend existente — usar direto. -->

GET /documents/{id}/audit (criado em 11-01) → DocumentAuditOut:
  { items: AuditEntryOut[], can_undo: boolean }
  AuditEntryOut = { id, action, status, source_path: str|null, dest_path: str|null, run_id: str|null, created_at }
POST /rescan (11-01) → { enqueued: number, skipped_duplicates: number }

frontend/src/lib/api.ts:
  request<T>(path) (l.45-63) — wrapper único (trata erro/204), NÃO reimplementar fetch.
  getDocumentDetail (l.76-78) — molde de getter por id.
  postRescan (l.80-82) — HOJE retorna { enqueued } → ATUALIZAR para { enqueued, skipped_duplicates }.
  postUndo({ document_id?, run_id? }) (l.302) — JÁ existe, reusar.
frontend/src/hooks/useDocuments.ts:
  useRescan (l.33-42) — molde de mutation com invalidateQueries no onSuccess.
frontend/src/pages/DocumentsPage.tsx:
  DocumentDetailModal (l.371-524) — ponto de plug para origem→destino + Reverter.
    detailQuery via useQuery queryKey ['document-detail', docId] (l.372-375).
  botão "Forçar varredura" dispara rescan.mutate() (~l.162-170).
  chip "{N} duplicados ignorados" no footer (l.302-310) — estilo de msg neutra var(--text-3).
  estilo de botão destrutivo + confirmação: confirmDelete (l.322-362).
  paths em cell-mono (l.493-494). Valores TEXTO PURO (sem dangerouslySetInnerHTML). Zero npm novo.
frontend/src/types.ts: DocumentDetail (~l.114-130) — estilo de interface a espelhar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos + api client (getDocumentAudit, postRescan novo shape) + useUndoDocument (item 1/3)</name>
  <read_first>
    - frontend/src/lib/api.ts (request l.45-63, getDocumentDetail l.76-78, postRescan l.80-82, postUndo l.302)
    - frontend/src/types.ts (DocumentDetail ~l.114-130)
    - frontend/src/hooks/useDocuments.ts (useRescan l.33-42)
    - .planning/phases/.../11-01-SUMMARY.md (shape exato de DocumentAuditOut/AuditEntryOut)
  </read_first>
  <action>
    (1) Em types.ts adicionar `AuditEntry` ({ id:number; action:string; status:string; source_path:string|null; dest_path:string|null; run_id:string|null; created_at:string }) e `DocumentAudit` ({ items: AuditEntry[]; can_undo: boolean }) espelhando o estilo de DocumentDetail. (2) Em api.ts adicionar `getDocumentAudit(id:number): Promise<DocumentAudit>` usando o wrapper `request` (`return request<DocumentAudit>(\`/documents/${id}/audit\`)`) — NÃO reimplementar fetch. (3) Atualizar a assinatura/retorno de `postRescan` (l.80-82) para `Promise<{ enqueued: number; skipped_duplicates: number }>`. (4) Em useDocuments.ts criar `useUndoDocument` copiando o molde de useRescan: `mutationFn: (id:number) => postUndo({ document_id: id })`, `onSuccess: (_d, id) => { qc.invalidateQueries(['documents']); qc.invalidateQueries(['document-detail', id]); qc.invalidateQueries(['document-audit', id]) }`. Reusar `postUndo` existente (l.302).
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -c "getDocumentAudit\|skipped_duplicates" frontend/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - types.ts exporta `AuditEntry` e `DocumentAudit`.
    - api.ts tem `getDocumentAudit` via `request<DocumentAudit>` e `postRescan` tipado com `skipped_duplicates`.
    - useDocuments.ts exporta `useUndoDocument` com invalidateQueries das 3 chaves.
    - `npm run build` verde.
  </acceptance_criteria>
  <done>Camada de tipos/api/hooks pronta para a UI de reverter e o toast.</done>
</task>

<task type="auto">
  <name>Task 2: UI de reverter (origem→destino + botão) no detalhe + toast pós-varredura (D-01/D-05)</name>
  <read_first>
    - frontend/src/pages/DocumentsPage.tsx (DocumentDetailModal l.371-524, rescan.mutate l.162-170, footer chip l.302-310, confirmDelete l.322-362, cell-mono l.493-494)
    - frontend/src/hooks/useDocuments.ts (useUndoDocument criado na Task 1, useRescan l.33-42)
  </read_first>
  <action>
    (Item 1, D-01) No `DocumentDetailModal` (l.371-524) adicionar uma seção "Operações aplicadas": um `useQuery` com queryKey `['document-audit', docId]` e queryFn `() => getDocumentAudit(docId)` (mesmo padrão do detailQuery l.372-375). Quando houver itens, renderizar para cada entrada `status === 'done'` os pares origem→destino (`source_path` → `dest_path`) em `cell-mono` (TEXTO PURO). Quando `can_undo` for true, renderizar um botão "Reverter para a origem" no estilo `btn-primary` (ou destrutivo) que, via `useUndoDocument`, chama o undo por `document_id` do doc aberto; reusar o padrão de confirmação do `confirmDelete` (l.322-362) antes de reverter. No onSuccess da mutation a lista, o detalhe e o audit invalidam (já configurado na Task 1) — o doc reabre CONCLUIDO→PROCESSANDO. (Item 3, D-05) No fluxo de "Forçar varredura" (rescan.mutate, l.162-170): no `onSuccess` da mutation (acessível via `rescan.data` ou callback) exibir um toast efêmero "X novos enfileirados, Y pulados por já existirem" usando `enqueued` e `skipped_duplicates`. NÃO há lib de toast — usar estado local efêmero (useState com timeout para sumir) + render inline no estilo de mensagem neutra do chip de footer (var(--text-3)). Zero npm novo; valores TEXTO PURO.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -c "getDocumentAudit\|useUndoDocument\|skipped_duplicates" frontend/src/pages/DocumentsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DocumentDetailModal busca o audit via useQuery `['document-audit', docId]` e renderiza origem→destino em cell-mono para entradas done.
    - O botão "Reverter para a origem" aparece quando can_undo e chama useUndoDocument com o document_id, com confirmação.
    - O toast pós-rescan mostra `enqueued` e `skipped_duplicates` (grep encontra skipped_duplicates no arquivo).
    - `npm run build` verde.
  </acceptance_criteria>
  <done>Detalhe mostra origem→destino + Reverter; varredura forçada mostra toast com enfileirados/pulados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>No detalhe do documento: seção "Operações aplicadas" com origem→destino (do GET /documents/{id}/audit) e botão "Reverter para a origem" (chama undo por document_id, restaura do CAS, reabre o doc). Toast pós-"Forçar varredura": "X novos enfileirados, Y pulados por já existirem" (do skipped_duplicates novo).</what-built>
  <how-to-verify>
    1. Ter um documento que já foi movido por automação (estado concluído). Abrir seu detalhe na aba Documentos.
    2. Confirmar que aparece a origem→destino (caminhos) e o botão "Reverter para a origem".
    3. Clicar "Reverter", confirmar. O arquivo deve voltar à origem (verificar no Windows Explorer / pasta), o doc reabrir (status muda de Concluído) e a lista/detalhe atualizarem.
    4. Colocar um arquivo de conteúdo idêntico a um já ingerido numa pasta monitorada e clicar "Forçar varredura".
    5. Confirmar o toast: deve dizer quantos foram enfileirados e quantos pulados por já existirem (ex.: "0 novos enfileirados, 1 pulado por já existir"), em vez de parecer que nada aconteceu.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que ficou errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (undo) | O botão Reverter chama POST /automations/undo com document_id; o backend confina/restaura do CAS com seu próprio guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Elevation of Privilege | botão Reverter | accept | Reusa POST /automations/undo existente (restaura do CAS, write-ahead audit); a UI envia apenas o document_id do doc aberto, sem construir paths |
| T-11-09 | Information Disclosure | render de source_path/dest_path | mitigate | Caminhos vêm do AuditLog já persistido (não de input) e são renderizados como TEXTO PURO em cell-mono |
| T-11-10 | Tampering | undo de doc arbitrário | mitigate | document_id é o do doc cujo detalhe está aberto; o endpoint de undo aplica seu próprio guard de estado/operações registradas — nenhuma operação não-auditada é revertida |
</threat_model>

<verification>
- `cd frontend && npm run build` verde.
- Verificação visual humana (checkpoint): reverter de fato volta o arquivo (CAS) e o toast aparece.
</verification>

<success_criteria>
- Detalhe do doc concluído mostra origem→destino + Reverter (chama undo por document_id; doc reabre).
- "Forçar varredura" mostra toast com enfileirados + pulados por duplicata.
</success_criteria>

<output>
Create `.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-04-SUMMARY.md` when done
</output>
