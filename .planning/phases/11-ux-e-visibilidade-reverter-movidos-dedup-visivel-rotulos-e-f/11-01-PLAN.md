---
phase: 11-ux-e-visibilidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/documents.py
  - backend/app/ingest/watcher.py
  - backend/tests/test_documents_api.py
autonomous: true
requirements:
  - "Backlog item 1 (D-01, D-02)"
  - "Backlog item 3 (D-04)"
  - "Backlog item 9 (D-13)"

must_haves:
  truths:
    - "Toda data serializada pela API de documentos sai como UTC tz-aware (sufixo Z/offset), não naive"
    - "POST /rescan informa quantos candidatos foram pulados por duplicata, além dos enfileirados"
    - "O detalhe de um documento pode listar as operações aplicadas (origem→destino/status/run_id) lidas do AuditLog"
  artifacts:
    - path: "backend/app/api/documents.py"
      provides: "Helper de serialização tz-aware + RescanOut.skipped_duplicates + GET /documents/{id}/audit"
      contains: "DocumentAuditOut"
    - path: "backend/app/ingest/watcher.py"
      provides: "scan_and_enqueue devolve (enqueued, skipped_duplicates)"
      contains: "skipped_duplicates"
  key_links:
    - from: "backend/app/api/documents.py rescan()"
      to: "scan_and_enqueue"
      via: "desempacota o par (enqueued, skipped)"
      pattern: "scan_and_enqueue"
    - from: "GET /documents/{id}/audit"
      to: "AuditLog"
      via: "select(AuditLog).where(document_id==...)"
      pattern: "select\\(AuditLog\\)"
---

<objective>
Fundação backend da Fase 11: três correções/exposições de leitura no backend que o frontend (Waves seguintes) consome — serialização de data com fuso correto (item 9), contagem de duplicatas pulada no rescan (item 3) e um endpoint GET de auditoria por documento (item 1). Nenhuma capacidade de motor nova; só leitura e apresentação.

Purpose: O frontend precisa de (a) datas tz-aware para `new Date(iso)` converter corretamente, (b) o número de pulados-por-duplicata para o toast pós-varredura, e (c) os pares origem→destino do audit para a tela de reverter.
Output: documents.py com helper tz-aware + `RescanOut.skipped_duplicates` + `GET /documents/{id}/audit`; watcher.py com `scan_and_enqueue` devolvendo o par enqueued/skipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-CONTEXT.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-PATTERNS.md

<interfaces>
<!-- Contratos extraídos do codebase — usar diretamente, sem reexplorar. -->

AuditLog (backend/app/models/audit_log.py), colunas relevantes:
  id: int
  document_id: int | None
  action: str            # "apply"/"copy" etc.
  status: str            # "intent" | "done" | "undone" | "undone_from_cas"
  source_path: str | None
  dest_path: str | None
  run_id: str | None
  content_hash: str | None
  created_at: datetime   # mapped_column(DateTime(timezone=True))

RescanOut atual (documents.py:177-180):
  class RescanOut(BaseModel): enqueued: int

rescan() atual (documents.py:897-904):
  enqueued = await scan_and_enqueue(engine, paths); return RescanOut(enqueued=enqueued)

scan_and_enqueue (watcher.py:179) retorna `int` (só enqueued). O dedup acontece em
_stabilize_hash_gate_enqueue (watcher.py:149-159): quando IngestedOriginal já existe,
incrementa duplicate_hits e retorna False (mesmo retorno False de "já enfileirado").
Call-sites de scan_and_enqueue: watcher.py:239 (startup) e documents.py:903 (/rescan).
NÃO há terceiro call-site (o supervisor usa _handle_changes, não scan_and_enqueue).

Padrão do endpoint de leitura — get_document (documents.py:619-650):
  @router.get("/documents/{document_id}", response_model=DocumentDetailOut)
  def get_document(request, document_id: int): with get_session(engine) as session: ...
  404 HTTPException(status.HTTP_404_NOT_FOUND) quando doc é None.

Referência tz-aware correta — watcher.py:221: LAST_SCAN_AT = datetime.now(UTC) (já carrega tzinfo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper de serialização tz-aware nos *Out de documento (item 9, D-13)</name>
  <read_first>
    - backend/app/api/documents.py (DocumentOut l.73-81, DocumentDetailOut l.159-168, _build_detail l.226-264, list em l.319-360)
    - backend/app/ingest/watcher.py l.221 (padrão datetime.now(UTC))
    - backend/app/api/watcher_status.py (referência: já emite Z)
  </read_first>
  <action>
    Implementar D-13: garantir que TODO `created_at` exposto por documents.py saia UTC tz-aware. A causa-raiz (PATTERNS l.129-142): o SQLite grava `func.now()` como string naive; ao ler vem datetime sem tzinfo e o Pydantic serializa sem offset. Criar UM helper reutilizável no topo de documents.py — `_as_utc(dt: datetime) -> datetime` que faz `dt.replace(tzinfo=UTC)` quando `dt.tzinfo is None`, senão retorna `dt` inalterado (NÃO converter, só marcar). Importar `from datetime import UTC`. Aplicar `_as_utc(...)` em todos os pontos que montam `created_at`: DocumentOut na lista (l.351), DocumentDetailOut em _build_detail (l.262), e em qualquer outro `*Out.created_at` deste módulo. Preferir o helper único a um field_serializer repetido em cada classe (discrição do planner, CONTEXT l.47). NÃO alterar o frontend formatDate (paliativo rejeitado, D-13). NÃO mudar o tipo de coluna no modelo (sem migração).
  </action>
  <verify>
    <automated>cd backend && python -c "from datetime import datetime; from app.api.documents import _as_utc; assert _as_utc(datetime(2026,6,24,18,4,2)).isoformat().endswith('+00:00'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_as_utc` existe em documents.py e marca naive como UTC sem deslocar a hora (18:04 permanece 18:04, ganha +00:00).
    - Grep confirma que `_as_utc` é aplicado tanto na montagem do DocumentOut da lista quanto no _build_detail: `grep -c "_as_utc" backend/app/api/documents.py` retorna ≥ 2.
    - Nenhuma migração Alembic criada (sem alteração de schema).
  </acceptance_criteria>
  <done>created_at de /documents e /documents/{id} serializa com offset UTC; hora não muda.</done>
</task>

<task type="auto">
  <name>Task 2: scan_and_enqueue conta duplicatas puladas + RescanOut.skipped_duplicates (item 3, D-04)</name>
  <read_first>
    - backend/app/ingest/watcher.py (_stabilize_hash_gate_enqueue l.112-176, scan_and_enqueue l.179-222, startup call-site l.239)
    - backend/app/api/documents.py (RescanOut l.177-180, rescan l.897-904)
  </read_first>
  <action>
    Implementar D-04: `/rescan` passa a retornar `skipped_duplicates` além de `enqueued`. Em watcher.py, distinguir "pulado por DUPLICATA" de "pulado por outro motivo/já-enfileirado". Mínima invasão: alterar `_stabilize_hash_gate_enqueue` para retornar um literal de 3 estados em vez de bool — `"enqueued" | "duplicate" | "skipped"` (o ramo de `existing is not None` em l.155-159 retorna `"duplicate"`; o ramo de job None em l.171-174 e os early-returns de erro retornam `"skipped"`; sucesso retorna `"enqueued"`). Atualizar `scan_and_enqueue` para devolver um dataclass/NamedTuple `ScanResult(enqueued: int, skipped_duplicates: int)` em vez de `int`, acumulando os contadores conforme o literal. Atualizar o call-site de startup (l.239): hoje faz `n = await scan_and_enqueue(...)` e usa `n` num log — passar a usar `result.enqueued`. Em documents.py: `RescanOut` ganha `skipped_duplicates: int`; `rescan` desempacota `result = await scan_and_enqueue(...)` e retorna `RescanOut(enqueued=result.enqueued, skipped_duplicates=result.skipped_duplicates)`. Manter a idempotência e o `LAST_SCAN_AT = datetime.now(UTC)` ao final (l.221).
  </action>
  <verify>
    <automated>cd backend && python -c "from app.ingest.watcher import ScanResult; r=ScanResult(enqueued=2, skipped_duplicates=3); assert r.enqueued==2 and r.skipped_duplicates==3; print('ok')" && python -c "from app.api.documents import RescanOut; assert 'skipped_duplicates' in RescanOut.model_fields; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_stabilize_hash_gate_enqueue` retorna o literal e o ramo `existing is not None` retorna `"duplicate"`.
    - `scan_and_enqueue` retorna `ScanResult(enqueued, skipped_duplicates)`; ambos os call-sites (startup + /rescan) atualizados — nenhum código ainda trata o retorno como `int` (grep não acha `n = await scan_and_enqueue` usado como inteiro cru).
    - `RescanOut` tem o campo `skipped_duplicates: int`.
  </acceptance_criteria>
  <done>POST /rescan responde {enqueued, skipped_duplicates}; startup não quebra.</done>
</task>

<task type="auto">
  <name>Task 3: GET /documents/{id}/audit lendo o AuditLog (item 1, D-02)</name>
  <read_first>
    - backend/app/api/documents.py (get_document l.619-650, imports l.40-47, schemas l.73-181)
    - backend/app/models/audit_log.py (colunas)
    - backend/app/automation/undo.py l.162-183 (quais status são revertidos → base do can_undo)
  </read_first>
  <action>
    Implementar D-02: novo `GET /documents/{document_id}/audit` (read-only) que alimenta a tela de reverter (item 1). Adicionar `from app.models.audit_log import AuditLog`. Criar dois schemas Pydantic no topo do módulo: `AuditEntryOut` com `id:int, action:str, status:str, source_path:str|None, dest_path:str|None, run_id:str|None, created_at:datetime` (aplicar `_as_utc` ao created_at na montagem, da Task 1); e `DocumentAuditOut` com `items:list[AuditEntryOut]` e `can_undo:bool`. A rota: tipa `document_id: int` (sem string-building SQL), abre `with get_session(engine) as session`, faz `session.get(Document, document_id)` → 404 `HTTPException(status.HTTP_404_NOT_FOUND, detail=f"documento {document_id} não encontrado")` se None; lê `session.scalars(select(AuditLog).where(AuditLog.document_id == document_id).order_by(AuditLog.id.desc())).all()`; deriva `can_undo = any(r.status == "done" for r in rows)` (consistente com undo_document que reverte os status "done"). Registrar a rota — o sufixo `/audit` é específico, não conflita com `/{document_id}` genérico. CONVENÇÕES obrigatórias (docstring do módulo l.25-28): NUNCA logar valores; document_id tipado int. Segurança (threat model): o endpoint só LÊ AuditLog filtrado por document_id; não expõe nada fora do que já está no audit e não dispara undo. NÃO retornar paths que não estejam já persistidos no AuditLog (não construir paths a partir de input).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_documents_api.py -k "audit" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Teste novo em tests/test_documents_api.py: GET /documents/{id}/audit de doc inexistente → 404; doc com AuditLog status="done" → 200 com `can_undo == true` e itens contendo source_path/dest_path/run_id.
    - `AuditEntryOut.created_at` sai tz-aware (usa `_as_utc`).
    - `grep -c "select(AuditLog)" backend/app/api/documents.py` ≥ 1; nenhuma escrita/undo disparada pela rota.
  </acceptance_criteria>
  <done>GET /documents/{id}/audit retorna a lista de operações (origem→destino/status/run_id) e can_undo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (browser) → API FastAPI | `document_id` da URL é input não-confiável; só usado como filtro tipado (int) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | GET /documents/{id}/audit | mitigate | Só retorna colunas do AuditLog já persistidas filtradas por document_id; NÃO constrói paths a partir de input nem expõe nada fora do data dir além do que o audit já gravou |
| T-11-02 | Tampering | GET /documents/{id}/audit | mitigate | Endpoint é estritamente read-only (select); não dispara undo nem escreve — undo continua exclusivo de POST /automations/undo com seu próprio guard |
| T-11-03 | Injection | document_id na rota | mitigate | `document_id: int` tipado pelo FastAPI + `session.get`/ORM where; sem SQL string-building (docstring do módulo l.25-28) |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_documents_api.py -x -q` verde.
- Suíte completa não regride: `cd backend && python -m pytest -q`.
</verification>

<success_criteria>
- created_at da API de documentos sai tz-aware (+00:00) sem deslocar a hora.
- POST /rescan retorna {enqueued, skipped_duplicates}.
- GET /documents/{id}/audit existe, lê o AuditLog e expõe can_undo; 404 para doc ausente.
</success_criteria>

<output>
Create `.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-01-SUMMARY.md` when done
</output>
