---
phase: 11-ux-e-visibilidade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/StatusPill.tsx
  - frontend/src/pages/DocumentsPage.tsx
  - frontend/src/App.tsx
  - frontend/src/hooks/useDocuments.ts
autonomous: false
requirements:
  - "Backlog item 8 (D-10, D-11, D-12)"

must_haves:
  truths:
    - "Um documento classificado e pronto mostra 'Classificado — pronto' em vez de 'Processando'"
    - "A lista de documentos oferece uma CTA (Pré-visualizar/Aprovar) na linha do doc pronto, sem precisar abrir o detalhe"
    - "Documentos não são auto-concluídos — a CTA só atalha aprovar/pré-visualizar"
  artifacts:
    - path: "frontend/src/components/StatusPill.tsx"
      provides: "Ramo derivado processando+classificado → rótulo 'Classificado — pronto'"
      contains: "classificado"
    - path: "frontend/src/pages/DocumentsPage.tsx"
      provides: "CTA na linha do doc pronto (Pré-visualizar/Aprovar)"
      contains: "Aprovar"
  key_links:
    - from: "StatusPill.resolvePill"
      to: "rótulo derivado"
      via: "state==='processando' && lastCompletedStep==='classificado'"
      pattern: "classificado"
    - from: "CTA Aprovar"
      to: "postApprove"
      via: "mutation useApproveDocument"
      pattern: "postApprove"
---

<objective>
Item 8 (D-10/D-11/D-12): um documento bem classificado fica de propósito em PROCESSANDO + last_completed_step="classificado" (estado "pronto, aguardando ação") — mas a UI mostra "Processando", parecendo travado. Derivar um rótulo distinto "Classificado — pronto" e oferecer uma CTA na própria lista (Pré-visualizar/Aprovar). NÃO auto-concluir (D-12).

Purpose: No teste, doc 4 (template Notas Fiscais, score 1.0, campos válidos) parecia travado em "processando". Mudança de APRESENTAÇÃO + atalho, sem inventar estado persistido novo (rótulo DERIVADO).
Output: StatusPill com novo ramo + DocumentsPage com CTA na linha + App passa onNavigate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-CONTEXT.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-PATTERNS.md

<interfaces>
<!-- Extraído do codebase — usar direto. -->

StatusPill.tsx resolvePill (l.28-35): já deriva rótulo de (state, lastCompletedStep);
  ramo molde é `aguardando_extracao` (l.31-33) → { label, token }.
  PillToken = 'encontrado' | 'leitura' | 'tratado' | 'erro' | 'quarentena' (tokens TRAVADOS, sem inventar).
  STATE_PILL: processando → { label:'Processando', token:'leitura' }; concluido → token:'tratado'.

Valor literal do step: classification/stage.py:69 CLASSIFIED_STEP = "classificado";
  undo.py:49 _REOPENED_STEP = "classificado". Confirmado: "classificado".

DocumentsPage.tsx: lista filtered.map((d) => <tr> ...) (l.248-286); StatusPill em l.280
  recebe state={d.state} lastCompletedStep={d.last_completed_step}.
  DocumentOut tem last_completed_step (vem do backend, já exposto).
DocumentsPage já recebe props de App (search/status/selected/onToggle...). NÃO tem onNavigate hoje.

App.tsx: navegação por `setPage` (useState<Page>, l.34); Sidebar onNavigate={setPage};
  DryRunPage em page==='dryrun' (l.103). DocumentsPage renderizada com props (l.75-84).

api.ts: postApprove(id) (l.131) → DocumentDetail; postDryRun(ids) (l.286).
useDocuments.ts: padrão de mutation useRescan (l.33-42) com qc.invalidateQueries no onSuccess.
Convenções: tokens --st-* travados; valores TEXTO PURO; zero npm novo; rótulo DERIVADO (sem estado persistido novo).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ramo derivado "Classificado — pronto" no StatusPill (D-10)</name>
  <read_first>
    - frontend/src/components/StatusPill.tsx (resolvePill l.28-35, STATE_PILL l.20-27, PillToken l.14)
    - backend/app/classification/stage.py l.69 (CLASSIFIED_STEP="classificado")
  </read_first>
  <action>
    Implementar D-10. Em `resolvePill` (StatusPill.tsx l.28-35), ANTES do `return STATE_PILL[state]`, adicionar um ramo: `if (state === 'processando' && lastCompletedStep === 'classificado') return { label: 'Classificado — pronto', token: 'tratado' }`. Usar um token EXISTENTE da paleta travada — `tratado` (verde, "pronto/concluído visualmente") é o mais adequado; NÃO inventar token novo nem estado persistido novo (rótulo é DERIVADO, code_context PATTERNS l.230). Manter o ramo `aguardando_extracao` existente intacto. O literal "classificado" deve casar exatamente com CLASSIFIED_STEP do backend.
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -c "classificado" frontend/src/components/StatusPill.tsx</automated>
  </verify>
  <acceptance_criteria>
    - resolvePill retorna label "Classificado — pronto" para (processando, classificado) usando um token de PillToken existente (não inventa token).
    - `grep -c "'classificado'" frontend/src/components/StatusPill.tsx` ≥ 1.
    - `npm run build` verde.
  </acceptance_criteria>
  <done>Doc processando+classificado mostra "Classificado — pronto", não "Processando".</done>
</task>

<task type="auto">
  <name>Task 2: CTA na linha (Pré-visualizar/Aprovar) + onNavigate + useApproveDocument (D-11/D-12)</name>
  <read_first>
    - frontend/src/pages/DocumentsPage.tsx (linha da tabela l.248-286, StatusPill l.280, props da função no topo)
    - frontend/src/App.tsx (l.34 setPage, l.75-84 render de DocumentsPage)
    - frontend/src/hooks/useDocuments.ts (useRescan l.33-42 como molde de mutation)
    - frontend/src/lib/api.ts (postApprove l.131)
  </read_first>
  <action>
    Implementar D-11/D-12. (1) Em useDocuments.ts criar `useApproveDocument` copiando o molde de useRescan: `mutationFn: (id:number) => postApprove(id)`, `onSuccess` invalida `['documents']` e `['document-detail', id]`. (2) Adicionar prop opcional `onNavigate?: (page: 'dryrun') => void` à assinatura de DocumentsPage e repassar de App.tsx: na render de DocumentsPage (App l.75-84) passar `onNavigate={setPage}` (o tipo Page já inclui 'dryrun'). (3) Na linha da tabela (l.248-286), quando o doc estiver "pronto" (mesma condição derivada: `d.state === 'processando' && d.last_completed_step === 'classificado'`), renderizar uma CTA com dois botões pequenos no estilo `row-action`/`btn-primary`: "Pré-visualizar" → `onNavigate?.('dryrun')`, e "Aprovar" → `approve.mutate(d.id)` (da mutation nova). NÃO auto-concluir nada (D-12): a CTA só dispara aprovar (que o backend processa) ou navega para o dry-run. Manter valores TEXTO PURO; zero npm novo. Reusar uma constante/helper local para a condição "pronto" se já houver derivação no StatusPill (evitar duplicar o literal — pode importar uma função utilitária ou só repetir a condição com comentário apontando para CLASSIFIED_STEP).
  </action>
  <verify>
    <automated>cd frontend && npm run build && grep -c "postApprove\|useApproveDocument" frontend/src/hooks/useDocuments.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useApproveDocument` existe em useDocuments.ts e usa postApprove + invalidateQueries.
    - DocumentsPage aceita `onNavigate` e App passa `setPage`; `grep -c "onNavigate" frontend/src/pages/DocumentsPage.tsx frontend/src/App.tsx` mostra a prop em ambos.
    - A linha de doc "pronto" renderiza botões "Pré-visualizar" e "Aprovar"; nenhum auto-conclude foi adicionado (grep não encontra chamada automática de approve em useEffect/render).
    - `npm run build` verde.
  </acceptance_criteria>
  <done>Doc pronto exibe CTA Pré-visualizar/Aprovar na própria lista; Pré-visualizar navega ao dry-run, Aprovar dispara approve.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Rótulo derivado "Classificado — pronto" no StatusPill (token verde existente) e CTA na linha da lista de Documentos: "Pré-visualizar" (navega ao dry-run) e "Aprovar" (dispara POST approve), para docs em processando+classificado. Sem auto-conclusão (D-12).</what-built>
  <how-to-verify>
    1. Abrir a aba Documentos com um doc bem classificado (ex.: o doc 4 Notas Fiscais do teste, score 1.0).
    2. Confirmar que a pílula de status mostra "Classificado — pronto" (verde), NÃO "Processando".
    3. Confirmar que a linha desse doc mostra os botões "Pré-visualizar" e "Aprovar".
    4. Clicar "Pré-visualizar" → deve navegar para a tela de Pré-visualização (dry-run).
    5. Voltar, clicar "Aprovar" no doc pronto → o doc deve concluir (status muda) e a lista atualizar.
    6. Confirmar que docs em outros estados (na fila/processando-real/quarentena) NÃO mostram a CTA nem o rótulo "pronto".
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que ficou errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API (approve) | A CTA "Aprovar" chama POST approve já existente, com seu próprio guard de estado no backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Elevation of Privilege | CTA Aprovar na lista | accept | Reusa POST /documents/{id}/approve existente (guard de pré-condição de estado no backend); a UI só atalha, não burla autorização |
| T-11-07 | Tampering | rótulo derivado | mitigate | Rótulo é DERIVADO de (state, last_completed_step) já expostos; nenhum estado persistido novo, sem alterar a máquina de estados (D-12) |
</threat_model>

<verification>
- `cd frontend && npm run build` verde.
- Verificação visual humana (checkpoint).
</verification>

<success_criteria>
- Doc processando+classificado mostra "Classificado — pronto" + CTA Pré-visualizar/Aprovar na lista.
- Sem auto-conclusão; demais estados inalterados.
</success_criteria>

<output>
Create `.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-03-SUMMARY.md` when done
</output>
