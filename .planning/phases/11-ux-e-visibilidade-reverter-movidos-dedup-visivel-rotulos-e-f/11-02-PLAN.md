---
phase: 11-ux-e-visibilidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/AutomationsPage.tsx
autonomous: false
requirements:
  - "Backlog item 4 (D-07, D-08, D-09)"

must_haves:
  truths:
    - "O nome do campo na condição 'Valor de campo' é escolhido num dropdown estrito dos campos do template, não digitado"
    - "Quando não há template determinável na automação, a condição 'Valor de campo' é bloqueada com aviso, sem fallback de texto livre"
  artifacts:
    - path: "frontend/src/pages/AutomationsPage.tsx"
      provides: "<select> de campo + guard de template determinável na condição field"
      contains: "activeTemplate"
  key_links:
    - from: "condição field (isField)"
      to: "activeTemplate.fields"
      via: "<select> populado por .map"
      pattern: "activeTemplate.fields.map"
---

<objective>
Item 4 (D-07/D-08): trocar o `<input>` de texto livre do nome do campo na condição "Valor de campo" por um `<select>` estrito dos campos do template referenciado pela automação, e bloquear a condição com aviso claro quando não houver template determinável. Elimina o off-by-nome silencioso (digitar o nome exato de memória).

Purpose: O atrito real do teste de usuário foi digitar o nome EXATO do campo; um typo faz a condição nunca casar sem aviso. O `<select>` mata isso na origem.
Output: AutomationsPage.tsx com `<select>` de campo + guard D-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-CONTEXT.md
@.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-PATTERNS.md

<interfaces>
<!-- Extraído de frontend/src/pages/AutomationsPage.tsx — usar direto. -->

CondDraft tem: field, operator, value, field_name (l.91, l.112, l.130).
isField = c.field === 'field' (l.780).
Input free-text atual (l.822-830): <input placeholder="nome do campo" value={c.field_name}
  onChange={(e) => patchCond(c.key, { field_name: e.target.value })} />.
activeTemplate (l.449-454): derivado da condição field==='template' com value preenchido;
  é Template | null. Template.fields é {id, name, hint, ...}[] (types.ts:114-130, shape {name, hint}).
patchCond(key, patch) (l.475) aplica patch parcial à condição.
validate(d) (l.563-589): hoje exige `c.field === 'field' && !c.field_name.trim()` → erro (l.569-571).
Padrão de <select> a copiar — select de template logo abaixo (l.843-856): className="select".
Padrão de aviso "sem template" — bloco nochip-box (l.675-683 em renderTokenBar).
Painel "Campos do template" (l.704-729) já lista activeTemplate.fields.map — mesma fonte.
Convenções: valores como TEXTO PURO (0 dangerouslySetInnerHTML); zero npm novo (code-and-config).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: <select> estrito de campo + guard D-08 na condição "Valor de campo"</name>
  <read_first>
    - frontend/src/pages/AutomationsPage.tsx (isField l.780, input free-text l.822-830, activeTemplate l.449-454, select de template l.843-856, painel campos l.704-729, nochip-box l.675-683, validate l.563-589)
    - frontend/src/types.ts l.114-130 (shape de Template.fields)
  </read_first>
  <action>
    Implementar D-07/D-08. Onde hoje há `{isField && (<input placeholder="nome do campo" .../>)}` (l.822-830), substituir por: quando `activeTemplate` existe, renderizar um `<select className="select">` (mesmo estilo do select de template em l.843-856) com `value={c.field_name}`, `onChange={(e) => patchCond(c.key, { field_name: e.target.value })}`, uma `<option value="">Escolha um campo…</option>` e `activeTemplate.fields.map((f) => <option key={f.id} value={f.name}>{f.name}</option>)`. Quando `activeTemplate == null`, NÃO renderizar input nenhum — renderizar um aviso no estilo `nochip-box` (molde de l.675-683): texto "Escolha um template na condição \"Tipo de documento\" para comparar um campo." (TEXTO PURO, sem dangerouslySetInnerHTML). Isto é o guard D-08: sem template determinável, a condição é desabilitada/avisada, SEM fallback de texto livre nem autocomplete global. Atualizar `validate` (l.563-589): manter a exigência de `field_name` preenchido para `field==='field'`, e ACRESCENTAR que `field==='field'` exige `activeTemplate` determinável (se `activeTemplate == null` e existe condição field, retornar erro claro impedindo salvar). Zero npm novo.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activeTemplate.fields.map" frontend/src/pages/AutomationsPage.tsx` ≥ 1 dentro do ramo isField (o select de campo usa os campos do template).
    - Não há mais `<input placeholder="nome do campo"` no arquivo.
    - `validate` bloqueia salvar quando há condição field sem activeTemplate (grep mostra checagem de activeTemplate na função validate).
    - `npm run build` verde (gate de drift de tipos).
  </acceptance_criteria>
  <done>Condição "Valor de campo" usa dropdown dos campos do template; sem template → aviso e bloqueio, sem texto livre.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>O `<select>` estrito dos campos do template na condição "Valor de campo" do construtor de automações, e o aviso de bloqueio quando não há template determinável (D-07/D-08). D-09 (sinalizar campo não extraído no dry-run) foi avaliado e NÃO incluído nesta fase — o select já elimina o off-by-nome na origem (ver SUMMARY).</what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev` (ou abrir a instância rodando) → aba Automações.
    2. Criar/editar uma automação. No bloco "Quando rodar", adicionar condição "Tipo de documento" e escolher um template. Adicionar condição "Valor de campo".
    3. Confirmar que o nome do campo agora é um DROPDOWN com os campos daquele template (não um campo de digitar).
    4. Remover/limpar a condição "Tipo de documento". Confirmar que a condição "Valor de campo" mostra o aviso "Escolha um template na condição 'Tipo de documento'…" e NÃO oferece input de texto livre, e que salvar é bloqueado.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que ficou errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (form) → estado local React | Mudança puramente de UI; o backend já valida field_name na avaliação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | <select> de campo | accept | O valor do select é restrito aos campos do template; o backend (rules.py) já trata campo ausente como falso-fechado — nenhuma superfície nova de injeção |
| T-11-05 | Information Disclosure | render do nome do campo | mitigate | Valores renderizados como TEXTO PURO (sem dangerouslySetInnerHTML), preservando a convenção do projeto |
</threat_model>

<verification>
- `cd frontend && npm run build` verde.
- Verificação visual humana (checkpoint) confirma select + guard.
</verification>

<success_criteria>
- Nome do campo na condição "Valor de campo" é escolhido por dropdown estrito.
- Sem template determinável → aviso + bloqueio de salvar, sem fallback de texto.
</success_criteria>

<output>
Create `.planning/phases/11-ux-e-visibilidade-reverter-movidos-dedup-visivel-rotulos-e-f/11-02-SUMMARY.md` when done.
Registrar no SUMMARY a decisão sobre D-09 (sinalizar campo não-extraído no dry-run): avaliado como NÃO necessário nesta fase porque o `<select>` estrito elimina o off-by-nome na origem; se o usuário pedir, vira item de uma fase futura de dry-run (relacionado ao item 12 deferido).
</output>
