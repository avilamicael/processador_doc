---
phase: 05-confian-a-revis-o-humana-e-quarentena
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - backend/app/api/documents.py
  - backend/app/api/config.py
  - backend/app/config.py
  - backend/app/main.py
  - backend/tests/test_api_review.py
  - backend/tests/test_api_config.py
autonomous: true
requirements: [REV-02, REV-03, REV-04, REV-05]
must_haves:
  truths:
    - "POST /documents/{id}/retry faz transition(FALHA→PROCESSANDO) e reenfileira o step apropriado por last_completed_step; doc não-FALHA → 409"
    - "POST /documents/{id}/reclassify recebe {template_id}, valida que o template existe, APAGA o ClassificationResult de quarentena, faz transition(QUARENTENA→PROCESSANDO) e requeue_step do classify com forced_template_id (D-09); doc não-QUARENTENA → 409"
    - "PATCH /documents/{id}/fields/{field_name} revalida via validate_field, marca manually_corrected=True, recalcula confidence_score no mesmo commit, NÃO chama IA (D-08)"
    - "POST /documents/{id}/approve faz transition(EM_REVISAO→CONCLUIDO) só quando todos os obrigatórios estão válidos (D-07); obrigatório inválido → 409"
    - "GET /documents/attention devolve os 3 baldes (FALHA/QUARENTENA/EM_REVISAO) com motivo e, para EM_REVISAO, os campos editáveis, num payload só (evita N+1 — Open Q3)"
    - "GET/PUT /config/review-threshold lê e persiste o limiar global de confiança (D-03/REV-02); PUT valida faixa [0,1] e refaz o cache de settings"
  artifacts:
    - path: "backend/app/api/documents.py"
      provides: "5 rotas novas: retry, reclassify, patch field, approve, GET attention"
      contains: "/attention"
    - path: "backend/app/api/config.py"
      provides: "GET/PUT /config/review-threshold (limiar global, D-03)"
      contains: "review-threshold"
    - path: "backend/tests/test_api_review.py"
      provides: "Cobertura dos 4 endpoints de ação + guards de allowlist + sem-IA no patch"
      min_lines: 60
  key_links:
    - from: "backend/app/api/documents.py"
      to: "transition"
      via: "todos os endpoints de ação usam transition (allowlist é o guard); InvalidTransition → 409"
      pattern: "except InvalidTransition"
    - from: "backend/app/api/documents.py"
      to: "validate_field"
      via: "patch de campo revalida sem IA"
      pattern: "validate_field\\("
    - from: "backend/app/api/documents.py"
      to: "repo.requeue_step"
      via: "reclassify reenfileira classify com forced_template_id"
      pattern: "requeue_step"
    - from: "backend/app/api/config.py"
      to: "review_confidence_threshold"
      via: "GET lê get_settings(); PUT persiste no .env e limpa o cache"
      pattern: "review_confidence_threshold"
---

<objective>
API de triagem em `api/documents.py`: as 4 ações (retry, reclassify-com-template, patch de campo, approve) — cada uma com a allowlist como guard via `transition` — e o `GET /documents/attention` dedicado que monta os 3 baldes num payload só (Open Q3, evita N+1). Mais o endpoint de config do limiar global (REV-02/D-03) que a UI S6 edita, e a cobertura de teste integrada.

Purpose: É a superfície que a UI (Plan 04) consome para resolver cada documento parado e para definir o limiar. Sem esses endpoints não há como aprovar, corrigir, reclassificar, reprocessar nem ajustar o limiar.
Output: 5 rotas em documents.py + GET/PUT /config/review-threshold + registro no main + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-CONTEXT.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-RESEARCH.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-01-SUMMARY.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-02-SUMMARY.md

<interfaces>
<!-- Extraídos do código atual. Use diretamente. -->

api/documents.py (backend/app/api/documents.py):
  - router = APIRouter(tags=["documents"]); padrão: engine = request.app.state.engine; with get_session(engine) as session
  - get_document (linhas 174-248): molde de montagem do DocumentDetailOut + bloco classification (join Template p/ nome; FilledField → ClassificationFieldOut)
  - DocumentDetailOut/ClassificationOut/ClassificationFieldOut já definidos (linhas 67-99) — ESTENDER ClassificationOut com confidence_score; ClassificationFieldOut com manually_corrected
  - 404 via HTTPException(status.HTTP_404_NOT_FOUND, ...)
  - list_documents (linhas 114-160): molde de query + counts

transition + InvalidTransition:
  from app.pipeline.state_machine import transition
  from app.pipeline.states import InvalidTransition
  transition(session, doc, DocState.X) — comita; rollback + raise InvalidTransition em par fora da allowlist
  allowlist: FALHA→PROCESSANDO; QUARENTENA→PROCESSANDO; EM_REVISAO→CONCLUIDO

repo (Plan 02): from app.queue import repo ; repo.requeue_step(session, content_hash=..., step="classify", payload=json.dumps({...})); repo.enqueue(...) existente
  steps via worker: EXTRACT_STEP="extract" / CLASSIFY_STEP="classify"; EXTRACTED_STEP="extraido"; AWAITING_EXTRACTION_STEP="aguardando_extracao"

validate_field (backend/app/validation/fields.py linhas 64-70):
  validate_field(*, field_type, raw, required=False, regex=None) -> FieldValidation(valid, raw_value, normalized_value, invalid_reason)

compute_confidence (Plan 01): compute_confidence(filled_fields, template_fields) -> (score, has_invalid_required)

config (backend/app/config.py): get_settings() é @lru_cache (linha 207); Settings é BaseSettings com env_file=".env" (linhas 43-47); review_confidence_threshold é Field do Plan 01
main.py (backend/app/main.py linhas 79-81): app.include_router(...) — registrar o novo config router

models: Document(.state, .content_hash, .last_completed_step, .original_filename); ClassificationResult(.document_id UNIQUE, .template_id, .confidence, .confidence_score, .filled_fields); FilledField(.field_name, .raw_value, .normalized_value, .valid, .invalid_reason, .manually_corrected); Template(.fields); TemplateField(.name, .field_type, .required, .regex)
DocState: FALHA, QUARENTENA, EM_REVISAO, PROCESSANDO, CONCLUIDO
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 endpoints de ação (retry/reclassify/patch/approve) com allowlist como guard</name>
  <files>backend/app/api/documents.py, backend/tests/test_api_review.py</files>
  <read_first>
    - backend/app/api/documents.py (ARQUIVO INTEIRO — molde get_document linhas 174-248; padrão engine/get_session; schemas Pydantic linhas 48-110)
    - backend/app/api/templates.py linhas 57-98 e 219-226 (schemas In Pydantic; IntegrityError→409)
    - backend/app/validation/fields.py linhas 64-75 (validate_field assinatura/retorno)
    - backend/app/pipeline/state_machine.py + states.py (transition + InvalidTransition + allowlist)
    - backend/app/queue/repo.py (requeue_step do Plan 02 + enqueue)
    - backend/app/queue/worker.py linhas 49-54 (constantes EXTRACT_STEP/CLASSIFY_STEP), linha 35 (EXTRACTED_STEP), linha 42 (AWAITING_EXTRACTION_STEP)
    - backend/tests/test_api_documents.py (fixture client sobre schema_engine linhas 35-43; _seed)
    - backend/tests/classification/conftest.py (respx mock OpenAI — para provar call_count==0 no patch)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seção api/documents.py linhas 178-259 — molde dos 4 endpoints, patch, reclassify-apaga-CR)
  </read_first>
  <behavior>
    - approve em doc EM_REVISAO com todos obrigatórios válidos → 200, doc vira CONCLUIDO
    - approve em doc EM_REVISAO com algum obrigatório inválido → 409 "corrija os campos obrigatórios inválidos antes de aprovar"; estado inalterado
    - approve em doc não-EM_REVISAO (ex.: QUARENTENA) → 409 (InvalidTransition)
    - patch de campo inválido→válido: valid vira True, normalized_value preenchido, manually_corrected=True, cr.confidence_score recalculado no mesmo commit, OpenAI call_count==0 (respx)
    - patch desbloqueia approve: após corrigir o último obrigatório inválido, approve passa (200)
    - reclassify em doc QUARENTENA com template_id válido → 200; CR de quarentena apagado; doc vira PROCESSANDO; job classify reenfileirado (requeue_step rowcount>=1 ou enqueue fallback)
    - reclassify com template_id inexistente → 404/422 (template não encontrado), sem mudar estado
    - reclassify em doc não-QUARENTENA → 409
    - retry em doc FALHA → 200, vira PROCESSANDO, job do step apropriado reenfileirado
    - retry em doc não-FALHA → 409 (InvalidTransition)
  </behavior>
  <action>
    Estender `ClassificationOut` com `confidence_score: float | None` e `ClassificationFieldOut` com `manually_corrected: bool`; popular ambos em get_document e nos retornos de detalhe.

    Adicionar imports: `transition`, `InvalidTransition`, `repo`, `validate_field`, `compute_confidence`, `json`, `Template`/`TemplateField` conforme necessário.

    Criar helper interno `_has_invalid_required(session, cr) -> bool`: junta os FilledField do cr com os TemplateField obrigatórios do cr.template_id; retorna True se algum obrigatório está com valid=False ou ausente. Re-deriva a validade ATUAL (não confia no score persistido — Pitfall 4). Extrair também `_build_detail(session, doc, folder_path)` da montagem de get_document para reuso nos retornos de ação.

    `POST /documents/{document_id}/approve` → response_model=DocumentDetailOut: 404 se doc ausente; busca CR; GUARD D-07: `cr is None or _has_invalid_required(...)` → 409; `transition(session, doc, DocState.CONCLUIDO)` em try/except InvalidTransition→409; retornar detalhe.

    `PATCH /documents/{document_id}/fields/{field_name}` → schema `FieldPatchIn(raw_value: str | None)`: busca cr (404), busca FilledField por (classification_result_id, field_name) (404), busca o TemplateField correspondente (join template→fields por name); `v = validate_field(field_type=tf.field_type, raw=body.raw_value, required=tf.required, regex=tf.regex)`; atualiza ff.raw_value/normalized_value/valid/invalid_reason; `ff.manually_corrected = True`; recalcula `cr.confidence_score, _ = compute_confidence(cr.filled_fields, list(template.fields))`; `session.commit()`. NÃO chama OpenAI. Doc permanece EM_REVISAO (sem transition).

    `POST /documents/{document_id}/reclassify` → schema `ReclassifyIn(template_id: int)`: 404 doc; valida `session.get(Template, body.template_id)` (None → 404/422 "template não encontrado"); busca o CR de quarentena e `session.delete(cr)` ANTES (cascade limpa FilledFields — Pitfall 3); `transition(session, doc, DocState.PROCESSANDO)` em try/except→409; `repo.requeue_step(session, content_hash=doc.content_hash, step=CLASSIFY_STEP, payload=json.dumps({"content_hash": doc.content_hash, "forced_template_id": body.template_id}))`; se rowcount==0, fallback `repo.enqueue(... payload com forced_template_id)`; retornar detalhe.

    `POST /documents/{document_id}/retry`: 404 doc; `transition(session, doc, DocState.PROCESSANDO)` em try/except→409; decidir o step por `doc.last_completed_step`: == EXTRACTED_STEP → classify; senão (AWAITING_EXTRACTION_STEP ou None) → extract; reenfileirar via `repo.requeue_step(... payload={"content_hash": doc.content_hash})` com fallback enqueue. Retornar detalhe.

    Preencher `test_api_review.py` (remover skips) cobrindo TODOS os casos do bloco behavior. Use a fixture `client` sobre schema_engine e o respx mock para provar call_count==0 no patch. Semear docs em cada estado-alvo com CR/FilledField/Template apropriados.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_api_review.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/documents/{document_id}/approve\|/documents/{document_id}/reclassify\|/documents/{document_id}/retry\|/documents/{document_id}/fields/{field_name}" backend/app/api/documents.py` mostra as 4 rotas
    - `grep -n "except InvalidTransition" backend/app/api/documents.py` aparece nos endpoints de transição
    - `grep -n "manually_corrected\|confidence_score" backend/app/api/documents.py` mostra a marca e o recálculo no patch + nos schemas Out
    - `grep -n "validate_field(" backend/app/api/documents.py` no patch; NENHUMA chamada a openai_client/disambiguate/fill_missing no patch
    - `cd backend && uv run pytest tests/test_api_review.py -x -q` verde, incluindo o assert de OpenAI call_count==0 no patch
  </acceptance_criteria>
  <done>Os 4 endpoints existem, usam a allowlist como guard (409 em transição ilegal), o patch revalida sem IA e recalcula o score, e o approve respeita o guard D-07; testes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: GET /documents/attention dedicado (3 baldes + motivo + campos editáveis)</name>
  <files>backend/app/api/documents.py, backend/tests/test_api_review.py</files>
  <read_first>
    - backend/app/api/documents.py (list_documents linhas 114-160 — molde de query + counts; get_document linhas 206-248 — montagem do bloco classification/fields)
    - backend/app/models/document.py (campos: state, last_completed_step, original_filename; confirmar se há coluna de motivo/erro de FALHA — senão usar fallback de copy)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-UI-SPEC.md (Copywriting: motivos por balde; colunas de campos S4)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (Pitfall 5 / Open Q3 — endpoint dedicado evita N+1)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-RESEARCH.md (Open Question 3 linhas 522-525)
  </read_first>
  <behavior>
    - GET /documents/attention retorna um objeto com 3 listas: falha, quarentena, em_revisao + contagens por balde
    - Cada item de FALHA traz id, original_filename, motivo (mensagem de erro persistida ou fallback do UI-SPEC)
    - Cada item de QUARENTENA traz id, original_filename, motivo ("Nenhum template casou…")
    - Cada item de EM_REVISAO traz id, original_filename, confidence_score, e os campos (field_name/raw/normalized/valid/invalid_reason/manually_corrected) — payload único, sem GET /documents/{id} por item
    - Documentos em PROCESSANDO/CONCLUIDO/RECEBIDO NÃO aparecem
  </behavior>
  <action>
    Adicionar `GET /documents/attention` → schema `AttentionOut` com 3 listas tipadas (`falha: list[AttentionItemOut]`, `quarentena: list[AttentionItemOut]`, `em_revisao: list[ReviewItemOut]`) + contagens. `AttentionItemOut`: id, original_filename, motivo (str | None). `ReviewItemOut` estende com `confidence_score: float | None` e `fields: list[ClassificationFieldOut]` (reusar o schema estendido na Task 1). Consultar Documents filtrando por `state.in_([FALHA, QUARENTENA, EM_REVISAO])`; para EM_REVISAO, eager-load/join do CR + FilledFields num passo (evitar N+1: `selectinload`/join, não loop de GET por id). Motivo de QUARENTENA: texto fixo do UI-SPEC. Motivo de FALHA: mensagem persistida se existir; senão o fallback do UI-SPEC. REGISTRAR esta rota ANTES de `GET /documents/{document_id}` no arquivo (defensivo contra captura de path).

    Estender `test_api_review.py`: semear 1 doc em cada balde + 1 doc PROCESSANDO/CONCLUIDO; assert que o payload traz os 3 baldes corretos, que EM_REVISAO inclui os campos editáveis, e que docs fora dos 3 estados não aparecem.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_api_review.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/documents/attention" backend/app/api/documents.py` presente, registrado ANTES de get_document(/documents/{document_id})
    - O payload de EM_REVISAO inclui confidence_score e a lista de campos (assertado no teste)
    - O teste prova que um doc PROCESSANDO/CONCLUIDO NÃO aparece em nenhum balde
    - Sem N+1: a montagem de EM_REVISAO usa join/selectinload (grep), não loop de get_document
    - `cd backend && uv run pytest tests/test_api_review.py -x -q` verde
  </acceptance_criteria>
  <done>GET /documents/attention devolve os 3 baldes num payload, com campos editáveis para EM_REVISAO e sem N+1; teste verde.</done>
</task>

<task type="auto">
  <name>Task 3: GET/PUT /config/review-threshold (limiar global, REV-02/D-03) + registro no main</name>
  <files>backend/app/api/config.py, backend/app/config.py, backend/app/main.py, backend/tests/test_api_config.py</files>
  <read_first>
    - backend/app/config.py linhas 35-47 (Settings BaseSettings, env_file=".env") + linhas 201-210 (get_settings @lru_cache); review_confidence_threshold do Plan 01
    - backend/app/api/watched_folders.py (molde de router fino com schema In/Out Pydantic + 422 de validação)
    - backend/app/main.py linhas 79-81 (include_router)
    - backend/tests/test_api_documents.py (fixture client / TestClient sobre app)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-UI-SPEC.md (S6: campo numérico 0-100% + hint; copy "Limiar de confiança")
  </read_first>
  <behavior>
    - GET /config/review-threshold → 200 com {threshold: 0.8} (valor efetivo atual de get_settings())
    - PUT /config/review-threshold body {threshold: 0.7} → 200, persiste; GET seguinte reflete 0.7
    - PUT com threshold fora de [0.0, 1.0] → 422 (validação Pydantic ge=0 le=1)
    - PUT limpa o cache de get_settings para que o stage leia o novo valor (sem reiniciar o processo)
  </behavior>
  <action>
    Criar `backend/app/api/config.py` com `router = APIRouter(tags=["config"])`. `GET /config/review-threshold` → schema `ReviewThresholdOut(threshold: float)` lendo `get_settings().review_confidence_threshold`. `PUT /config/review-threshold` → schema `ReviewThresholdIn(threshold: float = Field(ge=0.0, le=1.0))`: persistir o valor escrevendo/atualizando a chave `REVIEW_CONFIDENCE_THRESHOLD=<valor>` no arquivo `.env` (mesmo `.env` que `SettingsConfigDict(env_file=".env")` usa — ler o conteúdo atual, substituir/append a linha da chave, regravar de forma atômica), depois `get_settings.cache_clear()` para invalidar o lru_cache; retornar o valor efetivo relido. NÃO usar string-building de SQL (sem DB aqui). O limiar é config global (D-03), não por-template.

    Registrar o router em `main.py` com `app.include_router(config_api.router)` ao lado dos demais.

    Criar `backend/tests/test_api_config.py` cobrindo os 4 casos do bloco behavior (use um `.env` temporário/monkeypatch do caminho ou um tmp_path para não poluir o `.env` real; limpe o cache no teardown).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_api_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "review-threshold" backend/app/api/config.py` mostra GET e PUT
    - `grep -n "cache_clear" backend/app/api/config.py` presente no PUT (invalida lru_cache)
    - `grep -n "config_api\|api.config\|from app.api import config" backend/app/main.py` mostra o router registrado
    - PUT com threshold=1.5 retorna 422 (assertado no teste)
    - `cd backend && uv run pytest tests/test_api_config.py -x -q` verde
  </acceptance_criteria>
  <done>O limiar global é legível e editável por API, persiste no .env e invalida o cache; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → API | Bodies de ação (template_id, raw_value, threshold) e path ids (document_id, field_name) são input do operador |
| API → state machine / fila / .env | Mutação de estado/fila via transition/repo; persistência do limiar no .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | reclassify força template_id arbitrário/inexistente (D-09) | mitigate | `session.get(Template, template_id)` None → 404/422 antes de qualquer transição; template_id tipado int |
| T-05-08 | Tampering | Mass-assignment no patch de campo (alterar campo de outro doc / campos arbitrários) | mitigate | FilledField buscado por (classification_result_id do doc-alvo, field_name); só o campo do documento da rota é alterado; schema FieldPatchIn só expõe raw_value (D-08) |
| T-05-09 | Tampering | Transição de estado ilegal forçada por endpoint (ex.: approve em QUARENTENA) | mitigate | `transition` valida allowlist + rollback; InvalidTransition→409 em retry/reclassify/approve |
| T-05-10 | Elevation | Approve de doc com obrigatório inválido (burla o gate D-07) | mitigate | `_has_invalid_required` re-deriva validade ATUAL dos FilledField antes do transition; 409 (Pitfall 4) |
| T-05-11 | (custo) | Re-chamar IA no patch de campo gastaria tokens (D-08) | mitigate | Patch usa SÓ validate_field; teste prova respx call_count==0 |
| T-05-12 | Information Disclosure | Valores extraídos (CNPJ/CPF) em logs | mitigate | Endpoints não logam valores (padrão T-04-11); valores só no corpo da resposta |
| T-05-13 | Tampering/XSS | field_name concatenado em SQL / valores como HTML | mitigate | field_name em where(... == field_name) parametrizado; ids tipados int; render texto puro é responsabilidade do Plan 04 |
| T-05-14 | Tampering | Esta fase dispara automação de arquivo (Fase 6) por engano | mitigate | NENHUM endpoint move/renomeia arquivo; approve só transita estado; confirmar ausência de import de módulo de automação de arquivo |
| T-05-15 | Input Validation/DoS | PUT threshold fora de faixa corrompe o roteamento de revisão | mitigate | Schema Pydantic ge=0.0 le=1.0 → 422; .env reescrito de forma controlada (chave única), nunca interpolando input em SQL/shell |

Nenhuma ameaça `high` residual — todas as mutações passam por guards (transition/validate_field/get(Template)/Pydantic range).
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_api_review.py tests/test_api_config.py -x -q` verde
- `cd backend && uv run pytest -q` (suite completa) verde
- Guards: 409 em transição ilegal; 409 approve com obrigatório inválido; patch sem IA (call_count==0); 422 limiar fora de faixa
- Nenhuma automação de arquivo disparada
</verification>

<success_criteria>
- Visão de triagem alimentada por GET /documents/attention sem N+1 (REV-03)
- Aprovar/corrigir antes de automação; correção marcada manual; approve→CONCLUIDO com guard (REV-04)
- Quarentena resolúvel: atribuir template + reclassificar via fila (REV-05)
- Limiar global legível/editável por API (REV-02/D-03)
</success_criteria>

<output>
Create `.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-03-SUMMARY.md` when done
</output>
