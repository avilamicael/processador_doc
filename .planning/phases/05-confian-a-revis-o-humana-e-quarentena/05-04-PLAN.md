---
phase: 05-confian-a-revis-o-humana-e-quarentena
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - frontend/src/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useAttention.ts
  - frontend/src/components/ConfidenceBadge.tsx
  - frontend/src/pages/AttentionPage.tsx
  - frontend/src/pages/ConfigPage.tsx
  - frontend/src/components/Sidebar.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [REV-02, REV-03, REV-04, REV-05]
must_haves:
  truths:
    - "Existe uma página 'Precisam de atenção' navegável pela Sidebar que lista os 3 baldes (Falhas/Quarentena/Em revisão) com contagem, motivo e a ação leve de cada item, via polling TanStack Query (padrão useDocuments)"
    - "Item FALHA tem 'Tentar de novo'; item QUARENTENA tem select 'Atribuir template' + 'Reclassificar' (desabilitado até escolher); item EM_REVISAO mostra ConfidenceBadge + campos com correção inline + 'Aprovar documento' (desabilitado enquanto houver obrigatório inválido)"
    - "ConfidenceBadge deriva o rótulo (Alta/Média/Baixa) e a cor do score 0-100% pelas faixas TRAVADAS do 05-UI-SPEC, com o número em mono; reutilizável em S4 e no DocumentDetailModal"
    - "A página de Configurações tem o campo S6 'Limiar de confiança' (0-100%) que lê/salva via /config/review-threshold (D-03)"
    - "Após cada ação (retry/reclassify/patch/approve) as queries ['attention'] e ['documents'] são invalidadas; valores de campo renderizados como texto puro (sem dangerouslySetInnerHTML)"
  artifacts:
    - path: "frontend/src/pages/AttentionPage.tsx"
      provides: "Visão 'Precisam de atenção' com 3 baldes e ações leves (S1-S4)"
      min_lines: 80
    - path: "frontend/src/components/ConfidenceBadge.tsx"
      provides: "Indicador de confiança reutilizável (S5)"
      min_lines: 15
    - path: "frontend/src/hooks/useAttention.ts"
      provides: "useAttentionDocuments + mutations retry/reclassify/patch/approve"
      exports: ["useAttentionDocuments"]
  key_links:
    - from: "frontend/src/hooks/useAttention.ts"
      to: "frontend/src/lib/api.ts"
      via: "getAttention + postRetry/postReclassify/patchField/postApprove"
      pattern: "getAttention|postApprove"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/AttentionPage.tsx"
      via: "rota da página 'atencao' renderizada"
      pattern: "AttentionPage"
    - from: "frontend/src/pages/ConfigPage.tsx"
      to: "/config/review-threshold"
      via: "campo S6 lê/salva o limiar"
      pattern: "review.?threshold|Limiar"
---

<objective>
Frontend da Fase 5: a visão única "Precisam de atenção" (S1-S4) com os 3 baldes e as ações leves, o `ConfidenceBadge` reutilizável (S5), os hooks TanStack Query (polling + mutations), o cliente/tipos, o campo de limiar na Config (S6), e a navegação. Honra integralmente o design system TRAVADO (04/05-UI-SPEC). Sem visualizador de documento (D-06).

Purpose: É onde o usuário efetivamente triagem e resolve — aprova, corrige, reclassifica, reprocessa — sem confiar cegamente na IA. Fecha o loop REV-03/REV-04/REV-05 visualmente.
Output: AttentionPage, ConfidenceBadge, useAttention, api/types estendidos, S6 na Config, nav, + verificação visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-CONTEXT.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-UI-SPEC.md
@.planning/phases/04-templates-sub-templates-e-classifica-o/04-UI-SPEC.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-03-SUMMARY.md

<interfaces>
<!-- Extraídos do código atual. Use diretamente. -->

App.tsx: estado `page: Page`; render condicional por page (linhas 68-93); PAGE_META (linhas 13-18). Sidebar recebe page/onNavigate.
Sidebar.tsx: GROUPS de NavItem {page, label, icon} (linhas 7-17); grupo 'OPERAÇÃO' já tem 'documentos'.
types.ts: tipo `Page` (union de strings) — ESTENDER com 'atencao'. Interfaces Classification/ClassificationField (linhas ~137-163) — ESTENDER com confidence_score + manually_corrected.

lib/api.ts: helper `request<T>(path, init?)` (linhas 33-51) lança ApiError em !res.ok. Molde getDocumentDetail (GET tipado) + updateTemplate (PATCH com JSON body, linhas 109-114).
hooks/useDocuments.ts: useQuery com refetchInterval:4000 + refetchIntervalInBackground:false + placeholderData:keepPreviousData; useRescan = useMutation com invalidateQueries em onSuccess.
components/StatusPill.tsx (linhas 19-51): mapa estado→token + render <span> com var(--st-X)/var(--st-X-bg). Mapeamento: falha→erro, quarentena→leitura, em_revisao→leitura, concluido→tratado (NÃO alterar).
components/Icon.tsx: nomes existentes refresh/check/grid/dots/docMini/folder; adicionar ícone novo só no mesmo estilo de stroke se necessário.
pages/DocumentsPage.tsx: molde completo de página — .card, .stat-grid/.stat-card, .chips/.chip/.chip.active, table.docs/.cell-mono, StatusPill; estados isInitialLoading/isError/isEmpty (linhas 91-93); empty/error centralizado (linhas 183-216); DocumentDetailModal tabela campo→valor→normalizado→marca (linhas 397-445).
pages/ConfigPage.tsx: estrutura de abas/seções da Config — adicionar o campo S6 numa seção existente ou nova ("Confiança").

Backend (Plan 03): GET /documents/attention → {falha[], quarentena[], em_revisao[], counts}; POST /documents/{id}/retry; POST /documents/{id}/reclassify {template_id}; PATCH /documents/{id}/fields/{field_name} {raw_value}; POST /documents/{id}/approve; GET/PUT /config/review-threshold {threshold}.
GET /templates (existente) para o select "Atribuir template".

UI-SPEC faixas (TRAVADAS): score ≥0.80 Alta/verde(token tratado); 0.50-0.79 Média/âmbar(token leitura); <0.50 Baixa/vermelho(token erro). Copy em pt-BR no 05-UI-SPEC Copywriting Contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types + api client + ConfidenceBadge + hooks useAttention</name>
  <files>frontend/src/types.ts, frontend/src/lib/api.ts, frontend/src/components/ConfidenceBadge.tsx, frontend/src/hooks/useAttention.ts</files>
  <read_first>
    - frontend/src/types.ts (Page union; Classification/ClassificationField linhas ~137-163)
    - frontend/src/lib/api.ts (helper request<T> linhas 33-51; getDocumentDetail; updateTemplate PATCH)
    - frontend/src/hooks/useDocuments.ts (molde COMPLETO: useQuery polling + useRescan mutation com invalidateQueries)
    - frontend/src/components/StatusPill.tsx (molde token-driven: <span> com var(--st-X)/var(--st-X-bg))
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seções types.ts linhas 302-321; lib/api.ts linhas 325-348; useAttention linhas 352-381; ConfidenceBadge linhas 385-401)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-UI-SPEC.md (faixas de score; cores por token)
  </read_first>
  <behavior>
    - ConfidenceBadge com score 0.92 renderiza "92% · Alta" com token tratado (verde)
    - ConfidenceBadge com score 0.6 renderiza "60% · Média" com token leitura (âmbar)
    - ConfidenceBadge com score 0.3 renderiza "30% · Baixa" com token erro (vermelho)
    - ConfidenceBadge com score null/undefined renderiza um fallback neutro (ex.: "—") sem quebrar
    - useApproveDocument.onSuccess invalida ['attention'] e ['documents']
  </behavior>
  <action>
    Estender `types.ts`: `Page` ganha `'atencao'`. `ClassificationField` ganha `manually_corrected: boolean`; `Classification` ganha `confidence_score: number | null`. Adicionar tipos `AttentionList` (falha/quarentena/em_revisao + counts), `AttentionItem` (id, original_filename, motivo) e `ReviewItem` (estende com confidence_score + fields), e `ReviewThreshold {threshold:number}`.

    Estender `lib/api.ts` reusando `request<T>`: `getAttention()` GET /documents/attention; `postRetry(id)`, `postReclassify(id, templateId)` (body {template_id}), `patchField(id, fieldName, rawValue)` (PATCH, encodeURIComponent no fieldName, body {raw_value}), `postApprove(id)`; `getReviewThreshold()` e `putReviewThreshold(value)` (body {threshold}).

    Criar `ConfidenceBadge.tsx` espelhando StatusPill: recebe `score: number | null`; mapeia faixa→{label, token} pelas faixas TRAVADAS (≥0.8 Alta/tratado; ≥0.5 Média/leitura; <0.5 Baixa/erro); renderiza `<span className="badge" style={{color:var(--st-${token}), background:var(--st-${token}-bg)}}>` com `{pct}%` em `var(--font-mono)` + " · " + label. NÃO usar accent. score null → "—" neutro. Componente isolado e reutilizável (também no DocumentDetailModal).

    Criar `hooks/useAttention.ts` espelhando useDocuments/useRescan: `useAttentionDocuments()` useQuery key ['attention'] refetchInterval:4000 + refetchIntervalInBackground:false + placeholderData:keepPreviousData; mutations `useRetryDocument`, `useReclassifyDocument`, `usePatchField`, `useApproveDocument` — cada uma useMutation com `invalidateQueries(['attention'])` (e `['documents']`) em onSuccess; `useReviewThreshold`/`useSaveReviewThreshold` para S6.

    Adicionar/preencher um teste leve do ConfidenceBadge (vitest, se a infra existir — checar `frontend/package.json` por vitest; se não houver runner de teste frontend, marcar a verificação como visual no checkpoint e cobrir as faixas por inspeção).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'atencao'" frontend/src/types.ts` e `manually_corrected\|confidence_score` em types.ts
    - `grep -n "getAttention\|postReclassify\|patchField\|postApprove\|ReviewThreshold" frontend/src/lib/api.ts`
    - `grep -n "var(--st-" frontend/src/components/ConfidenceBadge.tsx` e `var(--font-mono)` (número em mono); NENHUM `var(--accent)` no badge
    - `grep -n "invalidateQueries" frontend/src/hooks/useAttention.ts` em cada mutation; `refetchInterval: 4000` na query
    - `cd frontend && npm run build` sem erros de tipo (tsc) nem de bundle
  </acceptance_criteria>
  <done>types/api/hooks/ConfidenceBadge prontos e tipados; build verde; faixas e invalidação corretas.</done>
</task>

<task type="auto">
  <name>Task 2: AttentionPage (3 baldes + ações) + S6 na Config + navegação</name>
  <files>frontend/src/pages/AttentionPage.tsx, frontend/src/pages/ConfigPage.tsx, frontend/src/components/Sidebar.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/pages/DocumentsPage.tsx (molde de página: .card, .stat-grid, .chips/.chip.active, table.docs, estados isInitialLoading/isError/isEmpty linhas 91-93, empty/error centralizado linhas 183-216, DocumentDetailModal tabela de campos linhas 397-445)
    - frontend/src/App.tsx (render condicional por page linhas 68-93; PAGE_META linhas 13-18)
    - frontend/src/components/Sidebar.tsx (GROUPS NavItem linhas 7-17 — adicionar 'atencao' ao grupo OPERAÇÃO)
    - frontend/src/pages/ConfigPage.tsx (estrutura de abas/seções — onde encaixar S6)
    - frontend/src/hooks/useAttention.ts (Task 1) + frontend/src/components/ConfidenceBadge.tsx (Task 1)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-UI-SPEC.md (TODO o Copywriting Contract; Interaction States; focal points por superfície)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seção AttentionPage linhas 405-441 — estados de tela, chips com contagem, tabela de campos editáveis, badge inválido, CTAs)
  </read_first>
  <action>
    Criar `AttentionPage.tsx` espelhando DocumentsPage. Topo: contagem por balde (`.stat-grid`/`.stat-card` ou `.chips` com `.chip-count`) — focal point quando populada. Alternar baldes via `.chips`/`.tabs` (Discretion: seções empilhadas OU chips — ambos honram o mock). Estados: `isInitialLoading` (skeleton), `isError` (bloco centralizado + "Tentar novamente"), `isEmpty` (empty state "Tudo em dia"). Copy EXATAMENTE do Copywriting Contract (títulos, subtítulos, motivos por balde, CTAs, empty/error). 

    Item FALHA (S2): motivo (mensagem persistida ou fallback) + botão "Tentar de novo" (`refresh`); em andamento "Reenviando…" disabled.
    Item QUARENTENA (S3): motivo + `select` "Atribuir template" (placeholder "Escolha um template…", opções de GET /templates) + botão "Reclassificar" disabled até escolher template; em andamento "Reclassificando…".
    Item EM_REVISAO (S4): `ConfidenceBadge` (rótulo "Confiança" + "{score}% · {Alta/Média/Baixa}") + tabela de campos (Campo/Valor/Normalizado/Marca) espelhando DocumentDetailModal; campo inválido → `<input className="search-input">` (mono) com `aria-label="Corrigir valor de {campo}"` + botão "Salvar correção" (`.btn-ghost`, chama patchField); badge válido/inválido com `title={invalid_reason}`; badge "corrigido manualmente" quando manually_corrected; botão "Aprovar documento" (`.btn-primary`, `check`) DISABLED enquanto houver obrigatório inválido com hint "Corrija os campos obrigatórios inválidos antes de aprovar o documento." Valores como TEXTO PURO (sem dangerouslySetInnerHTML — T-04-12). Erros de ação inline em `var(--st-erro)`, item permanece.

    S6 na ConfigPage: adicionar o campo "Limiar de confiança" (input numérico 0-100 com sufixo "%", hint do UI-SPEC) lendo `useReviewThreshold` e salvando via `useSaveReviewThreshold` (botão "Salvar limiar"/"Salvando…"). Converter 0-100 (UI) ↔ 0.0-1.0 (API).

    Navegação: adicionar `{ page:'atencao', label:'Precisam de atenção', icon:'<ícone existente, ex. refresh ou um novo de alerta no mesmo stroke>' }` ao grupo OPERAÇÃO em Sidebar; adicionar entrada em PAGE_META e o render condicional `{page==='atencao' && <AttentionPage/>}` em App.tsx.

    Reusar SOMENTE classes/tokens existentes (Component Inventory do UI-SPEC); classes novas só com prefixo coerente e valores múltiplos de 4 (`.attn-row`, `.conf-badge` se necessário). Pesos 600/700 conforme Typography.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "AttentionPage" frontend/src/App.tsx` e `'atencao'` em Sidebar.tsx + App.tsx (PAGE_META + render)
    - `grep -n "Precisam de atenção\|Tudo em dia\|Tentar de novo\|Reclassificar\|Aprovar documento" frontend/src/pages/AttentionPage.tsx` (copy do contrato)
    - `grep -n "Limiar de confiança" frontend/src/pages/ConfigPage.tsx`
    - `grep -rn "dangerouslySetInnerHTML" frontend/src/pages/AttentionPage.tsx` → 0 ocorrências
    - `grep -n "disabled" frontend/src/pages/AttentionPage.tsx` no botão Aprovar (gate D-07) e no Reclassificar (até escolher template)
    - `cd frontend && npm run build` verde
  </acceptance_criteria>
  <done>AttentionPage com 3 baldes e ações, S6 na Config e navegação prontos; build verde; copy e gates do UI-SPEC honrados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual da visão "Precisam de atenção"</name>
  <action>Checkpoint de verificação humana (não-autônomo). O executor sobe backend+frontend e pausa para o usuário confirmar visualmente a visão "Precisam de atenção" conforme os passos de <how-to-verify> antes de marcar a fase pronta.</action>
  <what-built>Página "Precisam de atenção" (3 baldes: Falhas/Quarentena/Em revisão) com polling, ConfidenceBadge, correção inline de campos, gates de Aprovar/Reclassificar, e o campo de Limiar na Config — tudo sobre o design system TRAVADO, sem visualizador de documento (D-06).</what-built>
  <how-to-verify>
    1. Suba o backend e o frontend (ex.: `cd backend && uv run uvicorn app.main:app --reload --port 8000` e `cd frontend && npm run dev`). Garanta a migração 0005 aplicada.
    2. Tenha (ou semeie) documentos em FALHA, QUARENTENA e EM_REVISAO. Abra "Precisam de atenção" na Sidebar.
    3. Confirme: as 3 contagens no topo; cada balde lista itens com o motivo; o polling atualiza sem piscar.
    4. EM_REVISAO: o ConfidenceBadge mostra "{score}% · {Alta/Média/Baixa}" na cor correta; um campo obrigatório inválido aparece em vermelho com input de correção; "Aprovar documento" está DESABILITADO até corrigir.
    5. Corrija o campo (Salvar correção) → a marca vira verde + "corrigido manualmente"; sem chamada à IA; "Aprovar documento" habilita; aprovar → o item sai da lista (vira Concluído verde).
    6. QUARENTENA: escolha um template no select → "Reclassificar" habilita → reclassificar reprocessa o doc.
    7. FALHA: "Tentar de novo" reenfileira o doc.
    8. Config: ajuste o "Limiar de confiança" e salve; confirme persistência (recarregar a página mantém o valor).
    9. Confirme que NÃO há visualizador de documento (imagem/PDF/texto bruto) em lugar nenhum (D-06); tema claro/escuro coerentes.
  </how-to-verify>
  <verify><human-check>Usuário confirma os 9 passos de how-to-verify; aprova ou descreve ajustes.</human-check></verify>
  <done>Usuário digitou "approved" após confirmar os 3 baldes, as ações, o ConfidenceBadge, os gates D-07, o S6 e a ausência de visualizador de documento (D-06).</done>
  <resume-signal>Digite "approved" ou descreva os ajustes necessários</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valores extraídos (IA/documento) → DOM | Render na UI deve ser texto puro (XSS) |
| input do operador → API | Correção de campo, template, limiar passam pela API com guards do Plan 03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering/XSS | Valor de campo renderizado como HTML | mitigate | React renderiza como texto puro; grep-gate confirma 0 ocorrências de dangerouslySetInnerHTML em AttentionPage (padrão T-04-12) |
| T-05-17 | Elevation | UI permite "Aprovar" com obrigatório inválido (burla D-07) | mitigate | Botão disabled enquanto houver obrigatório inválido + hint; o backend (Plan 03) é o guard autoritativo (409) — a UI é defesa em profundidade |
| T-05-18 | Information Disclosure | Valores sensíveis (CNPJ/CPF) em console.log da UI | mitigate | Hooks/página não logam valores; apenas exibem no DOM como texto |
| T-05-SC | Tampering | npm installs | accept | Fase code-and-config only — NENHUM pacote npm novo (RESEARCH: stack pinada); sem superfície de slopsquatting |

Nenhuma ameaça `high` — o backend permanece o guard autoritativo; a UI reforça os gates.
</threat_model>

<verification>
- `cd frontend && npm run build` verde (tsc + bundle)
- Checkpoint visual aprovado (3 baldes, ações, ConfidenceBadge, gates, S6, sem visualizador D-06)
- Polling sem flicker; invalidação após ações; render texto puro
</verification>

<success_criteria>
- Visão de triagem em 3 baldes navegável e funcional (REV-03)
- Aprovar/corrigir inline com gate D-07; correção marcada manual (REV-04)
- Quarentena resolúvel pela UI (REV-05)
- Limiar editável na Config (REV-02/D-03)
- Design system TRAVADO honrado; sem visualizador de documento (D-06)
</success_criteria>

<output>
Create `.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-04-SUMMARY.md` when done
</output>
