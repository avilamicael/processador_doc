---
phase: 05-confian-a-revis-o-humana-e-quarentena
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - backend/app/classification/stage.py
  - backend/app/queue/repo.py
  - backend/app/queue/worker.py
  - backend/tests/classification/test_stage_routing.py
  - backend/tests/classification/test_forced_template.py
autonomous: true
requirements: [REV-01, REV-02, REV-05]
must_haves:
  truths:
    - "Ao classificar um bloco que casou, classify_stage calcula o score (compute_confidence), persiste confidence_score no ClassificationResult e roteia: EM_REVISAO quando score < limiar OU obrigatório inválido (D-04); permanece PROCESSANDO+classificado quando passa (NÃO auto-CONCLUIDO — Open Q1 resolvida)"
    - "classify_stage aceita forced_template_id opcional que pula matcher/decide/desempate e vai direto a filler+IA-faltantes+validação com o template forçado (D-09)"
    - "repo.requeue_step reseta um job existente (content_hash, step) para pending com payload novo, resolvendo a UNIQUE(original_hash, step) na reclassificação (Open Q2)"
    - "O worker lê forced_template_id do payload do job classify e o repassa ao classify_stage; payload normal sem a chave mantém o caminho atual intacto"
  artifacts:
    - path: "backend/app/queue/repo.py"
      provides: "requeue_step(session, content_hash, step, payload) -> int"
      exports: ["requeue_step"]
    - path: "backend/app/classification/stage.py"
      provides: "classify_stage com forced_template_id + roteamento de estado por score"
      contains: "forced_template_id"
  key_links:
    - from: "backend/app/classification/stage.py"
      to: "backend/app/classification/confidence.py"
      via: "compute_confidence sobre filled_fields + template.fields no commit atômico"
      pattern: "compute_confidence"
    - from: "backend/app/classification/stage.py"
      to: "transition"
      via: "transition(EM_REVISAO) no caminho de revisão, sem session.commit() manual antes"
      pattern: "transition\\(session, doc, DocState.EM_REVISAO"
    - from: "backend/app/queue/worker.py"
      to: "classify_stage"
      via: "forced_template_id lido do payload e repassado"
      pattern: "forced_template_id"
---

<objective>
Coração da fase no backend: estender `classify_stage` para (A) calcular o indicador de confiança e ROTEAR o estado dentro do commit atômico único (EM_REVISAO vs permanecer PROCESSANDO+classificado — D-01/D-04, Open Q1 resolvida), e (B) aceitar `forced_template_id` para reclassificar quarentena pulando o matcher (D-09). Mais o helper `repo.requeue_step` (Open Q2) e a leitura de `forced_template_id` no worker.

Purpose: É o motor que transforma "classificou" em "precisa de atenção ou está pronto", determinística e atomicamente, e o caminho que viabiliza resolver quarentena. Os endpoints (Plan 03) só orquestram em cima disto.
Output: stage.py estendido, repo.requeue_step, worker com payload de forced_template_id, e os 2 testes de classificação preenchidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-CONTEXT.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-RESEARCH.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-01-SUMMARY.md

<interfaces>
<!-- Extraídos do código atual. Use diretamente. -->

classify_stage atual (backend/app/classification/stage.py):
  async def classify_stage(session: Session, *, content_hash: str) -> ClassifyStageResult
  - idempotência: ClassificationResult existente → no-op (linhas 162-173)
  - ramo quarentena (linhas 238-252): session.add(CR template_id=None) + usages; DEPOIS transition(QUARENTENA) que comita TUDO
  - caminho casou (linhas 254-316): filler.map_fields → IA-faltantes → validate_field por campo → cr.filled_fields.append(...)
  - passo 9 ATUAL (linhas 310-316): for u in usages: session.add(u); doc.last_completed_step = CLASSIFIED_STEP; session.commit()
  - CLASSIFIED_STEP = "classificado"; USAGE_STEP = "classify"
  - get_settings() já chamado (linha 193, var `settings`)

compute_confidence (Plan 01, backend/app/classification/confidence.py):
  compute_confidence(filled_fields, template_fields) -> tuple[float, bool]  # (score, has_invalid_required)

transition (backend/app/pipeline/state_machine.py):
  transition(session, doc, new_state, completed_step=None) — valida allowlist, COMITA internamente, rollback em inválida
  allowlist (states.py): PROCESSANDO→{EM_REVISAO, CONCLUIDO, QUARENTENA, FALHA}; EM_REVISAO→CONCLUIDO; QUARENTENA→PROCESSANDO; FALHA→PROCESSANDO

repo.requeue_running (backend/app/queue/repo.py linhas 185-200) — molde EXATO do novo requeue_step:
  UPDATE jobs SET status='pending', updated_at=CURRENT_TIMESTAMP WHERE status='running' ; commit ; return rowcount
  _utcnow() helper já existe (linha 39)

worker._dispatch ramo CLASSIFY_STEP (backend/app/queue/worker.py linhas 158-164):
  with get_session(engine) as session:
      await classify_stage(session, content_hash=original_hash)
  payload chega como string JSON; json já importado (linha 25)

Template model: session.get(Template, id); template.fields (list[TemplateField]); TemplateField.name/.field_type/.required/.regex
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roteamento de estado por score + forced_template_id no classify_stage</name>
  <files>backend/app/classification/stage.py, backend/tests/classification/test_stage_routing.py, backend/tests/classification/test_forced_template.py</files>
  <read_first>
    - backend/app/classification/stage.py (ARQUIVO INTEIRO — você edita 2 pontos: assinatura/idempotência+ramo forçado e o passo 9; preserve idempotência linhas 162-173 e a ordem add→transition do ramo quarentena linhas 238-252)
    - backend/app/classification/confidence.py (compute_confidence — Plan 01)
    - backend/app/pipeline/state_machine.py (transition: comita internamente; completed_step param)
    - backend/app/pipeline/states.py (allowlist; PROCESSANDO→EM_REVISAO presente)
    - backend/app/classification/filler.py (filler.map_fields assinatura — para o ramo forçado)
    - backend/tests/classification/test_stage.py (molde de teste do stage: como monta Document/Extraction/Template e mocka OpenAI via respx)
    - backend/tests/classification/conftest.py (fixtures schema_engine + mock OpenAI)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seção stage.py linhas 113-159 — excertos A e B; regra commit atômico linhas 476-479)
  </read_first>
  <behavior>
    - Bloco que casa template, todos obrigatórios válidos, score >= limiar → state permanece PROCESSANDO, last_completed_step="classificado", cr.confidence_score persistido (NÃO vai a CONCLUIDO — Open Q1 resolvida: aprovação humana/Fase 6 é quem conclui)
    - Bloco que casa, 1 obrigatório inválido, score alto → transition(EM_REVISAO) (D-04: erro determinístico sempre revisa) + cr.confidence_score persistido
    - Bloco que casa, score < review_confidence_threshold → transition(EM_REVISAO) + confidence_score persistido
    - Sem campos obrigatórios no template → compute_confidence devolve (1.0,False) → permanece PROCESSANDO+classificado
    - forced_template_id setado para um template existente → pula matcher/decide; usa filler+IA-faltantes+validação do template forçado; confidence (matcher) = None; roteia por score igual ao caminho normal
    - forced_template_id apontando para template inexistente → raise ValueError("Template forçado inexistente")
    - Atomicidade: nenhum session.commit() manual antes do transition no caminho de revisão (transition comita tudo); CR+FilledFields+Usages persistidos junto com o estado
  </behavior>
  <action>
    (A) forced_template_id (D-09): mudar a assinatura para `async def classify_stage(session, *, content_hash, forced_template_id: int | None = None)`. Após a idempotência (linha 173) e a leitura da Extraction, inserir um ramo: se `forced_template_id is not None`, `template = session.get(Template, forced_template_id)`; None → `raise ValueError("Template forçado inexistente")`; setar `matched_template_id = forced_template_id`, `confidence = None`, e PULAR o bloco matcher/decide/ambiguous (linhas 184-233). Caso contrário, o caminho atual roda. Estruture com if/else para que o passo 6 (quarentena) e o passo 7+ (casou) continuem iguais; no caminho forçado, `matched_template_id` nunca é None (não cai em quarentena).

    (B) Roteamento por score (D-01/D-04): substituir o passo 9 atual (linhas 310-316). Após montar `cr` + `cr.filled_fields` (passo 8): `score, has_invalid_required = compute_confidence(cr.filled_fields, list(template.fields))`; `cr.confidence_score = score`; `for u in usages: session.add(u)`; `below_threshold = score < settings.review_confidence_threshold`. Se `has_invalid_required or below_threshold`: NÃO commitar; chamar `transition(session, doc, DocState.EM_REVISAO, completed_step=CLASSIFIED_STEP)` (comita tudo atômico). Senão (passou): manter o comportamento terminal da Fase 4 — `doc.last_completed_step = CLASSIFIED_STEP; session.commit()` (state permanece PROCESSANDO; NÃO transition para CONCLUIDO — Open Q1: CONCLUIDO é terminal e a Fase 6 captura docs prontos; auto-CONCLUIR pularia o ponto de captura). Documentar essa decisão num comentário citando Open Q1. NUNCA `session.commit()` antes do `transition` no ramo de revisão (Pitfall 2).

    Importar `compute_confidence` de `app.classification.confidence`. Garantir `Template` importado (já está, linha 59) e `DocState.EM_REVISAO` disponível.

    Preencher `test_stage_routing.py` (remover skips do Plan 01): casos EM_REVISAO por score baixo; EM_REVISAO por obrigatório inválido com score alto; permanece PROCESSANDO+classificado quando passa; confidence_score persistido. Preencher `test_forced_template.py`: forced_template_id pula matcher (assert que o matcher não muda a escolha — usar template forçado diferente do que o matcher escolheria) e usa filler+validação; template inexistente → ValueError.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/classification/test_stage_routing.py tests/classification/test_forced_template.py tests/classification/test_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "forced_template_id" backend/app/classification/stage.py` mostra o parâmetro na assinatura e o ramo de pulo do matcher
    - `grep -n "compute_confidence\|confidence_score" backend/app/classification/stage.py` mostra o cálculo e a persistência no passo 9
    - `grep -n "transition(session, doc, DocState.EM_REVISAO" backend/app/classification/stage.py` presente
    - `grep -n "DocState.CONCLUIDO" backend/app/classification/stage.py` AUSENTE no stage (auto-CONCLUIDO não acontece — Open Q1)
    - Os testes test_stage_routing.py + test_forced_template.py passam; test_stage.py (Fase 4) permanece verde (não regrediu a idempotência/quarentena/atomicidade)
  </acceptance_criteria>
  <done>classify_stage calcula+persiste o score, roteia EM_REVISAO vs PROCESSANDO+classificado atômico, e aceita forced_template_id; testes verdes incluindo os da Fase 4.</done>
</task>

<task type="auto">
  <name>Task 2: repo.requeue_step (Open Q2) + worker lê forced_template_id do payload</name>
  <files>backend/app/queue/repo.py, backend/app/queue/worker.py</files>
  <read_first>
    - backend/app/queue/repo.py linhas 185-200 (requeue_running — molde EXATO do UPDATE...WHERE + commit + rowcount; _utcnow linha 39)
    - backend/app/queue/repo.py linhas 43-77 (enqueue — UNIQUE(original_hash, step) e por que re-enqueue é no-op)
    - backend/app/queue/worker.py linhas 141-171 (_dispatch; ramo CLASSIFY_STEP linhas 158-164; json importado)
    - backend/app/queue/worker.py linhas 291-358 (enqueue_pending_classifications + _sweep_pending — CONFIRMAR a interação: o sweep só enfileira para docs PROCESSANDO+extraido SEM ClassificationResult; após reclassify o doc volta PROCESSANDO+? — ver nota abaixo)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seção repo.py linhas 263-285; seção worker.py linhas 289-298)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-RESEARCH.md (Open Question 2 linhas 517-520)
  </read_first>
  <action>
    Adicionar `requeue_step(session, *, content_hash, step, payload) -> int` a `repo.py`, espelhando `requeue_running`: `UPDATE jobs SET status='pending', payload=:payload, next_run_at=:now, attempts=0, updated_at=CURRENT_TIMESTAMP WHERE original_hash=:hash AND step=:step`, bind `now=_utcnow()`, `session.commit()`, `return result.rowcount`. Docstring: reseta a linha existente (mantém a UNIQUE uq_jobs_hash_step) para retry/reclassify onde o job antigo já está 'done' e um enqueue novo seria no-op (Open Q2). Reset de `attempts=0` dá ao job reprocessado o ciclo completo de retries.

    No `worker._dispatch`, ramo CLASSIFY_STEP: ler `forced = json.loads(payload).get("forced_template_id")` (None no payload normal — o classify normal usa `{"content_hash": ...}`) e passar `await classify_stage(session, content_hash=original_hash, forced_template_id=forced)`. O `.get` retorna None no caminho atual → comportamento inalterado.

    NOTA de interação com o sweep (documentar em comentário): o endpoint de reclassify (Plan 03) APAGA o ClassificationResult de quarentena e usa `requeue_step` (não `enqueue`). Como `requeue_step` reseta a linha existente para pending com o payload de forced_template_id, e `_sweep_pending`/`enqueue_pending_classifications` só enfileira via `enqueue` (no-op por UNIQUE quando a linha existe), o sweep NÃO sobrescreve o payload forçado. Confirmar que o doc reclassificado está em PROCESSANDO+(não "extraido" ainda, pois transition(PROCESSANDO) não mexe no marcador) — o sweep de classify filtra por last_completed_step==EXTRACTED_STEP, então não cria job duplicado conflitante. Se o teste mostrar conflito, o reset de payload via requeue_step prevalece por ser o último UPDATE antes do claim.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/ -k "queue or worker or repo" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def requeue_step" backend/app/queue/repo.py` presente com assinatura (session, *, content_hash, step, payload) -> int
    - `grep -n "forced_template_id" backend/app/queue/worker.py` mostra a leitura do payload no ramo CLASSIFY_STEP
    - `cd backend && uv run python -c "from app.queue.repo import requeue_step; from app.queue import worker"` importa sem erro
    - `cd backend && uv run pytest -q` (suite completa) permanece verde
  </acceptance_criteria>
  <done>requeue_step reseta job existente para pending; worker repassa forced_template_id do payload; suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker payload (JSON) → classify_stage | forced_template_id vem do payload do job (enfileirado por endpoint interno) |
| classify_stage → state machine | roteamento de estado deve passar pela allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | forced_template_id no stage aponta para template inexistente | mitigate | `session.get(Template, id)` None → `raise ValueError` antes de qualquer persistência; o worker roteia a FALHA via dead-letter |
| T-05-04 | Tampering | Estado roteado fora da allowlist (ex.: commit direto sem transition) | mitigate | Caminho de revisão usa `transition(EM_REVISAO)`; allowlist PROCESSANDO→EM_REVISAO valida; rollback em inválida |
| T-05-05 | Elevation/Tampering | Auto-CONCLUIDO prematuro pula captura da Fase 6 (cross-fase, risco ALTO) | mitigate | Open Q1 RESOLVIDA: stage NUNCA transita para CONCLUIDO; passou → permanece PROCESSANDO+classificado; CONCLUIDO só via approve humano (Plan 03). grep-gate confirma ausência de DocState.CONCLUIDO no stage |
| T-05-06 | DoS | requeue_step reseta attempts=0 permitindo retry infinito por spam de reclassify | accept | Single-tenant local; reclassify é ação humana deliberada; max_attempts continua aplicado por ciclo |

Nenhuma ameaça `high` residual — T-05-05 (risco alto cross-fase) mitigada pela resolução da Open Q1.
</threat_model>

<verification>
- `cd backend && uv run pytest tests/classification/ -x -q` verde (routing + forced + stage Fase 4)
- `cd backend && uv run pytest -q` (suite completa) verde
- Roteamento: EM_REVISAO em score baixo/obrigatório inválido; PROCESSANDO+classificado quando passa; NUNCA CONCLUIDO no stage
- Atomicidade preservada (sem commit antes do transition no ramo de revisão)
</verification>

<success_criteria>
- Indicador de confiança calculado e persistido por bloco (REV-01)
- Limiar global decide EM_REVISAO vs pronto (REV-02)
- forced_template_id viabiliza reclassificar quarentena (REV-05, base)
- requeue_step resolve a UNIQUE(hash, step) na reclassificação (Open Q2)
- Open Q1 resolvida e materializada: sem auto-CONCLUIDO
</success_criteria>

<output>
Create `.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-02-SUMMARY.md` when done
</output>
