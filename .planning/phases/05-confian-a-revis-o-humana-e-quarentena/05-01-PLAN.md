---
phase: 05-confian-a-revis-o-humana-e-quarentena
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/classification.py
  - backend/alembic/versions/0005_confidence_review.py
  - backend/app/config.py
  - backend/app/classification/confidence.py
  - backend/tests/classification/test_confidence.py
  - backend/tests/classification/test_stage_routing.py
  - backend/tests/classification/test_forced_template.py
  - backend/tests/test_api_review.py
autonomous: true
requirements: [REV-01, REV-02]
must_haves:
  truths:
    - "A coluna confidence_score existe em classification_results e manually_corrected em filled_fields, ambas via migração Alembic 0005 sem recriar a tabela documents"
    - "compute_confidence devolve (score, has_invalid_required) como fração de obrigatórios válidos (D-01), com 100%/False quando não há obrigatórios"
    - "O tunable global review_confidence_threshold é lido da config (D-03) no mesmo padrão de classify_match_threshold"
    - "Os 4 arquivos de teste Wave 0 existem e seus testes-stub falham/passam de forma executável"
  artifacts:
    - path: "backend/alembic/versions/0005_confidence_review.py"
      provides: "Migração que adiciona confidence_score (classification_results) e manually_corrected (filled_fields)"
      contains: "batch_alter_table"
    - path: "backend/app/classification/confidence.py"
      provides: "Função pura compute_confidence(filled_fields, template_fields) -> tuple[float, bool]"
      exports: ["compute_confidence"]
    - path: "backend/tests/classification/test_confidence.py"
      provides: "Testes da função pura compute_confidence (REV-01)"
      min_lines: 30
  key_links:
    - from: "backend/app/models/classification.py"
      to: "backend/alembic/versions/0005_confidence_review.py"
      via: "colunas confidence_score + manually_corrected espelhadas no schema e na migração"
      pattern: "confidence_score|manually_corrected"
    - from: "backend/app/config.py"
      to: "review_confidence_threshold"
      via: "Field + AliasChoices"
      pattern: "review_confidence_threshold"
---

<objective>
Fundação code-and-config da Fase 5: adicionar as DUAS colunas novas (`confidence_score` em `classification_results`, `manually_corrected` em `filled_fields`) ao schema + migração Alembic 0005, o tunable global `review_confidence_threshold` na config, a função pura `compute_confidence` (cálculo do indicador determinístico, REV-01/D-01), e os scaffolds de teste Wave 0 que os planos seguintes preenchem.

Purpose: Sem score persistido nem cálculo puro, nada do roteamento de estado (Plan 02) nem dos endpoints (Plan 03) pode ser construído. Esta é a camada de contratos: colunas, tunable e função pura que tudo consome.
Output: Schema + migração 0005, `confidence.py`, tunable na config, 4 arquivos de teste Wave 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-CONTEXT.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-RESEARCH.md
@.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md

<interfaces>
<!-- Extraídos do código atual. Use diretamente — não re-explore o codebase. -->

ClassificationResult (backend/app/models/classification.py) — coluna molde `confidence`:
  confidence: Mapped[float | None] = mapped_column(Float, nullable=True)   # linha 64
  filled_fields: relationship(cascade="all, delete-orphan")               # linha 72

FilledField (mesmo arquivo) — coluna molde `valid`:
  valid: Mapped[bool] = mapped_column(Boolean, default=True, server_default="1", nullable=False)   # linhas 94-96
  field_name / raw_value / normalized_value / invalid_reason já existem

TemplateField (backend/app/models/template.py): atributos .name, .field_type, .required, .regex, .hint

config.py (backend/app/config.py) molde linhas 142-147:
  classify_match_threshold: float = Field(
      default=0.5,
      validation_alias=AliasChoices("CLASSIFY_MATCH_THRESHOLD", "classify_match_threshold"),
  )

Migração analog 0004 (backend/alembic/versions/0004_templates_classification.py):
  - cabeçalho: revision/down_revision/branch_labels/depends_on
  - usa op.batch_alter_table('<tabela>') as batch_op + batch_op.add_column(sa.Column(...))
  - NÃO toca a tabela `documents` (preserva trigger trg_documents_updated_at criado na 0002)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coluna confidence_score + manually_corrected no schema + função pura compute_confidence + tunable</name>
  <files>backend/app/models/classification.py, backend/app/classification/confidence.py, backend/app/config.py, backend/tests/classification/test_confidence.py</files>
  <read_first>
    - backend/app/models/classification.py (molde EXATO: `confidence` linha 64 em ClassificationResult; `valid` linhas 94-96 em FilledField; imports Float/Boolean já presentes linhas 25-33)
    - backend/app/config.py linhas 134-156 (molde do tunable `classify_match_threshold` com Field + AliasChoices)
    - backend/app/classification/stage.py linhas 285-308 (laço que produz FilledField.valid por campo — fonte do cálculo)
    - backend/app/models/template.py linhas 59-83 (TemplateField: .name/.field_type/.required/.regex)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seção `confidence.py` linhas 93-109; seção `models/classification.py` linhas 70-89; seção `config.py` linhas 163-174)
  </read_first>
  <behavior>
    - compute_confidence([], []) retorna (1.0, False) — sem obrigatórios, nada a revisar
    - compute_confidence com 2 obrigatórios ambos valid=True retorna (1.0, False)
    - compute_confidence com 2 obrigatórios, 1 valid=True 1 valid=False retorna (0.5, True)
    - compute_confidence com todos obrigatórios valid=False retorna (0.0, True)
    - Campo obrigatório AUSENTE em filled_fields (não está no dict) conta como inválido (valid_by_name.get(name, False) == False)
    - Campos NÃO obrigatórios não afetam o score nem o has_invalid_required
  </behavior>
  <action>
    Adicionar a `ClassificationResult` a coluna `confidence_score: Mapped[float | None] = mapped_column(Float, nullable=True)` — distinta semanticamente de `confidence` (D-01: qualidade de extração vs score do matcher; documentar no comentário). Adicionar a `FilledField` a coluna `manually_corrected: Mapped[bool] = mapped_column(Boolean, default=False, server_default="0", nullable=False)` (D-08). Float e Boolean já estão importados.

    Criar `backend/app/classification/confidence.py` com a função PURA `compute_confidence(filled_fields, template_fields) -> tuple[float, bool]` retornando `(score 0.0-1.0, has_invalid_required)`: `required = [f for f in template_fields if f.required]`; sem obrigatórios → `(1.0, False)`; senão `valid_by_name = {ff.field_name: ff.valid for ff in filled_fields}`, `valid_count = sum(1 for f in required if valid_by_name.get(f.name, False))`, `has_invalid = valid_count < len(required)`, retorna `(valid_count / len(required), has_invalid)`. Sem DB, sem IA, sem import de SQLAlchemy — pura, testável isolada.

    Adicionar a `config.py` o tunable `review_confidence_threshold: float = Field(default=0.8, validation_alias=AliasChoices("REVIEW_CONFIDENCE_THRESHOLD", "review_confidence_threshold"))` (D-03; default 0.8 alinha à faixa "Alta ≥80%" do 05-UI-SPEC).

    Criar `backend/tests/classification/test_confidence.py` cobrindo os 6 casos do bloco behavior. Use objetos leves (SimpleNamespace ou dataclasses locais com .field_name/.valid e .name/.required) — a função é pura, NÃO precisa de DB.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/classification/test_confidence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "confidence_score" backend/app/models/classification.py` mostra a coluna em ClassificationResult
    - `grep -n "manually_corrected" backend/app/models/classification.py` mostra a coluna em FilledField com server_default="0"
    - `grep -n "review_confidence_threshold" backend/app/config.py` mostra Field + AliasChoices REVIEW_CONFIDENCE_THRESHOLD
    - `cd backend && uv run python -c "from app.classification.confidence import compute_confidence"` importa sem erro
    - `cd backend && uv run pytest tests/classification/test_confidence.py -x -q` passa com os 6 casos verdes
  </acceptance_criteria>
  <done>As 2 colunas estão no schema; compute_confidence é pura e testada (6 casos); o tunable global existe na config.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Migração Alembic 0005 (2 colunas nullable, sem tocar documents) + scaffolds Wave 0</name>
  <files>backend/alembic/versions/0005_confidence_review.py, backend/tests/classification/test_stage_routing.py, backend/tests/classification/test_forced_template.py, backend/tests/test_api_review.py</files>
  <read_first>
    - backend/alembic/versions/0004_templates_classification.py (molde COMPLETO: cabeçalho de revisão linhas ~28-32; batch_alter_table + add_column; docstring que documenta NÃO tocar `documents`)
    - backend/alembic/versions/0002_ingestion.py (referência: como a 0002 RECRIA o trigger trg_documents_updated_at após batch recreate — confirma por que NÃO devemos tocar documents)
    - backend/tests/classification/conftest.py (fixtures schema_engine + mock OpenAI respx reusáveis)
    - backend/tests/test_api_documents.py linhas 35-48 (fixture `client` sobre schema_engine; helper `_seed`)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-PATTERNS.md (seção migração 0005 linhas 35-66; seção test_api_review linhas 443-460)
    - .planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-RESEARCH.md (Validation Architecture → Wave 0 Gaps linhas 579-586)
  </read_first>
  <action>
    Criar `backend/alembic/versions/0005_confidence_review.py` com `revision='0005'`, `down_revision='0004'`. No `upgrade()`: `op.batch_alter_table('classification_results')` → `add_column(sa.Column('confidence_score', sa.Float(), nullable=True))`; `op.batch_alter_table('filled_fields')` → `add_column(sa.Column('manually_corrected', sa.Boolean(), nullable=False, server_default='0'))`. No `downgrade()`: drop_column de ambas (ordem inversa). CRÍTICO (Pitfall 1): NENHUMA das tabelas é `documents` → o trigger `trg_documents_updated_at` NÃO é afetado; documentar isso no docstring espelhando a 0004. Após escrever, rodar `cd backend && uv run alembic upgrade head` para aplicar.

    Criar os 3 scaffolds de teste Wave 0 (testes-stub executáveis, com asserts mínimos ou `pytest.skip` documentado para o que o Plan 02/03 implementa, mas o ARQUIVO + fixtures devem existir e a coleta passar):
    - `backend/tests/classification/test_stage_routing.py` — cobre roteamento EM_REVISAO vs estado-pronto + persistência de confidence_score (preenchido no Plan 02). Importa classify_stage; fixture de DB com schema_engine.
    - `backend/tests/classification/test_forced_template.py` — cobre caminho forced_template_id (preenchido no Plan 02).
    - `backend/tests/test_api_review.py` — cobre os 4 endpoints retry/reclassify/patch/approve (preenchido no Plan 03). Reusa a fixture `client` sobre schema_engine (molde test_api_documents linhas 35-43) e o respx de conftest para provar sem-IA no patch.
    Cada scaffold deve coletar e rodar sem erro de import (use `pytest.mark.skip(reason="implementado no Plan 0X")` nos corpos ainda não cobertos para manter a suite verde).
  </action>
  <verify>
    <automated>cd backend && uv run alembic upgrade head && uv run alembic current && uv run pytest tests/classification/test_stage_routing.py tests/classification/test_forced_template.py tests/test_api_review.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && uv run alembic current` mostra a revisão `0005` aplicada
    - `grep -n "classification_results\|filled_fields" backend/alembic/versions/0005_confidence_review.py` mostra as 2 tabelas; `grep -c "documents" backend/alembic/versions/0005_confidence_review.py | grep -v '^#'` confirma que `documents` só aparece em comentário/docstring, nunca em batch_alter_table
    - `cd backend && uv run python -c "import sqlite3,glob; con=sqlite3.connect(glob.glob('**/*.db',recursive=True)[0] if glob.glob('**/*.db',recursive=True) else ':memory:')"` não falha (smoke) — OU verificar via `PRAGMA table_info` que confidence_score existe em classification_results
    - Os 3 arquivos de teste coletam sem ImportError (`--collect-only` retorna 0)
    - `cd backend && uv run pytest -q` permanece verde (suite completa não regride)
  </acceptance_criteria>
  <done>Migração 0005 aplicada (alembic current == 0005), as 2 colunas existem no banco, o trigger documents intacto, e os 3 scaffolds Wave 0 coletam/rodam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → DB | Mudança de schema; risco de destruir trigger se tocar `documents` |
| config env → app | Tunable `REVIEW_CONFIDENCE_THRESHOLD` lido de env (não secreto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Migração 0005 toca `documents` e destrói trg_documents_updated_at | mitigate | As 2 colunas vão em `classification_results`/`filled_fields`; grep-gate confirma que `documents` só aparece em comentário (Pitfall 1) |
| T-05-02 | Denial of Service | review_confidence_threshold fora de [0,1] | accept | Valor de produto single-tenant via env; comportamento degradado mas não corrompe dado; calibração documentada (A2). Default seguro 0.8 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Fase code-and-config only — NENHUM pacote novo (RESEARCH Package Legitimacy Audit: N/A). Sem superfície de slopsquatting nesta fase |

Nenhuma ameaça de severidade `high` neste plano.
</threat_model>

<verification>
- `cd backend && uv run alembic current` == 0005
- `cd backend && uv run pytest tests/classification/test_confidence.py -x -q` verde
- `cd backend && uv run pytest -q` (suite completa) verde — nenhuma regressão
- Schema: confidence_score em classification_results (nullable), manually_corrected em filled_fields (NOT NULL default 0)
</verification>

<success_criteria>
- compute_confidence é pura, importável, e cobre os 6 casos (REV-01)
- review_confidence_threshold lido da config (REV-02 — base do limiar)
- Migração 0005 aplicada sem tocar documents (trigger intacto)
- 4 arquivos de teste Wave 0 existem e coletam
</success_criteria>

<output>
Create `.planning/phases/05-confian-a-revis-o-humana-e-quarentena/05-01-SUMMARY.md` when done
</output>
