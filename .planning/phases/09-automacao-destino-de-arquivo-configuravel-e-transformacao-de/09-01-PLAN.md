---
phase: 09-automacao-destino-de-arquivo-configuravel-e-transformacao-de
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/automation/naming.py
  - backend/app/automation/stage.py
  - backend/tests/automation/test_naming.py
  - backend/tests/automation/test_stage.py
  - backend/tests/automation/test_undo.py
autonomous: true
requirements: [BL-10]

must_haves:
  truths:
    - "Um destino com drive Windows (C:\\...\\{fornecedor}) resolve para o caminho absoluto LITERAL — sem prefixo do CWD do backend e sem C:→C_"
    - "Um destino UNC (\\\\srv\\share\\{x}) resolve absoluto e literal"
    - "Um destino sem drive/UNC continua relativo → juntado à base padrão (AUTOMATION_DEST_ROOT ou data_dir/organizados)"
    - "A detecção absoluto-vs-relativo independe do OS do runner (passa em Linux/WSL e em Windows)"
    - "Os SEGMENTOS após o anchor são sanitizados; o anchor (drive/UNC) NUNCA é sanitizado"
    - "Um destino absoluto fora da base não vira None (sem is_relative_to no ramo absoluto)"
    - "Aplicar com a raiz/drive inexistente (ex.: Z:\\) bloqueia no dry-run e no apply — nunca tenta criar a unidade"
    - "Undo de uma operação com destino absoluto devolve o arquivo à origem (ou restaura do CAS)"
  artifacts:
    - path: "backend/app/automation/naming.py"
      provides: "resolve_dest_folder com política absoluto/relativo (ntpath) + helper _is_abs_windows + _split_anchor"
      contains: "PureWindowsPath"
    - path: "backend/app/automation/stage.py"
      provides: "checagem de anchor existente (D-05) no dry_run e no apply_stage"
      contains: "anchor"
  key_links:
    - from: "backend/app/automation/naming.py::resolve_dest_folder"
      to: "ntpath / PureWindowsPath"
      via: "detecção de drive/UNC OS-independente"
      pattern: "PureWindowsPath|ntpath"
    - from: "backend/app/automation/stage.py::apply_stage"
      to: "Path(anchor).exists()"
      via: "bloqueio de raiz inexistente antes do mkdir"
      pattern: "anchor"
---

<objective>
Reescrever a POLÍTICA DE DESTINO da automação (backlog item 10): destino absoluto por
automação aceito literal (D-01/D-02/D-03), confinamento V4 removido para o ramo absoluto
(D-03), validação de raiz/drive existente sem mutilar (D-04/D-05). A detecção
absoluto-vs-relativo usa semântica Windows (`ntpath`/`PureWindowsPath`) mesmo rodando o
dev/CI em Linux/WSL.

Purpose: hoje o destino é confinado sob `data_dir\organizados` e um caminho absoluto
digitado é mutilado (`C:`→`C_`) e aninhado — gerando lixo como
`C:\ProgramData\...\organizados\C_\Users\...`. Esta fase torna o mover/copiar utilizável
de verdade.
Output: `naming.resolve_dest_folder` com dois ramos (absoluto literal / relativo+base),
sanitização só de segmentos, e checagem de anchor existente no `stage`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-CONTEXT.md
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-RESEARCH.md

# Código a alterar (já lido pelo planner — contratos relevantes abaixo):
@backend/app/automation/naming.py
@backend/app/automation/stage.py

<interfaces>
<!-- Contratos que o executor usa diretamente — sem explorar o codebase. -->

naming.py (atual):
- `strip_quotes(value: str | None) -> str` — normaliza aspas nas pontas (reusar).
- `sanitize_component(value: str, max_len: int | None = None) -> str` — sanitiza UM segmento (manter, é o passo por-segmento).
- `resolve_dest_folder(pattern: str, fields: dict[str,str], *, base_root: Path) -> Path | None` — HOJE confina V4 + sanitiza tudo. ALVO da reescrita.
- `_substitute(seg, fields, *, sanitize=False)` — substitui tokens por segmento (NÃO alterar aqui; é o Plano 02).
- `_MissingField` — exceção interna (campo faltante → None).

stage.py (atual):
- `_base_root() -> Path` — base relativa (AUTOMATION_DEST_ROOT ou data_dir/organizados). MANTER.
- `dry_run(session, *, content_hash) -> ApplyStageResult | None` — simula; ALVO: marcar blocked quando o anchor não existe.
- `apply_stage(session, *, content_hash, run_id, dry_run) -> ApplyStageResult` — aplica; ALVO: bloquear (revisão) quando o anchor não existe, ANTES do write-ahead/mkdir.
- `ApplyStageResult(blocked: bool, ...)` — já existe; reusar `blocked=True` para raiz inexistente.

Verificado no RESEARCH (rodado em WSL/Linux):
- `os.path.isabs(r"C:\\x")` → False (BUG no Linux). `ntpath.isabs(r"C:\\x")` → True.
- `PureWindowsPath(r"C:\\a\\b").anchor` → 'C:\\' ; `.parts` → ('C:\\','a','b').
- `PureWindowsPath(r"\\srv\\share\\NF").anchor` → '\\srv\\share\\' (UNC).
- `PureWindowsPath("rel/x").drive` → '' (relativo).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes RED da política de destino (naming + stage + undo)</name>
  <files>backend/tests/automation/test_naming.py, backend/tests/automation/test_stage.py, backend/tests/automation/test_undo.py</files>
  <behavior>
    Em test_naming.py (funções puras, sem disco):
    - test_dest_absolute_kept_literal: `resolve_dest_folder("C:\\Users\\x\\NF\\{cliente}", fields, base_root=tmp)` → str do resultado COMEÇA com "C:\\Users\\x\\NF\\" e NÃO contém o str(base_root) nem "C_" (anchor não mutilado, sem CWD prefixado).
    - test_dest_unc_absolute: `resolve_dest_folder("\\\\srv\\share\\{cliente}", ...)` → resultado preserva o anchor UNC "\\\\srv\\share\\".
    - test_dest_relative_uses_base: `resolve_dest_folder("NotasFiscais/{cliente}", fields, base_root=tmp)` → resultado é relativo a `tmp` (str começa com str(tmp)).
    - test_abs_detection_cross_os: a detecção de "C:\\x" como absoluto vale RODANDO EM LINUX — não usa os.path.isabs (o teste roda no CI Linux/WSL e deve passar).
    - test_segments_sanitized_anchor_kept: `resolve_dest_folder("C:\\NF\\{cliente}", {"cliente":"a:b/c"})` → o segmento vira "a_b_c" mas o anchor "C:\\" intacto.
    - test_abs_missing_field_blocks: token de campo faltante no ramo absoluto → None (D-07 preservado).
    - test_abs_no_confinement: `resolve_dest_folder("D:\\fora\\da\\base\\{cliente}", ...)` → NÃO retorna None (sem is_relative_to no ramo absoluto).
    Em test_stage.py (com tmp_path/fixtures existentes — classified_doc, automation_factory):
    - test_missing_root_blocks_dry_run: automação com dest_folder num drive/anchor inexistente (ex.: usar um caminho cujo anchor é uma pasta tmp inexistente OU um drive "Z:\\" e monkeypatch de Path.exists do anchor) → `dry_run(...)` retorna `blocked=True` (não levanta exceção).
    - test_missing_root_blocks_apply: o mesmo no `apply_stage(...)` → `blocked=True`, doc rebaixado a EM_REVISAO, SEM AuditLog de operação e SEM mkdir.
    Em test_undo.py:
    - test_undo_absolute_dest_restores_source: aplicar move com destino ABSOLUTO (sob tmp_path absoluto) e depois `undo_document` devolve o arquivo à origem (estende o padrão de test_undo_per_doc_restores_source com um dest_path absoluto fora de "organizados").
  </behavior>
  <action>Escreva os testes acima como RED (falham antes da implementação). Reaproveite as fixtures de `conftest.py` (`classified_doc`, `automation_factory`, `src_dir`/`dst_dir`, `tmp_path`). Para "anchor inexistente" sem depender do OS, use um anchor que comprovadamente não existe no runner: construa o dest_folder a partir de um drive Windows fictício (`"Z:\\inexistente\\{cliente}"`) e faça o teste tolerar ambos os ambientes via `monkeypatch.setattr` em `pathlib.Path.exists` para o anchor — OU prefira um caminho absoluto POSIX inexistente (`/nao_existe_xyz/{cliente}`) se o suporte POSIX-absoluto for incluído na Task 2 (alinhe com a decisão A2 do RESEARCH). NÃO logar valores. Use `pytest.importorskip` no topo se já houver (não duplicar). Rode `cd backend && uv run pytest tests/automation/test_naming.py tests/automation/test_stage.py tests/automation/test_undo.py -x` e confirme que os NOVOS casos FALHAM (RED). Commit: `test(09): falhando p/ politica de destino absoluto/relativo + anchor existente`.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/automation/test_naming.py -x -k "dest_absolute or dest_unc or dest_relative or abs_detection or segments_sanitized or abs_missing or abs_no_confinement" 2>&1 | grep -E "failed|error" | head</automated>
  </verify>
  <done>Os novos casos existem e FALHAM (RED) antes da implementação; testes legados de naming/stage/undo continuam coletáveis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever resolve_dest_folder (absoluto literal / relativo+base, sem .resolve(), sem is_relative_to no abs)</name>
  <files>backend/app/automation/naming.py</files>
  <behavior>
    - "C:\\Users\\x\\{cliente}" → Path absoluto literal: anchor "C:\\" preservado, segmentos após o anchor sanitizados, SEM .resolve(), SEM prefixo de base.
    - "\\\\srv\\share\\{x}" (UNC) → absoluto literal com anchor UNC preservado.
    - "rel/{x}" (sem drive/UNC) → base_root / segmentos sanitizados (comportamento atual mantido).
    - Detecção via ntpath/PureWindowsPath (OS-independente).
    - Ramo absoluto: NUNCA chama is_relative_to (não bloqueia destinos fora da base).
    - Campo faltante → None (D-07) em AMBOS os ramos.
  </behavior>
  <action>Reescreva `resolve_dest_folder` conforme RESEARCH Pattern 1/2 e o esboço de Code Examples. Importe `ntpath` e `PureWindowsPath` no topo. Adicione helpers privados: `_is_abs_windows(p: str) -> bool` (`bool(PureWindowsPath(p).drive) or ntpath.isabs(p)`; opcionalmente também POSIX-absoluto `/...` via `PurePosixPath(p).is_absolute()` — decisão A2 do RESEARCH: alinhe com a escolha feita nos testes da Task 1). Fluxo: `pattern = strip_quotes(pattern)`. Resolva os tokens+sanitize POR SEGMENTO como hoje (`_substitute(seg, fields, sanitize=False)` → `sanitize_component`), levantando/None em campo faltante (D-07 preservado). RAMO ABSOLUTO: `win = PureWindowsPath(pattern_bruto)`; anchor = `win.anchor` (NUNCA sanitizar); resolva os tokens/sanitize SÓ nos segmentos `win.parts[1:]`; monte `PureWindowsPath(anchor, *safe_parts)` e devolva `Path(str(...))` — SEM `.resolve()`, SEM `is_relative_to`. RAMO RELATIVO: `base_root / *safe_segmentos` (comportamento atual; a checagem `is_relative_to` no relativo é discrição — a sanitização por segmento já neutraliza traversal; pode mantê-la ou removê-la, documente a escolha). ATUALIZE o docstring de módulo e da função: remover a afirmação "confina V4" e registrar a mudança de postura D-03 (single-tenant; absoluto escreve onde o processo tiver permissão; sanitização por segmento mantida). NÃO logar valores. Rode os testes de naming. Commit: `feat(09): resolve_dest_folder absoluto literal/relativo via ntpath (sem .resolve, sem confinamento V4 no abs)`.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/automation/test_naming.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>Todos os testes de test_naming.py passam (GREEN), incluindo os legados de `{campo}`/`{data:aaaa-mm}` (não-regressão) e os novos casos absoluto/UNC/relativo/anchor/sem-confinamento.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Checagem de anchor existente (D-05) no dry_run e apply_stage</name>
  <files>backend/app/automation/stage.py, backend/tests/automation/test_stage.py</files>
  <behavior>
    - dry_run: destino cujo anchor (drive/UNC raiz) NÃO existe → ApplyStageResult.blocked=True, sem exceção, sem disco.
    - apply_stage: o mesmo → blocked=True; doc PROCESSANDO → EM_REVISAO; SEM AuditLog de operação; NUNCA chama mkdir/materialize.
    - Destino cujo anchor EXISTE (mkdir recursivo cria as subpastas) → fluxo normal materializa (subpastas criadas pelo fileops `_verified_write` já fazem `mkdir(parents=True)`).
  </behavior>
  <action>Adicione um helper de módulo em `stage.py` que extrai o anchor de um destino e verifica a existência: usar `PureWindowsPath(str(dest)).anchor or Path(str(dest)).anchor`; se houver anchor e `not Path(anchor).exists()` → o destino é inalcançável (D-05: nunca criar a unidade). Aplique a checagem APÓS resolver o plano (quando `plan.matched and not plan.blocked`) tanto no `dry_run` quanto no `apply_stage` (antes do write-ahead e do mkdir). Quando o anchor não existir: em `dry_run`, devolva `ApplyStageResult(... blocked=True ...)`; em `apply_stage`, trate como o ramo `plan.blocked` já existente — transite para EM_REVISAO (se PROCESSANDO) SEM AuditLog de operação e SEM tocar o disco, e logue só metadados (NUNCA o caminho com dados sensíveis — pode logar só "anchor inexistente"). Aplique a checagem também para os destinos de CÓPIA (`plan.copies`): se QUALQUER cópia ou o move tiver anchor inexistente → bloqueia tudo (consistente: nada materializa). Atualize o docstring de `stage.py` mencionando D-05 (raiz deve existir; subpastas são criadas). Rode os testes de stage. Commit: `feat(09): bloqueia destino com raiz/drive inexistente no dry-run e apply (D-05)`.</action>
  <verify>
    <automated>cd backend && uv run pytest tests/automation/test_stage.py tests/automation/test_undo.py -x 2>&1 | tail -5</automated>
  </verify>
  <done>test_missing_root_blocks_dry_run e test_missing_root_blocks_apply passam; nenhuma regressão em test_stage.py/test_undo.py; mkdir nunca é chamado para anchor inexistente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valor de campo (IA/documento) → caminho de arquivo | input não-confiável vira segmento de path |
| padrão de destino configurado pelo usuário → filesystem | caminho absoluto arbitrário (postura D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | resolve_dest_folder (valor de campo com `../` ou separadores) | mitigate | `sanitize_component` neutraliza `/ \ : ..` em CADA segmento; o anchor NÃO vem de campo |
| T-09-02 | Elevation | destino absoluto fora da base escreve em qualquer pasta | accept | ACEITO por D-03 (single-tenant, máquina do cliente). Risco residual documentado no docstring; allowlist é deferred |
| T-09-03 | Tampering | `.resolve()` reintroduzido canoniza contra CWD (lixo `backend\C:\...`) | mitigate | NÃO chamar `.resolve()` no ramo absoluto; teste fixa o destino literal (test_dest_absolute_kept_literal) |
| T-09-04 | Denial of Service | `mkdir(parents=True)` num drive inexistente estoura no meio do apply | mitigate | checar `Path(anchor).exists()` ANTES do mkdir, no dry-run E no apply (D-05) |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/automation -q` verde (suíte de automação completa).
- Detecção absoluto/relativo passa NO RUNNER LINUX/WSL (não usa os.path.isabs).
- Nenhum `.resolve()` no ramo absoluto de `resolve_dest_folder` (`grep -n "\.resolve()" backend/app/automation/naming.py` revisado).
</verification>

<success_criteria>
- Destino absoluto Windows/UNC resolve literal (sem CWD, sem `C_`).
- Destino relativo continua sob a base padrão.
- Anchor inexistente bloqueia (dry-run e apply) sem criar unidade nem mkdir.
- Undo com destino absoluto devolve à origem.
- Sem mudança de schema (confirmado pelo RESEARCH: `dest_folder`/`dest_path` já são string).
</success_criteria>

<output>
Create `.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-01-SUMMARY.md` when done
</output>
