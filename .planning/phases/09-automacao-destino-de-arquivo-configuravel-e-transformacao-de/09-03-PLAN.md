---
phase: 09-automacao-destino-de-arquivo-configuravel-e-transformacao-de
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - frontend/src/pages/AutomationsPage.tsx
autonomous: false
requirements: [BL-10, BL-11]

must_haves:
  truths:
    - "No construtor de automações, a prévia de uma ação 'mover'/'copiar' com destino absoluto mostra o caminho absoluto final (C:\\...\\segmentos) — não mais um caminho relativo nem mutilado"
    - "A prévia aplica os filtros inline (palavras/maiusc/sem_acento/truncar/substituir/formato/padrao) — não apenas {campo} cru"
    - "A prévia continua sanitizando chars proibidos do Windows por segmento (D-08) e mostra o resultado já sanitizado"
    - "Na DryRunPage, a coluna Destino exibe o caminho ABSOLUTO real resolvido pelo backend (origem→destino), sem C_ e sem aninhamento sob organizados — COBERTO AUTOMATICAMENTE pelo teste de integração test_dry_run_absolute_dest_shows_real_path (plano 09-01 Task 4); a verificação humana abaixo apenas confirma a renderização visual"
  artifacts:
    - path: "frontend/src/pages/AutomationsPage.tsx"
      provides: "resolvePattern client-side com filtros inline + prévia de caminho absoluto"
      contains: "palavras"
  key_links:
    - from: "frontend/src/pages/AutomationsPage.tsx::resolvePattern"
      to: "espelho dos filtros do backend (_substitute)"
      via: "aplicação client-side dos mesmos filtros para a prévia"
      pattern: "palavras|sem_acento|maiusc"
    - from: "frontend/src/pages/DryRunPage.tsx (dest_path)"
      to: "DryRunRow.dest_path (caminho absoluto real do backend)"
      via: "render existente da coluna Destino"
      pattern: "dest_path"
---

<objective>
Refletir as mudanças da Phase 9 na UI (D-04/D-09): no CONSTRUTOR de automações, a
pré-visualização ao vivo deve (1) mostrar o CAMINHO ABSOLUTO final quando o destino é
absoluto e (2) aplicar os FILTROS inline (não só `{campo}` cru). Na DryRunPage, a coluna
Destino já exibe `dest_path` — confirmar que ela mostra o caminho absoluto real resolvido
pelo backend (após 09-01/09-02) e que a sinalização blocked cobre o caso de raiz
inexistente.

Purpose: D-09 — o usuário precisa VER o resultado final (nome + caminho absoluto + filtros
aplicados) com dados de exemplo, antes de aplicar. Liga com o caso real do teste do piloto.
Output: `resolvePattern` client-side com filtros + prévia de caminho absoluto no
AutomationsPage; verificação ao vivo do dry-run mostrando o caminho real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-CONTEXT.md
@backend/app/automation/naming.py
@frontend/src/pages/AutomationsPage.tsx
@frontend/src/pages/DryRunPage.tsx

# Depende dos contratos de 09-01 (destino absoluto literal) e 09-02 (filtros).
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-01-SUMMARY.md
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-02-SUMMARY.md

<interfaces>
AutomationsPage.tsx (atual):
- `resolvePattern(pattern, fields)` (linhas ~173-185): HOJE faz `token.split(':')[0]` (descarta o filtro) e remove `WINDOWS_INVALID` (`[<>:"|?*]`). ALVO: aplicar os filtros inline + manter a sanitização por segmento.
- `renderPreview(a: ActDraft)` (linhas ~574-609): monta a string de prévia. ALVO: para move/copy com destino absoluto, mostrar o caminho absoluto montado por segmento.
- `sampleValue(field)` → usa `field.hint` ou `field.name` como valor de exemplo.
- `stripQuotes(value)` (linha ~161) espelha o backend.

DryRunPage.tsx (atual): a coluna "Destino" renderiza `r.dest_path ?? '—'` (linhas ~346-348). NÃO precisa de mudança de código (o backend já devolve o dest_path absoluto após 09-01); a verificação ao vivo confirma o comportamento.

Backend (referência para espelhar os filtros — NÃO importável no front):
- Filtros v1 (09-02): palavras=N, letras=N/truncar=N, maiusc, minusc, sem_acento, padrao=X, substituir=de>para, formato=aaaa-mm-dd (+ atalho legado {data:aaaa-mm}).
- Sanitização por segmento (sanitize_component): substitui `< > : " / \ | ? *` por "_", DEPOIS dos filtros (D-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: resolvePattern client-side com filtros inline + prévia de caminho absoluto</name>
  <files>frontend/src/pages/AutomationsPage.tsx</files>
  <action>Reescreva `resolvePattern` para espelhar o engine de filtros do backend (09-02) na PRÉVIA. Para cada token `{campo:filtro=arg:filtro}`: separe `campo` e a cadeia de filtros (split por `:`), resolva o valor de exemplo (`sampleValue`), e aplique os filtros em pipeline com dispatch explícito (NUNCA eval): `palavras=N` (primeiras N palavras), `letras=N`/`truncar=N` (N chars), `maiusc`/`minusc`, `sem_acento` (use `String.prototype.normalize('NFD').replace(/\p{Diacritic}/gu,'')` — equivalente client-side do NFKD+drop combining), `substituir=de>para` (replace literal), `formato=aaaa-mm-dd` (replace de aaaa/mm/dd como o backend; se não houver data de exemplo, mostre o token cru), `padrao=X` (usa X quando o campo não tem exemplo). Filtro desconhecido → inerte. Mantenha a retrocompat do atalho legado de data (`{data:aaaa-mm}`). Aplique a sanitização de chars Windows POR SEGMENTO e DEPOIS dos filtros (D-08): para a prévia de PASTA (move/copy), fatie o padrão por `/` e `\`, aplique a sanitização (`WINDOWS_INVALID`) só nos segmentos APÓS o anchor — detecte o anchor absoluto Windows com uma regex simples no front (`^[A-Za-z]:[\\/]` para drive, `^\\\\[^\\]+\\[^\\]+` para UNC) e NÃO sanitize o anchor (espelha 09-01); para a prévia de NOME (rename), sanitize o componente inteiro como hoje. Em `renderPreview`: quando a ação for move/copy e o padrão for absoluto, exiba o caminho absoluto montado (anchor + segmentos resolvidos, juntados por `\`); quando relativo, mantenha a prévia atual (segmentos). Atualize o texto de ajuda da seção de tokens para mencionar a sintaxe de filtros (ex.: `{fornecedor:maiusc:palavras=2}`, `{data:formato=aaaa-mm-dd}`) — copy curto, em pt-BR. Rode o build do frontend.</action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -8</automated>
  </verify>
  <done>O build do frontend passa; `resolvePattern` aplica os filtros inline e a prévia de move/copy absoluto mostra o caminho absoluto (anchor preservado). Sem npm novo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação visual — prévia com filtros + caminho absoluto + dry-run real</name>
  <what-built>
    A prévia do construtor de automações agora aplica os filtros inline e mostra o caminho
    absoluto final; o backend resolve destinos absolutos literais e a DryRunPage exibe o
    caminho real (origem→destino). NOTA: o comportamento "DryRunPage mostra o caminho
    absoluto real" já está coberto por teste automatizado de integração
    (test_dry_run_absolute_dest_shows_real_path, plano 09-01 Task 4); este checkpoint humano
    cobre apenas o que NÃO é automatizável — a renderização visual da prévia client-side
    (filtros aplicados + anchor preservado no DOM) e a UX do bloqueio por raiz inexistente.
  </what-built>
  <how-to-verify>
    1. Suba o backend e o frontend em dev (uvicorn + `npm run dev`).
    2. Em Automações, crie/edite uma automação com uma ação "Mover" e destino absoluto, ex.:
       `C:\Users\Usuario\Downloads\NOTAS_FISCAIS\{fornecedor:maiusc:palavras=1}` (ou um
       caminho absoluto que exista na sua máquina, p.ex. um diretório de teste).
       Verifique: a PRÉVIA mostra o caminho absoluto completo, com o anchor `C:\...` intacto
       (sem `C_`) e o `{fornecedor}` transformado (maiúsculo + 1 palavra).
    3. Adicione uma ação "Renomear" com `{cliente}_{numero}` e um filtro (ex.: `{cliente:sem_acento}`)
       e confirme a prévia do nome.
    4. Vá à tela de Pré-visualização (dry-run) com ao menos 1 documento pronto. Confira que a
       coluna Destino mostra o caminho ABSOLUTO real (não `...\organizados\C_\...`).
    5. (Opcional) Configure um destino com raiz inexistente (ex.: `Z:\nao_existe\{cliente}`) e
       confirme que a linha aparece como "Campo faltando — enviado para revisão" (bloqueado), e
       que nada é movido ao aplicar.
  </how-to-verify>
  <action>Checkpoint de verificação visual — a automação da Task 1 já está construída. Apresente os passos de <how-to-verify> ao usuário e aguarde o sinal de aprovação. Não há código novo neste task.</action>
  <verify><human-check>Usuário confirma a prévia com filtros + caminho absoluto e o dry-run com caminho real.</human-check></verify>
  <done>Usuário responde "aprovado" (ou os ajustes pedidos foram aplicados e re-verificados).</done>
  <resume-signal>Digite "aprovado" ou descreva o que apareceu diferente do esperado (anchor mutilado, filtro não aplicado, caminho relativo, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valor de exemplo do template → prévia (string no DOM) | só render de texto; sem execução |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | parser de filtros client-side | mitigate | dispatch explícito por nome; sem `eval`/`Function`; filtro desconhecido inerte |
| T-09-09 | Info Disclosure | valor real de campo na prévia | accept | a prévia usa SÓ valores de EXEMPLO do template (hint/name), nunca dados reais do documento |
</threat_model>

<verification>
- `cd frontend && npm run build` verde.
- Cobertura AUTOMÁTICA do dest_path absoluto no dry-run: `cd backend && uv run pytest tests/test_api_automations.py -k absolute_dest -q` (teste do plano 09-01 Task 4) — o truth da DryRunPage não depende de verificação só-humana.
- Verificação humana ao vivo confirma o que não é automatizável: prévia client-side com filtros + caminho absoluto no DOM; UX do bloqueio por raiz inexistente.
</verification>

<success_criteria>
- Prévia do construtor reflete filtros inline e caminho absoluto (D-09).
- DryRunPage mostra o caminho final REAL (D-04).
- Sanitização por segmento visível na prévia, anchor preservado.
- Zero dependência npm nova.
</success_criteria>

<output>
Create `.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-03-SUMMARY.md` when done
</output>
