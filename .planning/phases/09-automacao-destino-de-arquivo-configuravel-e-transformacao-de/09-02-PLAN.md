---
phase: 09-automacao-destino-de-arquivo-configuravel-e-transformacao-de
plan: 02
type: tdd
wave: 2
depends_on: ["09-01"]
files_modified:
  - backend/app/automation/naming.py
  - backend/tests/automation/test_naming.py
autonomous: true
requirements: [BL-11]

must_haves:
  truths:
    - "{fornecedor:palavras=1} sobre 'IGUACU DIST. DE PROD.' produz 'IGUACU'"
    - "{x:letras=8} e {x:truncar=8} truncam a 8 chars; {x:maiusc}/{x:minusc} mudam a caixa"
    - "{x:sem_acento} sobre 'IGUAÇU AÇÃO' produz 'IGUACU ACAO'"
    - "{x:substituir=de>para} faz substituição literal simples"
    - "Campo ausente + {x:padrao=Geral} produz 'Geral' (não bloqueia)"
    - "{data:formato=aaaa-mm-dd} formata a data; o atalho legado {data:aaaa-mm} continua funcionando"
    - "Filtros encadeáveis: {x:maiusc:palavras=2} aplica maiusc e depois 2 palavras (pipeline)"
    - "A sanitização de chars Windows roda DEPOIS dos filtros (D-08)"
    - "{campo} simples (sem filtro) e os testes legados continuam intactos (não-regressão)"
    - "Filtro desconhecido é inerte (não quebra o token; nunca eval)"
  artifacts:
    - path: "backend/app/automation/naming.py"
      provides: "parser de filtros inline no _substitute + _strip_accents + dispatch explícito"
      contains: "unicodedata"
  key_links:
    - from: "backend/app/automation/naming.py::_substitute"
      to: "pipeline de filtros (split por ':')"
      via: "dispatch explícito por nome de filtro (sem eval)"
      pattern: "palavras|sem_acento|substituir|formato"
    - from: "_substitute (filtros)"
      to: "sanitize_component"
      via: "ordem D-08: filtros ANTES, sanitize DEPOIS"
      pattern: "sanitize"
---

<objective>
Implementar o ENGINE DE TRANSFORMAÇÃO de valores (backlog item 11): filtros inline
encadeáveis no token (`{campo:filtro=arg:filtro}`, D-06) com o conjunto v1 (D-07):
`palavras=N`, `letras=N`/`truncar=N`, `maiusc`, `minusc`, `sem_acento`, `padrao=…`,
`substituir=de>para`, e `formato=` (expondo o `_fmt_date` existente). A sanitização de
chars proibidos do Windows continua automática, aplicada DEPOIS das transformações
(D-08, por segmento).

Purpose: hoje o nome/pasta só aceita `{campo}` cru + sanitize — não dá para encurtar
"IGUACU DIST. DE PROD. OTICOS LTDA" para "IGUACU". Os filtros tornam o nome
transformável como o usuário deseja.
Output: `_substitute` com um pipeline de filtros (dispatch explícito, nunca `eval`),
afetando tanto `name_pattern` (rename) quanto os segmentos de `dest_folder` (move/copy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-CONTEXT.md
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-RESEARCH.md
@backend/app/automation/naming.py

# Depende de 09-01 (resolve_dest_folder já reescrito); este plano só mexe em
# _substitute/_fmt_date/_TOKEN_RE e nos testes de naming.
@.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-01-SUMMARY.md

<interfaces>
naming.py (relevante):
- `_TOKEN_RE = re.compile(r"\{([^{}:]+)(?::([^{}]+))?\}")` — group(1)=campo, group(2)=spec (tudo após o 1º ':'). REUSAR o group(2) como cadeia de filtros.
- `_substitute(pattern, fields, *, sanitize)` — função onde os filtros entram (dentro de `repl`, ANTES do sanitize_component).
- `_fmt_date(iso: str, spec: str) -> str | None` — formatação de data existente; expor como filtro `formato=`.
- `_MissingField` — campo ausente/vazio levanta isto (→ None). ATENÇÃO: `padrao=` deve SUPRIMIR este bloqueio quando presente (RESEARCH Pattern 4 / A3).
- `sanitize_component(value)` — passo final por segmento (D-08).

RESEARCH (verificado):
- `_strip_accents(s)`: `"".join(c for c in unicodedata.normalize("NFKD", s) if not unicodedata.combining(c))` → "IGUAÇU AÇÃO"→"IGUACU ACAO".
- Retrocompat (Pitfall 4 / A1, agora RESOLVED): spec sem `=` contendo `aaaa`/`mm`/`dd` → tratar como `_fmt_date` (legado `{data:aaaa-mm}`); senão pipeline de filtros.
</interfaces>
</context>

<feature>
  <name>Filtros inline encadeáveis no token (conjunto v1, D-06/D-07/D-08)</name>
  <files>backend/app/automation/naming.py, backend/tests/automation/test_naming.py</files>
  <behavior>
    Cada caso é um teste unitário puro (funções de naming, sem disco). Use `resolve_pattern`
    para nome e, onde fizer sentido, `resolve_dest_folder` para confirmar que os filtros
    valem por segmento de pasta também.

    - test_filter_palavras: `resolve_pattern("{fornecedor:palavras=1}", {"fornecedor":"IGUACU DIST. DE PROD."})` → contém "IGUACU" e NÃO "DIST".
    - test_filter_letras / test_filter_truncar: `{x:letras=8}` e `{x:truncar=8}` sobre "ABCDEFGHIJ" → "ABCDEFGH".
    - test_filter_maiusc / test_filter_minusc: caixa alterada.
    - test_filter_sem_acento: `{x:sem_acento}` sobre "IGUAÇU AÇÃO" → "IGUACU ACAO".
    - test_filter_substituir: `{x:substituir=LTDA>}` ou `{x:substituir=a>e}` → substituição literal simples (de>para).
    - test_filter_padrao_default_when_missing: campo AUSENTE + `{tipo:padrao=Geral}` → "Geral" (NÃO bloqueia / não retorna None).
    - test_filter_formato_explicit: `{data:formato=aaaa-mm-dd}` sobre "2026-06-17" → "2026-06-17"; `{data:formato=aaaa-mm}` → "2026-06".
    - test_legacy_date_shortcut_still_works: `{data:aaaa-mm}` (sem `formato=`, atalho legado) continua formatando — NÃO regressão.
    - test_filter_chain: `{x:maiusc:palavras=2}` sobre "iguacu dist prod" → "IGUACU DIST".
    - test_sanitize_after_filter: um filtro que produza um char proibido (ex.: `substituir=a>/`) → o resultado é sanitizado DEPOIS (o "/" vira "_"), provando a ordem D-08.
    - test_unknown_filter_is_inert: `{x:filtroinexistente}` → o valor cru passa (token não quebra, sem eval).
    - test_plain_token_unchanged: `{campo}` simples segue idêntico ao comportamento atual.
  </behavior>
  <implementation>
    Estenda `_substitute` (dentro de `repl`), após obter o `value` cru do campo:
    1. Se `spec is None` → nenhum filtro (caminho atual intacto, retrocompat de `{campo}`).
    2. Se o spec NÃO contém `=` E casa o atalho de data (contém `aaaa`/`mm`/`dd`) → `_fmt_date(value, spec)` (legado; None → _MissingField como hoje).
    3. Caso contrário → PIPELINE: dividir `spec` por `:` numa lista de filtros e aplicar em ordem, com DISPATCH EXPLÍCITO por nome (NUNCA `eval`/`format` dinâmico). Para cada filtro `f`:
       - `palavras=N` → `" ".join(value.split()[:int(N)])`
       - `letras=N` / `truncar=N` → `value[:int(N)]`
       - `maiusc` → `value.upper()` ; `minusc` → `value.lower()`
       - `sem_acento` → `_strip_accents(value)`
       - `substituir=de>para` → `de, _, para = arg.partition(">"); value.replace(de, para)`
       - `formato=spec` → `_fmt_date(value, spec)` (None → _MissingField: pediu formato e o valor não é data)
       - `padrao=arg` → tratado ESPECIAL (ver abaixo)
       - filtro desconhecido / `int()` inválido → INERTE (não altera o value; nunca eval) — falha-fechada amigável.
    `padrao=` (D-07 / RESEARCH Pattern 4 / A3 RESOLVED): o bloqueio por campo ausente acontece HOJE antes de chegar ao spec. Para `padrao=` funcionar, detecte se o spec contém um filtro `padrao=` ANTES de levantar `_MissingField`: se o campo está ausente/vazio E há `padrao=X` na cadeia → use `X` como value (NÃO levante). Confirme com o teste `test_filter_padrao_default_when_missing`.
    Adicione `import unicodedata` no topo e o helper `_strip_accents(s: str) -> str` (RESEARCH Code Examples).
    `_TOKEN_RE`: o group(2) atual já captura tudo após o 1º `:` (inclusive `:` internos), então o split por `:` dentro do `repl` é suficiente — NÃO precisa mudar a regex (confirme com `test_filter_chain`). NÃO logar valores (V7/V9). A sanitização permanece a cargo de `resolve_pattern`/`resolve_dest_folder` (sanitize_component DEPOIS, D-08) — NÃO sanitizar dentro do pipeline.
    Commits (RED→GREEN): `test(09): filtros inline falhando (conjunto v1 D-07)` → `feat(09): engine de filtros inline encadeaveis no token (dispatch explicito, sem eval)`.
  </implementation>
  <verify>
    <automated>cd backend && uv run pytest tests/automation/test_naming.py -x -k "filter_ or legacy_date or plain_token or sanitize_after" 2>&1 | tail -5</automated>
  </verify>
  <done>Todos os casos de filtro (palavras/letras/truncar/maiusc/minusc/sem_acento/substituir/padrao/formato/chain/unknown), o atalho legado de data e `{campo}` simples passam (GREEN); a ordem D-08 é comprovada por test_sanitize_after_filter; nenhum `eval`/`exec` em naming.py.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec de filtro no padrão (usuário) → execução | nome de filtro + arg interpretados |
| valor de campo (IA) → transformação → segmento de path | input não-confiável transformado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | parser de filtros (injeção via nome/arg) | mitigate | dispatch EXPLÍCITO por nome literal; nunca `eval`/`format` dinâmico; filtro desconhecido = inerte (espelha rules._OPERATORS, V5) |
| T-09-06 | Tampering | filtro produz separador/`..` no valor | mitigate | `sanitize_component` roda DEPOIS dos filtros, por segmento (D-08) — neutraliza qualquer char proibido introduzido |
| T-09-07 | Info Disclosure | valor de campo vazado em log | mitigate | NÃO logar valores nem o padrão resolvido (V7/V9, regra do projeto) |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/automation/test_naming.py -q` verde (legados + novos filtros).
- `grep -n "eval(\|exec(" backend/app/automation/naming.py` → vazio (sem eval/exec).
- Ordem D-08 comprovada por `test_sanitize_after_filter`.
</verification>

<success_criteria>
- Todos os filtros v1 (D-07) funcionam e são encadeáveis (D-06).
- `padrao=` supre o bloqueio de campo ausente (A3).
- Atalho legado `{data:aaaa-mm}` e `{campo}` simples sem regressão.
- Sanitização aplicada após os filtros (D-08).
- Filtro desconhecido inerte; nenhum `eval`.
</success_criteria>

<output>
Create `.planning/phases/09-automacao-destino-de-arquivo-configuravel-e-transformacao-de/09-02-SUMMARY.md` when done
</output>
