---
phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
plan: 03
subsystem: frontend
tags: [templates, frontend, signals, boolean-groups, regex, tooltips, react, tsc-drift-gate]

# Dependency graph
requires:
  - phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
    plan: 02
    provides: "contrato HTTP /templates com signals = list[list[{mode,value}]] (grupos E/OU); doc_type dormente; field.name byte-a-byte"
provides:
  - "TemplatesPage redesenhada: Passo 1 (sinais, sem IA) / Passo 2 (campos, com IA); construtor de grupos E/OU texto|regex; campos densos com ⚙ Avançado; tooltips ⓘ hover CSS"
  - "types.ts: SignalMode/SignalCondition/SignalGroup/Signals espelhando o contrato do Plano 02"
  - "index.css: token --tip-shadow (light+dark) + classes do construtor (.group/.cond/.or-div/.frow/.adv/.info/.tip) tokenizadas"
affects: [frontend, templates, ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Drafts com chave local React (CondDraft/GroupDraft/FieldDraft com {key}) + handlers imutáveis (map/filter) para grupos/condições/campos"
    - "Tooltip ⓘ via CSS :hover puro (.info .tip) — zero JS de estado de hover, zero lib npm (T-06.1-14)"
    - "⚙ Avançado por linha via estado React (advOpen no draft) — NÃO via DOM querySelector"
    - "Regex de sinal string-only no frontend: apenas armazena/envia, nunca compila/executa (T-06.1-12)"

key-files:
  created: []
  modified:
    - "frontend/src/types.ts"
    - "frontend/src/index.css"
    - "frontend/src/pages/TemplatesPage.tsx"

key-decisions:
  - "signals re-tipado como Signals (list[list[SignalCondition]]) em Template/Create/Patch; doc_type mantido nos tipos (compat backend) mas removido do form (D-T5); api.ts sem mudança de assinatura"
  - "Construtor portado 1:1 do mockup, mas com estado React (não DOM): grupos/condições/campos em drafts com key estável; ⚙ Avançado via advOpen no draft (não querySelector)"
  - "CSS do construtor tokenizado: classes do mockup (.info/.tip/.group/.cond/.or-div/.frow/.adv) adicionadas a index.css usando tokens (--surface/--border/--tip-shadow/--text-*); tokens --info/--ai/--tip-shadow adicionados (light+dark)"
  - "Submit descarta condições vazias e grupos sem condição (signals pode ficar []); field.name enviado como digitado (sem snake_case — D-T6/D-T9)"

patterns-established:
  - "Espelhamento de schema backend↔frontend validado por gate tsc/npm run build no mesmo plano/wave (T-06.1-13)"
  - "Tooltip explicativo ⓘ reutilizável via .info/.tip (hover CSS puro), variante .info.r alinhada à direita"

requirements-completed: [D-T0, D-T3, D-T4, D-T6, D-T7, D-T8, D-T9]

# Metrics
duration: ~6min
completed: 2026-06-18
---

# Phase 06.1 Plan 03: Frontend Redesign de Templates — Grupos E/OU + Campos Densos + Tooltips Summary

**`TemplatesPage.tsx` reescrita 1:1 com o mockup aprovado: pipeline explícito Passo 1 "Como reconhecer este tipo — sem IA" (construtor de grupos OU de condições E, cada uma `contém o texto`|`corresponde ao padrão (regex)`) e Passo 2 "O que extrair — com IA" (linhas densas Nome|Tipo|Obrig.|⚙ Avançado|remover, com regex+dica reveladas no ⚙), tooltips ⓘ via CSS `:hover` puro (zero lib), campo `doc_type` removido do form (D-T5) e `field.name` preservado byte-a-byte (D-T9); tipos TS alinhados ao contrato de grupos do Plano 02 com `npm run build` verde (gate de drift T-06.1-13).**

## Performance
- **Duration:** ~6 min
- **Started:** 2026-06-18
- **Completed:** 2026-06-18
- **Tasks:** 2 (feat + feat)
- **Files modified:** 3 (0 criados, 3 modificados)

## Accomplishments
- **Task 1 — Tipos + token CSS:** `types.ts` ganhou `SignalMode = 'texto'|'regex'`, `SignalCondition {mode,value}`, `SignalGroup = SignalCondition[]`, `Signals = SignalGroup[]` espelhando o contrato HTTP do Plano 02. `signals: string[]` → `signals: Signals` em `Template`/`TemplateCreate`; `signals?` → `Signals` em `TemplatePatch`. `doc_type` mantido (dormente, opcional no Create) e `TemplateField`/`TemplateFieldCreate` inalterados (ponte D-T9). `index.css` recebeu o token `--tip-shadow` em `:root` (light) e `[data-theme="dark"]` (dark), além dos tokens `--info`/`--info-bg`/`--ai`/`--ai-bg` exigidos pelos badges. `api.ts` sem mudança de assinatura (já tipado por `TemplateCreate`/`TemplatePatch`).
- **Task 2 — TemplatesPage redesign:** reescrita portando o mockup com **estado React** (não DOM):
  - **Passo 1 (sem IA, D-T0/D-T3/D-T4):** grupos `.group` ("Grupo N — todas (E)"), cada condição `.cond` com `<select>` `contém o texto`|`corresponde ao padrão (regex)` + input (mono quando regex) + remover; prefixo "E" a partir da 2ª condição; divisor "OU" entre grupos; botões "+ E — adicionar condição" e "+ OU — adicionar grupo". Handlers imutáveis (`addGroup`/`addCond`/`patchCond`/`removeCond`).
  - **Passo 2 (com IA, D-T7):** cabeçalho `fhead` + linhas densas `frow` grid `1fr 140px 96px 36px 34px` = Nome | Tipo (FIELD_TYPES) | Obrig. (Switch) | ⚙ (toggle `advOpen` por campo via estado React) | remover. ⚙ revela regex de validação (mono) + dica para a IA.
  - **Tooltips ⓘ (D-T8):** classes `.info`/`.tip` com `:hover` puro, variante `.info.r`; microcopy do mockup portada. Zero JS de hover, zero lib (package.json inalterado, T-06.1-14).
  - **Submit:** payload `{name, signals, fields}` SEM `doc_type` (D-T5); condições vazias/grupos vazios descartados; `field.name` enviado como digitado (D-T6/D-T9).
  - **Grid de cards:** subtítulo agora mostra "N grupos de sinais" e o rodapé conta condições (`signals.reduce`), substituindo o antigo `doc_type`/contagem plana.
- **Gate de drift (T-06.1-13):** `npm run build` (`tsc -b && vite build`) verde — 82 módulos, sem erros de tipo backend↔frontend.

## Task Commits
1. **Task 1: Tipos TS de sinais em grupos + token --tip-shadow** - `c728c14` (feat)
2. **Task 2: TemplatesPage redesign — grupos E/OU + campos densos + tooltips (mockup)** - `77f58e8` (feat)

**Plan metadata:** (final docs commit)

## Files Created/Modified
- `frontend/src/types.ts` - `SignalMode`/`SignalCondition`/`SignalGroup`/`Signals` exportados; `signals` re-tipado em `Template`/`TemplateCreate`/`TemplatePatch`; `doc_type` dormente; `TemplateField`/`TemplateFieldCreate` intactos (D-T9).
- `frontend/src/index.css` - token `--tip-shadow` (light+dark) e `--info`/`--ai` (light+dark); bloco "Template builder (Fase 06.1 redesign)" com classes `.info`/`.tip`/`.group`/`.cond`/`.or-div`/`.add-link`/`.fhead`/`.frow`/`.adv` tokenizadas (sem hex hardcode fora de definição de token).
- `frontend/src/pages/TemplatesPage.tsx` - reescrita completa: drafts com key local, handlers imutáveis de grupos/condições/campos, Passo 1/Passo 2, ⚙ Avançado por estado React, tooltips ⓘ, submit sem doc_type, valores texto puro.

## Decisions Made
- **Estado React, não DOM:** o mockup usa `querySelector`/`classList.toggle`; a porta usa estado controlado (`advOpen` no draft de campo, `mode`/`value` nos drafts de condição) com keys estáveis, mantendo o padrão `FieldDraft & { key }` já existente.
- **CSS tokenizado em index.css:** as classes do mockup foram adicionadas ao design system global usando os tokens (`--surface`/`--border`/`--tip-shadow`/`--text-*`), não inline; tokens faltantes (`--info`/`--ai`/`--tip-shadow`) adicionados em ambos os temas.
- **doc_type fora do form mas no tipo:** removido do formulário e do payload (D-T5); mantido nas interfaces TS por compat com o backend (coluna dormente).

## Deviations from Plan
None - plano executado como escrito. (Tokens auxiliares `--info`/`--ai` foram adicionados junto ao `--tip-shadow` por serem dependência das classes `.sec-tag`/`.t-noai`/`.t-ai` portadas do mockup — extensão natural da Task 1, sem mudança de contrato.)

## Issues Encountered
- O grep de aceitação `doc_type` retornava 2 ocorrências em comentários (documentando a AUSÊNCIA do campo). Reescritos para "tipo de documento" — o grep agora retorna 0, confirmando que nem código nem payload referenciam `doc_type` (D-T5). Sem impacto funcional.

## User Setup Required
None - nenhuma configuração externa.

## Verification (human-check, fim de fase)
A Task 2 inclui uma verificação visual humana (não-bloqueante neste plano autônomo) a ser feita ao fim da fase: abrir a TemplatesPage, criar "Nota Fiscal — TryLab" com sinais (contém "TryLab" E corresponde a `\d{44}`) OU (contém "12.345.678/0001-99") e campos Emitente/CNPJ do emitente/Número da NF/Valor total; conferir densidade das linhas, ⚙ Avançado abre/fecha, tooltips ⓘ no hover, badges "sem IA"/"com IA"; salvar, reabrir e confirmar persistência dos grupos e campos contra `templates-redesign-mockup.html`.

## Next Phase Readiness
- Wave 3 (frontend) completa: a UX validada em mockup está fiada ao contrato HTTP de grupos do Plano 02; build verde sem drift. Fase 06.1 (redesign + sinais E/OU) pronta para verificação visual e fechamento.

## Threat Surface Notes
- T-06.1-11 (XSS): mitigado — valores de sinal/regex/nome renderizados como texto puro; `grep dangerouslySetInnerHTML` = 0.
- T-06.1-12 (DoS regex no frontend): aceito/mitigado — frontend só armazena/envia a string; não compila nem executa regex no navegador.
- T-06.1-13 (drift backend↔frontend): mitigado — `npm run build` (tsc) verde como gate, tipos atualizados no mesmo plano/wave do schema.
- T-06.1-14 (supply chain tooltip): mitigado — tooltip via CSS `:hover` puro; package.json inalterado (zero npm novo).
Nenhuma nova superfície de ameaça fora do `<threat_model>` do plano.

## Self-Check: PASSED
- FOUND: frontend/src/types.ts
- FOUND: frontend/src/index.css
- FOUND: frontend/src/pages/TemplatesPage.tsx
- FOUND commit: c728c14 (feat Task 1)
- FOUND commit: 77f58e8 (feat Task 2)

---
*Phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex*
*Completed: 2026-06-18*
