---
phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
plan: 03
type: execute
wave: 3
depends_on: ["06.1-02"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/index.css
  - frontend/src/pages/TemplatesPage.tsx
autonomous: true
requirements: [D-T0, D-T3, D-T4, D-T6, D-T7, D-T8, D-T9]
nyquist_compliant: true

must_haves:
  truths:
    - "A tela de Template explicita o pipeline: Passo 1 'Como reconhecer este tipo — sem IA' (sinais) e Passo 2 'O que extrair — com IA' (campos) (D-T0)"
    - "O usuário monta sinais em grupos: dentro do grupo condições E; entre grupos OU; cada condição é 'contém o texto' ou 'corresponde ao padrão (regex)' (D-T3/D-T4)"
    - "Cada campo é uma linha densa Nome | Tipo | Obrigatório | ⚙ Avançado | remover; ⚙ revela regex de validação + dica para a IA (D-T7)"
    - "Explicações aparecem via ícone ⓘ com tooltip no hover (CSS puro, sem JS/lib) (D-T8)"
    - "O campo 'Tipo de documento' (doc_type) não existe mais no formulário (D-T5)"
    - "field.name é enviado exatamente como digitado (sem snake_case forçado) — tokens de automação batem (D-T9/D-T6)"
    - "`npm run build` (tsc) passa — sem drift de tipos backend↔frontend"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "SignalCondition/SignalGroup/Signals; Template/Create/Patch com signals em grupos"
      contains: "SignalGroup"
    - path: "frontend/src/pages/TemplatesPage.tsx"
      provides: "Construtor de grupos E/OU + linhas densas de campo + tooltips ⓘ, conforme mockup"
      contains: "OU"
    - path: "frontend/src/index.css"
      provides: "Token --tip-shadow (light + dark) para o box-shadow do tooltip"
      contains: "--tip-shadow"
  key_links:
    - from: "frontend/src/pages/TemplatesPage.tsx"
      to: "createTemplate/updateTemplate (api.ts)"
      via: "envia signals: SignalGroup[] no body"
      pattern: "signals"
    - from: "frontend/src/types.ts"
      to: "backend TemplateIn.signals (Plano 02)"
      via: "list[list[{mode,value}]] espelhado em TS"
      pattern: "SignalCondition"
---

<objective>
Reescrever `TemplatesPage.tsx` 1:1 com o mockup aprovado (`templates-redesign-mockup.html`): pipeline explícito Passo 1 (sinais, sem IA) / Passo 2 (campos, com IA) (D-T0), construtor de **grupos E/OU** de condições texto|regex (D-T3/D-T4), linhas densas de campo com ⚙ Avançado (D-T7), tooltips ⓘ via CSS `:hover` puro (D-T8), remoção do `doc_type` do form (D-T5). Atualizar `types.ts`/`api.ts` ao schema de grupos do Plano 02 (gate de drift `tsc`) e adicionar o token `--tip-shadow` em `index.css`. Preservar `field.name` como digitado (ponte D-T9/D-T6).

Purpose: Entregar a UX validada em mockup, alinhando os tipos TS ao contrato HTTP do Plano 02 sem quebrar o build nem a ponte campo→token das automações.

Output: TemplatesPage reescrita + tipos/cliente atualizados + token CSS; `npm run build` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-CONTEXT.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-PATTERNS.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-02-SUMMARY.md
@.planning/design/templates-redesign-mockup.html

<interfaces>
<!-- Schema final de sinais (Plano 02) espelhado em TS: -->
<!-- type SignalMode = 'texto' | 'regex'; interface SignalCondition { mode: SignalMode; value: string } -->
<!-- type SignalGroup = SignalCondition[]; type Signals = SignalGroup[] -->

<!-- types.ts ATUAL (linhas 82-132) — signals: string[] vira Signals. Source: frontend/src/types.ts -->
interface Template { id, name, doc_type: string|null, signals: string[], fields: TemplateField[], created_at, updated_at }
interface TemplateCreate { name, doc_type: string|null, signals: string[], fields: TemplateFieldCreate[] }
interface TemplatePatch { name?, doc_type?: string|null, signals?: string[], fields?: TemplateFieldCreate[] }
type FieldType = 'texto'|'numero'|'data'|'moeda'|'cpf_cnpj'|'booleano'   # NÃO muda
interface TemplateField/TemplateFieldCreate { name, field_type, required, regex: string|null, hint: string|null }  # NÃO muda (ponte D-T9)

<!-- api.ts (linhas 156-175) — só o TIPO do body muda, NÃO a assinatura. Source: frontend/src/lib/api.ts -->
getTemplates(): Promise<Template[]>; createTemplate(body: TemplateCreate); updateTemplate(id, body: TemplatePatch); deleteTemplate(id)

<!-- TemplatesPage ATUAL (analog de padrão React) — draft + key local + patch imutável. Source: frontend/src/pages/TemplatesPage.tsx -->
type FieldDraft = TemplateFieldCreate & { key: number }
const patchField/addField/removeField (map/filter imutável)   # REUSAR a mesma ideia p/ grupos/condições
useTemplates/useCreateTemplate/useUpdateTemplate/useDeleteTemplate (hooks — NÃO mudam)
componentes existentes: <Icon/>, <Switch/>

<!-- index.css — tokens existentes (NÃO hardcodar hex). Source: frontend/src/index.css -->
--surface --border --border-strong --text-3 --font-mono --radius-sm --accent-soft --accent --shadow-card
<!-- FALTA: --tip-shadow (o mockup usa). Adicionar light (:root) e dark (.dark/[data-theme]) -->
<!-- mockup valores: light = 0 6px 24px -8px rgba(20,26,36,.30); dark = 0 8px 26px -8px rgba(0,0,0,.7) -->

<!-- Estrutura do mockup a portar. Source: .planning/design/templates-redesign-mockup.html -->
<!-- groups state (linha 139): [[{mode,val}...](E)](OU); classes .group/.cond/.seg/.and/.or-div/.add-link/.info/.tip/.frow/.adv/.switch -->
<!-- grid .frow: 1fr 140px 96px 36px 34px ; tipos TYPES (linha 138): texto/número/data/moeda/CPF/CNPJ/booleano -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos TS + cliente + token CSS --tip-shadow</name>
  <files>frontend/src/types.ts, frontend/src/lib/api.ts, frontend/src/index.css</files>
  <read_first>
    - frontend/src/types.ts (linhas 82-132 — Template/Create/Patch; TemplateField NÃO muda)
    - frontend/src/lib/api.ts (linhas 156-175 — funções de template; só o tipo do body)
    - frontend/src/index.css (linhas 16-44 — bloco de tokens light/dark; localizar onde --shadow-card é definido p/ adicionar --tip-shadow ao lado)
    - .planning/design/templates-redesign-mockup.html (linhas 17,25 — valores de --tip-shadow light/dark; usar o token, não hardcode na regra)
  </read_first>
  <action>
    Em `types.ts`: adicionar `export type SignalMode = 'texto' | 'regex'`, `export interface SignalCondition { mode: SignalMode; value: string }`, `export type SignalGroup = SignalCondition[]`, `export type Signals = SignalGroup[]`. Trocar `signals: string[]` → `signals: Signals` em `Template`, `TemplateCreate`; `signals?: string[]` → `signals?: Signals` em `TemplatePatch`. Manter `doc_type` nos tipos (compat com backend) mas o form deixará de alimentá-lo (D-T5). `TemplateField`/`TemplateFieldCreate` INALTERADOS (ponte D-T9). Em `api.ts`: nenhuma mudança de assinatura — as funções já usam os tipos atualizados. Em `index.css`: adicionar `--tip-shadow: 0 6px 24px -8px rgba(20,26,36,.30);` no bloco `:root` (light) e `--tip-shadow: 0 8px 26px -8px rgba(0,0,0,.7);` no bloco dark, ao lado de `--shadow-card`. Commit: `feat(06.1-03): tipos TS de sinais em grupos + token --tip-shadow`.
  </action>
  <verify>
    <automated>cd frontend && grep -q "SignalGroup" src/types.ts && grep -q -- "--tip-shadow" src/index.css && npx tsc -b --noEmit 2>&1 | grep -v TemplatesPage | grep -Eq "error TS" && echo DRIFT || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `SignalCondition`/`SignalGroup`/`Signals` exportados em types.ts; `signals` tipado como `Signals` em Template/Create/Patch.
    - `TemplateField`/`TemplateFieldCreate` inalterados (grep confirma `regex`/`hint`/`name` presentes).
    - `--tip-shadow` definido em DOIS lugares (light + dark) em index.css; nenhum hex hardcoded fora de uma definição de token.
    - tsc não acusa erros em types.ts/api.ts (erros remanescentes só em TemplatesPage.tsx, resolvidos na Task 2).
  </acceptance_criteria>
  <done>Tipos de grupos exportados, signals re-tipado, token --tip-shadow (light+dark) presente; api.ts sem mudança de assinatura.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever TemplatesPage.tsx conforme o mockup (grupos E/OU + campos densos + tooltips ⓘ, sem doc_type)</name>
  <files>frontend/src/pages/TemplatesPage.tsx</files>
  <read_first>
    - frontend/src/pages/TemplatesPage.tsx (módulo inteiro — REUSAR draft+key local+patch imutável; remover o parse de signals por vírgula/linha e o campo doc_type)
    - .planning/design/templates-redesign-mockup.html (estrutura HTML/CSS/JS de groups[][], .group/.cond/.seg/.or-div/.add-link, .frow/.adv/.switch, .info/.tip, tooltips por seção)
    - frontend/src/types.ts (PÓS Task 1 — Signals/SignalGroup/SignalCondition)
    - frontend/src/components/Switch.tsx e frontend/src/components/Icon.tsx (componentes reusados pelo form atual)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-PATTERNS.md (seção TemplatesPage.tsx: drafts com key, patch imutável, estrutura do mockup, segurança T-04-12)
  </read_first>
  <action>
    Reescrever `TemplatesPage.tsx` portando o mockup: (1) FormState perde `doc_type` (D-T5) e troca `signals: string` por `signals: SignalGroup[]` (com chave local React por grupo/condição para keys estáveis — espelhar o padrão `FieldDraft & { key }`). (2) Passo 1 — seção "Como reconhecer este tipo" com badge `sem IA` e tooltip ⓘ (E/OU + texto vs regex, copiar a microcopy do mockup linhas 106/109); render dos grupos: cada grupo um bloco `.group` ("Grupo N — todas (E)"), cada condição uma `.cond` com seletor `contém o texto`|`corresponde ao padrão (regex)` (segmented `.seg` OU `<select>`) + input (mono quando regex) + botão remover; prefixo "E" da 2ª condição; divisor "OU" entre grupos; botões "+ E — adicionar condição" e "+ OU — adicionar grupo". Handlers imutáveis (add/remove/patch de grupo e de condição) no estilo `patchField`. (3) Passo 2 — seção "O que extrair" com badge `com IA`; cabeçalho `fhead` e linhas densas `frow` grid `1fr 140px 96px 36px 34px` = Nome | Tipo (FIELD_TYPES existente) | Obrig. (Switch) | ⚙ (toggle `.adv` por linha, via estado React por campo — NÃO via DOM querySelector) | remover. ⚙ revela regex de validação (input mono) + dica para a IA (input) — D-T7. (4) Tooltips ⓘ via classe `.info .tip` (CSS `:hover` puro — ZERO lib, ZERO JS de estado de hover); portar as classes CSS do mockup para `index.css` usando os tokens (`--surface`/`--border`/`--tip-shadow`/`--text-2`), variante `.info.r` alinhada à direita. (5) Submit: `createTemplate.mutate({ name, signals, fields })` / `updateTemplate.mutate({ id, body: { name, signals, fields } })` — SEM `doc_type` no payload; `field.name` enviado como digitado (sem transformação — D-T6/D-T9). (6) Segurança T-04-12: todos os valores (sinais/regex/nomes) renderizados como texto puro — ZERO `dangerouslySetInnerHTML`. Manter `useTemplates`/`useCreateTemplate`/`useUpdateTemplate`/`useDeleteTemplate` (hooks inalterados). Commit: `feat(06.1-03): TemplatesPage redesign — grupos E/OU + campos densos + tooltips (mockup)`.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b && vite build) passa SEM erros (gate de drift backend↔frontend resolvido).
    - `grep -nE "dangerouslySetInnerHTML" frontend/src/pages/TemplatesPage.tsx` retorna 0 (T-04-12).
    - `grep -nE "doc_type" frontend/src/pages/TemplatesPage.tsx` retorna 0 no payload de submit (doc_type fora do form, D-T5).
    - O JSX contém o construtor de grupos (texto "OU" e "E" / botões "+ OU"/"+ E") e linhas densas de campo com ⚙ Avançado.
    - Tooltips usam classe CSS `.info`/`.tip` (hover puro); nenhuma lib de tooltip importada (grep não acha `floating-ui`/`tooltip` em package.json — zero npm novo).
    - `field.name` é enviado sem snake_case/transformação (inspeção do submit).
  </acceptance_criteria>
  <verify>
    <human-check>
      Abrir a TemplatesPage no app, criar o template "Nota Fiscal — TryLab" com sinais (contém "TryLab" E corresponde a `\d{44}`) OU (contém "12.345.678/0001-99") e campos Emitente/CNPJ do emitente/Número da NF/Valor total. Conferir visualmente contra `.planning/design/templates-redesign-mockup.html`: densidade das linhas, ⚙ Avançado abre/fecha, tooltips ⓘ aparecem no hover, badges "sem IA"/"com IA". Salvar, reabrir e confirmar que os grupos e campos persistiram.
    </human-check>
  </verify>
  <done>TemplatesPage reescrita conforme mockup (grupos E/OU, campos densos, tooltips ⓘ, sem doc_type); build verde; valores texto puro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → form React | digita valores de sinal/regex/nomes que serão renderizados e enviados à API |
| frontend → backend | body de template (signals em grupos) cruza a fronteira HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-11 | Tampering (XSS) | render de valores de sinal/regex/nome | mitigate | React escapa por padrão; ZERO `dangerouslySetInnerHTML` (T-04-12, verificado por grep); valores como texto puro |
| T-06.1-12 | (DoS) regex no frontend | input de regex | accept | Frontend apenas armazena/envia a string; NÃO compila nem executa regex no navegador; execução só no matcher backend com tetos (Plano 01) |
| T-06.1-13 | (drift) tipos backend↔frontend | types.ts | mitigate | `npm run build` (tsc) como gate obrigatório (Pitfall 4); tipos atualizados no mesmo plano/wave do schema |
| T-06.1-14 | (supply chain) tooltip/lib nova | dependências npm | mitigate | Tooltip via CSS `:hover` puro; ZERO npm novo (Don't Hand-Roll); package.json inalterado |
</threat_model>

<verification>
- `cd frontend && npm run build` verde.
- `grep -nE "dangerouslySetInnerHTML|doc_type" frontend/src/pages/TemplatesPage.tsx` → 0.
- `grep -q -- "--tip-shadow" frontend/src/index.css` (light + dark).
- package.json sem dependência nova.
- Verificação visual humana contra o mockup (fim de fase).
</verification>

<success_criteria>
- Pipeline explícito Passo 1 (sem IA) / Passo 2 (com IA) na tela (D-T0).
- Construtor de grupos E/OU texto|regex funcional (D-T3/D-T4).
- Linhas densas de campo com ⚙ Avançado (regex de validação + dica IA) (D-T7).
- Tooltips ⓘ via hover CSS puro (D-T8); sem doc_type (D-T5).
- field.name preservado (ponte D-T9/D-T6); build verde sem drift.
</success_criteria>

<output>
Create `.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-03-SUMMARY.md` when done
</output>
