---
phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - backend/app/api/templates.py
  - backend/app/models/template.py
  - backend/tests/test_api_templates.py
autonomous: true
requirements: [D-T2, D-T5, D-T9]
nyquist_compliant: true

must_haves:
  truths:
    - "POST/PATCH /templates aceita `signals` como lista de grupos (cada grupo = lista de condições {mode,value}) e persiste em signals_json como JSON estruturado"
    - "GET /templates devolve `signals` na forma de grupos; a forma plana legada armazenada é lida sem erro (forward-compatible)"
    - "Um `mode` inválido (não texto|regex) em uma condição retorna 422 (Literal Pydantic), sem eval, sem 500"
    - "A regex de uma condição é guardada como STRING; o endpoint NÃO compila nem executa regex (T-04-10)"
    - "O contrato de criação/edição não exige mais `doc_type` no form; a coluna doc_type permanece dormente no banco"
    - "field.name persiste exatamente como digitado (ponte D-T9 com tokens de automação intacta)"
  artifacts:
    - path: "backend/app/api/templates.py"
      provides: "Schema In/Patch/Out de sinais como grupos + serialização forward-compatible; regex string-only"
      contains: "SignalConditionIn"
    - path: "backend/tests/test_api_templates.py"
      provides: "Cobertura de grupos no CRUD + 422 mode inválido + regex não compilada + legado lido"
      contains: "def test_"
  key_links:
    - from: "backend/app/api/templates.py"
      to: "signals_json (coluna Text)"
      via: "json.dumps(grupos) na criação/edição; _loads_signals_groups na leitura"
      pattern: "json.dumps"
    - from: "backend/app/api/templates.py"
      to: "app.classification.matcher._parse_groups"
      via: "MESMA forma de grupos serializada que o matcher consome (parser sincronizado)"
      pattern: "_parse_groups|_loads_signals"
---

<objective>
Adaptar a API e o modelo de templates ao novo schema de **sinais como grupos E/OU** (D-T2) definido no Plano 01: schemas Pydantic `In/Patch/Out` com `signals: list[list[SignalConditionIn]]`, serialização forward-compatible para a coluna `signals_json` existente, e remoção do `doc_type` do contrato obrigatório do form (D-T5, coluna dormente). A regex segue guardada como STRING (T-04-10) — nunca compilada no endpoint. A ponte campo→token (D-T9) é preservada: `field.name` persiste como digitado.

Purpose: Expor o contrato HTTP que o frontend (Plano 03) consome, mantendo o seam de serialização sincronizado com o matcher do Plano 01 e sem tocar a tabela `documents` (sem migração de schema — JSON na coluna existente).

Output: `api/templates.py` e `models/template.py` no novo schema de grupos; testes de integração estendidos (grupos no CRUD, 422 em mode inválido, regex não compilada, legado lido).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-CONTEXT.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-RESEARCH.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-PATTERNS.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-01-SUMMARY.md

<interfaces>
<!-- Forma canônica do JSON de sinais (definida no Plano 01, signals_json Text). -->
<!-- [ [ {"mode":"texto","value":"TryLab"}, {"mode":"regex","value":"\\d{44}"} ], [ {"mode":"texto","value":"12.345.678/0001-99"} ] ] -->

<!-- api/templates.py ATUAL — pontos a evoluir. Source: backend/app/api/templates.py -->
def _loads_signals(raw: str | None) -> list[str]      # → trocar por _loads_signals_groups (forward-compatible)
class TemplateIn:  signals: list[str] = []            # → signals: list[list[SignalConditionIn]] = []
class TemplatePatch: signals: list[str] | None = None # → list[list[SignalConditionIn]] | None
class TemplateOut: signals: list[str]                 # → signals: list[list[SignalConditionOut]] (ou list[dict])
create_template: signals_json=json.dumps(body.signals)  # body.signals agora é grupos de modelos → dump dict
update_template: template.signals_json = json.dumps(body.signals)
# T-04-10 (docstring linhas 14-19): regex guardada como string; NÃO re.compile/re.fullmatch neste módulo.

<!-- matcher._parse_groups (Plano 01) — manter sincronizado o formato lido/escrito. -->

<!-- models/template.py — signals_json Text existente (NÃO muda mapeamento); doc_type dormente. -->
signals_json: Mapped[str] = mapped_column(Text, nullable=False, default="[]")
doc_type: Mapped[str | None] = mapped_column(String, nullable=True)   # dormente (D-T5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema de grupos na API + serialização forward-compatible + doc_type dormente</name>
  <files>backend/app/api/templates.py, backend/app/models/template.py</files>
  <read_first>
    - backend/app/api/templates.py (módulo inteiro — _loads_signals, TemplateIn/Patch/Out, _serialize, create_template, update_template; docstring T-04-10 linhas 14-19)
    - backend/app/models/template.py (signals_json, doc_type, TemplateField — NÃO mudar mapeamento; só docstring)
    - backend/app/classification/matcher.py (PÓS Plano 01: `_parse_groups` — copiar a MESMA tolerância de leitura para o endpoint)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-PATTERNS.md (seção api/templates.py: SignalConditionIn Literal, _loads_signals sincronizado, _serialize)
  </read_first>
  <action>
    Em `api/templates.py`: (1) Adicionar `class SignalConditionIn(BaseModel)` com `mode: Literal["texto", "regex"]` e `value: str` + `field_validator("value")` que rejeita vazio/só-espaços (`raise ValueError("Informe o valor da condição.")`, `.strip()`). O `Literal` faz o dispatch por etiqueta sem eval (V5) e devolve 422 limpo em mode inválido. NÃO compilar regex aqui (T-04-10). (2) Trocar em `TemplateIn` e `TemplatePatch`: `signals: list[list[SignalConditionIn]] = []` / `list[list[SignalConditionIn]] | None = None`. (3) `doc_type` deixa de ser exigido — manter `doc_type: str | None = None` no In/Patch para compat (não removê-lo do schema, só não é mais alimentado pelo form do Plano 03); D-T5: marcar como legado opcional. (4) Substituir `_loads_signals` por `_loads_signals_groups(raw)` forward-compatible: replicar a lógica de `matcher._parse_groups` (try/except json → []; lista de str legada → cada termo vira `[{"mode":"texto","value":s}]`; lista de listas de dict → normaliza). (5) `TemplateOut.signals` → `list[list[dict]]` (ou um `SignalConditionOut` espelho); `_serialize` usa `_loads_signals_groups(template.signals_json)`. (6) `create_template`/`update_template`: `signals_json=json.dumps([[c.model_dump() for c in g] for g in body.signals])` (serializa os modelos Pydantic → dicts {mode,value}). Em `models/template.py`: atualizar SOMENTE a docstring de `signals_json` (descrever grupos E/OU) e marcar `doc_type` como dormente (D-T5); NÃO alterar mapeamento, NÃO criar migração. Commit: `feat(06.1-02): API de templates com sinais em grupos E/OU (forward-compatible)`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "from app.api.templates import SignalConditionIn, TemplateIn, _loads_signals_groups; TemplateIn(name='x', signals=[[{'mode':'texto','value':'a'}]], fields=[{'name':'f'}]); print(_loads_signals_groups('[\"DANFE\"]'))"</automated>
  </verify>
  <acceptance_criteria>
    - `SignalConditionIn` existe com `mode: Literal["texto","regex"]`; construir com `mode="x"` levanta ValidationError.
    - `_loads_signals_groups('["DANFE","NF"]')` (legado) retorna `[[{"mode":"texto","value":"DANFE"}], [{"mode":"texto","value":"NF"}]]` sem erro.
    - `grep -nE "re\.compile|re\.fullmatch|re\.search" backend/app/api/templates.py` retorna 0 (T-04-10: regex nunca compilada no endpoint).
    - `models/template.py`: nenhuma mudança de `mapped_column` (apenas docstring); `grep -c "signals_json" backend/app/models/template.py` inalterado.
    - Nenhum arquivo novo em `backend/alembic/versions/` (sem migração de schema).
  </acceptance_criteria>
  <done>API e modelo no schema de grupos; serialização forward-compatible; regex string-only; doc_type dormente; sem migração.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes de integração — grupos no CRUD, 422 mode inválido, regex não compilada, legado, ponte D-T9</name>
  <files>backend/tests/test_api_templates.py</files>
  <read_first>
    - backend/tests/test_api_templates.py (molde de testes do CRUD: client, asserções de status/payload, fixtures)
    - backend/app/api/templates.py (PÓS Task 1 — schema final)
    - backend/tests/conftest.py (fixtures de app/engine/client)
  </read_first>
  <behavior>
    - test_cria_template_com_grupos: POST com signals=[[{texto TryLab},{regex \d{44}}],[{texto CNPJ}]] → 201; GET devolve os mesmos grupos.
    - test_mode_invalido_422: POST com uma condição `mode="qualquer"` → 422 (Literal), sem 500.
    - test_value_vazio_422: condição com value "" → 422.
    - test_legado_lido_como_grupos: inserir um Template via ORM com signals_json='["DANFE"]' (forma antiga) e GET /templates devolve `[[{"mode":"texto","value":"DANFE"}]]`.
    - test_patch_signals_substitui: PATCH só com signals novos preserva fields; PATCH sem signals preserva signals atuais.
    - test_regex_guardada_como_string: POST com regex catastrófico "(a+)+$" como value → 201 (endpoint não compila/executa; só persiste string).
    - test_field_name_preservado_D_T9: POST com field name "CNPJ do emitente" → GET devolve name idêntico (sem snake_case forçado — ponte de token intacta).
  </behavior>
  <action>
    Estender `test_api_templates.py` com os casos acima, reusando as fixtures de client/engine existentes. Para o caso legado, inserir diretamente via sessão ORM um `Template(signals_json='["DANFE"]', ...)` e então chamar o endpoint GET. Confirmar 422 vem com status_code 422 (não 500). Commit: `test(06.1-02): grupos no CRUD + 422 mode + regex string + legado + ponte D-T9`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_api_templates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Todos os novos `test_` passam (GREEN).
    - `test_mode_invalido_422` e `test_value_vazio_422` asseguram status 422 (não 500).
    - `test_regex_guardada_como_string` passa com 201 (sem ReDoS no caminho HTTP — endpoint não executa a regex).
    - `test_field_name_preservado_D_T9` confirma name idêntico ao enviado.
    - `cd backend && .venv/bin/python -m pytest tests/test_migrations.py -q` passa (trigger trg_documents_updated_at intacto; nenhuma migração nova tocou documents).
  </acceptance_criteria>
  <done>Suite test_api_templates verde cobrindo grupos, 422, regex string, legado e ponte D-T9; migrations verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → api/templates | body JSON com sinais/regex do operador cruza o endpoint |
| api/templates → signals_json (SQLite) | serialização de grupos para coluna Text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-06 | Tampering/DoS | regex de sinal no endpoint | mitigate | T-04-10: regex guardada APENAS como string; endpoint NÃO `re.compile`/`re.search`/`re.fullmatch` (verificado por grep); execução só no matcher (Plano 01) com tetos |
| T-06.1-07 | Tampering/RCE | `mode` da condição | mitigate | `Literal["texto","regex"]` no Pydantic → dispatch por etiqueta, 422 em valor desconhecido, nunca `eval` (V5) |
| T-06.1-08 | Tampering | signals_json malformado/legado | mitigate | `_loads_signals_groups` tolerante (try/except json → []; isinstance guard); forward-compatible com forma plana |
| T-06.1-09 | Tampering | migração que toca `documents` | mitigate | Esta fase NÃO cria migração (JSON na coluna existente); test_migrations confirma trigger trg_documents_updated_at intacto |
| T-06.1-10 | Information Disclosure | logs do endpoint | accept | Endpoint não loga conteúdo de documento (não recebe full_text); risco baixo |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/test_api_templates.py tests/test_migrations.py -q` verde.
- `grep -nE "re\.compile|re\.fullmatch|re\.search" backend/app/api/templates.py` → 0 linhas.
- Nenhum arquivo novo em `backend/alembic/versions/`.
</verification>

<success_criteria>
- API aceita/serializa/lê sinais como grupos E/OU (D-T2) forward-compatible.
- mode inválido → 422; regex nunca compilada no endpoint (T-04-10).
- doc_type fora do contrato obrigatório, coluna dormente (D-T5).
- field.name preservado como digitado (ponte D-T9).
- Sem migração de schema; trigger de documents intacto.
</success_criteria>

<output>
Create `.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-02-SUMMARY.md` when done
</output>
