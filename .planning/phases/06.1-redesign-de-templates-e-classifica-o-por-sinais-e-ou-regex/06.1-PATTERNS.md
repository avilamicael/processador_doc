# Phase 06.1: Redesign de Templates e Classificação por Sinais (E/OU + Regex) - Pattern Map

**Mapped:** 2026-06-18
**Files analyzed:** 8 (7 modificados + 0 novos de produção; testes em Wave 0)
**Analogs found:** 8 / 8 (todos os arquivos a tocar JÁ existem — fase de evolução, não greenfield)

> **Insight central (do RESEARCH):** O risco desta fase NÃO é "achar a lib certa" — é **replicar padrões existentes sem quebrar seams**. Quase todo arquivo a modificar é seu próprio melhor analog (o padrão correto já vive no código adjacente). Os analogs abaixo são, em sua maioria, **funções/blocos vizinhos no mesmo módulo** que já resolvem o problema gêmeo (regex como string, serialização JSON tolerante, parser forward-compatible, despacho por etiqueta sem `eval`).

## File Classification

| Arquivo a criar/modificar | Role | Data Flow | Analog mais próximo | Match Quality |
|---------------------------|------|-----------|---------------------|---------------|
| `backend/app/classification/matcher.py` | service (engine puro) | transform (booleano E/OU) | ele mesmo (`_signals`/`_haystack`/`match_templates`) + `validation/fields.py` (regex segura) | self / exact |
| `backend/app/models/template.py` | model | CRUD (coluna Text JSON) | ele mesmo (`signals_json` Mapped Text) | self / exact |
| `backend/app/api/templates.py` | route (router fino) | request-response (CRUD) | ele mesmo (`_loads_signals`/`_serialize`/`field_validator`) | self / exact |
| `backend/app/validation/fields.py` | utility (regex teto) | transform | `_MAX_REGEX_LEN` + `re.compile`/`try-except` (lê, NÃO edita) | reference (reuse) |
| `backend/app/classification/stage.py` | service (orquestrador) | event-driven (pipeline) | ele mesmo (chama `match_templates`/`decide`) | self / no-change esperado |
| `frontend/src/pages/TemplatesPage.tsx` | component (form) | request-response | mockup `templates-redesign-mockup.html` + `AutomationsPage.tsx` (grupos/listas dinâmicas) | role-match / reescrita |
| `frontend/src/types.ts` | model (tipos TS) | — | ele mesmo (`Template`/`TemplateCreate`/`SignalGroup` análogo a `Automation`) | self / exact |
| `frontend/src/lib/api.ts` | service (cliente) | request-response | ele mesmo (`getTemplates`/`createTemplate` — sem mudança de assinatura) | self / no-change |
| `frontend/src/hooks/useTemplates.ts` | hook | request-response | ele mesmo (genérico sobre os tipos — NÃO muda) | self / no-change |
| migração Alembic (SE necessária) | migration | DDL forward-only | `0005_confidence_review.py` (não toca `documents`) | exact |
| `backend/tests/classification/test_matcher.py` | test | unit | ele mesmo (casos existentes a estender) | self / extend |
| `backend/tests/test_api_templates.py` | test | integration | ele mesmo (a estender) | self / extend |

---

## Pattern Assignments

### `backend/app/classification/matcher.py` (service, transform booleano)

**Analog primário:** o próprio módulo (preservar a estrutura: helpers puros + `match_templates` + `decide` intacto).
**Analog de segurança de regex:** `backend/app/validation/fields.py` (lines 131-147).

**O que MUDA:** `_signals` (parser plano) → parser de grupos forward-compatible; `_haystack` → alvo de busca = `full_text` (ver A2); `match_templates` → pontuação booleana 1.0/0.0; `decide` **INTACTO** (não tocar).

**Parser tolerante atual a EVOLUIR** (lines 55-64) — manter o `try/except (ValueError, TypeError)` e o guard `isinstance(..., list)`:
```python
def _signals(template: Template) -> list[str]:
    raw = template.signals_json or "[]"
    try:
        parsed = json.loads(raw)
    except (ValueError, TypeError):
        return []
    if not isinstance(parsed, list):
        return []
    return [str(s) for s in parsed]
```
→ evoluir para `_parse_groups` forward-compatible (RESEARCH Pattern 2): detecta forma antiga (`list[str]`) e a envelopa; nova é `list[list[dict]]`.

**Seam `decide()` a PRESERVAR sem alteração** (lines 112-132) — é consumido por `stage.py`, Fase 5 e desambiguação por IA. Só a função de PONTUAÇÃO muda:
```python
def decide(matches: list[TemplateMatch], *, threshold: float) -> MatchDecision:
    if not matches or matches[0].confidence < threshold:
        return MatchDecision(status="quarantine", template_id=None)
    best = matches[0]
    if len(matches) > 1:
        second = matches[1]
        if (second.confidence >= threshold
                and (best.confidence - second.confidence) < _AMBIGUITY_MARGIN):
            return MatchDecision(status="ambiguous", template_id=None)
    return MatchDecision(status="matched", template_id=best.template_id)
```
> Mapeamento booleano→`decide()`: grupo casado → `1.0`; nenhum → `0.0`. Vários templates a 1.0 → margem 0 < `_AMBIGUITY_MARGIN` (0.1) → "ambiguous" (IA desempata, correto); um → "matched"; nenhum → "quarantine". Preserva os três ramos.

**Regex segura — COPIAR o padrão de `validation/fields.py` (lines 28-29, 131-147):**
```python
_MAX_REGEX_LEN = 4096  # teto do VALOR antes de aplicar regex (ReDoS — V5)
...
if regex is not None:
    if len(raw) > _MAX_REGEX_LEN:
        return FieldValidation(valid=False, ..., invalid_reason=f"valor excede o limite de {_MAX_REGEX_LEN} caracteres")
    pattern = re.compile(regex)
    if pattern.fullmatch(raw) is None:
        return FieldValidation(valid=False, ..., invalid_reason="valor não casa o padrão (regex)")
```
**Diferenças críticas para o matcher (do RESEARCH):**
- Sinal usa `re.search` + `re.IGNORECASE` (âncora em qualquer ponto do `full_text`), NÃO `fullmatch` — `fullmatch` no full_text nunca casaria (Assumption A4).
- Adicionar `try/except re.error` → regex inválido = falha fechada (NÃO casa), NUNCA propaga 500 (V5). Note que `fields.py` faz `re.compile(regex)` SEM try/except porque o valor é controlado; aqui o pattern é colado pelo operador, então o `try/except re.error` é obrigatório.
- Dois tetos: pattern (`_MAX_SIGNAL_REGEX_LEN`, ex. 512) E input/haystack (`_MAX_HAYSTACK_LEN`, ex. 200_000) — full_text excede 4096.

**Forma de pontuação proposta (do RESEARCH Code Examples) — `_condition_matches`/`_group_matches`/`_template_matches`:**
```python
def _condition_matches(cond: dict, haystack: str) -> bool:
    value = str(cond.get("value", ""))
    if not value:
        return False
    if cond.get("mode") == "regex":
        if len(value) > _MAX_SIGNAL_REGEX_LEN:
            return False
        try:
            pattern = re.compile(value, re.IGNORECASE)
        except re.error:
            return False
        return pattern.search(haystack[:_MAX_HAYSTACK_LEN]) is not None
    return value.strip().lower() in haystack  # modo "texto"
# E dentro do grupo (all), OU entre grupos (any). Grupo/lista vazia → não casa (falha fechada).
```

**Destino do `_DOC_TYPE_BONUS`** (lines 27, 104-105): com `doc_type` fora do form (D-T5), o ramo `doc_type_guess` perde sentido — RESEARCH recomenda REMOVER (lógica morta, A3). Decisão do planejador.

**V7/LGPD:** o matcher NÃO loga `full_text`/valores de sinal — só metadados. Padrão já seguido em `stage.py` (`logger.info("Documento %s → QUARENTENA ...", doc.id)`).

---

### `backend/app/models/template.py` (model, coluna Text JSON)

**Analog:** o próprio mapeamento `signals_json` (line 42).

**Coluna EXISTENTE — reusada sem mudança de tipo** (Text, default "[]"):
```python
signals_json: Mapped[str] = mapped_column(Text, nullable=False, default="[]")
```
A forma do JSON dentro da coluna muda (plano → grupos), mas o **mapeamento SQLAlchemy NÃO muda** → nenhuma migração de schema necessária por causa dos sinais.

**`doc_type` (line 39) vira coluna dormente (D-T5)** — manter a coluna; só sair do contrato/form. NÃO dropar (evita migração que toque `templates`).
```python
doc_type: Mapped[str | None] = mapped_column(String, nullable=True)  # dormente, D-T5
```
Atualizar a docstring do módulo (lines 1-20) para descrever `signals_json` como **grupos E/OU** e marcar `doc_type` como dormente.

**`TemplateField` (lines 59-82) NÃO muda** — `name`/`field_type`/`required`/`regex`/`hint` permanecem. Crítico para a ponte D-T9: `field.name` persistido = token de automação.

---

### `backend/app/api/templates.py` (route, CRUD request-response)

**Analog:** o próprio módulo. Espelha `api/watched_folders.py` (schemas In/Patch/Out, `request.app.state.engine` + `with get_session(engine)`, 409/404/204).

**REGRA T-04-10 — regex como STRING, nunca compilada no endpoint** (docstring lines 16-19). A regex de SINAL segue a MESMA regra da regex de validação de campo:
```python
# SEGURANÇA — regex do operador (T-04-10): a `regex` ... é guardada APENAS como
# string aqui; este endpoint NÃO compila nem executa a regex. ... Não adicionar
# `re.compile`/`re.fullmatch` neste módulo.
```
→ Estender para sinais: o `value` regex de cada condição é serializado em `signals_json` como string crua. NÃO `re.compile` aqui.

**Serialização JSON tolerante a evoluir** (`_loads_signals` lines 42-54) — espelha `matcher._signals`. Manter sincronizado com o novo parser de grupos:
```python
def _loads_signals(raw: str | None) -> list[str]:
    try:
        parsed = json.loads(raw or "[]")
    except (ValueError, TypeError):
        return []
    if not isinstance(parsed, list):
        return []
    return [str(s) for s in parsed]
```

**Schema In com `field_validator` Pydantic (lines 77-97)** — molde para validar a FORMA dos grupos (V5: dispatch por etiqueta `mode`, sem `eval`; mode desconhecido = rejeita/falha fechada):
```python
class TemplateIn(BaseModel):
    name: str
    doc_type: str | None = None
    signals: list[str] = []          # → MUDAR para o tipo de grupos (list[list[Condition]])
    fields: list[TemplateFieldIn] = []

    @field_validator("name")
    @classmethod
    def _name_not_blank(cls, v: str) -> str:
        if not v or not v.strip():
            raise ValueError("Informe o nome do template.")
        return v.strip()
```
→ Adicionar um modelo Pydantic `SignalConditionIn(mode: Literal["texto","regex"]; value: str)` e `signals: list[list[SignalConditionIn]]`. `Literal` faz o dispatch por etiqueta sem `eval` (V5) e dá 422 limpo em mode inválido.

**Serialização Out (`_serialize` lines 154-167)** e persistência (`create_template` line 215: `signals_json=json.dumps(body.signals)`; `update_template` line 247) — adaptar para a estrutura de grupos. `json.dumps`/`json.loads` é o padrão do projeto (NÃO encoder custom).

**`TemplatePatch` (lines 100-126):** `signals: list[str] | None` → tipo de grupos opcional; `None` = preserva (mesma semântica de `fields`).

---

### `backend/app/validation/fields.py` (utility — REFERÊNCIA, read-only nesta fase)

Este arquivo é o **molde de segurança de regex** a copiar para o matcher. NÃO precisa ser editado nesta fase (a regex de campo já funciona). Lido como fonte do padrão `_MAX_REGEX_LEN` + `re.compile` + falha-marca-não-bloqueia. Ver excerto na seção do `matcher.py`.

> Não confundir semânticas: `fields.py` usa `re.fullmatch` (valida o VALOR inteiro de um campo); o matcher usa `re.search` (âncora no TEXTO do documento). A4.

---

### `backend/app/classification/stage.py` (service, orquestrador — provavelmente NÃO muda)

**Analog:** ele mesmo. Consome `matcher.match_templates` (lines 206-211) e `matcher.decide` (lines 214-216). Se a **assinatura** de ambas for preservada (recomendado), `stage.py` NÃO muda.

**Pontos a NÃO quebrar:**
- `match_templates(fields_json=..., full_text=..., doc_type_guess=..., templates=...)` (lines 206-211) — se `doc_type_guess` sair da assinatura (A3), `stage.py` precisará ajustar a chamada. Decisão do planejador; preferir manter a assinatura e ignorar o param internamente para zero-diff aqui.
- Ramo quarentena (lines 256-270): `session.add(ClassificationResult(...))` ANTES de `transition(QUARENTENA)` — ordem atômica obrigatória (T-04-14). Não tocar.
- V7: `logger.info` só com `doc.id`/`template_id` (lines 267, 366-372) — nunca full_text.

---

### `frontend/src/pages/TemplatesPage.tsx` (component, reescrita 1:1 com mockup)

**Analog visual (fonte de verdade):** `.planning/design/templates-redesign-mockup.html`.
**Analog de PADRÃO React (listas dinâmicas aninhadas + drafts):** o próprio `TemplatesPage.tsx` atual (estado de campos com `key` local) + `AutomationsPage.tsx` (condições/ações dinâmicas, `resolvePattern`, `stripQuotes`).

**Padrão de draft com chave local React (atual, lines 30-49) — REUSAR para condições/grupos:**
```tsx
type FieldDraft = TemplateFieldCreate & { key: number }
let nextFieldKey = 1
const newField = (): FieldDraft => ({ key: nextFieldKey++, name: '', field_type: 'texto', required: false, regex: null, hint: null })
```
→ Aplicar a mesma ideia a grupos/condições de sinal (cada grupo e cada condição com `key` local para o React).

**Padrão de patch imutável de item em lista (atual, lines 101-117) — REUSAR:**
```tsx
const patchField = (key: number, patch: Partial<FieldDraft>) => {
  if (!form) return
  setForm({ ...form, fields: form.fields.map((f) => (f.key === key ? { ...f, ...patch } : f)) })
}
const addField = () => { if (!form) return; setForm({ ...form, fields: [...form.fields, newField()] }) }
const removeField = (key: number) => { if (!form) return; setForm({ ...form, fields: form.fields.filter((f) => f.key !== key) }) }
```

**Mutations + fechar form (atual, lines 152-162) — INALTERADO:**
```tsx
if (form.id == null) {
  createTemplate.mutate({ name, doc_type, signals, fields }, { onSuccess: closeForm, onError })
} else {
  updateTemplate.mutate({ id: form.id, body: { name, doc_type, signals, fields } }, { onSuccess: closeForm, onError })
}
```
> `signals` muda de `string[]` para `SignalGroup[]`. O parse "split por vírgula/linha" (lines 136-139) é SUBSTITUÍDO pelo construtor de grupos.

**Estrutura do mockup a portar (groups[][], lines 139-158 do mockup):**
- Seção "Como reconhecer este tipo `sem IA`" (passo 1, D-T0) com badge `t-noai` e tooltip ⓘ explicando E/OU + texto vs regex.
- Cada condição = `<select>` "contém o texto" | "corresponde ao padrão (regex)" + `<input>` (mono quando regex) + botão remover. Prefixo "E" a partir da 2ª condição; divisor "OU" entre grupos. Botões "+ E — adicionar condição" e "+ OU — adicionar grupo".
- Seção "O que extrair `com IA`" (passo 2, D-T0) com badge `t-ai`.
- Linha densa por campo: grid `1fr 140px 96px 36px 34px` = `Nome | Tipo | Obrig. | ⚙ Avançado | remover` (mockup `.frow` line 71). ⚙ revela regex de validação + dica IA (`.adv` line 80).
- Tooltips via CSS `:hover` puro (`.info .tip`, mockup lines 39-44) — ZERO lib de tooltip, ZERO JS de estado (Don't Hand-Roll).

**Tipos de campo (atual, lines 12-20) — REUSAR; remover `doc_type` do form (D-T5):**
```tsx
const FIELD_TYPES: { value: FieldType; label: string }[] = [
  { value: 'texto', label: 'Texto' }, { value: 'numero', label: 'Número' },
  { value: 'data', label: 'Data' }, { value: 'moeda', label: 'Moeda' },
  { value: 'cpf_cnpj', label: 'CPF/CNPJ' }, { value: 'booleano', label: 'Booleano' },
]
```

**Segurança UI (T-04-12):** valores de sinal/regex renderizados como texto puro — React escapa por padrão; ZERO `dangerouslySetInnerHTML`. Padrão de toda a Fase 5 e do cliente em `api.ts` (line 10).

---

### `frontend/src/types.ts` (model, tipos TS — atualizar em conjunto)

**Analog:** o bloco `Automation` (lines 170-252) como molde de tipos aninhados Out/Create/Patch.

**Tipos atuais a EVOLUER (lines 82-132):**
```ts
export interface Template { id: number; name: string; doc_type: string | null; signals: string[]; fields: TemplateField[]; created_at: string; updated_at: string }
export interface TemplateCreate { name: string; doc_type: string | null; signals: string[]; fields: TemplateFieldCreate[] }
export interface TemplatePatch { name?: string; doc_type?: string | null; signals?: string[]; fields?: TemplateFieldCreate[] }
```
→ `signals: string[]` vira tipo de grupos (RESEARCH Code Examples):
```ts
type SignalMode = 'texto' | 'regex'
interface SignalCondition { mode: SignalMode; value: string }
type SignalGroup = SignalCondition[]   // E entre condições
type Signals = SignalGroup[]            // OU entre grupos
```
> **Pitfall 4 (drift de tipo):** os tipos TS são mantidos À MÃO (não há geração OpenAPI ativa). `types.ts`, `api.ts`, `TemplatesPage.tsx` e os schemas Pydantic MUDAM NO MESMO plano/wave. `npm run build` (tsc) é o gate de drift.

`TemplateField`/`TemplateFieldCreate` (lines 88-115) NÃO mudam (ponte D-T9 intacta).

---

### `frontend/src/lib/api.ts` (service — sem mudança de assinatura)

**Analog:** ele mesmo (lines 157-175). As funções `getTemplates`/`createTemplate`/`updateTemplate`/`deleteTemplate` só mudam o TIPO do body (via `types.ts`), não a forma:
```ts
return request<Template[]>('/templates')
return request<Template>('/templates', { method: 'POST', body: JSON.stringify(body) })
return request<Template>(`/templates/${id}`, { method: 'PATCH', body: JSON.stringify(body) })
return request<void>(`/templates/${id}`, { method: 'DELETE' })
```

### `frontend/src/hooks/useTemplates.ts` (hook — NÃO muda)
Genérico sobre os tipos (`TemplateCreate`/`TemplatePatch`). `invalidateQueries(['templates'])` já cobre cache. Nenhuma edição esperada.

---

### Migração Alembic (SE necessária — provavelmente NÃO)

**Analog:** `backend/alembic/versions/0005_confidence_review.py`.

A persistência de grupos usa a coluna `signals_json` EXISTENTE → **nenhuma migração de schema é necessária** para os sinais. Migração só entraria se o planejador decidir marcar `doc_type` deprecado (não recomendado — manter dormente).

**Se houver QUALQUER migração, copiar o cabeçalho forward-only + caveat do trigger (0005 lines 1-32):**
```python
revision: str = '0009'
down_revision: Union[str, Sequence[str], None] = '0008'
# CAVEAT trg_documents_updated_at (0002): NÃO tocar `documents` (sem batch recreate)
```
Só `batch_alter_table` em tabelas que NÃO sejam `documents` (preserva o trigger `trg_documents_updated_at`). Padrão consolidado em 0003–0008.

---

## Shared Patterns

### Regex segura (string no contrato → compilação com teto só no engine)
**Source:** `backend/app/validation/fields.py` lines 28-29, 131-147 + docstring `api/templates.py` lines 16-19 (T-04-10).
**Apply to:** `matcher.py` (sinais), `api/templates.py` (não compilar), schema Pydantic dos grupos.
```python
_MAX_REGEX_LEN = 4096
if len(value) > _MAX_REGEX_LEN: return <falha fechada>
try: pattern = re.compile(value)   # + re.IGNORECASE no matcher
except re.error: return <não casa>  # OBRIGATÓRIO no matcher (pattern colado pelo operador)
```
Regra: regex é STRING no endpoint; compila SÓ onde há teto (matcher/validação). Matcher usa `search`; fields usa `fullmatch`.

### Serialização JSON tolerante em coluna Text (parser forward-compatible)
**Source:** `matcher._signals` (lines 55-64) + `api/templates._loads_signals` (lines 42-54) — DOIS pontos que devem ficar sincronizados.
**Apply to:** novo `_parse_groups` (detecta forma antiga `list[str]` e envelopa; nova `list[list[dict]]`). `json.dumps`/`json.loads`, nunca encoder custom (Don't Hand-Roll).
```python
try: parsed = json.loads(raw or "[]")
except (ValueError, TypeError): return []
if not isinstance(parsed, list): return []
```

### Despacho por etiqueta sem `eval` (V5)
**Source:** `validation/fields.validate_field` (dispatch por `field_type`, lines 99-123) + `automation/rules.py` (citado no RESEARCH).
**Apply to:** modo de condição (`texto`/`regex`) — `if mode == "regex": ... else: <texto>`; mode desconhecido = falha fechada. No Pydantic, `Literal["texto","regex"]` dá 422 limpo. NUNCA `eval`.

### Logging sem vazar conteúdo (V7/LGPD)
**Source:** `classification/stage.py` (`logger.info("Documento %s → ...", doc.id)`, lines 267, 347-352, 366-372).
**Apply to:** matcher — logar só `template_id`/`document_id`; nunca `full_text`/valores de sinal.

### Listas dinâmicas aninhadas no React (draft + key local + patch imutável)
**Source:** `TemplatesPage.tsx` atual (lines 30-117) + `AutomationsPage.tsx` (condições/ações).
**Apply to:** construtor de grupos/condições de sinal e linhas densas de campo.

### Tooltip explicativa (CSS `:hover` puro, zero JS/lib)
**Source:** mockup `.info .tip` / `.info:hover .tip` (lines 39-44).
**Apply to:** todos os ⓘ (D-T8). Tokens de cor/raio vêm de `frontend/src/index.css`.
> **GAP de token:** `index.css` (lido) tem `--surface`/`--border`/`--surface-3`/`--accent-soft`/`--font-mono`/`--radius-sm`, mas **NÃO** define `--tip-shadow` (o mockup usa). O planejador deve **adicionar `--tip-shadow`** (light + dark) a `index.css` ao portar o CSS do tooltip, OU usar `--shadow-card` existente. NÃO hardcodar hex.

### Ponte campo→token de automação (D-T9) — NÃO quebrar
**Source backend:** `automation/naming.py` `_TOKEN_RE = re.compile(r"\{([^{}:]+)(?::([^{}]+))?\}")` (line 47), substitui tokens pelos `field_name` persistidos.
**Source frontend:** `AutomationsPage.tsx` `resolvePattern` (lines 170-182) resolve `{campo}` por `f.name` dos campos reais do template.
**Garantia:** esta fase NÃO muda `TemplateField.name` nem persiste com snake_case forçado (D-T6) — os tokens batem. Apenas SINAIS mudam. Verificado: nenhuma quebra esperada.

---

## No Analog Found

Nenhum arquivo sem analog. Todos os arquivos de produção a tocar já existem e são seus próprios melhores moldes (fase de evolução). Os únicos artefatos verdadeiramente "novos" são CASOS DE TESTE em arquivos existentes (Wave 0), cujo molde é o próprio arquivo de teste a estender.

| Arquivo | Role | Data Flow | Razão |
|---------|------|-----------|-------|
| (nenhum) | — | — | — |

---

## Metadata

**Analog search scope:** `backend/app/classification/`, `backend/app/models/`, `backend/app/api/`, `backend/app/validation/`, `backend/app/automation/`, `backend/alembic/versions/`, `frontend/src/pages/`, `frontend/src/hooks/`, `frontend/src/lib/`, `frontend/src/`, `.planning/design/`
**Files scanned (read):** matcher.py, template.py (model), templates.py (api), fields.py, stage.py, TemplatesPage.tsx, types.ts, useTemplates.ts, api.ts (parcial + grep), naming.py (parcial), AutomationsPage.tsx (bridge lines), 0005_confidence_review.py, templates-redesign-mockup.html, index.css (grep tokens)
**Pattern extraction date:** 2026-06-18
