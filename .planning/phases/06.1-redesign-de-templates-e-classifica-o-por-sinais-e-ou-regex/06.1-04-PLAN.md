---
phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend/pyproject.toml
  - backend/app/classification/matcher.py
  - backend/app/config.py
  - backend/tests/classification/test_matcher_groups.py
autonomous: false
requirements: [D-T1, D-T2]
nyquist_compliant: true

must_haves:
  truths:
    - "Uma regex maliciosa com backtracking catastrófico (ex.: (a+)+$) contra um quase-match (ex.: 'a'*40+'X') NÃO trava o matcher: o casamento aborta por timeout e a condição falha-fechada (não casa)"
    - "Um documento sem nenhum sinal casando (confiança 0.0) NUNCA é roteado como 'matched' independente do valor do threshold (incluindo 0 e negativo)"
    - "A docstring do matcher não afirma mais que o teto de input neutraliza patterns catastróficos (ReDoS); os tetos permanecem como defesa em profundidade (cheap pre-check)"
    - "decide() preserva o contrato consumido por stage.py: 1 grupo casando → matched; vários a 1.0 → ambiguous; nenhum/abaixo do piso → quarantine"
  artifacts:
    - path: "backend/app/classification/matcher.py"
      provides: "Timeout REAL de casamento de regex via lib regex; falha-fechada de decide() para confiança 0.0"
      contains: "timeout"
    - path: "backend/tests/classification/test_matcher_groups.py"
      provides: "Teste ReDoS patológico com asserção de deadline + teste de falha-fechada de decide() com threshold 0/negativo"
      contains: "monotonic"
    - path: "backend/pyproject.toml"
      provides: "Dependência regex declarada e travada"
      contains: "regex"
  key_links:
    - from: "backend/app/classification/matcher.py"
      to: "decide()"
      via: "match_templates retorna confidence 1.0/0.0; decide exige confiança estritamente positiva para matched/ambiguous"
      pattern: "def decide"
    - from: "backend/app/classification/stage.py"
      to: "matcher.match_templates / matcher.decide"
      via: "assinaturas preservadas (fields_json/full_text/doc_type_guess/templates; decide(matches, threshold=))"
      pattern: "doc_type_guess"
---

<objective>
Fechar a lacuna de SEGURANÇA da Fase 06.1 (CR-01 BLOCKER do VERIFICATION e WR-01 WARNING do REVIEW), ambas concentradas em `backend/app/classification/matcher.py`:

1. **CR-01 (ReDoS real):** Os tetos de tamanho (`_MAX_SIGNAL_REGEX_LEN`, `_MAX_HAYSTACK_LEN`) NÃO impedem backtracking catastrófico — o pattern `(a+)+$` (5 chars) contra `'a'*40+'X'` (41 chars) trava o processo, ambos dentro dos limites, porque o custo vem da estrutura LOCAL do pattern, não do tamanho do input. Introduzir um TIMEOUT REAL de casamento usando a lib `regex` (drop-in do `re`) com `search(text, timeout=...)`, tratando `TimeoutError` (e mantendo `regex.error`) como falha-fechada. Manter os tetos como defesa em profundidade e corrigir a docstring falsa.

2. **WR-01 (falha-fechada de decide):** Com `threshold <= 0`, `decide([0.0], 0.0)` retorna "matched", classificando documentos SEM nenhum sinal — fura a falha-fechada. Exigir confiança estritamente positiva para "matched"/"ambiguous" e validar a faixa do threshold em `config.py`, SEM quebrar o contrato `decide()` consumido por `stage.py`.

Purpose: A goal da fase promete sinais "avaliados com segurança (ReDoS)". Hoje essa promessa é falsa: uma regex colada pelo operador trava o worker de classificação (DoS da fila). E o threshold zero fura a falha-fechada que o resto do módulo persegue. Este plano honra a promessa de segurança sem mexer no motor booleano E/OU já entregue (Planos 01/02/03).

Output: `matcher.py` com timeout real de regex (lib `regex`) + falha-fechada de `decide()`; docstring corrigida; `config.py` com faixa válida de threshold; `pyproject.toml` com `regex` travada; testes reforçados (ReDoS patológico com deadline + decide com threshold 0/negativo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-CONTEXT.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-VERIFICATION.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-REVIEW.md

<interfaces>
<!-- matcher.py ESTADO ATUAL — assinaturas a PRESERVAR. Source: backend/app/classification/matcher.py -->
<!-- @dataclass(frozen=True) TemplateMatch: template_id: int; confidence: float -->
<!-- @dataclass(frozen=True) MatchDecision: status: str; template_id: int | None -->
<!-- def match_templates(*, fields_json: str, full_text: str, doc_type_guess: str, templates: list[Template]) -> list[TemplateMatch] -->
<!-- def decide(matches: list[TemplateMatch], *, threshold: float) -> MatchDecision -->
<!-- _MAX_SIGNAL_REGEX_LEN = 512   # teto do PATTERN (manter como pre-check barato) -->
<!-- _MAX_HAYSTACK_LEN = 200_000   # teto do INPUT (manter como defesa em profundidade) -->
<!-- _AMBIGUITY_MARGIN = 0.1       # PRESERVAR -->

<!-- _condition_matches ATUAL (modo regex) — ponto exato a endurecer com timeout. Source: matcher.py:116-123 -->
<!--   if mode == "regex": if not value or len(value) > _MAX_SIGNAL_REGEX_LEN: return False -->
<!--   try: pattern = re.compile(value, re.IGNORECASE) except re.error: return False -->
<!--   return pattern.search(haystack[:_MAX_HAYSTACK_LEN]) is not None   <-- SEM timeout: trava aqui -->

<!-- decide() ATUAL — guarda de quarentena a endurecer. Source: matcher.py:181 -->
<!--   if not matches or matches[0].confidence < threshold: return MatchDecision("quarantine", None) -->
<!--   com threshold=0.0 e confidence=0.0: 0.0 < 0.0 == False → cai em matched (BUG WR-01) -->

<!-- config.py ATUAL — campo sem validação de faixa. Source: backend/app/config.py:142-145 -->
<!--   classify_match_threshold: float = Field(default=0.5, validation_alias=AliasChoices(...)) -->

<!-- stage.py CONSOME (NÃO quebrar). Source: backend/app/classification/stage.py:206-216 -->
<!--   matcher.match_templates(fields_json=..., full_text=..., doc_type_guess=..., templates=...) -->
<!--   matcher.decide(matches, threshold=settings.classify_match_threshold) -->

<!-- Fix prescrito (VERIFICATION linhas 205-224 / REVIEW CR-01): -->
<!--   import regex ; pattern = regex.compile(value, regex.IGNORECASE) -->
<!--   pattern.search(haystack[:_MAX_HAYSTACK_LEN], timeout=_REGEX_TIMEOUT_S) is not None -->
<!--   except (regex.error, TimeoutError): return False  # falha fechada -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Gate de legitimidade do pacote `regex` (PyPI) antes do install</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <action>Checkpoint de legitimidade — não escreve código. Antes de qualquer `uv add`/install da lib `regex`, um humano verifica pypi.org/project/regex (mantida, cp312, `timeout=` real, anti-typosquat) e informa a versão a fixar. A instalação efetiva ocorre na Task 3 após aprovação. Detalhes em &lt;what-built&gt;/&lt;how-to-verify&gt;.</action>
  <verify>Verificação humana via pypi.org/project/regex (ver &lt;how-to-verify&gt;). Sem comando automatizado: gate blocking-human não auto-aprovável.</verify>
  <done>Humano confirmou a legitimidade de `regex` e informou a versão a fixar no pyproject; execução pode prosseguir para a Task 3.</done>
  <what-built>
    Nenhum código ainda. Este é o gate de legitimidade exigido porque o RESEARCH desta fase
    marcou a Package Legitimacy Audit como N/A ("esta fase NÃO instala pacotes externos") e
    recomendou explicitamente NÃO adotar a lib `regex`. O gap closure REVERTE essa recomendação:
    CR-01 prova que `re` (stdlib, sem timeout) não tem como abortar backtracking catastrófico, e
    o único timeout real de regex em Python é a lib `regex` (3rd-party). Logo, `regex` entra como
    nova dependência e DEVE passar pelo gate antes de qualquer `uv add`/install.

    Disposição: `regex` é tratada como [ASSUMED] até verificação humana (RESEARCH linha 60).
  </what-built>
  <how-to-verify>
    1. Abrir https://pypi.org/project/regex/ e confirmar:
       - O projeto existe, é mantido (releases recentes, downloads expressivos), autor "Matthew Barnett".
       - É a lib `regex` (módulo drop-in do `re`) que suporta o argumento `timeout=` em `search`/`match`.
       - NÃO é um typosquat (conferir o nome EXATO: `regex`, não `re-gex`/`regexp`/`pyregex`).
    2. Confirmar compatibilidade com Python 3.12 (wheels publicadas para cp312).
    3. Decidir a versão a travar (linha de versão estável vigente) seguindo o padrão de
       pinning do `backend/pyproject.toml` (versões exatas ou `==X.Y.*`).
  </how-to-verify>
  <acceptance_criteria>
    - Humano confirma que `regex` em pypi.org/project/regex é legítima, mantida, cp312-compatível, com `timeout=` no match.
    - Humano informa a versão (ou linha de versão) a fixar no pyproject.
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" (e a versão a fixar, ex.: "regex==2024.11.6") ou descreva o problema</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reforçar testes — ReDoS patológico com deadline + falha-fechada de decide() (RED)</name>
  <files>backend/tests/classification/test_matcher_groups.py</files>
  <read_first>
    - backend/tests/classification/test_matcher_groups.py (suite atual; o `test_redos_nao_trava` em uso dá falsa cobertura — usa input que casa rápido)
    - backend/tests/classification/test_matcher.py (molde de casos de decide(): matched/ambiguous/quarantine; construção de TemplateMatch ou via match_templates)
    - backend/app/classification/matcher.py (decide() atual linha 181; _condition_matches modo regex linhas 116-123 — alvo dos testes)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-VERIFICATION.md (gap CR-01: caso patológico exato e evidência de exit 124)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-REVIEW.md (WR-01: decide([0.0],0.0)=="matched", decide([0.0,0.0],0.0)=="ambiguous")
  </read_first>
  <behavior>
    - test_redos_quase_match_termina_no_deadline: pattern `(a+)+$` (modo regex) contra full_text `'a'*40+'X'` (quase-match patológico, ambos dentro dos tetos). Medir `time.monotonic()` antes/depois de `match_templates`; asserir que retorna (não trava) ABAIXO de um deadline folgado (ex.: 2.0s) E que a confiança é 0.0 (falha-fechada: a regex não casa porque o casamento abortou por timeout). Este é o caso que o atual `test_redos_nao_trava` NÃO exercita.
    - test_redos_caso_bom_continua_casando: garantir que o reforço de timeout NÃO regrediu o caso bem-comportado — pattern `(a+)+$` contra `'a'*100` (all-a, casa gulosamente em tempo linear) ainda retorna confiança 1.0. Protege contra um timeout agressivo demais que mate matches legítimos.
    - test_decide_sem_sinal_nunca_matched_threshold_zero: confiança 0.0 (nenhum grupo casou) com threshold 0.0 → status "quarantine" (NÃO "matched"). Cobre WR-01.
    - test_decide_sem_sinal_nunca_matched_threshold_negativo: confiança 0.0 com threshold negativo (ex.: -1.0) → "quarantine" (NÃO "matched"). Falha-fechada independe do threshold.
    - test_decide_dois_zeros_nao_ambiguo: dois templates a 0.0 com threshold 0.0 → "quarantine" (NÃO "ambiguous"). Cobre o segundo caso do WR-01.
    - test_decide_match_legitimo_preservado: confiança 1.0 (≥ threshold padrão 0.5) e único → "matched"; dois a 1.0 → "ambiguous". Guard de regressão do contrato consumido por stage.py.
  </behavior>
  <action>
    Editar `test_matcher_groups.py`. SUBSTITUIR o `test_redos_nao_trava` atual (que usa `'a'*(_EXPECTED_MAX_HAYSTACK_LEN+10_000)+'!'`, um match guloso bem-comportado de falsa cobertura) pelos dois novos testes de ReDoS descritos em `<behavior>`. Importar `time` (usar `time.monotonic()` em volta da chamada a `match_templates`) e definir um `_REDOS_DEADLINE_S` local FOLGADO (ex.: 2.0) — não importar a constante de timeout do módulo; o teste mede o comportamento observável (termina rápido), não o valor interno. Reaproveitar o helper `_confidence(tpl, full_text)` existente. Para os testes de `decide()`, exercitar via a API pública: construir os `TemplateMatch` chamando `match_templates` com templates cujos sinais casam/não casam (preferível) OU, se o estilo de `test_matcher.py` já constrói `MatchDecision`/`TemplateMatch` diretamente, seguir esse mesmo estilo; chamar `matcher.decide(matches, threshold=...)` e asserir `.status`. NÃO importar helpers internos. Como o GREEN ainda não existe, os testes novos devem FALHAR por comportamento (ReDoS: timeout/exit 124 ou deadline estourado; decide: status "matched"/"ambiguous" em vez de "quarantine") — RED. Commit RED: `test(06.1-04): ReDoS patológico com deadline + falha-fechada de decide() (RED)`.
  </action>
  <verify>
    <automated>cd backend && timeout 30 .venv/bin/python -m pytest tests/classification/test_matcher_groups.py -k "redos or decide" -q 2>&1 | grep -Eq "failed|error|timeout"</automated>
  </verify>
  <acceptance_criteria>
    - `test_redos_nao_trava` antigo foi REMOVIDO/substituído (não coexiste com os novos): `grep -c "def test_redos_nao_trava" backend/tests/classification/test_matcher_groups.py` == 0.
    - Existem `def test_redos_quase_match_termina_no_deadline`, `def test_redos_caso_bom_continua_casando`, e ≥3 testes `def test_decide_` cobrindo threshold 0, negativo e o caso legítimo.
    - `grep -c "monotonic" backend/tests/classification/test_matcher_groups.py` ≥ 1.
    - A suite filtrada por `-k "redos or decide"` está RED (falha por comportamento/timeout, não por ImportError).
  </acceptance_criteria>
  <done>Testes reforçados existem e estão RED: o ReDoS patológico trava/estoura o deadline e decide() ainda "casa" sem sinal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — timeout real de regex (lib regex) + falha-fechada de decide() + docstring corrigida</name>
  <files>backend/pyproject.toml, backend/app/classification/matcher.py, backend/app/config.py</files>
  <read_first>
    - backend/app/classification/matcher.py (módulo inteiro — preservar TemplateMatch/MatchDecision/assinatura de match_templates; alterar SÓ _condition_matches modo regex, decide() e a docstring)
    - backend/pyproject.toml (bloco [project].dependencies — padrão de pinning exato a seguir; uv.lock será atualizado pelo uv)
    - backend/app/config.py (linhas 134-145 — campo classify_match_threshold; padrão de Field/AliasChoices a manter)
    - backend/app/classification/stage.py (linhas 204-216 — confirma o contrato decide(matches, threshold=); NÃO deve precisar mudar)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-VERIFICATION.md (fix prescrito linhas 205-224)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-REVIEW.md (CR-01 e WR-01 — fix exato e faixa de threshold ge=0.0 le=1.0)
  </read_first>
  <action>
    (1) **Dependência:** Em `backend/pyproject.toml`, adicionar `regex` ao `[project].dependencies` com a versão aprovada na Task 1 (pinning exato no padrão do arquivo, ex.: `"regex==2024.11.6"`). Rodar `uv lock` (ou `uv add regex==<versao>`) no diretório `backend/` para atualizar `uv.lock` e instalar no venv. NÃO usar `pip install` solto nem `npx`.
    (2) **CR-01 — timeout real:** Em `matcher.py`, trocar `import re` por `import regex` (drop-in) OU adicionar `import regex` mantendo o nome `re` apenas se outro ponto do módulo o usar (hoje só `_condition_matches` usa — preferir substituir por `regex`). Definir constante `_REGEX_TIMEOUT_S = 0.25` (deadline por condição; single-tenant — comentar que é calibrável). No ramo `mode == "regex"` de `_condition_matches`, manter o pre-check de teto de pattern (`len(value) > _MAX_SIGNAL_REGEX_LEN → False`) como defesa em profundidade, compilar com `regex.compile(value, regex.IGNORECASE)` e casar com `pattern.search(haystack[:_MAX_HAYSTACK_LEN], timeout=_REGEX_TIMEOUT_S) is not None`, capturando `(regex.error, TimeoutError)` → `return False` (falha-fechada, V5). Manter o corte de haystack como cheap pre-check.
    (3) **Docstring:** Corrigir a docstring do módulo (linhas ~22-25 e ~48-51) e o comentário da constante `_MAX_HAYSTACK_LEN`: REMOVER a afirmação de que o teto de input "neutraliza patterns catastróficos (ReDoS)". Substituir por: o teto é defesa em profundidade (cheap pre-check); a proteção REAL contra ReDoS é o `timeout=_REGEX_TIMEOUT_S` da lib `regex`, que aborta backtracking catastrófico e falha-fechada.
    (4) **WR-01 — falha-fechada de decide():** Em `decide()`, endurecer a guarda de quarentena para exigir confiança ESTRITAMENTE positiva, sem quebrar o seam: `if not matches or matches[0].confidence <= 0.0 or matches[0].confidence < threshold: return MatchDecision(status="quarantine", template_id=None)`. NÃO alterar a assinatura `decide(matches, *, threshold)` nem a lógica de `_AMBIGUITY_MARGIN`/ambiguous abaixo dela (a guarda de positividade já impede dois 0.0 chegarem ao ramo ambiguous). Em `config.py`, restringir `classify_match_threshold` à faixa `Field(default=0.5, ge=0.0, le=1.0, validation_alias=...)` mantendo o `AliasChoices` existente.
    Commit GREEN: `fix(06.1-04): timeout real de regex (lib regex) + decide() falha-fechada + docstring`.
  </action>
  <verify>
    <automated>cd backend && timeout 60 .venv/bin/python -m pytest tests/classification/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && timeout 30 .venv/bin/python -m pytest tests/classification/test_matcher_groups.py -q` passa inteira (GREEN), incluindo o ReDoS patológico terminando sob o deadline.
    - `grep -nE "timeout=" backend/app/classification/matcher.py` retorna a linha do `pattern.search(..., timeout=_REGEX_TIMEOUT_S)`.
    - `grep -c "import regex" backend/app/classification/matcher.py` == 1; `grep -c "import re$\|^import re\b" backend/app/classification/matcher.py` == 0 (re stdlib removido do matcher) OU justificado se ainda usado em outro ponto.
    - `grep -c "regex" backend/pyproject.toml` ≥ 1 (dependência declarada); `regex` presente em `backend/uv.lock`.
    - `grep -vE '^\s*#' backend/app/classification/matcher.py | grep -ic "neutraliza patterns catastróficos"` — a afirmação falsa não está mais no corpo da docstring (somente, se houver, em comentário que a contradiz explicitamente).
    - `grep -nE "ge=0.0|le=1.0" backend/app/config.py` mostra a faixa no campo `classify_match_threshold`.
    - `grep -c "def decide" backend/app/classification/matcher.py` == 1 e a assinatura `def decide(matches: list[TemplateMatch], *, threshold: float) -> MatchDecision` está preservada.
    - `cd backend && timeout 60 .venv/bin/python -m pytest tests/classification/test_stage.py tests/classification/test_stage_routing.py -q` passa (seam decide()/match_templates intacto; stage.py não mudou).
    - Reprodução do gap NÃO trava mais: `cd backend && timeout 5 .venv/bin/python -c "import regex; p=regex.compile(r'(a+)+\$', regex.I); print(p.search('a'*40+'X', timeout=0.25))"` retorna em < 5s (imprime None ou lança TimeoutError tratável), exit code != 124.
  </acceptance_criteria>
  <done>Matcher usa timeout real de regex (falha-fechada em ReDoS); decide() nunca casa sem sinal; threshold validado em faixa; docstring corrigida; classification/ e stage verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → matcher | Regex de sinal colada pelo operador (single-tenant) é compilada e executada contra `full_text` em `_condition_matches` |
| signals_json (SQLite) → matcher | Padrão de regex persistido como STRING na coluna Text, compilado SOMENTE dentro de `_condition_matches` (nunca no endpoint) |
| config/env → decide() | `classify_match_threshold` (env, sem deploy) governa o roteamento matched/ambiguous/quarantine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Denial of Service | `_condition_matches` (regex catastrófica → backtracking exponencial) | mitigate | **FIX CR-01:** timeout REAL via lib `regex` — `pattern.search(haystack[:_MAX_HAYSTACK_LEN], timeout=_REGEX_TIMEOUT_S)`; `TimeoutError` capturado → condição NÃO casa (falha-fechada). Aborta o backtracking que os tetos de tamanho NÃO impedem (estrutura local do pattern, não tamanho do input). O worker da fila não trava mais. |
| T-06.1-02 | Denial of Service | `_condition_matches` (regex inválida) | mitigate | `try/except (regex.error, TimeoutError)` → condição não casa (V5); nunca propaga 500/trava ao worker |
| T-06.1-06 | Denial of Service (defesa em profundidade) | tetos de tamanho `_MAX_SIGNAL_REGEX_LEN`/`_MAX_HAYSTACK_LEN` | mitigate | Mantidos como cheap pre-check (descartam patterns/inputs absurdos antes do compile/search), mas docstring corrigida para NÃO mais alegar que neutralizam ReDoS — a proteção real é o timeout |
| T-06.1-07 | Elevation of Privilege / falha-fechada furada | `decide()` com `threshold <= 0` | mitigate | **FIX WR-01:** exigir confiança estritamente positiva (`matches[0].confidence <= 0.0 → quarantine`) + validar faixa `ge=0.0, le=1.0` em `config.py`. Documento sem sinal nunca é "matched" mesmo com threshold 0/negativo |
| T-06.1-04 | Tampering/RCE | dispatch de modo texto\|regex | mitigate | Dispatch explícito por etiqueta; NUNCA `eval` (preservado do Plano 01) |
| T-06.1-SC | Tampering | install da lib `regex` (uv/pip) | mitigate | Task 1 é checkpoint blocking-human de legitimidade (pypi.org/project/regex; anti-typosquat; cp312; `timeout=` real). `regex` tratada como [ASSUMED] até verificação humana; não auto-aprovável |
</threat_model>

<verification>
- `cd backend && timeout 60 .venv/bin/python -m pytest tests/classification/ -q` verde (incluindo ReDoS patológico sob deadline e decide falha-fechada).
- `cd backend && timeout 60 .venv/bin/python -m pytest tests/classification/test_stage.py tests/classification/test_stage_routing.py -q` verde (seam intacto).
- Reprodução do CR-01 NÃO trava mais (exit code != 124 no comando de 5s com `regex` + `timeout=`).
- `decide()` mantém assinatura e contrato matched/ambiguous/quarantine consumido por stage.py.
- `regex` declarada em `pyproject.toml` e presente em `uv.lock`; nenhuma outra dependência alterada.
</verification>

<success_criteria>
- Regex maliciosa com backtracking catastrófico aborta por timeout e falha-fechada (não casa) sem travar o matcher/worker — CR-01 fechado.
- Documento sem nenhum sinal nunca é "matched"/"ambiguous" independente do threshold (incluindo 0 e negativo); threshold restrito à faixa [0.0, 1.0] — WR-01 fechado.
- Docstring do matcher não afirma mais proteção ReDoS pelos tetos; tetos permanecem como defesa em profundidade.
- Seam `decide()` e a integração com `stage.py` preservados; nenhum plano-base (01/02/03) modificado.
- D-T1, D-T2 (aspectos de segurança/avaliação) cobertos por testes automatizados reforçados.
</success_criteria>

<output>
Create `.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-04-SUMMARY.md` when done
</output>
</content>
</invoke>
