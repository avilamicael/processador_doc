---
phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/classification/test_matcher_groups.py
  - backend/tests/classification/test_matcher.py
  - backend/app/classification/matcher.py
autonomous: true
requirements: [D-T1, D-T2]
nyquist_compliant: true

must_haves:
  truths:
    - "Um documento cujo full_text bate em QUALQUER grupo (OU) e, dentro do grupo, em TODAS as condições (E) recebe confiança 1.0 daquele template"
    - "Uma condição 'texto' casa por substring case-insensitive no full_text do documento"
    - "Uma condição 'regex' casa via re.search IGNORECASE; regex inválida ou maior que o teto NÃO casa (falha fechada) e nunca derruba o matcher"
    - "Um pattern catastrófico (ReDoS) não trava o matcher porque o input é cortado pelo teto antes de aplicar a regex"
    - "signals_json na forma plana legada (list[str]) continua parseável sem erro (parser forward-compatible)"
    - "decide() permanece intacto: 1 grupo casando → matched; vários templates a 1.0 → ambiguous; nenhum → quarantine"
  artifacts:
    - path: "backend/app/classification/matcher.py"
      provides: "Avaliação booleana de grupos E/OU texto|regex com tetos de regex; decide() preservado"
      contains: "_template_matches"
    - path: "backend/tests/classification/test_matcher_groups.py"
      provides: "Suite RED→GREEN de grupos E/OU, texto, regex, ReDoS, legado"
      contains: "def test_"
  key_links:
    - from: "backend/app/classification/matcher.py"
      to: "decide()"
      via: "match_templates retorna confidence 1.0/0.0 preservando a assinatura"
      pattern: "def decide"
    - from: "backend/app/classification/stage.py"
      to: "matcher.match_templates"
      via: "assinatura fields_json/full_text/doc_type_guess/templates preservada (param doc_type_guess aceito e ignorado)"
      pattern: "doc_type_guess"
---

<objective>
Evoluir o motor de classificação `matcher.py` de "fração de termos literais presentes" para **avaliação booleana de grupos E/OU de condições texto|regex** (D-T1, D-T2), endurecendo a execução de regex colada pelo operador (ReDoS) e PRESERVANDO o seam `decide()` que a Fase 5 e o pipeline de revisão consomem. Este é o coração da fase — define a forma canônica do JSON de sinais que o Plano 02 (API) e o Plano 03 (frontend) consomem.

Purpose: Permitir templates "geral vs por-cliente" (ex.: NF com âncora `DANFE` + regex `\d{44}`, OU CNPJ literal do cliente) com lógica booleana determinística e custo-zero, sem quebrar o roteamento matched/ambiguous/quarantine.

Output: `matcher.py` com parser forward-compatible de grupos + avaliação booleana + tetos de regex; suite de testes RED→GREEN cobrindo D-T1/D-T2/ReDoS/legado/decide().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-CONTEXT.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-RESEARCH.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-PATTERNS.md
@.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-VALIDATION.md

<interfaces>
<!-- Forma canônica do JSON de sinais (DEFINIDA NESTA FASE, consumida por 02 e 03). -->
<!-- signals_json (coluna Text existente) = lista de grupos; cada grupo = lista de condições. -->
<!-- OU entre grupos, E dentro do grupo. Forma list-of-pairs-friendly (03-01). -->
<!--
[
  [ {"mode": "texto", "value": "TryLab"}, {"mode": "regex", "value": "\\d{44}"} ],   # grupo 1: E
  [ {"mode": "texto", "value": "12.345.678/0001-99"} ]                                # OU grupo 2
]
-->

<!-- matcher.py ATUAL — assinaturas a preservar (decide INTACTO). Source: backend/app/classification/matcher.py -->
@dataclass(frozen=True) TemplateMatch: template_id: int; confidence: float
@dataclass(frozen=True) MatchDecision: status: str; template_id: int | None
def match_templates(*, fields_json: str, full_text: str, doc_type_guess: str, templates: list[Template]) -> list[TemplateMatch]
def decide(matches: list[TemplateMatch], *, threshold: float) -> MatchDecision
_AMBIGUITY_MARGIN = 0.1   # PRESERVAR

<!-- stage.py chama (PRESERVAR a assinatura — linha 206-211): -->
matcher.match_templates(fields_json=extraction.fields_json, full_text=extraction.full_text, doc_type_guess=extraction.doc_type_guess, templates=templates)
matcher.decide(matches, threshold=settings.classify_match_threshold)

<!-- Molde de regex segura (REFERÊNCIA read-only). Source: backend/app/validation/fields.py linhas 29,133-140 -->
_MAX_REGEX_LEN = 4096
if len(raw) > _MAX_REGEX_LEN: return <invalid>
pattern = re.compile(regex)         # fields usa fullmatch; matcher usa search + IGNORECASE + try/except re.error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED — suite de grupos E/OU, texto, regex, ReDoS, legado</name>
  <files>backend/tests/classification/test_matcher_groups.py</files>
  <read_first>
    - backend/tests/classification/test_matcher.py (molde de casos existentes: construção de Template com signals_json/doc_type, asserções de confidence e de decide())
    - backend/tests/classification/conftest.py (fixtures de sessão/template se houver)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-RESEARCH.md (Code Examples: _condition_matches/_group_matches/_template_matches; Assumptions A2/A4; tetos)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <behavior>
    - test_grupo_todas_condicoes_E: Template com 1 grupo [texto "DANFE", regex "\d{44}"]; full_text contendo ambos → confidence 1.0; faltando um → 0.0.
    - test_qualquer_grupo_OU: Template com 2 grupos; full_text bate só no grupo 2 → confidence 1.0.
    - test_texto_case_insensitive: condição texto "trylab" casa full_text "...TryLab..." (substring, IGNORECASE).
    - test_regex_search_no_full_text: regex "\d{44}" casa uma chave de 44 dígitos embutida no meio do full_text (re.search, NÃO fullmatch — A4).
    - test_regex_invalida_falha_fechada: regex "(" (inválida) → condição não casa, sem exceção propagada.
    - test_regex_pattern_acima_do_teto: pattern com len > _MAX_SIGNAL_REGEX_LEN → não casa, sem compilar.
    - test_redos_nao_trava: pattern catastrófico "(a+)+$" contra full_text longo (> _MAX_HAYSTACK_LEN de 'a') termina rápido (input cortado) e não casa/erra.
    - test_legado_lista_plana_parseavel: signals_json = '["DANFE","NOTA FISCAL"]' (forma antiga) é parseado sem erro pelo parser forward-compatible (cada termo vira 1 grupo OU de 1 condição texto).
    - test_grupo_vazio_e_sem_grupos_nao_casam: signals_json '[]' e grupo vazio '[[]]' → confidence 0.0 (falha fechada).
  </behavior>
  <action>
    Criar `test_matcher_groups.py` em `backend/tests/classification/` espelhando o estilo de `test_matcher.py` (construção direta de `Template(signals_json=json.dumps(...), doc_type=None)`, chamada a `matcher.match_templates(...)` e asserções sobre `confidence`). Importar de `app.classification.matcher`. Como `matcher.py` ainda não terá os símbolos novos no commit RED, usar `pytest.importorskip` ou referenciar apenas a API pública `match_templates` (que já existe) e deixar os testes FALHANDO por valor esperado (RED) — NÃO por ImportError de função interna. Os nomes dos helpers internos (`_template_matches` etc.) NÃO devem ser importados pelos testes (testar comportamento via `match_templates`, não internals). Definir as constantes esperadas localmente no teste (`_MAX_SIGNAL_REGEX_LEN`, `_MAX_HAYSTACK_LEN`) só como valores de borda para construir os casos, sem importá-las. Commit RED: `test(06.1-01): grupos E/OU + texto/regex/ReDoS/legado falhando`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/classification/test_matcher_groups.py -q 2>&1 | grep -Eq "failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com ≥9 funções `def test_` cobrindo grupos E/OU, texto, regex, regex inválida, teto de pattern, ReDoS, legado plano e grupo vazio.
    - A suite FALHA (RED) — os comportamentos novos ainda não estão implementados em matcher.py.
    - Nenhum teste depende de importar helpers internos (`_condition_matches`/`_group_matches`/`_template_matches`); todos exercitam via `match_templates`.
  </acceptance_criteria>
  <done>test_matcher_groups.py existe e a suite está RED (asserções de valor, não ImportError).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — avaliação booleana de grupos + tetos de regex + parser forward-compatible (decide intacto, doc_type bonus removido)</name>
  <files>backend/app/classification/matcher.py, backend/tests/classification/test_matcher.py</files>
  <read_first>
    - backend/app/classification/matcher.py (módulo inteiro — é seu próprio melhor analog; preservar TemplateMatch/MatchDecision/decide e a assinatura de match_templates)
    - backend/app/validation/fields.py (linhas 29,131-147 — molde _MAX_REGEX_LEN + re.compile + falha-marca; matcher usa search+IGNORECASE+try/except, NÃO fullmatch)
    - backend/app/classification/stage.py (linhas 200-219 — confirma que doc_type_guess é passado; manter o param na assinatura, ignorá-lo internamente)
    - backend/tests/classification/test_matcher.py (test_zona_cinzenta_marca_ambiguo / test_nenhum_sinal_vai_para_quarentena / test_doc_type_guess_casando_soma_pontos — ajustar à pontuação booleana)
    - .planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-PATTERNS.md (seção matcher.py: forma de pontuação, regex segura, A3 doc_type bonus)
  </read_first>
  <action>
    Em `matcher.py`: (1) Adicionar constantes `_MAX_SIGNAL_REGEX_LEN = 512` (teto do PATTERN) e `_MAX_HAYSTACK_LEN = 200_000` (teto do INPUT antes de aplicar regex — full_text excede _MAX_REGEX_LEN). (2) Substituir `_signals` por `_parse_groups(raw: str | None) -> list[list[dict]]` forward-compatible: `json.loads` tolerante (try/except ValueError/TypeError → []); se `parsed` é `list[str]` (forma legada) mapear cada termo para `[{"mode":"texto","value":s}]` (1 grupo OU por termo, preserva "qualquer termo basta"); se é lista de listas, normalizar mantendo só dicts. (3) Adicionar helpers puros `_condition_matches(cond, haystack)` (modo "regex": rejeita se `len(value) > _MAX_SIGNAL_REGEX_LEN`; `try: re.compile(value, re.IGNORECASE) except re.error: return False`; `pattern.search(haystack[:_MAX_HAYSTACK_LEN]) is not None`. modo "texto"/default: `value.strip().lower() in haystack`. value vazio → False), `_group_matches(group, haystack)` = `bool(group) and all(...)` (E; grupo vazio não casa), `_template_matches(groups, haystack)` = `any(_group_matches(g, haystack) for g in groups)` (OU; sem grupos não casa). (4) Reescrever `match_templates`: manter assinatura COM `doc_type_guess` (aceito e IGNORADO — comentar "param mantido p/ compat de assinatura; doc_type saiu do form D-T5/A3"); construir haystack do `full_text` lower (alvo primário A2) — manter as keys dos pares no haystack OU só full_text: usar `full_text` como alvo conforme A2/D-T2, removendo a dependência das field keys para sinais; `confidence = 1.0 if _template_matches(_parse_groups(tpl.signals_json), haystack) else 0.0`; ordenar desc; PRESERVAR `decide()` SEM tocar. (5) Remover `_DOC_TYPE_BONUS` e o ramo `doc_type_guess`/`tpl.doc_type` (lógica morta, A3). (6) Atualizar a docstring do módulo descrevendo grupos E/OU + booleano + tetos. V7/LGPD: NÃO logar full_text/valores. Em `test_matcher.py`: ajustar `test_zona_cinzenta_marca_ambiguo` (montar dois templates que ambos casam → ambos 1.0 → ambiguous), `test_nenhum_sinal_vai_para_quarentena` (mantém), e converter/remover `test_doc_type_guess_casando_soma_pontos` (bônus removido — substituir por um teste que confirma que doc_type não influencia a pontuação). Commit GREEN: `feat(06.1-01): matcher booleano de grupos E/OU texto|regex + tetos ReDoS`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/classification/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/classification/test_matcher_groups.py` passa inteira (GREEN).
    - `tests/classification/test_matcher.py` passa (casos de decide() ambiguous/quarantine/matched verdes com a nova pontuação booleana).
    - `grep -nE "_DOC_TYPE_BONUS|doc_type_guess.*==|tpl\.doc_type" backend/app/classification/matcher.py` retorna 0 linhas de lógica de bônus (assinatura ainda contém o PARÂMETRO doc_type_guess, mas sem ramo de pontuação).
    - `grep -c "def decide" backend/app/classification/matcher.py` == 1 e a função decide está byte-idêntica à atual (seam preservado).
    - `re.compile` aparece SOMENTE dentro de `_condition_matches` com `try/except re.error` adjacente.
    - `cd backend && .venv/bin/python -m pytest tests/classification/test_stage.py tests/classification/test_stage_routing.py -q` passa (assinatura de match_templates/decide intacta; stage.py não precisou mudar).
  </acceptance_criteria>
  <done>Matcher avalia grupos E/OU texto|regex com tetos; decide() intacto; doc_type bonus removido; suite classification/ verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → matcher | regex de sinal colada pelo operador (single-tenant) é compilada e executada contra full_text no matcher |
| signals_json (SQLite) → matcher | JSON possivelmente malformado/legado lido da coluna Text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Denial of Service | `_condition_matches` (regex) | mitigate | Teto de PATTERN (`_MAX_SIGNAL_REGEX_LEN=512`) antes de `re.compile` + teto de INPUT (`_MAX_HAYSTACK_LEN=200_000`) cortando full_text antes de `.search`; `re` stdlib sem timeout aceito por threat model single-tenant (A5) |
| T-06.1-02 | Denial of Service | `_condition_matches` (regex inválida) | mitigate | `try/except re.error` → condição não casa (falha fechada, V5); nunca propaga 500 ao worker |
| T-06.1-03 | Tampering | `_parse_groups` (JSON malformado/legado) | mitigate | `try/except (ValueError, TypeError)` → []; `isinstance(..., list)` guard; parser forward-compatible lê forma plana legada sem erro |
| T-06.1-04 | Tampering/RCE | dispatch de modo texto|regex | mitigate | Dispatch explícito por etiqueta (`if mode == "regex"` else texto); NUNCA `eval` (V5) |
| T-06.1-05 | Information Disclosure | logging do matcher | mitigate | V7/LGPD: matcher NÃO loga full_text/valores de sinal; só metadados (template_id/document_id) se logar |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/classification/ -q` verde.
- `cd backend && .venv/bin/python -m pytest tests/classification/test_stage.py tests/classification/test_stage_routing.py -q` verde (stage não quebrou).
- `decide()` byte-idêntica à versão atual; assinatura de `match_templates` preservada (param `doc_type_guess` presente).
</verification>

<success_criteria>
- Avaliação booleana E/OU texto|regex implementada com confidence 1.0/0.0.
- Tetos de regex (pattern + input) + falha fechada para regex inválida.
- Parser forward-compatible lê forma plana legada e grupos novos.
- decide() e a integração com stage.py intactas (Fase 5 / quarentena preservadas).
- D-T1, D-T2 cobertas por testes automatizados.
</success_criteria>

<output>
Create `.planning/phases/06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex/06.1-01-SUMMARY.md` when done
</output>
