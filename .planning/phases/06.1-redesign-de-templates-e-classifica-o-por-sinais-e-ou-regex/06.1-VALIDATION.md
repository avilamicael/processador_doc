---
phase: 06.1
slug: redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-18
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) + frontend build/typecheck (vite tsc) |
| **Config file** | backend/pyproject.toml (pytest) — frontend: tsconfig + vite |
| **Quick run command** | `cd backend && pytest tests/ -q` |
| **Full suite command** | `cd backend && pytest -q` && `cd frontend && npm run build` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && pytest tests/ -q`
- **After every plan wave:** Run full suite (pytest + frontend build)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 01 | 0 | D-T1/D-T2 | T-04-10 | regex string-only no endpoint; compilada só no matcher | unit | `cd backend && pytest tests/test_matcher_groups.py -q` | ❌ W0 | ⬜ pending |

*Planner fills the rest. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> From RESEARCH.md `## Validation Architecture`. Planner expands into RED stubs.

- [ ] `backend/tests/test_matcher_groups.py` — avaliação booleana de grupos E/OU (qualquer grupo OU; dentro do grupo todas E) — D-T1
- [ ] condições texto literal (case-insensitive) vs regex (`\d{44}`, CNPJ) — D-T2
- [ ] mitigação ReDoS: teto de tamanho de pattern/input + falha fechada (T-04-10, V5/V7)
- [ ] parser forward-compatible: lê `signals_json` plano legado E grupos novos sem migração de dados
- [ ] mapeamento score booleano → `decide()` preservado (1 grupo→matched, vários→ambiguous, nenhum→quarantine)
- [ ] frontend build/typecheck gate (drift de tipos backend↔frontend após novo schema de sinais)

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Densidade visual / dicas hover ⓘ / construtor de grupos E/OU conforme mockup | D-T7/D-T8 | Visual/interação — não automatizável de forma confiável | Abrir TemplatesPage, criar template TryLab (sinais E/OU texto+regex), conferir contra templates-redesign-mockup.html |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
