---
phase: 06.1-redesign-de-templates-e-classifica-o-por-sinais-e-ou-regex
verified: 2026-06-18T12:00:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "Um pattern catastrófico (ReDoS) não trava o matcher porque o input é cortado pelo teto antes de aplicar a regex"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Verificacao visual da TemplatesPage redesenhada"
    expected: |
      Abrir a TemplatesPage no app, criar o template "Nota Fiscal — TryLab" com sinais
      (contém "TryLab" E corresponde a \d{44}) OU (contém "12.345.678/0001-99") e campos
      Emitente / CNPJ do emitente / Número da NF / Valor total. Conferir visualmente contra
      .planning/design/templates-redesign-mockup.html: densidade das linhas, ⚙ Avançado
      abre/fecha, tooltips ⓘ aparecem no hover, badges "sem IA"/"com IA". Salvar, reabrir
      e confirmar que os grupos e campos persistiram.
    why_human: "Layout/densidade visual, comportamento de hover do tooltip e persistencia no round-trip exigem execucao real do app"
---

# Phase 06.1: Redesign de Templates e Classificacao por Sinais (E/OU + Regex) — Verification Report

**Phase Goal:** A tela de criacao/edicao de Template comunica o pipeline (Passo 1 sinais sem IA; Passo 2 campos com IA) e o motor de classificacao por sinais evolui para grupos booleanos E/OU de condicoes texto|regex, persistidos como JSON estruturado e **avaliados com seguranca (ReDoS)** — preservando o seam `decide()`, o roteamento de quarentena/revisao e a ponte campo->token.
**Verified:** 2026-06-18
**Status:** human_needed
**Re-verification:** Yes — after gap closure 06.1-04

---

## Re-Verification Summary

A verificacao anterior (06.1-VERIFICATION.md, status gaps_found, 9/10) reprovou exatamente 1 must_have: T4 — "Sinais avaliados com seguranca (ReDoS)". O plano 06.1-04 executou o gap closure (CR-01 + WR-01). Esta re-verificacao confirma que:

1. O gap CR-01 esta FECHADO: `matcher.py` agora usa `import regex` (lib 3rd-party) com `timeout=_REGEX_TIMEOUT_S` real no `.search()`, capturando `TimeoutError` como falha-fechada. O caso patologico reproduzido independentemente (`(a+)+$` contra `'a'*40+'X'`) retorna em ~0.001s com `None` (falha fechada) — exit code 0 (antes: exit 124).
2. O gap WR-01 esta FECHADO: `decide()` exige confianca estritamente positiva (`<= 0.0 -> quarantine`) — threshold 0 ou negativo nao "casa" mais.
3. Os 9 must_haves restantes nao regrediram: 378 testes verdes, frontend build verde.
4. O unico item pendente e a verificacao visual humana da TemplatesPage — inalterado da verificacao anterior (nao-bloqueante para a meta tecnica).

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| T1 | Um documento cujo full_text bate em QUALQUER grupo (OU) e, dentro do grupo, em TODAS as condicoes (E) recebe confianca 1.0 | VERIFIED | `any(_group_matches(...))` / `all(_condition_matches(...))` em matcher.py; 44 testes de classification/ verdes |
| T2 | Uma condicao 'texto' casa por substring case-insensitive no full_text | VERIFIED | `needle in haystack` (lower) em `_condition_matches`; coberto por `test_texto_case_insensitive` |
| T3 | Uma condicao 'regex' casa via regex.search IGNORECASE; regex invalida ou maior que o teto NAO casa (falha fechada) e nunca derruba o matcher | VERIFIED | `try/except (regex.error, TimeoutError)` + `len(value) > _MAX_SIGNAL_REGEX_LEN -> False` em `_condition_matches`; coberto por `test_regex_invalida_falha_fechada` e `test_regex_pattern_acima_do_teto` |
| T4 | Um pattern catastrofico (ReDoS) nao trava o matcher — o casamento aborta por timeout REAL e falha fechada (nao casa) | **VERIFIED** | Lib `regex==2026.5.9` com `timeout=_REGEX_TIMEOUT_S` (0.25s) em `pattern.search(haystack[:_MAX_HAYSTACK_LEN], timeout=...)`. Reproducao independente: `regex.compile(r'(a+)+$', regex.I).search('a'*40+'X', timeout=0.25)` retorna `None` em ~0.001s, exit 0. `test_redos_quase_match_termina_no_deadline` (caso patologico com `time.monotonic()` < 2s) verde. |
| T5 | signals_json na forma plana legada (list[str]) continua parseavel sem erro (parser forward-compatible) | VERIFIED | `_parse_groups` e `_loads_signals_groups` tratam forma plana mapeando cada termo para `[{"mode":"texto","value":s}]`; coberto por `test_legado_lista_plana_parseavel` |
| T6 | decide() permanece intacto: 1 grupo casando -> matched; varios templates a 1.0 -> ambiguous; nenhum -> quarantine | VERIFIED | `decide()` preservado com falha-fechada adicional (WR-01); 10 stage tests verdes; `test_decide_match_legitimo_preservado` verde (confianca 1.0 unico -> matched; dois a 1.0 -> ambiguous) |
| T7 | A tela de Template explicita o pipeline: Passo 1 'Como reconhecer este tipo — sem IA' e Passo 2 'O que extrair — com IA' (D-T0) | VERIFIED | `TemplatesPage.tsx` linha 287: badge `sem IA`; linha 375: badge `com IA`; comentarios explicitos de Passo 1/Passo 2 no JSX |
| T8 | O usuario monta sinais em grupos E/OU; cada condicao e 'contem o texto' ou 'corresponde ao padrao (regex)' (D-T3/D-T4) | VERIFIED | `TemplatesPage.tsx` linhas 317-370: construtor com `.group`/`.cond`, labels "E"/"OU", botoes "+ E" e "+ OU"; `<select>` com opcoes texto/regex |
| T9 | Campos com layout denso Nome/Tipo/Obrig./⚙ Avancado/remover; ⚙ revela regex de validacao + dica para a IA (D-T7) | VERIFIED | `.frow` grid `1fr 140px 96px 36px 34px` em index.css; `advOpen` por campo (estado React); ⚙ toggle; secao `.adv` com regex + dica |
| T10 | field.name enviado exatamente como digitado (sem snake_case) e tokens de automacao intactos (D-T9) | VERIFIED | `submitForm` linha 215-222: `f.name.trim()` sem transformacao; `test_field_name_preservado_D_T9` verde |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/app/classification/matcher.py` | Avaliacao booleana grupos E/OU + timeout REAL ReDoS (lib regex) + decide() falha-fechada | VERIFIED | `import regex` (linha 47); `timeout=_REGEX_TIMEOUT_S` no `.search` (linha 139); `<= 0.0 -> quarantine` em `decide()` (linha 209); docstring corrigida (tetos como defesa em profundidade, protecao REAL e o timeout) |
| `backend/tests/classification/test_matcher_groups.py` | Suite com ReDoS patologico (deadline) + decide() falha-fechada | VERIFIED | `test_redos_quase_match_termina_no_deadline` + `test_redos_caso_bom_continua_casando` + 3x `test_decide_*`; `test_redos_nao_trava` (falsa cobertura) removido; 2x `time.monotonic()` presente; 14 testes verdes |
| `backend/pyproject.toml` | Dependencia regex declarada e travada | VERIFIED | `"regex==2026.5.9"` em `[project].dependencies`; 20 ocorrencias de `regex` em `uv.lock` |
| `backend/app/api/templates.py` | Schema In/Patch/Out de sinais como grupos; regex string-only | VERIFIED | `SignalConditionIn`, `_loads_signals_groups`, `_dump_signals`; 16 testes API verdes |
| `backend/app/models/template.py` | Apenas docstring atualizada; sem nova migracao | VERIFIED | `mapped_column` inalterado; nenhum arquivo novo em `alembic/versions/` |
| `frontend/src/types.ts` | SignalMode/SignalCondition/SignalGroup/Signals exportados | VERIFIED | Linhas 94-106: todos exportados; `signals: Signals` em Template/Create/Patch |
| `frontend/src/pages/TemplatesPage.tsx` | Construtor grupos E/OU + campos densos + tooltips ⓘ | VERIFIED | Pipeline Passo 1/2 presente; construtor funcional; `dangerouslySetInnerHTML` = 0; `doc_type` = 0 no payload |
| `frontend/src/index.css` | Token --tip-shadow (light+dark) + classes do construtor | VERIFIED | Linha 29: light; linha 46: dark; classes `.info`/`.tip`/`.group`/`.cond`/`.or-div`/`.frow`/`.adv` presentes |
| `backend/app/config.py` | classify_match_threshold restrito a faixa [0.0, 1.0] | VERIFIED | `ge=0.0, le=1.0` em linhas 147-148 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `matcher.py` | `decide()` | `match_templates` retorna confidence 1.0/0.0; `decide` exige confianca ESTRITAMENTE positiva para matched/ambiguous | WIRED | `decide()` com guard `<= 0.0 -> quarantine` (linha 209); chamado por stage.py linha 214 |
| `stage.py` | `matcher.match_templates` | Assinatura fields_json/full_text/doc_type_guess/templates preservada | WIRED | `stage.py` linhas 206-211; 10 stage tests verdes |
| `api/templates.py` | `signals_json` (coluna Text) | `json.dumps([[c.model_dump() for c in g] for g in signals])` via `_dump_signals` | WIRED | Linha 244: serializacao confirmada |
| `api/templates.py` | `matcher._parse_groups` (forma canonica sincronizada) | `_loads_signals_groups` replica a logica de `_parse_groups` | WIRED | IN-01 inofensivo — resultados identicos para todos casos de borda |
| `TemplatesPage.tsx` | `createTemplate/updateTemplate (api.ts)` | `mutate({ name, signals, fields })` com `signals: SignalGroup[]` | WIRED | Linha 229/232: signals enviados no body |
| `types.ts` | `backend TemplateIn.signals` | `Signals = list[list[{mode,value}]]` espelhado em TS | WIRED | `npm run build` (tsc + vite) verde; 82 modulos, sem erros de tipo |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `TemplatesPage.tsx` | `templates` | `useTemplates()` -> `GET /templates` -> `_serialize()` -> `_loads_signals_groups()` | Sim — dados do banco via ORM | FLOWING |
| `matcher.py` | `haystack` | `full_text` do documento (parametro de `match_templates`) | Sim — texto extraido do PDF/imagem | FLOWING |
| `api/templates.py` | `signals` saida | `_serialize(template)` -> `_loads_signals_groups(template.signals_json)` | Sim — le da coluna `signals_json` do SQLite | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| E/OU + grupos corretos | `pytest tests/classification/test_matcher_groups.py -q` | 14 passed | PASS |
| ReDoS patologico nao trava | `python -c "import regex; p=regex.compile(r'(a+)+\$', regex.I); print(p.search('a'*40+'X', timeout=0.25))"` | `None` (falha fechada), exit 0, ~0.001s | PASS |
| decide() falha-fechada threshold zero | `test_decide_sem_sinal_nunca_matched_threshold_zero` | quarantine | PASS |
| decide() falha-fechada threshold negativo | `test_decide_sem_sinal_nunca_matched_threshold_negativo` | quarantine | PASS |
| decide() preservado + stage intacto | `pytest tests/classification/ tests/classification/test_stage*.py -q` | 44+10 passed | PASS |
| API schema grupos CRUD | `pytest tests/test_api_templates.py -q` | 16 passed | PASS |
| Sem migracao nova | `pytest tests/test_migrations.py -q` | 14 passed | PASS |
| Frontend build sem drift TS | `npm run build` | 82 modulos, 0 erros | PASS |
| Suite backend completa | `pytest -q` | 378 passed | PASS |

---

### Probe Execution

Nenhum probe scripts/*/tests/probe-*.sh declarado para esta fase.

---

### Requirements Coverage (D-T0..D-T9)

| Decision | Plano | Descricao | Status | Evidencia |
|----------|-------|-----------|--------|-----------|
| D-T0 | 03 | Pipeline explicito: Passo 1 sem IA / Passo 2 com IA | SATISFIED | Badges `sem IA`/`com IA` + microcopy em TemplatesPage.tsx linhas 287/375 |
| D-T1 | 01 | Logica booleana por grupos: OU entre grupos, E dentro do grupo | SATISFIED | `any(_group_matches)` / `all(_condition_matches)` em matcher.py; 44 testes classification/ verdes |
| D-T2 | 01/02/03/04 | Condicoes texto (substring) ou regex (regex.search com timeout REAL) contra full_text; ReDoS abortado por timeout, falha fechada | SATISFIED | CR-01 fechado: `import regex`; `timeout=_REGEX_TIMEOUT_S` no `.search`; `test_redos_quase_match_termina_no_deadline` verde |
| D-T3 | 03 | Sem ajudante de IA para regex; usuario escreve/cola | SATISFIED | Nenhuma chamada de IA no form de sinais; package.json sem dependencia nova |
| D-T4 | 01/02/03 | Template geral vs por-cliente via E/OU + condicoes literais (CNPJ/nome) | SATISFIED | Grupos E/OU funcionais; mockup do exemplo TryLab portado |
| D-T5 | 01/02/03 | doc_type removido do form; coluna dormente | SATISFIED | `grep doc_type TemplatesPage.tsx` = 0 no payload; `doc_type: str|None = None` no schema como legado |
| D-T6 | 03 | field.name = dado que o usuario quer; nomes amigaveis | SATISFIED | `f.name.trim()` sem snake_case; test_field_name_preservado_D_T9 verde |
| D-T7 | 03 | Layout denso: Nome\|Tipo\|Obrig.\|⚙\|remover; ⚙ revela regex+dica | SATISFIED | Grid `.frow 1fr 140px 96px 36px 34px`; `.adv` com regex+dica; estado React por campo |
| D-T8 | 03 | Tooltips ⓘ via CSS hover puro, sem lib | SATISFIED | `.info:hover .tip { display: block }` em index.css; zero lib npm nova; zero JS de hover |
| D-T9 | 02/03 | field.name = tokens das automacoes; persistido como digitado | SATISFIED | `f.name.trim()` sem transformacao; `test_field_name_preservado_D_T9` verde; `TemplateField.name` inalterado em models |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `TemplatesPage.tsx` | 206-234 | `signals` sempre incluido no PATCH body; `signals: []` apaga sinais silenciosamente (WR-04) | WARNING | Operador que limpa todas condicoes perde sinais sem aviso — template para de casar |
| `matcher.py` | 142 | `fields_json` e `doc_type_guess` mortos na assinatura sem marcador de remocao (IN-02) | INFO | Divida tracavel esperada — sem issue/TODO referenciando remocao futura |
| `TemplatesPage.tsx` | 224 | Erro generico mascara 409 de nome duplicado (IN-03) | INFO | UX degradada mas nao bloqueia o goal |
| `api/templates.py` | 73 | Guard `parsed and` diverge de `matcher._parse_groups` (ausencia de `parsed and`) — IN-01 | INFO | Funcional (resultados identicos para todos casos de borda), mas as "replicas exatas" divergem |

Nenhum BLOCKER (TBD/FIXME/XXX nao referenciados) encontrado nos arquivos modificados pelo plano 04.

---

### Human Verification Required

#### 1. Verificacao visual da TemplatesPage redesenhada

**Test:** Abrir a TemplatesPage no app, criar o template "Nota Fiscal — TryLab" com sinais
(contem "TryLab" E corresponde a `\d{44}`) OU (contem "12.345.678/0001-99") e campos
Emitente / CNPJ do emitente / Numero da NF / Valor total.

**Expected:** Conferir visualmente contra `.planning/design/templates-redesign-mockup.html`:
densidade das linhas, ⚙ Avancado abre/fecha, tooltips ⓘ aparecem no hover, badges
"sem IA"/"com IA" presentes. Salvar, reabrir e confirmar que os grupos e campos
persistiram.

**Why human:** Layout/densidade visual, comportamento de hover do tooltip CSS e persistencia
no round-trip POST->GET exigem execucao real do app — nao verificavel por grep.

---

## Gaps Summary

Nenhum gap tecnico restante. O unico bloqueio anterior (CR-01 ReDoS) foi fechado pelo plano 06.1-04. O status `human_needed` deve-se exclusivamente ao item de verificacao visual da TemplatesPage — presente desde a verificacao inicial e nao-bloqueante para a entrega tecnica.

---

_Verified: 2026-06-18_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — after gap closure 06.1-04 (CR-01 ReDoS + WR-01 decide() fail-closed)_
