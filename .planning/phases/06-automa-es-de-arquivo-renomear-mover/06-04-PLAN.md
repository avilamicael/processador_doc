---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - backend/app/automation/stage.py
  - backend/app/api/automations.py
  - backend/app/queue/worker.py
  - backend/app/api/documents.py
  - backend/app/main.py
autonomous: true
requirements: [AUT-01, AUT-02, AUT-03, AUT-04, AUT-05, AUT-06, TPL-02]
must_haves:
  truths:
    - "apply_stage avalia regras → resolve destino → checa colisão → escreve AuditLog(intent) ANTES de tocar o disco → materializa → AuditLog(done) + transição, num fluxo idempotente (AUT-03/AUT-04)"
    - "Campo faltante/inválido usado no padrão rebaixa o documento para EM_REVISAO sem aplicar (D-07)"
    - "Documentos de alta confiança (score ≥ review_confidence_threshold) são auto-aplicados via sweep enqueue_pending_applications; baixa confiança só aplica após aprovação humana (D-01/D-02)"
    - "Existem endpoints CRUD de regras, dry-run (preview origem→destino sem tocar disco), apply (por-doc e por-lote) e undo (por-doc e por-run) (TPL-02/AUT-03/AUT-05/D-03)"
    - "Crash entre intent e done é reconciliável no startup do worker (Pitfall 7)"
  artifacts:
    - path: "backend/app/automation/stage.py"
      provides: "apply_stage idempotente + write-ahead + reconcile_orphan_intents"
      contains: "def apply_stage"
    - path: "backend/app/api/automations.py"
      provides: "Router /automations: CRUD regras + POST /dry-run + POST /apply + POST /undo"
      contains: "APIRouter(prefix=\"/automations\""
  key_links:
    - from: "backend/app/queue/worker.py"
      to: "apply_stage"
      via: "dispatch step APPLY_STEP + enqueue_pending_applications"
      pattern: "apply_stage|APPLY_STEP"
    - from: "backend/app/automation/stage.py"
      to: "automation.rules / automation.naming / automation.fileops"
      via: "orquestração"
      pattern: "from app.automation"
    - from: "backend/app/main.py"
      to: "automations router"
      via: "include_router"
      pattern: "automations"
---

<objective>
A orquestração: `apply_stage` (liga rules→naming→fileops→audit write-ahead→estado num fluxo idempotente), o novo step `apply` no worker (dispatch + sweep auto-aplica + FALHA por step + reconciliação de intents órfãos), e a API `/automations` (CRUD de regras + dry-run + apply por-doc/lote + undo por-doc/run). Torna GREEN `test_stage.py` e `test_api_automations.py` e fecha o pipeline end-to-end.

Purpose: É onde as peças puras (Plan 02) e a operação de disco (Plan 03) viram comportamento real do produto. O write-ahead + materialização-do-CAS + estado via transition entregam AUT-03/04/05/06 com a garantia "nunca perde". O auto-aplica (D-01) e a aprovação manual disparam o mesmo apply.
Output: `app/automation/stage.py`, `app/api/automations.py`, e modificações em `worker.py`, `documents.py`, `main.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-CONTEXT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md

<interfaces>
<!-- Extraído do codebase — usar diretamente. -->

Molde EXATO do stage (backend/app/classification/stage.py):
  async def classify_stage(session, *, content_hash, forced_template_id=None) -> ClassifyStageResult
  - localiza Document por content_hash (None→ValueError, worker re-tenta)
  - IDEMPOTÊNCIA: checa registro existente ANTES de agir → no-op
  - NUNCA session.commit() manual antes de transition (transition comita tudo junto)
  - transition(session, doc, DocState.X, completed_step="...") — comita internamente

Worker (backend/app/queue/worker.py):
  INGEST_STEP/EXTRACT_STEP/CLASSIFY_STEP; _dispatch bifurca por step;
  enqueue_pending_classifications(session) — molde de sweep idempotente;
  _sweep_pending(engine) chama os sweeps; _fail_for_step roteia FALHA por step;
  requeue_running(session) — resume de jobs running no startup (molde de reconcile)

Repo (backend/app/queue/repo.py):
  enqueue(session, *, original_hash, step, payload) -> Row|None  # no-op se UNIQUE(hash,step) existe
  requeue_step(session, *, content_hash, step, payload) -> int   # reseta linha p/ pending
  _requeue helper (documents.py:462-472): requeue_step OU enqueue

API molde (backend/app/api/templates.py): In/Patch/Out Pydantic, request.app.state.engine,
  with get_session(engine), IntegrityError→409, 404 ausente, 204 DELETE, coleção filha
  substituída inteira no PATCH (delete-orphan).
Ações molde (backend/app/api/documents.py): approve_document (guard estado + transition),
  retry_document, _requeue. CONCLUIDO é terminal (allowlist states.py).

Allowlist (backend/app/pipeline/states.py): PROCESSANDO → {EM_REVISAO, CONCLUIDO, ...};
  EM_REVISAO → {PROCESSANDO, CONCLUIDO, ...}; CONCLUIDO terminal (sem saídas).

Peças puras (Plans 02/03):
  automation.rules.evaluate_rules(rules, fields) -> AutomationRule | None
  automation.naming.resolve_pattern(pattern, fields) -> str | None
  automation.naming.resolve_folder_pattern(pattern, fields, base) -> Path | None
  automation.fileops.resolve_collision(dst, content_hash) -> Path | None
  automation.fileops.materialize_to_dest(content_hash, dst) -> None
  automation.undo.undo_document(session, document_id) / undo_run(session, run_id)

DECISÃO DE ESTADO (planner, Open Q3): o `apply` é o que CONCLUI o documento.
  Auto-aplica (D-01): sweep enqueue_pending_applications pega docs PROCESSANDO +
  last_completed_step="classificado" com score ≥ review_confidence_threshold e SEM
  AuditLog(status="done"); apply_stage materializa e faz transition(CONCLUIDO,
  completed_step="aplicado"). Aprovação manual (approve_document): em vez de
  transitar direto a CONCLUIDO, enfileira o step apply (que conclui). Doc sem
  regra/padrão aplicável → conclui sem operação de arquivo (apply é no-op de disco
  mas conclui o doc). Campo faltante no padrão → transition(EM_REVISAO) (D-07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apply_stage — orquestração write-ahead idempotente + reconciliação (AUT-03/AUT-04, D-01/D-07)</name>
  <read_first>
    - backend/app/classification/stage.py (LER NA ÍNTEGRA: forma, idempotência, commit único via transition, logging só-metadados)
    - backend/app/automation/rules.py + naming.py + fileops.py (peças puras dos Plans 02/03 a orquestrar)
    - backend/app/models/audit_log.py (colunas write-ahead do Plan 01)
    - backend/app/models/classification.py + document.py (FilledField, content_hash, origin_original_id)
    - backend/app/models/ingested_original.py + app/api/documents.py:440-459 (como reconstruir source_path: WatchedFolder.path / original_filename)
    - backend/tests/automation/test_stage.py (alvo RED — dry_run, reconcile, idempotência)
  </read_first>
  <files>backend/app/automation/stage.py</files>
  <behavior>
    - apply_stage(session, content_hash, run_id, dry_run=False): localiza doc; idempotência = AuditLog(document_id, status="done") existe → no-op
    - carrega FilledFields (dict por field_name); avalia regras (evaluate_rules) → escolhe name_pattern/folder_pattern
    - resolve destino (resolve_folder_pattern + resolve_pattern); campo faltante → None → transition(EM_REVISAO) sem tocar disco (D-07)
    - resolve_collision: idêntico → pula (D-10, conclui sem mover); diferente → sufixo (D-09)
    - dry_run=True: retorna par origem→destino + sinal de colisão/bloqueio SEM tocar disco e SEM escrever AuditLog (AUT-03)
    - dry_run=False: escreve AuditLog(status="intent", source_path, dest_path, run_id, content_hash) + commit ANTES de materializar; materialize_to_dest; atualiza audit.status="done" + transition(CONCLUIDO, completed_step="aplicado") num commit
    - reconcile_orphan_intents(session): para cada AuditLog(status="intent") órfão, checa o destino (existe com hash esperado? → done; senão → re-tentável) — espelha requeue_running
  </behavior>
  <action>
    Criar `apply_stage(session, *, content_hash, run_id=None, dry_run=False) -> ApplyStageResult` espelhando `classify_stage`: localiza `Document` por content_hash (None→ValueError); IDEMPOTÊNCIA = `AuditLog(document_id=doc.id, status="done")` existente → no-op (NÃO re-materializa). Lê os `FilledField` via o `ClassificationResult` do doc, monta `dict[field_name, FilledField]`. Carrega as `AutomationRule` ativas ordenadas por priority e chama `rules.evaluate_rules`; a regra vencedora dá `name_pattern`/`folder_pattern` (sem regra → política default: conclui sem operação de disco). Reconstrói o `source_path` do original (`WatchedFolder.path / IngestedOriginal.original_filename` via `origin_original_id`, padrão de documents.py:440-459). Resolve `base` de confinamento = `settings.automation_dest_root` (ou a raiz da pasta destino); chama `resolve_folder_pattern` + `resolve_pattern`; resultado None (campo faltante/confinamento) → `transition(EM_REVISAO, completed_step="classificado")` SEM tocar disco e SEM AuditLog de operação (D-07), retorna cedo. `resolve_collision(dst, content_hash)`: None → duplicata idêntica, conclui sem mover (D-10); senão usa o caminho livre (D-09). Se `dry_run` → retorna `ApplyStageResult` com origem/destino/flag de colisão/bloqueio, SEM AuditLog, SEM disco (AUT-03). Senão: `session.add(AuditLog(status="intent", action="materialize", source_path, dest_path, run_id, content_hash, document_id))` + `session.commit()` ANTES de `fileops.materialize_to_dest` (write-ahead, AUT-04). Após materializar: setar `audit.status="done"` e `transition(session, doc, DocState.CONCLUIDO, completed_step="aplicado")` (transition comita tudo junto — NUNCA commit manual antes). Erros de disco propagam ao worker. Logar SÓ metadados (doc.id, paths, run_id, status) — nunca valores de campo (V7/V9). Adicionar `reconcile_orphan_intents(session) -> int` que varre `AuditLog(status="intent")`, checa integridade do destino, e marca done ou deixa para retry (Pitfall 7). Exportar `APPLY_STEP = "apply"` (ou definir no worker — alinhar).
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/automation/test_stage.py -x -q` 100% verde (dry_run, reconcile, idempotência).
    - `grep -c "def apply_stage\|def reconcile_orphan_intents" backend/app/automation/stage.py` retorna 2.
    - `grep -c "status=\"intent\"\|status = \"intent\"" backend/app/automation/stage.py` retorna ≥1 (write-ahead presente).
    - `grep -c "session.commit()" ` antes do `transition` final NÃO existe no ramo de conclusão (transition comita) — confirmar que o caminho CONCLUIDO usa `transition`, não commit manual.
    - Teste dry_run prova: nenhum AuditLog escrito e nenhum arquivo criado.
  </acceptance_criteria>
  <done>apply_stage idempotente com write-ahead, materialização, transição CONCLUIDO via transition, bloqueio→EM_REVISAO (D-07), dry-run puro, reconciliação de intents; test_stage.py verde.</done>
</task>

<task type="auto">
  <name>Task 2: Worker — step apply + sweep auto-aplica + FALHA por step + reconcile no startup (D-01/D-02, Pitfall 6/7)</name>
  <read_first>
    - backend/app/queue/worker.py (LER NA ÍNTEGRA: _dispatch, enqueue_pending_classifications, _sweep_pending, _fail_for_step, run_worker startup)
    - backend/app/automation/stage.py (apply_stage/reconcile recém-criados)
    - backend/app/config.py (review_confidence_threshold — fronteira do auto-aplica)
    - backend/app/models/classification.py (confidence_score para o filtro de alta confiança)
  </read_first>
  <files>backend/app/queue/worker.py</files>
  <action>
    Adicionar `APPLY_STEP = "apply"`. Em `_dispatch`, adicionar `elif step == APPLY_STEP:` abrindo sessão própria e chamando `apply_stage`; se `apply_stage` for coroutine → `await` direto (sem to_thread); se for `def` síncrona (fileops é IO síncrono) → `asyncio.to_thread` com sessão própria por thread — alinhar com a assinatura definida na Task 1 (preferir `def` síncrona despachada em to_thread, já que não há chamada OpenAI). Adicionar `APPLY_STEP` ao `_fail_for_step` (rota por content_hash, como extract/classify). Criar `enqueue_pending_applications(session) -> int` espelhando `enqueue_pending_classifications`: seleciona `Document` em `state == PROCESSANDO`, `last_completed_step == "classificado"`, com `ClassificationResult.confidence_score >= settings.review_confidence_threshold` (alta confiança, D-01), e SEM `AuditLog(status="done")` para o doc; enfileira `(content_hash, APPLY_STEP)` com payload `{"content_hash": ...}`. Idempotente por UNIQUE(content_hash, step). Chamar `enqueue_pending_applications` dentro de `_sweep_pending` (depois de classify). No startup de `run_worker`, chamar `reconcile_orphan_intents` UMA vez (após `requeue_running`, espelhando o padrão de resume) para reconciliar intents órfãos de crash (Pitfall 7). Lock de arquivo Windows (PermissionError/OSError WinError 32) já é tratado: sobe ao `_run_once` → `schedule_retry` (Pitfall 6) — não capturar dentro do stage.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && python -c "from app.queue.worker import APPLY_STEP, enqueue_pending_applications; print(APPLY_STEP)" && pytest tests/automation/test_stage.py -x -q && pytest tests/test_worker.py -q 2>/dev/null | tail -3 || true</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app.queue.worker import APPLY_STEP, enqueue_pending_applications"` importa sem erro e imprime `apply`.
    - `grep -c "APPLY_STEP" backend/app/queue/worker.py` retorna ≥3 (dispatch + _fail_for_step + enqueue).
    - `grep -c "review_confidence_threshold" backend/app/queue/worker.py` retorna ≥1 (filtro de alta confiança no sweep, D-01).
    - `grep -c "reconcile_orphan_intents" backend/app/queue/worker.py` retorna ≥1 (reconcile no startup).
    - Suíte existente do worker (se houver) permanece verde.
  </acceptance_criteria>
  <done>step apply despachado; enqueue_pending_applications filtra alta confiança (D-01); FALHA roteada; reconcile no startup; lock Windows = retry (Pitfall 6/7).</done>
</task>

<task type="auto">
  <name>Task 3: API /automations + approve dispara apply + registro no main (TPL-02/AUT-03/AUT-05/D-03)</name>
  <read_first>
    - backend/app/api/templates.py (LER NA ÍNTEGRA: In/Patch/Out, 409/422/404/204, coleção filha no PATCH)
    - backend/app/api/documents.py (approve_document, retry_document, _requeue — a MODIFICAR approve; molde de ações POST)
    - backend/app/automation/stage.py (apply_stage dry_run) + automation/undo.py (undo_document/undo_run)
    - backend/app/main.py (include_router — padrão de registro)
    - backend/tests/test_api_automations.py (alvo RED — espelha test_api_templates)
  </read_first>
  <files>backend/app/api/automations.py, backend/app/api/documents.py, backend/app/main.py</files>
  <action>
    Criar `automations.py` com `router = APIRouter(prefix="/automations", tags=["automations"])` espelhando `templates.py`: schemas `RuleIn`/`RulePatch`/`RuleOut` + `ConditionIn`/`ConditionOut` (1:N aninhado, coleção `conditions` substituída inteira no PATCH — delete-orphan); CRUD `POST ""` (201, IntegrityError→409), `GET ""` (lista ordenada por priority), `GET /{id}` (404), `PATCH /{id}`, `DELETE /{id}` (204). `operator` validado para o conjunto `eq/gt/lt/contains` (422 fora dele, V5). Ações: `POST /dry-run` (body: lista de document_ids OU vazio = todos os prontos) → chama `apply_stage(..., dry_run=True)` por doc e retorna os pares origem→destino + sinalização de colisão/bloqueio (AUT-03); `POST /apply` (body: document_ids para lote D-03, gerando um `run_id` único; ou um único id) → reenfileira `(content_hash, APPLY_STEP)` via o helper `_requeue` (não materializa síncrono no request — o worker aplica); `POST /undo` (body: document_id OU run_id, D-03/AUT-05) → chama `undo_document`/`undo_run`; pós-undo, transicionar o estado se apropriado (ex.: CONCLUIDO não tem saída — definir: o undo registra status no audit e a UI reflete; NÃO inventar auto-laço). Em `documents.py`, MODIFICAR `approve_document`: após o guard D-07, em vez de `transition(CONCLUIDO)` direto, enfileirar o step `apply` (via `_requeue`) para que o apply conclua o doc com a automação; manter o guard e o 404/409. Garantir que `approve` ainda funcione para docs sem regra (apply conclui sem operação). Em `main.py`, `app.include_router(automations.router)`. NÃO logar valores extraídos (só ids/paths). Tornar GREEN `test_api_automations.py`.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/test_api_automations.py -x -q && pytest tests/test_api_documents.py -q 2>/dev/null | tail -3 || true</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_api_automations.py -x -q` 100% verde (CRUD 409/422/404/204 + dry-run + apply + undo).
    - `grep -c "prefix=\"/automations\"" backend/app/api/automations.py` retorna 1.
    - `grep -c "automations" backend/app/main.py` retorna ≥1 (router registrado).
    - `grep -c "APPLY_STEP\|step=\"apply\"\|_requeue" backend/app/api/documents.py` retorna ≥1 (approve dispara apply).
    - Suíte `test_api_documents.py` permanece verde (approve não quebrou).
  </acceptance_criteria>
  <done>Router /automations com CRUD de regras + dry-run + apply (por-doc/lote) + undo (por-doc/run); approve dispara apply; registrado no main; test_api_automations verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → API /automations | operador define regras/padrões e dispara apply/undo |
| worker → filesystem do cliente | apply_stage materializa no disco |
| valor de campo (IA) → caminho destino | resolvido em naming, confinado (V4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering / data loss | aplicar sem write-ahead | mitigate | AuditLog(status="intent") + commit ANTES de materialize (AUT-04); teste de stage cobre ordem |
| T-06-13 | Denial / data loss | crash entre intent e done | mitigate | reconcile_orphan_intents no startup do worker (Pitfall 7); idempotência status="done"→no-op |
| T-06-14 | Elevation | path traversal via padrão/campo | mitigate | confinamento em naming (V4) consumido por apply_stage; base = automation_dest_root |
| T-06-15 | Tampering | operador inválido em regra (injeção/eval) | mitigate | operator validado contra conjunto eq/gt/lt/contains → 422 (V5); avaliador sem eval (Plan 02) |
| T-06-16 | Info disclosure | vazar valores de campo em log/resposta | mitigate | logar só metadados (ids/paths/run_id/status); padrão classify_stage (V7/V9) |
| T-06-SC | Tampering | instalações de pacote | accept | nenhuma dependência nova (stdlib + libs já instaladas, 06-RESEARCH §Standard Stack) |
</threat_model>

<verification>
- `pytest tests/automation tests/test_api_automations.py -q` verde + suíte completa `pytest -q` verde.
- approve dispara apply (não transita direto a CONCLUIDO).
- write-ahead e reconcile cobertos por teste de stage.
</verification>

<success_criteria>
- apply_stage idempotente write-ahead + materialização + estado via transition; D-07 rebaixa para revisão.
- Worker auto-aplica alta confiança (D-01) + reconcilia intents; FALHA roteada; lock = retry.
- API /automations completa (CRUD + dry-run + apply lote + undo run); approve dispara apply.
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-04-SUMMARY.md` when done
</output>
