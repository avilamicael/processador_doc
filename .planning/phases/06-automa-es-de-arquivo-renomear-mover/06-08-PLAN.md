---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 08
type: execute
wave: 3
depends_on: ["06-07"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useAutomations.ts
  - frontend/src/pages/AutomationsPage.tsx
  - frontend/src/pages/DryRunPage.tsx
autonomous: false
requirements: [AUT-01, AUT-02, AUT-03, AUT-05, TPL-02]
must_haves:
  truths:
    - "O usuário vê o pipeline como uma lista ORDENADA de etapas numeradas (1,2,3...) com conector descendente, comunicando que a ordem importa (D-12)"
    - "O usuário cria/edita uma etapa escolhendo 1 ação (Mover/Renomear/Identificar tipo/Decidir tratativa) + 0..N filtros de entrada combinados por E/OU (D-13/D-14)"
    - "Ao editar um padrão com tokens {campo}, o usuário vê uma PRÉ-VISUALIZAÇÃO AO VIVO do valor resolvido com dados de exemplo (S3)"
    - "O dry-run mostra UM par origem→destino-final por documento com situação sinalizada por cor (sufixo/duplicata/bloqueio/roteado/sem-etapa/pronto) — AUT-03"
    - "O usuário aplica por-documento e por-lote e pode desfazer (AUT-05); nenhuma ação é apresentada como irreversível"
  artifacts:
    - path: "frontend/src/pages/AutomationsPage.tsx"
      provides: "Construtor de pipeline (S1) + editor de etapa (S2) + editor de token (S3)"
      contains: "Pipeline"
    - path: "frontend/src/pages/DryRunPage.tsx"
      provides: "Dry-run/preview origem→destino do pipeline (S4) + aplicar/desfazer (S5/S6)"
    - path: "frontend/src/hooks/useAutomations.ts"
      provides: "Hooks TanStack Query do CRUD de pipeline + dry-run/apply/undo"
  key_links:
    - from: "frontend/src/hooks/useAutomations.ts"
      to: "frontend/src/lib/api.ts"
      via: "getPipelines/createPipeline/.../postDryRun/postApply/postUndo"
      pattern: "Pipeline"
    - from: "frontend/src/pages/AutomationsPage.tsx"
      to: "frontend/src/hooks/useAutomations.ts"
      via: "useAutomations / mutations invalidando ['automations']"
      pattern: "useAutomations"
---

<objective>
Reescrever o frontend das automações para o modelo de PIPELINE honrando o 06-UI-SPEC APROVADO: `AutomationsPage.tsx` vira o CONSTRUTOR DE PIPELINE (S1 lista ordenada de etapas com conector descendente + S2 editor de etapa com ação + filtros E/OU + S3 editor de token com pré-visualização ao vivo) e `DryRunPage.tsx` mostra o preview origem→destino-final do pipeline (S4) com aplicar/desfazer (S5/S6). Tipos/api/hooks são atualizados do modelo de regra para o de pipeline. Code-and-config: ZERO dependência npm nova; reordenação por botões ↑/↓; ícones via Icon.tsx (todos já existem). Sem visualizador de documento.

Purpose: Fechar o REDESIGN na superfície que o usuário toca. A essência (D-12) é tornar a sequência ordenada/encadeada visualmente inescapável (S1) e dissolver a abstração do token {campo} (S3). S4 é a rede de confiança visível antes de tocar o disco (AUT-03).
Output: tipos/api/hooks de pipeline; AutomationsPage e DryRunPage reescritos; verificação visual humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-UI-SPEC.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-07-SUMMARY.md

<interfaces>
<!-- BACKEND (06-07) — contratos da API que o frontend consome: -->
GET    /automations                → lista de Pipeline (com steps aninhados, steps ordenados por position)
GET    /automations/{id}           → Pipeline
POST   /automations                → cria Pipeline (PipelineIn: name, active, steps[]; step: action_type, conjunction, params, active, filters[])
PATCH  /automations/{id}           → edita (steps informado SUBSTITUI a coleção)
DELETE /automations/{id}           → 204
POST   /automations/dry-run        → { rows: DryRunRow[] } (DryRunRow: document_id, original_filename, source_path, dest_path, blocked, collision, skipped_identical, routed, route_target, no_match)
POST   /automations/apply          → { run_id, enqueued } (ApplyIn: document_id | document_ids — D-03)
POST   /automations/undo           → { reverted } (UndoIn: document_id | run_id — AUT-05)

action_type ∈ {move, rename, identify_type, route}; filter_type ∈ {field, source_folder, extension, filename, size, template}; operator ∈ {eq, gt, lt, contains}; conjunction ∈ {and, or}; route target ∈ {em_revisao, nao_tratar, ignorar}.

<!-- FRONTEND ANÁLOGOS (copiar a forma exata) — 06-PATTERNS: -->
frontend/src/pages/TemplatesPage.tsx — molde S1/S2/S3 (form inline schema-first, loading/erro/vazio, confirmRemove overlay+card padding:22, reorder ↑/↓ com row-action + aria-label/title).
frontend/src/pages/DocumentsPage.tsx — molde S4 (table.docs, checkbox por linha + por lote allIds/allSel, estados de tela).
frontend/src/hooks/useTemplates.ts — query + mutations invalidando a key; frontend/src/hooks/useDocuments.ts — polling (refetchInterval 4000, placeholderData prev, refetchIntervalInBackground:false).
frontend/src/lib/api.ts — request<T> tipado (já trata 204 sem corpo); já tem getAutomationRules/createAutomationRule/.../postDryRun/postApply/postUndo (RENOMEAR p/ pipeline).
frontend/src/components/Icon.tsx — TODOS os ícones necessários já existem (auditado): bolt, folder, docMini, doc, grid, arrowRight, plus, arrowUp, arrowDown, dots, refresh, check, checkSmall, undo, alert. NÃO importar lib de ícones.
frontend/src/components/StatusPill.tsx — StatusPill state="concluido" (verde) p/ aplicado; ConfidenceBadge (Fase 5) reusado tal-qual.
frontend/src/index.css — design system TRAVADO: .card .btn-primary .btn-ghost .rule-card .auto-card .auto-flow .flow-pill .flow-pill.action .flow-tag .badge .badge-ok .badge-off .row-action .row-actions .select .search-input .chips .chip .chip-count .tabs .tab .cell-mono .file-name table.docs .stack .stat-grid .stat-card .stat-num .checkbox; tokens --st-* (--st-tratado verde, --st-leitura âmbar, --st-encontrado azul, --st-erro vermelho, --surface-3/--text-3 neutro), --accent, --font-mono. NUNCA hex hardcoded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos + api + hooks do pipeline (substituem o modelo de regra)</name>
  <read_first>
    - frontend/src/types.ts (tipos AutomationRule/RuleCreate/RulePatch/RuleOperator a substituir)
    - frontend/src/lib/api.ts (fns getAutomationRules/.../postDryRun/postApply/postUndo a renomear/reescrever)
    - frontend/src/hooks/useAutomations.ts (hooks a reescrever)
    - frontend/src/hooks/useTemplates.ts (molde query+mutations)
  </read_first>
  <action>
    (a) frontend/src/types.ts: substituir os tipos de regra por tipos de pipeline. Definir:
    - uniões: `StepActionType = 'move' | 'rename' | 'identify_type' | 'route'`; `StepFilterType = 'field' | 'source_folder' | 'extension' | 'filename' | 'size' | 'template'`; `RuleOperator` (manter eq/gt/lt/contains); `RuleConjunction` (manter and/or); `RouteTarget = 'em_revisao' | 'nao_tratar' | 'ignorar'`.
    - `StepFilter`/`StepFilterCreate` (filter_type, operator, value, field_name?); `PipelineStep`/`PipelineStepCreate` (id?, position, action_type, conjunction, params (Record<string,unknown> ou tipado por ação), active, filters[]); `AutomationPipeline`/`PipelineCreate`/`PipelinePatch` (id, name, active, steps[]).
    - Atualizar `DryRunResult`/`DryRunRow` para incluir routed, route_target, no_match (além de blocked/collision/skipped_identical). Manter ApplyResult/UndoResult.
    - REMOVER AutomationRule/RuleCondition/RuleCreate/RulePatch/RuleConditionCreate (modelo antigo) — ou manter SÓ se algum mock de fase futura ainda referencia (verificar grep; o comentário linha 4 menciona telas mock — não quebrá-las).

    (b) frontend/src/lib/api.ts: renomear/reescrever as fns de regra para pipeline: `getPipelines()`, `getPipeline(id)`, `createPipeline(body)`, `updatePipeline(id, body)`, `deletePipeline(id)` (DELETE 204 já tratado). Manter `postDryRun(documentIds)`, `postApply(...)`, `postUndo(...)` (mesmo contrato; só os tipos de retorno DryRunResult mudam). Atualizar os imports de tipos no topo.

    (c) frontend/src/hooks/useAutomations.ts: reescrever espelhando useTemplates.ts: `AUTOMATIONS_KEY = ['automations']`; `useAutomations()` (useQuery getPipelines); `useCreatePipeline/useUpdatePipeline/useDeletePipeline` (mutations invalidando AUTOMATIONS_KEY); manter/ajustar `useDryRun`/`useApply`/`useUndo` (mutations que invalidam ['documents'] + ['automations']). Polling do dry-run reusa o padrão useDocuments.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StepActionType\|StepFilterType\|AutomationPipeline\|PipelineCreate" frontend/src/types.ts` >= 3
    - `grep -c "getPipelines\|createPipeline\|updatePipeline\|deletePipeline" frontend/src/lib/api.ts` == 4
    - `grep -c "useAutomations\|useCreatePipeline" frontend/src/hooks/useAutomations.ts` >= 2
    - DryRunRow inclui routed/route_target/no_match
    - `npx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Tipos/api/hooks refletem o modelo de pipeline; build TS limpo; contratos batem com a API do 06-07.</done>
</task>

<task type="auto">
  <name>Task 2: AutomationsPage — construtor de pipeline (S1) + editor de etapa (S2) + editor de token (S3)</name>
  <read_first>
    - frontend/src/pages/AutomationsPage.tsx (mock de regra a reescrever; reusar SAMPLE_TOKENS/SAMPLE_VALUES e a pré-visualização ao vivo já existentes)
    - frontend/src/pages/TemplatesPage.tsx (molde form inline + confirmRemove + reorder ↑/↓)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-UI-SPEC.md §"Surfaces" S1/S2/S3 + §"Vocabulário" + §"Copywriting Contract" + §"Visuals — Encadeamento" + §"Notas de acessibilidade"
  </read_first>
  <action>
    Reescrever frontend/src/pages/AutomationsPage.tsx como o CONSTRUTOR DE PIPELINE, honrando o UI-SPEC TRAVADO:

    - S1 (lista ordenada): renderizar os steps do pipeline como cards numerados (1,2,3...) em ordem de position. Cada card (reusar .rule-card/.auto-card + .auto-flow): nº de ordem (.chip-count mono), pílula da AÇÃO destacada (.flow-pill.action, accent suave) + pílulas de FILTRO neutras (.flow-pill surface-3), resumo dos params, Switch ativar/pausar (etapa pausada = opacidade .6 + .badge-off "Pausada"), ações ↑/↓/editar/remover (.row-action icon-only com aria-label+title; ↑ da 1ª e ↓ da última disabled). CONECTOR DESCENDENTE entre cards (classe nova permitida `.step-connector`: linha 1px/2px em --border-strong OU seta arrowDown em --text-3 — só múltiplos de 4 / tokens herdados). CTA "Adicionar etapa" (lista vazia: empty state "Nenhuma etapa de automação ainda" + "Criar primeira etapa"). Loading/erro espelhando DocumentsPage. Step sem filtros → badge neutra "Aplica-se a todos os documentos" (P10, dito explicitamente).
    - S2 (editor de etapa inline em .card): bloco (a) Tipo de ação — 4 opções (Mover/Renomear/Identificar tipo/Decidir tratativa) com hints do Copywriting; bloco (b) Filtro de entrada — 0..N linhas combináveis E/OU (combinador "E (todas as condições)"/"OU (qualquer condição)"), cada linha = select filter_type + (campo se field) + select operador (= > < contém) + input valor; hint do filtro de tamanho ("Tamanho em KB..."); bloco (c) Parâmetros da ação dependentes do tipo (move→padrão de pasta via S3; rename→padrão de nome via S3; identify_type→select de template; route→select target em_revisao/nao_tratar/ignorar). CTAs contextuais "Salvar etapa"/"Salvar alterações" + "Descartar etapa/alterações" (sem Cancelar extra).
    - S3 (editor de padrão+token, sub-bloco de S2 p/ move/rename): input do padrão (mono) + chips de tokens clicáveis (botões com aria-label/title "Inserir {campo} no padrão") que inserem {campo} no cursor + PRÉ-VISUALIZAÇÃO AO VIVO abaixo (reusar SAMPLE_VALUES/lógica já presente no mock, incl. {data:aaaa-mm}) com a microcopy "Cada {campo} é trocado pelo valor extraído do documento" e o aviso de campo faltante. Pré-visualização é o focal point.
    - confirmRemove de etapa (overlay+card padding:22, molde TemplatesPage): copy "Remover etapa «{ação}»? As etapas seguintes continuam valendo..." — NUNCA linguagem destrutiva.

    Reordenação por ↑/↓ (sem drag-and-drop). Valores como texto puro (zero dangerouslySetInnerHTML). Accent só na CTA primária + pílula de ação. Cores SEMPRE via tokens --st-*/--accent (sem hex). Persistir as mudanças do pipeline via useUpdatePipeline/useCreatePipeline (reordenar = atualizar positions e salvar).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" frontend/src/pages/AutomationsPage.tsx` == 0
    - `grep -c "drag\|Drag\|dnd\|react-beautiful\|sortable" frontend/src/pages/AutomationsPage.tsx` == 0 (sem lib de DnD — code-and-config)
    - `grep -c "arrowUp\|arrowDown" frontend/src/pages/AutomationsPage.tsx` >= 1 (reorder ↑/↓)
    - `grep -cE "#[0-9a-fA-F]{6}" frontend/src/pages/AutomationsPage.tsx` == 0 (sem hex hardcoded; cores via var(--...))
    - `grep -c "aria-label" frontend/src/pages/AutomationsPage.tsx` >= 3 (icon-only ↑/↓/editar/remover + chips de token)
    - `grep -c "identify_type\|route\|move\|rename" frontend/src/pages/AutomationsPage.tsx` >= 4 (4 ações)
    - `npx tsc --noEmit` limpo; `npm run build` sucesso
  </acceptance_criteria>
  <done>Construtor de pipeline com sequência numerada/encadeada visualmente óbvia (S1), editor de etapa ação+filtros E/OU (S2) e editor de token com pré-visualização ao vivo (S3); zero dependência npm nova; design tokens travados.</done>
</task>

<task type="auto">
  <name>Task 3: DryRunPage — preview origem→destino do pipeline (S4) + aplicar/desfazer (S5/S6)</name>
  <read_first>
    - frontend/src/pages/DryRunPage.tsx (a reescrever p/ UM par origem→destino-final por doc)
    - frontend/src/pages/DocumentsPage.tsx (molde table.docs + checkbox lote allIds/allSel + estados)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-UI-SPEC.md §"Surfaces" S4/S5/S6 + §"Cores semânticas de situação no dry-run" + §"Copywriting Contract"
  </read_first>
  <action>
    Reescrever frontend/src/pages/DryRunPage.tsx para o preview do PIPELINE (materialização única — UM par origem→destino-final por documento, P8):

    - S4: table.docs com colunas origem→destino (mono, --font-mono) + coluna de SITUAÇÃO sinalizada por token de cor conforme o §"Cores semânticas": .badge âmbar (--st-leitura) "Renomeado p/ evitar colisão" (D-09, collision); .badge azul (--st-encontrado) "Já existe (idêntico) — pulado" (D-10, skipped_identical); .badge vermelha (--st-erro) "Campo faltando — enviado para revisão" (D-07, blocked); âmbar/quarentena "Enviado para revisão"/"Marcado para não tratar"/"Ignorado pelo pipeline" (routed/route_target, P9); .badge-off neutra "Nenhuma etapa se aplica — mantido na origem" (no_match, P10); .badge verde "Pronto" (caso normal). Cada badge com title do Copywriting. Contagem-resumo no topo (.stat-grid/.stat-num: N prontos, N pulados, N bloqueados/roteados). Seleção por linha + por lote (checkbox allIds/allSel; D-03); linhas bloqueadas (D-07) com checkbox disabled+opacidade. CTA "Aplicar automações"/"Aplicar selecionados" (accent, único) DESABILITADA até o preview carregar (AUT-03). Empty state "Nenhum documento pronto para automação...". Loading/erro.
    - S5 (ação por-doc/lote): após aplicar (useApply → run_id), exibir "Desfazer aplicação" por linha/lote (.row-action discreta, neutra — NÃO accent, NÃO destrutiva). Documentos rebaixados (D-07) aparecem com o motivo (e existem em "Precisam de atenção"/AttentionPage — não duplicar a tela).
    - S6 (confirmação de undo): diálogo molde confirmRemove (overlay+card padding:22) em LINGUAGEM DE REVERSIBILIDADE — "Desfeito. O arquivo voltou ao local de origem." / "...Restauramos a cópia íntegra preservada pelo sistema...". NUNCA linguagem de exclusão permanente / alerta vermelho. CTAs "Manter"/"Desfazer".

    Polling do dry-run reusa useDryRun + padrão useDocuments. Valores texto puro (zero dangerouslySetInnerHTML). Cores via tokens --st-* (sem hex). Sem visualizador de documento (apenas caminhos como texto).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" frontend/src/pages/DryRunPage.tsx` == 0
    - `grep -cE "#[0-9a-fA-F]{6}" frontend/src/pages/DryRunPage.tsx` == 0 (cores via var(--st-*))
    - `grep -c "routed\|no_match\|skipped_identical\|collision\|blocked" frontend/src/pages/DryRunPage.tsx` >= 4 (todas as situações P8/P9/P10/D-07/D-09/D-10)
    - `grep -c "disabled" frontend/src/pages/DryRunPage.tsx` >= 1 (Aplicar disabled até preview carregar + linhas bloqueadas)
    - `grep -c "undo\|Desfazer" frontend/src/pages/DryRunPage.tsx` >= 1
    - `npx tsc --noEmit` limpo; `npm run build` sucesso
  </acceptance_criteria>
  <done>Dry-run mostra UM par origem→destino-final por doc com todas as situações sinalizadas por cor travada; aplicar por-doc/lote com Aplicar disabled até carregar; desfazer em linguagem de reversibilidade.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (checkpoint): Verificação visual do construtor de pipeline + dry-run</name>
  <action>Pausar para verificação humana visual/funcional do frontend reescrito (S1..S6) conforme os passos em how-to-verify. Não há código automatizado adicional aqui — o executor já entregou Tasks 1-3; este checkpoint confirma o resultado visual e o comportamento reversível antes de fechar a fase.</action>
  <what-built>
    Frontend do pipeline de automações reescrito honrando o 06-UI-SPEC APROVADO: AutomationsPage (S1 construtor de pipeline ordenado/encadeado + S2 editor de etapa ação+filtros E/OU + S3 editor de token com pré-visualização ao vivo) e DryRunPage (S4 preview origem→destino com situação sinalizada + S5/S6 aplicar/desfazer). Tipos/api/hooks de pipeline; zero dependência npm nova; design tokens travados; sem visualizador de documento.
  </what-built>
  <how-to-verify>
    1. Subir backend (uvicorn, --workers 1) e frontend (npm run dev). Garantir migrações até head (0007).
    2. Abrir a aba "Automações": confirmar o construtor de PIPELINE (não mais regra única). Criar etapas: 1) Identificar tipo, 2) Renomear (padrão com {campo} — ver a PRÉ-VISUALIZAÇÃO ao vivo mudar com dados de exemplo + a microcopy do token), 3) Mover (padrão de pasta). Confirmar a numeração 1,2,3 + o CONECTOR DESCENDENTE entre cards (a ordem importa visualmente).
    3. Adicionar filtros a uma etapa (campo / extensão / tamanho), combinar por E/OU. Etapa sem filtro → badge "Aplica-se a todos os documentos". Reordenar com ↑/↓; pausar uma etapa (Switch → opacidade + "Pausada").
    4. Abrir o Dry-run: confirmar UM par origem→destino-final por documento, com a coluna de situação por cor (verde Pronto / âmbar colisão / azul duplicata / vermelho campo faltando→revisão / neutro sem-etapa / roteado). "Aplicar automações" deve ficar desabilitado até o preview carregar.
    5. Aplicar (por-doc e por-lote) e Desfazer: confirmar a linguagem de REVERSIBILIDADE no diálogo de undo (nunca "excluir permanentemente"). Conferir no Windows Explorer (ou no filesystem) que o arquivo foi materializado no destino e que Desfazer o devolve à origem.
    6. Conferir: tema claro E escuro (cores via tokens); sem visualizador de documento (só caminhos como texto); nenhuma lib npm nova instalada.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os ajustes visuais/de comportamento necessários.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valores de campo/caminhos vindos da API → DOM | render como texto pode virar XSS se via innerHTML |
| ação Aplicar/Desfazer → filesystem do cliente | toda ação que toca disco precede de dry-run e é reversível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Information disclosure / XSS | render de caminhos/valores no React | mitigate | texto puro via JSX (zero dangerouslySetInnerHTML — gate == 0); sem visualizador de documento (V7) |
| T-06-16 | Denial / data loss (percebida) | UI de Aplicar/Desfazer | mitigate | Aplicar disabled até dry-run carregar (AUT-03); Desfazer sempre apresentado; diálogo de undo em linguagem de reversibilidade, nunca "exclusão permanente" |
| T-06-SC | Tampering | npm installs | accept | code-and-config: NENHUMA dependência npm nova (sem DnD, sem lib de ícones) — 06-UI-SPEC §Registry Safety; gates grep `drag/dnd/sortable` == 0 |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` sem erros; `npm run build` sucesso
- Checkpoint humano: construtor de pipeline ordenado/encadeado, token com pré-visualização, dry-run sinalizado, aplicar/desfazer reversível, tema claro+escuro, sem visualizador, zero npm novo
</verification>

<success_criteria>
- Pipeline visualizado como lista ordenada numerada com conector descendente (D-12 óbvio)
- Etapa = 1 ação + 0..N filtros E/OU; token com pré-visualização ao vivo (S2/S3)
- Dry-run: UM par origem→destino-final por doc, situação sinalizada por cor travada (AUT-03)
- Aplicar por-doc/lote + Desfazer em linguagem de reversibilidade (AUT-05)
- Zero dependência npm nova; tokens de design travados; sem visualizador de documento
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-08-SUMMARY.md` when done
</output>
