---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/app/automation/__init__.py
  - backend/app/automation/naming.py
  - backend/app/automation/rules.py
autonomous: true
requirements: [AUT-01, AUT-02, TPL-02]
must_haves:
  truths:
    - "Um padrão {campo}/{campo:fmt} resolve para um caminho sanitizado e confinado, ou retorna sinal-de-bloqueio quando um campo usado está faltante/inválido (AUT-01/AUT-02/D-07)"
    - "Componentes de nome são sanitizados (9 chars Windows + nomes reservados + trailing dot/space + teto de comprimento) (D-08, Pitfalls 4/5)"
    - "O destino resolvido é confinado sob a raiz-base configurada (path traversal V4) — campo com .. ou caminho absoluto não escapa"
    - "Um avaliador puro casa regras na ordem de prioridade (primeira-que-casa-vence) com operadores =,>,<,contém combinados por E/OU, comparando moeda/número via Decimal e data via ISO (TPL-02/D-04/D-05, Pitfall 2)"
  artifacts:
    - path: "backend/app/automation/naming.py"
      provides: "resolve_pattern (tokens→caminho ou None se campo faltante) + sanitize_component + confinamento V4"
      contains: "def resolve_pattern"
    - path: "backend/app/automation/rules.py"
      provides: "Avaliador puro de AutomationRule sobre FilledField normalizado; primeira-que-casa-vence"
      contains: "def evaluate_rules"
  key_links:
    - from: "backend/app/automation/naming.py"
      to: "app.validation.dates / app.validation.money"
      via: "import para format de {data:...} e coerção"
      pattern: "from app.validation"
    - from: "backend/app/automation/rules.py"
      to: "decimal.Decimal"
      via: "coerção numérica das condições"
      pattern: "Decimal"
---

<objective>
Os dois motores PUROS da fase: `naming.py` (tokens `{campo}` → caminho sanitizado e confinado, com bloqueio→revisão quando falta campo) e `rules.py` (avaliador de regras condicionais primeira-que-casa-vence). Ambos sem IA, sem disco, sem banco — funções puras testáveis, tornando GREEN os testes Wave 0 de `test_naming.py` e `test_rules.py`.

Purpose: AUT-01/AUT-02 (padrões de nome/pasta) e TPL-02 (regras condicionais) são lógica determinística pura; isolá-las do efeito de disco (Plan 03) e da orquestração (Plan 04) mantém a qualidade e a testabilidade. O confinamento V4 vive aqui, na fronteira onde o valor não-confiável da IA vira caminho.
Output: `app/automation/naming.py`, `app/automation/rules.py`, `app/automation/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-CONTEXT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md

<interfaces>
<!-- Extraído do codebase — usar diretamente. -->

Funções puras reusáveis (backend/app/validation/):
  dates.normalize_date(raw: str) -> str | None       # devolve ISO "YYYY-MM-DD" (lexicograficamente ordenável)
  money.normalize_money_brl(raw: str) -> str | None   # devolve string Decimal-comparável
  fields._MAX_REGEX_LEN = 4096                         # teto p/ regex (reusar se houver operador regex)

Modelo de dados que o naming/rules consomem (backend/app/models/classification.py):
  FilledField(field_name: str, raw_value: str|None, normalized_value: str|None,
              valid: bool, invalid_reason: str|None)

Modelos de regra (criados no Plan 01, backend/app/models/automation_rule.py):
  AutomationRule(priority: int, conjunction: str("and"/"or"), name_pattern, folder_pattern, active, conditions)
  RuleCondition(field_name, operator: "eq"|"gt"|"lt"|"contains", value, position)

Confinamento (molde backend/app/api/watched_folders.py:43-81):
  usa Path.resolve() + checagem; padrão is_symlink() para não seguir symlink.

Código de referência (NÃO copiar cego — derivar): 06-RESEARCH §Code Examples
  "Resolução de token com format de data e sanitização" e Pattern 4 (avaliador).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: naming.py — resolução de tokens + sanitização + confinamento (AUT-01/AUT-02/D-06/D-07/D-08)</name>
  <read_first>
    - backend/app/validation/dates.py (normalize_date → ISO; disciplina parse-falho→None)
    - backend/app/validation/money.py (normalize_money_brl → Decimal-string)
    - backend/app/api/watched_folders.py (linhas ~43-81: _normalize_path + resolve() + is_symlink())
    - backend/tests/automation/test_naming.py (alvo RED a tornar GREEN — nomes de teste exatos)
    - 06-RESEARCH.md §Code Examples (sanitize_component, resolve_pattern, _fmt_date, _WIN_RESERVED)
  </read_first>
  <files>backend/app/automation/__init__.py, backend/app/automation/naming.py</files>
  <behavior>
    - resolve_pattern("{cliente}_{numero}.pdf", fields) com campos válidos → "ACME_123.pdf"
    - test_missing_field_blocks: campo usado no padrão faltante OU FilledField.valid=False → retorna None (caller → EM_REVISAO, D-07)
    - sanitize: valor com `\ / : * ? " < > |` → cada char vira "_"; trailing space/dot removidos; nome reservado (CON/NUL/COM1…) → prefixado "_"; componente acima de automation_max_component_len → truncado preservando extensão
    - {data:aaaa-mm} sobre normalized_value ISO "2026-04-03" → "2026-04"
    - folder: resolve_folder_pattern("Documentos/{cliente}/{data:aaaa-mm}/", fields, base) → Path confinado; cada segmento sanitizado individualmente
    - V4: campo cujo valor é "..\\..\\Windows" ou caminho absoluto NÃO escapa da raiz-base (resolved.is_relative_to(base) falso → bloqueia/None)
  </behavior>
  <action>
    Criar `app/automation/__init__.py` (vazio, package init, molde `classification/__init__.py`). Criar `naming.py` com: `sanitize_component(value, max_len)` (substitui os 9 chars de D-08 por "_", `rstrip(" .")`, checa stem case-insensitive contra o set `_WIN_RESERVED` (CON/PRN/AUX/NUL/COM1-9/LPT1-9) prefixando "_", trunca a `max_len` preservando a extensão — Pitfalls 4/5); `_fmt_date(iso, spec)` que fatiando `y,m,d = iso.split("-")` substitui `aaaa/mm/dd` no spec; `resolve_pattern(pattern, fields: dict[str, FilledField]) -> str | None` que faz `re.sub(r"\{([^}]+)\}", ...)` particionando `name:spec`, levanta um `_MissingField` interno (campo ausente OU `not ff.valid` OU `not ff.normalized_value`) capturado para retornar `None` (D-07); `resolve_folder_pattern(pattern, fields, base: Path) -> Path | None` que sanitiza cada segmento e confina via `resolved = (base / rel).resolve()` + `resolved.is_relative_to(base.resolve())` (V4) — falha de confinamento ou campo faltante → None. NÃO criar a pasta no disco aqui (mkdir é responsabilidade do fileops, Plan 03) — naming é PURO. Não logar valores de campo.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_naming.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/automation/test_naming.py -x -q` 100% verde (inclui test_missing_field_blocks, -k sanitize, -k folder).
    - `grep -c "def resolve_pattern\|def sanitize_component\|def resolve_folder_pattern" backend/app/automation/naming.py` retorna 3.
    - `grep -c "is_relative_to" backend/app/automation/naming.py` retorna ≥1 (confinamento V4 presente).
    - `naming.py` NÃO contém `mkdir` nem `os.replace` (módulo puro, sem efeito de disco).
  </acceptance_criteria>
  <done>resolve_pattern/sanitize_component/resolve_folder_pattern implementados; campo faltante→None (D-07); sanitização cobre 9 chars+reservados+truncamento (D-08); destino confinado (V4); test_naming.py verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rules.py — avaliador de regras condicionais primeira-que-casa-vence (TPL-02/D-04/D-05)</name>
  <read_first>
    - backend/app/validation/fields.py (estilo de dispatch por etiqueta; _MAX_REGEX_LEN; coerção Decimal)
    - backend/app/validation/money.py (normalize_money_brl → string Decimal-comparável)
    - backend/app/validation/dates.py (normalize_date → ISO ordenável)
    - backend/app/models/automation_rule.py (AutomationRule/RuleCondition — formas do Plan 01)
    - backend/tests/automation/test_rules.py (alvo RED — nomes: precedence, operadores, numérico)
    - 06-RESEARCH.md Pattern 4 + Pitfall 2 (coerção numérica obrigatória)
  </read_first>
  <files>backend/app/automation/rules.py</files>
  <behavior>
    - evaluate_rules(rules ordenadas por priority, fields) → a PRIMEIRA AutomationRule cujas condições casam (D-05); nenhuma casa → None
    - operador eq: igualdade sobre normalized_value (string, case-insensitive razoável)
    - operador gt/lt: campo moeda/numero → comparar via Decimal; data → comparar ISO; comparar como string é PROIBIDO (Pitfall 2: "500">"3000" lexicográfico daria errado)
    - operador contains: substring case-insensitive sobre raw_value/normalized_value
    - conjunction "and": todas as condições casam; "or": qualquer uma
    - precedence: duas regras casam → vence a de menor priority (ordem do usuário)
    - regra com active=False é ignorada
  </behavior>
  <action>
    Criar `rules.py` com `evaluate_rules(rules: list[AutomationRule], fields: dict[str, FilledField]) -> AutomationRule | None`: itera as regras JÁ ordenadas por `priority` (caller ordena ou ordenar aqui via `sorted(..., key=priority)`), pula `active=False`, e para cada regra avalia suas `conditions` combinando por `rule.conjunction` ("and"/"or"); retorna a primeira que casa (D-05) ou None. O casamento de uma condição é um dispatch explícito por `operator` (NUNCA `eval`): `eq`/`gt`/`lt`/`contains`. Para `gt`/`lt`, coerção obrigatória: se o `FilledField.normalized_value` parece numérico/moeda → comparar `Decimal(normalized) [op] Decimal(value)`; se data ISO → comparar strings ISO (já ordenáveis); fallback texto → comparação que evite a armadilha lexicográfica (documentar). Valor do campo ausente/`None` → condição falsa (não levanta). Se houver suporte a operador regex no futuro, reusar `_MAX_REGEX_LEN` de `validation.fields` — no v1 não há operador regex, então não compilar regex. Função 100% pura: sem DB, sem disco, sem IA. Não logar valores.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/automation/test_rules.py -x -q` 100% verde (inclui -k precedence e o caso numérico 500 vs 3000 via Decimal).
    - `grep -c "def evaluate_rules" backend/app/automation/rules.py` retorna 1.
    - `grep -c "eval(" backend/app/automation/rules.py` retorna 0 (NUNCA eval — V5).
    - `grep -c "Decimal" backend/app/automation/rules.py` retorna ≥1 (coerção numérica presente).
  </acceptance_criteria>
  <done>evaluate_rules com dispatch por operador, coerção Decimal para >/<, conjunção E/OU, primeira-que-casa-vence (D-05), sem eval; test_rules.py verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valor de campo (IA/documento) → caminho de destino | input não-confiável vira caminho de filesystem |
| condição de regra (operador do app) → avaliação | input estruturado do operador; nunca código |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation / Tampering | resolve_folder_pattern (path traversal via {campo}=..\\..) | mitigate | sanitize_component remove `\ / :` + confinamento `resolved.is_relative_to(base)` (V4); teste -k folder cobre escape |
| T-06-04 | Tampering | nomes reservados Windows (CON/NUL.pdf) | mitigate | _WIN_RESERVED check no sanitize_component → prefixa "_" (Pitfall 4) |
| T-06-05 | Denial | avaliação de condição via eval | mitigate | dispatch explícito por operador; grep gate `eval(` == 0 (V5) |
| T-06-06 | Denial | MAX_PATH (260) por padrões longos | mitigate | truncamento por automation_max_component_len preservando extensão (Pitfall 5) |
| T-06-SC | Tampering | instalações de pacote | accept | nenhuma dependência nova (stdlib + validation/* já instalados) |
</threat_model>

<verification>
- `pytest tests/automation/test_naming.py tests/automation/test_rules.py -q` verde.
- `grep "eval(" backend/app/automation/rules.py` vazio.
- Confinamento V4 e nomes reservados cobertos por teste.
</verification>

<success_criteria>
- naming.py resolve tokens, sanitiza (9 chars + reservados + truncamento), confina destino (V4), bloqueia (None) campo faltante (D-07).
- rules.py avalia regras primeira-que-casa-vence com coerção numérica e sem eval.
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-02-SUMMARY.md` when done
</output>
