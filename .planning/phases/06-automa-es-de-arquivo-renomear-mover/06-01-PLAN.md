---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/audit_log.py
  - backend/app/models/automation_rule.py
  - backend/app/models/__init__.py
  - backend/app/pipeline/states.py
  - backend/alembic/versions/0006_automations.py
  - backend/app/config.py
  - backend/tests/automation/__init__.py
  - backend/tests/automation/conftest.py
  - backend/tests/automation/test_naming.py
  - backend/tests/automation/test_rules.py
  - backend/tests/automation/test_fileops.py
  - backend/tests/automation/test_stage.py
  - backend/tests/automation/test_undo.py
  - backend/tests/test_api_automations.py
  - backend/tests/test_migrations.py
autonomous: true
requirements: [AUT-04, AUT-05, TPL-02]
must_haves:
  truths:
    - "O AuditLog persiste origem→destino, status (intent/done/undone), run_id e content_hash para suportar undo (AUT-04/AUT-05)"
    - "Existem tabelas automation_rules + rule_conditions para expressar regras condicionais {campo}[op]valor → automação (TPL-02)"
    - "A migração 0006 sobe e desce sem recriar o banco e sem tocar o trigger trg_documents_updated_at da tabela documents"
    - "A allowlist de transições tem a aresta CONCLUIDO→PROCESSANDO para permitir reabrir um documento aplicado durante o undo (AUT-05, pós-undo)"
    - "Toda a suíte de testes da pasta automation existe e está em RED (scaffold Wave 0), provendo o alvo Nyquist para as waves seguintes"
  artifacts:
    - path: "backend/app/models/automation_rule.py"
      provides: "Modelos AutomationRule (priority, action, name_pattern, folder_pattern, active) 1:N RuleCondition (field_name, operator, value, conjunction)"
      contains: "class AutomationRule"
    - path: "backend/alembic/versions/0006_automations.py"
      provides: "Migração 0006: estende audit_log + cria automation_rules/rule_conditions"
      contains: "down_revision = \"0005\""
    - path: "backend/tests/automation/conftest.py"
      provides: "Fixtures de doc classificado com FilledFields + temp dirs (mesmo e diferente volume)"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "AutomationRule, RuleCondition"
      via: "import + __all__"
      pattern: "AutomationRule"
    - from: "backend/alembic/versions/0006_automations.py"
      to: "0005"
      via: "down_revision"
      pattern: "down_revision"
---

<objective>
Fundação da Fase 6: estender o `AuditLog` (write-ahead + undo), criar os modelos de regra condicional (TPL-02), a migração Alembic 0006, os tunables de config e TODOS os scaffolds de teste Wave 0 (RED) que as waves seguintes vão tornar GREEN.

Purpose: Sem o schema do audit estendido nenhuma garantia de reversibilidade (AUT-04/AUT-05) é possível; sem as tabelas de regra não há TPL-02; sem os testes Wave 0 as tarefas de lógica seguintes não têm alvo Nyquist.
Output: Migração 0006, modelos novos/estendidos, config, e a árvore `tests/automation/` + `tests/test_api_automations.py` em RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-CONTEXT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-VALIDATION.md

<interfaces>
<!-- Extraído do codebase — usar diretamente, sem explorar. -->

Modelo atual a ESTENDER (backend/app/models/audit_log.py):
  AuditLog(id, document_id FK→documents ondelete SET NULL nullable, action: str,
           details: Text nullable, created_at, document relationship)

Molde de modelo pai 1:N (backend/app/models/template.py):
  Template(id, name unique, doc_type nullable, signals_json Text default "[]",
           created_at, updated_at, fields: relationship(cascade="all, delete-orphan"))
  TemplateField(id, template_id FK→templates ondelete CASCADE index,
                name, field_type, required, regex nullable, hint nullable,
                template relationship)

Molde de migração (alembic/versions/0004 create_table + 0005 batch add_column):
  - 0005 fez `with op.batch_alter_table("classification_results") as b: b.add_column(...)`
  - NENHUMA migração 0004/0005 tocou a tabela `documents` → trigger trg_documents_updated_at intacto

Config existente (backend/app/config.py): Settings via pydantic-settings;
  review_confidence_threshold já existe (governará o auto-aplica, D-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender AuditLog + criar modelos de regra condicional (TPL-02)</name>
  <read_first>
    - backend/app/models/audit_log.py (arquivo a modificar)
    - backend/app/models/template.py (molde EXATO de pai 1:N com cascade delete-orphan)
    - backend/app/models/classification.py (estilo de colunas Mapped/mapped_column/server_default/nullable)
    - backend/app/models/__init__.py (registro em __all__)
    - backend/app/pipeline/states.py (TRANSITIONS allowlist — CONCLUIDO é terminal hoje; aresta a adicionar)
  </read_first>
  <files>backend/app/models/audit_log.py, backend/app/models/automation_rule.py, backend/app/models/__init__.py, backend/app/pipeline/states.py</files>
  <action>
    Em `audit_log.py`, adicionar colunas (sem tocar nenhum outro modelo): `status` (String, nullable=False, server_default="done"), `source_path` (Text nullable), `dest_path` (Text nullable), `run_id` (String nullable, identificador de lote/execução para undo por-run, AUT-05), `content_hash` (String(64) nullable, base do undo via CAS — AUT-05). Manter `action`/`details`/`document` intactos.
    Criar `automation_rule.py` com DOIS modelos espelhando `template.py`: `AutomationRule` (id; `name` String; `priority` Integer index — ordenável, primeira-que-casa-vence D-05; `conjunction` String default "and" — combinador E/OU entre condições da regra; `name_pattern` Text nullable — padrão de renomeação AUT-01; `folder_pattern` Text nullable — padrão de pasta destino AUT-02; `active` Boolean default True server_default="1"; `created_at`/`updated_at`) com relationship `conditions` cascade="all, delete-orphan". `RuleCondition` (id; `rule_id` FK→automation_rules ondelete CASCADE index nullable=False; `field_name` String; `operator` String — um de `eq`/`gt`/`lt`/`contains` mapeando `= > < contém` D-04; `value` String; `position` Integer — ordem da condição) com relationship reversa `rule`.
    Registrar `AutomationRule` e `RuleCondition` em `models/__init__.py` (`__all__`) seguindo o estilo dos imports existentes — necessário para o Alembic autogenerate/metadata enxergar as tabelas.
    Em `states.py`, ADICIONAR à allowlist `TRANSITIONS` a aresta `DocState.CONCLUIDO: {DocState.PROCESSANDO}` (hoje CONCLUIDO é terminal, conjunto vazio). Essa aresta permite que o undo (AUT-05) REABRA um documento já aplicado: o endpoint /undo (Plan 04) reverte o arquivo e transita CONCLUIDO→PROCESSANDO para o doc voltar a ser acionável. NÃO inventar auto-laços; só adicionar esta aresta. É a única mudança na máquina de estados desta fase.
    NÃO criar a migração nesta task (Task 2). NÃO escrever lógica de avaliação aqui (Plan 02).
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && python -c "from app.models.automation_rule import AutomationRule, RuleCondition; from app.models.audit_log import AuditLog; assert hasattr(AuditLog,'status') and hasattr(AuditLog,'source_path') and hasattr(AuditLog,'dest_path') and hasattr(AuditLog,'run_id') and hasattr(AuditLog,'content_hash'); assert hasattr(AutomationRule,'priority') and hasattr(AutomationRule,'name_pattern') and hasattr(AutomationRule,'folder_pattern') and hasattr(RuleCondition,'operator'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c` acima imprime `ok` (todas as colunas/atributos presentes).
    - `grep -n "AutomationRule" backend/app/models/__init__.py` retorna ≥1 linha (registro em __all__).
    - `AutomationRule.conditions` usa `cascade="all, delete-orphan"` (igual a `Template.fields`).
    - `RuleCondition.rule_id` usa `ForeignKey(..., ondelete="CASCADE")`.
    - `python -c "from app.pipeline.states import is_valid_transition; from app.models.enums import DocState; assert is_valid_transition(DocState.CONCLUIDO, DocState.PROCESSANDO); print('edge-ok')"` imprime `edge-ok` (aresta de undo presente).
  </acceptance_criteria>
  <done>AuditLog estendido com 5 colunas; AutomationRule + RuleCondition criados e registrados em __all__; aresta CONCLUIDO→PROCESSANDO adicionada à allowlist (undo); nenhum outro modelo tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Migração Alembic 0006 + tunables de config</name>
  <read_first>
    - backend/alembic/versions/0005_confidence_review.py (molde batch add_column; comprovar que NÃO toca documents)
    - backend/alembic/versions/0004_templates_classification.py (molde create_table com FK ondelete + create_index)
    - backend/app/config.py (onde acrescentar tunables, estilo pydantic-settings)
    - backend/tests/test_migrations.py (teste a estender)
  </read_first>
  <files>backend/alembic/versions/0006_automations.py, backend/app/config.py</files>
  <action>
    Criar `0006_automations.py` com `revision = "0006"`, `down_revision = "0005"`. No `upgrade()`: `with op.batch_alter_table("audit_log") as b:` add_column das 5 colunas da Task 1 (`status` server_default="done", `source_path` Text, `dest_path` Text, `run_id` String, `content_hash` String(64)); depois `op.create_table("automation_rules", ...)` e `op.create_table("rule_conditions", ...)` com as colunas/FK/índices dos modelos da Task 1 (FK rule_conditions.rule_id ondelete CASCADE; índice em automation_rules.priority e rule_conditions.rule_id). NUNCA fazer `batch_alter_table("documents")` — preservar o trigger `trg_documents_updated_at` da 0002 (CAVEAT travado em 06-PATTERNS). No `downgrade()`: dropar `rule_conditions` antes de `automation_rules` (filha antes da pai), depois remover as 5 colunas de `audit_log`.
    Em `config.py`, adicionar tunables com defaults: `automation_dest_root` (str|None, raiz-base de confinamento dos destinos — V4 path traversal; None = sem confinamento adicional além da sanitização, mas o campo existe para o cliente travar) e `automation_max_component_len` (int default 200 — teto de comprimento por componente de nome para mitigar MAX_PATH, Pitfall 5). Seguir o estilo de `review_confidence_threshold`.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && alembic upgrade head && alembic downgrade -1 && alembic upgrade head && python -c "from app.config import get_settings; s=get_settings(); assert hasattr(s,'automation_max_component_len'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` → `downgrade -1` → `upgrade head` completa sem erro (migração reversível).
    - `grep -c "batch_alter_table(\"documents\")" backend/alembic/versions/0006_automations.py` retorna 0 (NÃO toca documents).
    - `python -c` confirma `automation_max_component_len` presente em Settings.
    - Após `upgrade head`, as tabelas `automation_rules` e `rule_conditions` existem e `audit_log` tem coluna `status`.
  </acceptance_criteria>
  <done>Migração 0006 sobe/desce limpa, cria as tabelas de regra, estende audit_log, não toca documents; config tem os 2 tunables novos.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold de testes Wave 0 (RED) — fixtures + arquivos de teste</name>
  <read_first>
    - backend/tests/conftest.py (fixtures de sessão/engine SQLite em memória — molde)
    - backend/tests/test_api_templates.py (molde de teste de API fina)
    - backend/tests/test_migrations.py (teste de migração a estender p/ 0006)
    - backend/app/models/classification.py (forma de ClassificationResult + FilledField p/ as fixtures)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-VALIDATION.md (mapa requisito→teste e nomes de teste exatos)
  </read_first>
  <files>backend/tests/automation/__init__.py, backend/tests/automation/conftest.py, backend/tests/automation/test_naming.py, backend/tests/automation/test_rules.py, backend/tests/automation/test_fileops.py, backend/tests/automation/test_stage.py, backend/tests/automation/test_undo.py, backend/tests/test_api_automations.py, backend/tests/test_migrations.py</files>
  <action>
    Criar a árvore `tests/automation/` com `__init__.py` e `conftest.py`. O `conftest.py` provê fixtures reusáveis pelas waves seguintes: um `Document` classificado (state PROCESSANDO, last_completed_step="classificado") com `ClassificationResult` + `FilledField`s (alguns válidos, um inválido, um obrigatório faltante) ligado a um template; e fixtures de temp dirs `src_dir`/`dst_dir` (mesmo volume) para os testes de fileops/undo. Reusar as fixtures de engine/sessão de `tests/conftest.py`.
    Criar os arquivos de teste em RED, com os nomes de teste EXATOS do mapa de 06-VALIDATION (cada um falhando por `import`/atributo ausente do módulo-alvo que será criado nas waves seguintes): `test_naming.py` (test_missing_field_blocks, sanitize, folder — AUT-01/02/D-07/D-08); `test_rules.py` (precedence, operadores =/>/</contém + E/OU, numérico via Decimal — TPL-02/D-04/D-05); `test_fileops.py` (no_overwrite, collision_suffix, collision_duplicate, cross_device, integrity, original_removed_after_apply — AUT-04/06/D-09/D-10); `test_stage.py` (dry_run, reconcile, idempotência, e `test_intent_before_materialize` — que mocka/intercepta `fileops.materialize_to_dest` e assevera que `AuditLog(status="intent")` JÁ está persistido no banco ANTES da chamada de disco, provando a ordem causal do write-ahead — AUT-03/AUT-04); `test_undo.py` (undo por-doc/por-run + cas_fallback, e o pós-undo reabre o doc CONCLUIDO→PROCESSANDO via a aresta nova — AUT-05). Marcar cada teste com asserts concretos mas que importam de módulos `app.automation.*` ainda inexistentes (RED esperado).
    Criar `tests/test_api_automations.py` espelhando `test_api_templates.py` (TestClient; rotas `/automations` ainda inexistentes → RED).
    Estender `tests/test_migrations.py` com um teste que aplica 0006 e verifica que o trigger `trg_documents_updated_at` permanece (consultar sqlite_master por 'trg_documents_updated_at' após upgrade head).
    Para preservar a coleta do pytest com módulos-alvo ainda ausentes, usar `pytest.importorskip("app.automation.naming")` (e análogos) no topo de cada arquivo OU `xfail(strict=False)` — o importante é que `pytest tests/automation -q` colete sem erro de coleção e os testes fiquem pendentes/red, não que quebrem a coleção.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation tests/test_api_automations.py -q --co && pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/automation tests/test_api_automations.py --co` (collect-only) NÃO falha na coleção (importorskip/xfail evitam ImportError fatal).
    - `ls backend/tests/automation/` lista: `__init__.py conftest.py test_naming.py test_rules.py test_fileops.py test_stage.py test_undo.py`.
    - `grep -c "test_missing_field_blocks\|cas_fallback\|collision_duplicate\|precedence" backend/tests/automation/*.py` retorna ≥4 (nomes exatos do mapa presentes).
    - `grep -c "test_intent_before_materialize\|original_removed_after_apply" backend/tests/automation/*.py` retorna ≥2 (write-ahead order AUT-04 + remoção da origem AUT-06 crit 5 no scaffold).
    - `pytest tests/test_migrations.py -q` passa (cobre 0006 + trigger intacto) — sem `2>/dev/null` nem `;` mascarando erro de coleção.
  </acceptance_criteria>
  <done>Árvore tests/automation/ criada com fixtures e testes RED nomeados conforme 06-VALIDATION; test_api_automations e test_migrations(0006) presentes; coleção do pytest verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migração ↔ banco do cliente | DDL irreversível mal-feito poderia corromper o schema/trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial / data loss | migração 0006 vs trigger trg_documents_updated_at | mitigate | NÃO fazer batch_alter_table("documents"); teste em test_migrations.py confirma o trigger após upgrade (grep gate = 0 ocorrências) |
| T-06-02 | Tampering | downgrade ordem das FKs | mitigate | downgrade dropa rule_conditions (filha) antes de automation_rules (pai); verify roda upgrade/downgrade/upgrade |
| T-06-SC | Tampering | instalações npm/pip/cargo | accept | Nenhuma dependência nova nesta fase (stdlib + libs já instaladas, confirmado 06-RESEARCH §Standard Stack); sem gate de pacote |
</threat_model>

<verification>
- `alembic upgrade head` / `downgrade -1` / `upgrade head` limpo.
- Trigger `trg_documents_updated_at` presente após 0006.
- `pytest tests/automation --co` coleta sem erro; testes em RED aguardando as waves de lógica.
</verification>

<success_criteria>
- AuditLog estendido (5 colunas) + AutomationRule/RuleCondition criados e registrados.
- Migração 0006 reversível, não toca documents.
- Config tem `automation_dest_root` + `automation_max_component_len`.
- Scaffold Wave 0 completo em RED com nomes de teste do mapa de validação.
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-01-SUMMARY.md` when done
</output>
