---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/automation_pipeline.py
  - backend/app/models/automation_rule.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/0007_automation_pipeline.py
  - backend/tests/test_migrations.py
  - backend/tests/automation/conftest.py
  - backend/tests/automation/test_pipeline_model.py
autonomous: true
requirements: [TPL-02, AUT-01, AUT-02]
must_haves:
  truths:
    - "Um pipeline de automação é uma lista ORDENADA de etapas (steps), cada etapa com 0..N filtros de entrada e UMA ação atômica"
    - "Apagar um pipeline apaga suas etapas e os filtros das etapas (cascade delete-orphan), igual a Template→TemplateField"
    - "A migração 0007 dropa automation_rules/rule_conditions e cria automation_pipelines/pipeline_steps/step_filters sem tocar documents nem audit_log"
    - "O trigger trg_documents_updated_at permanece intacto após aplicar a 0007"
  artifacts:
    - path: "backend/app/models/automation_pipeline.py"
      provides: "AutomationPipeline 1:N PipelineStep 1:N StepFilter (modelos ORM)"
      contains: "class AutomationPipeline"
    - path: "backend/alembic/versions/0007_automation_pipeline.py"
      provides: "Migração forward-only: drop regras, create pipeline/steps/filters"
      contains: "down_revision"
    - path: "backend/tests/automation/test_pipeline_model.py"
      provides: "Testes do modelo de pipeline (cascade, posição, FKs)"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "AutomationPipeline / PipelineStep / StepFilter"
      via: "import + __all__"
      pattern: "AutomationPipeline"
    - from: "backend/alembic/versions/0007_automation_pipeline.py"
      to: "0006"
      via: "down_revision = '0006'"
      pattern: "down_revision"
---

<objective>
Substituir o modelo de dados de "regra única" (automation_rules/rule_conditions, criadas na 0006) pelo modelo de PIPELINE ordenado de etapas componíveis (D-12..D-16): `AutomationPipeline 1:N PipelineStep 1:N StepFilter`. Inclui a migração Alembic 0007 forward-only que dropa as tabelas de regra e cria as de pipeline, e a atualização dos scaffolds de teste (RED) para o novo modelo.

Purpose: Esta é a fundação (Wave 1) sobre a qual o executor do pipeline (06-07) e o frontend (06-08) constroem. Sem o novo schema, nada do REDESIGN existe. As tabelas antigas não têm dados em prod (CONTEXT autoriza redesenhar — A1).
Output: 3 modelos ORM novos, migração 0007, registro em models/__init__, fixtures de pipeline e testes RED do modelo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md

<interfaces>
<!-- Modelo análogo EXATO a copiar: Template 1:N TemplateField (cascade delete-orphan). -->
<!-- Modelo a SUBSTITUIR (será dropado pela 0007): -->

backend/app/models/automation_rule.py — AutomationRule(id, name, priority idx, conjunction, name_pattern, folder_pattern, active, created_at, updated_at) 1:N RuleCondition(id, rule_id FK CASCADE idx, field_name, operator, value, position). Cascade "all, delete-orphan". Registrado em models/__init__.py linhas 9/23/33.

backend/alembic/versions/0006_automations.py — cabeçalho: revision='0006', down_revision='0005'. CAVEAT travado: estendeu audit_log via batch add_column (status/source_path/dest_path/run_id/content_hash) e criou automation_rules + rule_conditions. NÃO tocou documents (trigger trg_documents_updated_at da 0002 intacto). A 0007 herda esse caveat: NÃO tocar documents NEM audit_log.

backend/app/models/audit_log.py — AuditLog já tem as 5 colunas write-ahead (status server_default "done", source_path, dest_path, run_id, content_hash). MANTER intacto — o write-ahead/undo independem do modelo regra↔pipeline.

backend/alembic/versions/0004_templates_classification.py — molde de create_table com FK ondelete + batch create_index (op.create_table + with op.batch_alter_table(...) as batch_op: batch_op.create_index(...)).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelos AutomationPipeline / PipelineStep / StepFilter + registro</name>
  <read_first>
    - backend/app/models/automation_rule.py (modelo a substituir — copiar a forma do cascade/FK)
    - backend/app/models/template.py (análogo cascade delete-orphan Template 1:N TemplateField, citado em 06-PATTERNS §automation_rule.py)
    - backend/app/models/__init__.py (onde registrar)
  </read_first>
  <action>
    Criar backend/app/models/automation_pipeline.py com TRÊS modelos ORM (SQLAlchemy 2.0, Mapped/mapped_column, espelhando template.py), conforme 06-RESEARCH §"Modelo de dados do pipeline":

    - `AutomationPipeline` (__tablename__ "automation_pipelines"): id PK; name (String, nullable=False); active (Boolean, default True, server_default "1", nullable=False); created_at/updated_at (DateTime tz, server_default func.now(), updated_at com onupdate). relationship `steps: Mapped[list["PipelineStep"]]` com cascade="all, delete-orphan".

    - `PipelineStep` (__tablename__ "pipeline_steps"): id PK; pipeline_id (FK "automation_pipelines.id" ondelete="CASCADE", index=True, nullable=False); position (Integer, index=True, nullable=False, default 0, server_default "0") — ordem D-12; action_type (String, nullable=False) — "move"|"rename"|"identify_type"|"route" (D-13); conjunction (String, default "and", server_default "and", nullable=False) — combinação dos filtros (D-14); params_json (Text, nullable=True) — JSON dos params da ação (folder_pattern | name_pattern | template_id | target); active (Boolean, default True, server_default "1", nullable=False) — etapa ligada/pausada (Switch da UI). relationship back_populates "pipeline" + filters: Mapped[list["StepFilter"]] cascade="all, delete-orphan".

    - `StepFilter` (__tablename__ "step_filters"): id PK; step_id (FK "pipeline_steps.id" ondelete="CASCADE", index=True, nullable=False); filter_type (String, nullable=False) — "field"|"source_folder"|"extension"|"filename"|"size"|"template" (D-14); operator (String, nullable=False) — eq|gt|lt|contains; value (String, nullable=False); field_name (String, nullable=True) — só para filter_type="field"; position (Integer, default 0, server_default "0", nullable=False). relationship back_populates "step".

    Docstring de módulo explicando D-12 (ordem)/D-13 (1 filtro + 1 ação)/D-14 (filtros componíveis) e que a LÓGICA (executor/filtros) é do 06-07 — aqui só o schema. Schema evolui SOMENTE via Alembic.

    Registrar os 3 nomes em backend/app/models/__init__.py (import + __all__) e REMOVER os imports/__all__ de AutomationRule/RuleCondition (o modelo antigo). DELETAR backend/app/models/automation_rule.py (substituído — não fica órfão importável). Verificar que nenhum outro módulo ainda importa automation_rule além de stage.py/api/automations.py (que serão reescritos no 06-07, mesma wave-chain — aceitável quebrarem até o 06-07; mas para manter a suíte coletável, manter automation_rule.py SE houver import vivo; caso contrário deletar). Decisão: DELETAR automation_rule.py e deixar stage.py/api/automations.py com import quebrado documentado — eles são reescritos no 06-07 (wave 2, depende deste plano).
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && python -c "from app.models.automation_pipeline import AutomationPipeline, PipelineStep, StepFilter; from app.models import AutomationPipeline as P; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class AutomationPipeline\|class PipelineStep\|class StepFilter" backend/app/models/automation_pipeline.py` == 3
    - `grep -c 'cascade="all, delete-orphan"' backend/app/models/automation_pipeline.py` >= 2 (pipeline→steps e step→filters)
    - `grep -c 'ondelete="CASCADE"' backend/app/models/automation_pipeline.py` >= 2
    - `grep "AutomationPipeline" backend/app/models/__init__.py` retorna linha em import E em __all__
    - automation_rule.py removido OU sem referências vivas fora dos arquivos reescritos no 06-07
    - `ruff check backend/app/models/automation_pipeline.py` limpo
  </acceptance_criteria>
  <done>Os 3 modelos existem, importáveis via app.models, com cascade/FK espelhando Template→TemplateField; modelo de regra antigo removido do registro.</done>
</task>

<task type="auto">
  <name>Task 2: Migração Alembic 0007 (drop regras, create pipeline) + testes RED do modelo</name>
  <read_first>
    - backend/alembic/versions/0006_automations.py (cabeçalho + caveat documents intacto a herdar)
    - backend/alembic/versions/0004_templates_classification.py (molde create_table + batch create_index)
    - backend/tests/test_migrations.py (atualizar p/ cobrir 0007)
    - backend/tests/automation/conftest.py (estender fixtures de pipeline — ler a forma de classified_doc)
  </read_first>
  <action>
    (a) Criar backend/alembic/versions/0007_automation_pipeline.py conforme 06-RESEARCH §"Migração Alembic 0007":
    - revision="0007", down_revision="0006".
    - `upgrade()`: `op.drop_table("rule_conditions")` então `op.drop_table("automation_rules")` (filha antes da pai); depois `op.create_table("automation_pipelines", ...)` (id, name NOT NULL, active NOT NULL server_default "1", created_at/updated_at server_default now); `op.create_table("pipeline_steps", ...)` (id, pipeline_id FK ondelete CASCADE, position, action_type NOT NULL, conjunction NOT NULL server_default "and", params_json Text, active NOT NULL server_default "1"); `op.create_table("step_filters", ...)` (id, step_id FK ondelete CASCADE, filter_type NOT NULL, operator NOT NULL, value NOT NULL, field_name nullable, position). Criar índices: ix em pipeline_steps(pipeline_id, position) e ix em pipeline_steps.pipeline_id e step_filters.step_id (batch create_index igual à 0004).
    - `downgrade()`: dropar step_filters, pipeline_steps, automation_pipelines (ordem inversa), depois recriar automation_rules e rule_conditions na forma exata da 0006 (reversibilidade do histórico).
    - CAVEAT no docstring (copiar tom da 0006): esta migração NÃO faz batch_alter_table('documents') NEM batch_alter_table('audit_log') — `documents` aparece SÓ no comentário; o trigger trg_documents_updated_at (0002) e as colunas write-ahead de audit_log (0006) permanecem intactos.

    (b) Atualizar backend/tests/test_migrations.py: adicionar/ajustar um teste que aplica as migrações até head (0007) e assert: (1) tabelas automation_pipelines/pipeline_steps/step_filters EXISTEM; (2) automation_rules/rule_conditions NÃO existem; (3) o trigger trg_documents_updated_at ainda existe (mesmo assert das migrações anteriores — copiar a query de sqlite_master WHERE type='trigger' já usada no arquivo); (4) audit_log mantém a coluna `status`. Seguir o padrão de execução de migração já presente no arquivo.

    (c) Estender backend/tests/automation/conftest.py com uma fixture `pipeline_factory` (ou similar) que cria um AutomationPipeline com N PipelineStep (cada um com action_type/params_json e 0..N StepFilter) numa sessão — base para os testes do 06-07. E uma fixture `classified_doc_attrs` que estende o `classified_doc` existente com `file_attrs` (ext=".pdf", size, source_folder_id, original_filename) — NÃO redefinir `classified_doc`, só adicionar.

    (d) Criar backend/tests/automation/test_pipeline_model.py (RED — pode usar importorskip("app.models.automation_pipeline") por simetria, mas o modelo já existe da Task 1, então import direto): testes que (1) criam um pipeline com 2 steps ordenados e assert position preservado; (2) criam um step com 2 filtros e assert cascade — apagar o pipeline apaga steps e filters (consultar count após delete); (3) assert FK ondelete CASCADE via apagar pipeline. Usar a fixture schema_engine do conftest raiz.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/test_migrations.py tests/automation/test_pipeline_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "down_revision = \"0006\"\|down_revision = '0006'" backend/alembic/versions/0007_automation_pipeline.py` == 1
    - `grep -c "drop_table(\"rule_conditions\"\|drop_table('rule_conditions'" backend/alembic/versions/0007_automation_pipeline.py` >= 1 e drop_table de automation_rules presente
    - `grep -c "create_table(\"automation_pipelines\"\|create_table('automation_pipelines'" backend/alembic/versions/0007_automation_pipeline.py` >= 1 (pipeline_steps e step_filters idem)
    - `grep -c "batch_alter_table('documents'\|batch_alter_table(\"documents\"" backend/alembic/versions/0007_automation_pipeline.py` == 0 (documents NÃO tocado)
    - test_migrations.py contém assert de trigger trg_documents_updated_at presente após 0007
    - test_pipeline_model.py: assert de cascade delete (apagar pipeline → steps/filters somem)
    - Suite alvo verde
  </acceptance_criteria>
  <done>0007 aplica forward-only (drop regras, create pipeline) sem tocar documents/audit_log; trigger intacto provado em teste; testes de cascade do modelo verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema migration → DB existente | A 0007 dropa tabelas; deve ser forward-only e não destruir documents/audit_log/trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | trigger trg_documents_updated_at | mitigate | 0007 NÃO faz batch_alter_table('documents'); gate grep == 0 + teste de trigger presente após migração |
| T-06-15 | Denial / data loss | audit_log write-ahead | mitigate | 0007 NÃO toca audit_log; as 5 colunas write-ahead permanecem (independem do modelo regra↔pipeline) — base de AUT-04/05 preservada |
| T-06-SC | Tampering | npm/pip installs | accept | NENHUMA dependência nova (stdlib + SQLAlchemy/Alembic já instalados) — 06-RESEARCH §Standard Stack "zero dependência nova"; sem superfície de supply-chain neste plano |
</threat_model>

<verification>
- `cd backend && . .venv/bin/activate && pytest tests/test_migrations.py tests/automation/test_pipeline_model.py -x -q` verde
- `python -c "from app.models.automation_pipeline import AutomationPipeline, PipelineStep, StepFilter"` sem erro
- Migração até head aplica sem tocar documents (trigger intacto)
</verification>

<success_criteria>
- AutomationPipeline 1:N PipelineStep 1:N StepFilter existem e importáveis via app.models
- Migração 0007 forward-only: drop regras + create pipeline; documents e audit_log intactos
- Trigger trg_documents_updated_at presente após 0007 (provado em teste)
- Fixtures de pipeline + file_attrs disponíveis para o 06-07
- Modelo de regra antigo removido do registro
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-06-SUMMARY.md` when done
</output>
