---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/app/automation/fileops.py
  - backend/app/automation/undo.py
autonomous: true
requirements: [AUT-04, AUT-05, AUT-06]
must_haves:
  truths:
    - "A operação materializa o destino a partir do CAS (cas.read_bytes → escreve → verifica hash), nunca movendo o original (D-11)"
    - "A operação NUNCA sobrescreve um arquivo existente: colisão por conteúdo diferente → sufixo _1/_2 (D-09); conteúdo idêntico (mesmo SHA) → pula (D-10)"
    - "Materializar entre volumes verifica o hash pós-escrita antes de considerar feito; hash divergente → aborta sem deixar arquivo corrompido (AUT-06)"
    - "Undo reverte destino→origem quando o destino bate o hash; se o destino sumiu/mudou → restaura do CAS para a origem e marca undone_from_cas; nunca perde (AUT-05, Open Q2)"
  artifacts:
    - path: "backend/app/automation/fileops.py"
      provides: "materialize_to_dest (CAS→destino+verifica hash), resolve_collision (D-09/D-10)"
      contains: "def resolve_collision"
    - path: "backend/app/automation/undo.py"
      provides: "undo_operation (reverte por AuditLog; fallback CAS)"
      contains: "def undo_operation"
  key_links:
    - from: "backend/app/automation/fileops.py"
      to: "app.storage.cas"
      via: "cas.read_bytes / hashing SHA-256"
      pattern: "cas\\.|sha256"
    - from: "backend/app/automation/undo.py"
      to: "app.storage.cas.read_bytes"
      via: "rede final de recuperação"
      pattern: "read_bytes"
---

<objective>
A operação física segura e o undo: `fileops.py` (materializa o destino a partir do CAS, anti-colisão por hash, verificação de integridade cross-device) e `undo.py` (reverte por AuditLog, com fallback de restauração do CAS quando o destino foi alterado). Torna GREEN os testes Wave 0 de `test_fileops.py` e `test_undo.py`.

Purpose: É a operação de MAIOR risco do produto ("nunca pode causar perda"). A materialização-do-CAS (D-11) torna a operação intrinsecamente segura — o CAS é a fonte da verdade, o original na pasta de origem nunca é movido/destruído. Anti-colisão a montante + verificação de hash + CAS como rede final fecham AUT-04/05/06.
Output: `app/automation/fileops.py`, `app/automation/undo.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-CONTEXT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md

<interfaces>
<!-- Extraído do codebase — usar diretamente. -->

CAS (backend/app/storage/cas.py) — REUSAR, não reinventar:
  cas.read_bytes(content_hash: str) -> bytes      # rede de recuperação (undo/materialização)
  cas.path_for(content_hash) -> Path; cas.exists(content_hash) -> bool
  _CHUNK_SIZE = 64*1024; padrão de hashing por streaming (hashlib.sha256 em chunks)
  store(): grava num tmp no MESMO diretório do destino → os.replace atômico; cleanup só do tmp, NUNCA do final

AuditLog estendido (Plan 01): status, source_path, dest_path, run_id, content_hash, document_id, action, details

Decisão de produto (planner) — POLÍTICA DO ORIGINAL: o arquivo original na pasta
de origem é MANTIDO intacto no v1 (materialização do CAS cria uma cópia derivada
no destino; o CAS já preserva o conteúdo). Não remover/quarentenar o original.

Código de referência: 06-RESEARCH Pattern 3 (safe_move EXDEV) e §Code Examples
  "Resolução anti-colisão por hash" (resolve_collision). DERIVAR, não copiar cego.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fileops.py — materializar do CAS + anti-colisão + verificação de integridade (AUT-04/AUT-06/D-09/D-10/D-11)</name>
  <read_first>
    - backend/app/storage/cas.py (LER NA ÍNTEGRA: store atômico, read_bytes, hashing em chunks, cleanup defensivo no finally)
    - backend/app/ingest/hashing.py (o MESMO SHA-256 do bloco = Document.content_hash)
    - backend/tests/automation/test_fileops.py (alvo RED — nomes: no_overwrite, collision_suffix, collision_duplicate, cross_device, integrity)
    - 06-RESEARCH.md Pattern 3 + §Code Examples "resolve_collision" + Pitfalls 1/3
  </read_first>
  <files>backend/app/automation/fileops.py</files>
  <behavior>
    - resolve_collision(dst, content_hash): dst não existe → dst; dst existe com MESMO sha256 → None (D-10 duplicata, pula); dst existe com sha256 DIFERENTE → procura dst_1/dst_2 livre (D-09)
    - materialize_to_dest(content_hash, dst): cria dst.parent (mkdir parents=True exist_ok=True), escreve bytes do CAS num tmp no MESMO diretório do dst, verifica que sha256 do tmp == content_hash, os.replace(tmp, dst) atômico; hash divergente → remove tmp, levanta IntegrityError, NÃO deixa dst corrompido (integrity test)
    - no_overwrite: chamar materialize com dst já ocupado por conteúdo diferente SEM resolver colisão antes → defesa: nunca sobrescreve (resolução é a montante; materialize cria exclusivo/cria só em caminho livre)
    - cross_device: como o destino é SEMPRE materializado do CAS (não move), o caminho cross-device é o MESMO código (escreve bytes + verifica hash); não há ramo EXDEV de mover — a escrita do CAS para qualquer volume + verificação de hash É a operação AUT-06
    - finally nunca remove o dst final, só o tmp da própria chamada (espelha cas.py cleanup)
  </behavior>
  <action>
    Criar `fileops.py` reusando o hashing do CAS. `_sha256_of_file(path)` por streaming em chunks (`_CHUNK_SIZE = 64*1024`, igual cas.py). `resolve_collision(dst: Path, content_hash: str) -> Path | None` exatamente como o §Code Example do research: dst inexistente→dst; mesmo hash→None (D-10); senão incrementa sufixo `{stem}_{i}{suffix}` até achar livre (D-09), retornando None se um candidato bater o hash. `materialize_to_dest(content_hash: str, dst: Path) -> None` (D-11 + AUT-06): `dst.parent.mkdir(parents=True, exist_ok=True)`; escreve `cas.read_bytes(content_hash)` (ou stream via `cas.open_blob`) num tmp `dst.parent / f".{uuid4}.tmp"`; `os.fsync`; recomputa sha256 do tmp e compara com `content_hash` — divergente → `tmp.unlink(missing_ok=True)` + `raise IntegrityError`; igual → `os.replace(tmp, dst)`. `finally` limpa só o tmp da chamada, NUNCA o dst (defesa contra perda, espelha cas.py). Como a materialização é uma CÓPIA do CAS, não há "remover origem" e cross-device deixa de ser caso especial (D-11). Levantar `PermissionError`/`OSError` do disco SEM capturar (o worker do Plan 04 roteia como FALHA retryável, Pitfall 6). Não logar conteúdo.
    Definir uma exceção `IntegrityError` própria do módulo (ou reusar uma do projeto, conferir em storage/) para o hash divergente.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_fileops.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/automation/test_fileops.py -x -q` 100% verde (no_overwrite, collision_suffix, collision_duplicate, cross_device, integrity).
    - `grep -c "def resolve_collision\|def materialize_to_dest" backend/app/automation/fileops.py` retorna 2.
    - `grep -c "cas\." backend/app/automation/fileops.py` retorna ≥1 (materializa do CAS, D-11).
    - Teste integrity prova: ao forçar hash divergente, `dst` NÃO é criado/corrompido e a origem (CAS) intacta.
  </acceptance_criteria>
  <done>materialize_to_dest escreve do CAS + verifica hash; resolve_collision aplica sufixo (D-09)/pula duplicata (D-10); nunca sobrescreve; hash divergente aborta sem corromper; test_fileops.py verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: undo.py — reversão por-doc e por-run com fallback CAS (AUT-05/Open Q2)</name>
  <read_first>
    - backend/app/storage/cas.py (read_bytes — rede final; padrão de escrita atômica)
    - backend/app/automation/fileops.py (irmão recém-criado: materialize_to_dest, _sha256_of_file, IntegrityError)
    - backend/app/models/audit_log.py (colunas status/source_path/dest_path/run_id/content_hash do Plan 01)
    - backend/tests/automation/test_undo.py (alvo RED — undo por-doc/por-run + cas_fallback)
    - 06-RESEARCH.md Open Question 2 + §Code Examples (read_bytes)
  </read_first>
  <files>backend/app/automation/undo.py</files>
  <behavior>
    - undo_operation(session, audit): destino (dest_path) existe e sha256==content_hash → reverte (remove o destino materializado; o original na origem foi mantido, então origem já está intacta — ou re-materializa do CAS na source_path se a origem não existir mais); marca audit.status="undone"
    - cas_fallback: dest_path sumiu OU sha256 diferente → restaura cas.read_bytes(content_hash) para source_path; marca status="undone_from_cas"
    - undo por-doc: seleciona AuditLog(document_id=X, status="done") e reverte
    - undo por-run: seleciona todos AuditLog(run_id=R, status="done") e reverte cada um (lote, D-03/AUT-05)
    - NUNCA perde: se nem destino nem CAS disponíveis (impossível por desenho, CAS é imutável) → falha controlada, audit não vira "done" corrompido
  </behavior>
  <action>
    Criar `undo.py` com `undo_operation(session, audit: AuditLog) -> str` (retorna o status resultante "undone"/"undone_from_cas") e dois orquestradores: `undo_document(session, document_id) -> list[...]` (filtra `AuditLog.document_id == X, status == "done"`) e `undo_run(session, run_id) -> list[...]` (filtra `AuditLog.run_id == R, status == "done"`). A lógica central: dado um `AuditLog` com `dest_path`/`source_path`/`content_hash`, checar integridade do destino via `fileops._sha256_of_file(dest_path)`; se o destino existe e bate o hash → restaurar a origem garantindo o conteúdo na `source_path` (re-materializar do CAS se a origem sumiu) e remover o arquivo materializado no destino; status="undone". Se o destino sumiu ou o hash diverge → restaurar `cas.read_bytes(content_hash)` para `source_path` (rede final), status="undone_from_cas". Persistir `audit.status` via session.commit (alinhado ao padrão de persistência atômica — não comitar parcial inconsistente). Falha de disco (PermissionError) propaga como falha controlada sem corromper o audit. Não logar conteúdo (só ids/paths/status). NÃO transicionar o estado do documento aqui — o endpoint (Plan 04) decide a transição pós-undo; undo.py é a mecânica de arquivo.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_undo.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/automation/test_undo.py -x -q` 100% verde (undo por-doc, undo por-run, -k cas_fallback).
    - `grep -c "def undo_operation\|def undo_document\|def undo_run" backend/app/automation/undo.py` retorna 3.
    - `grep -c "read_bytes\|undone_from_cas" backend/app/automation/undo.py` retorna ≥2 (fallback CAS presente).
    - Teste cas_fallback prova: destino removido pelo "usuário" → conteúdo íntegro restaurado do CAS na origem, status="undone_from_cas".
  </acceptance_criteria>
  <done>undo_operation reverte com checagem de integridade; fallback restaura do CAS quando destino sumiu/mudou; undo por-doc e por-run; nunca perde; test_undo.py verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend ↔ filesystem NTFS do cliente | escrita/replace toca o disco mutável externo do cliente |
| destino mutável ↔ undo | usuário pode alterar/apagar o destino entre aplicar e desfazer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Denial / data loss | sobrescrita silenciosa do destino | mitigate | anti-colisão a montante (resolve_collision D-09/D-10); materialize escreve em tmp+replace só em caminho resolvido-livre; nunca sobrescreve (teste no_overwrite) |
| T-06-08 | Tampering / data loss | hash divergente pós-escrita cross-device | mitigate | verifica sha256 == content_hash ANTES do os.replace; divergente → aborta + unlink tmp, dst não criado (teste integrity, AUT-06) |
| T-06-09 | Denial / data loss | finally remove o blob/destino final | mitigate | cleanup só do tmp da chamada (espelha cas.py); blob CAS imutável é a rede final |
| T-06-10 | Denial / data loss | undo sob destino alterado pelo usuário | mitigate | checagem de integridade por hash → fallback restaura do CAS (undone_from_cas), nunca perde (Open Q2) |
| T-06-11 | Denial | lock de arquivo Windows (WinError 32) | mitigate | PermissionError/OSError propaga como FALHA retryável ao worker (Plan 04, Pitfall 6); estado reconciliável pelo write-ahead |
| T-06-SC | Tampering | instalações de pacote | accept | nenhuma dependência nova (stdlib + cas.py/hashing.py já instalados) |
</threat_model>

<verification>
- `pytest tests/automation/test_fileops.py tests/automation/test_undo.py -q` verde.
- Teste integrity: hash divergente não cria/corrompe destino.
- Teste cas_fallback: destino sumido → restaura do CAS, status undone_from_cas.
</verification>

<success_criteria>
- materialize_to_dest materializa do CAS + verifica hash (D-11/AUT-06); resolve_collision (D-09/D-10).
- undo reverte com integridade + fallback CAS (AUT-05/Open Q2); nunca perde.
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-03-SUMMARY.md` when done
</output>
