---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - frontend/src/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useAutomations.ts
  - frontend/src/pages/AutomationsPage.tsx
  - frontend/src/pages/DryRunPage.tsx
  - frontend/src/components/Icon.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [AUT-01, AUT-02, AUT-03, AUT-05, TPL-02]
user_setup: []
must_haves:
  truths:
    - "A aba Automações lista as regras condicionais reais (da API) ordenadas por prioridade e permite criar/editar/remover/reordenar regras com condições {campo}[op]valor → padrão de nome/pasta (TPL-02/D-05)"
    - "O editor de padrão mostra os tokens {campo} disponíveis e uma pré-visualização ao vivo do nome/caminho resolvido com sanitização (AUT-01/AUT-02/D-08)"
    - "A tela de Dry-run mostra pares origem→destino (texto mono) com colisão/bloqueio sinalizados e só habilita 'Aplicar' após o preview (AUT-03)"
    - "Documentos aplicados ganham ação 'Desfazer' (por-doc e por-lote), nunca apresentada como destrutiva (AUT-05/D-03)"
  artifacts:
    - path: "frontend/src/pages/AutomationsPage.tsx"
      provides: "Aba Automações real (S1/S2/S3) substituindo o mock"
      contains: "useAutomations"
    - path: "frontend/src/pages/DryRunPage.tsx"
      provides: "Tela de dry-run/preview origem→destino (S4)"
      contains: "DryRunPage"
    - path: "frontend/src/hooks/useAutomations.ts"
      provides: "Query + mutations (CRUD regras, dry-run, apply, undo)"
      contains: "useAutomations"
  key_links:
    - from: "frontend/src/pages/AutomationsPage.tsx"
      to: "useAutomations"
      via: "hook (substitui import de data/mock)"
      pattern: "useAutomations"
    - from: "frontend/src/lib/api.ts"
      to: "/automations"
      via: "fetch tipado"
      pattern: "/automations"
---

<objective>
O frontend da Fase 6 honrando o UI-SPEC APROVADO: a aba Automações real (substitui o mock — S1/S2/S3), a tela de Dry-run/Preview (S4), as ações Aplicar/Desfazer na lista (S5/S6), com tipos/api/hooks tipados. Sem visualizador de documento (restrição de visão absoluta). Verificação visual humana no fim.

Purpose: É onde o usuário ganha confiança ANTES de qualquer efeito no disco (S4 = core surface, AUT-03) e configura as regras com ordem depurável (S1, D-05). Reusa o design system travado (mesmos tokens das Fases 2/4/5); nenhuma dependência npm nova.
Output: `types.ts`/`api.ts`/`useAutomations.ts` (tipos+cliente+hooks), `AutomationsPage.tsx` (reescrito), `DryRunPage.tsx` (novo), `Icon.tsx` (ícones novos), `App.tsx` (navegação).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-CONTEXT.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-UI-SPEC.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md

<interfaces>
<!-- Extraído do codebase — usar diretamente. -->

Cliente tipado (frontend/src/lib/api.ts): request<T>(path, init) — checa res.ok,
  lança ApiError, trata 204 sem corpo. Adicionar funções /automations seguindo o
  estilo de getTemplates/createTemplate/updateTemplate/deleteTemplate.

Hooks molde (frontend/src/hooks/useTemplates.ts): useQuery(queryKey) +
  useMutation invalidando a key. useDocuments.ts: polling refetchInterval 4000 +
  placeholderData/keepPreviousData (sem flicker).

types.ts já tem: Template/TemplateCreate/TemplatePatch, ClassificationField,
  AttentionList/ReviewItem (estrutura dos baldes). O mock Automation (campos
  trigger/cond/action/runs) será SUBSTITUÍDO por tipos reais da API /automations.

Endpoints (Plan 04): GET/POST/PATCH/DELETE /automations (RuleOut/RuleIn/RulePatch
  com conditions[] {field_name, operator: eq|gt|lt|contains, value, position},
  name_pattern, folder_pattern, priority, active); POST /automations/dry-run
  (→ pares origem→destino + colisão/bloqueio); POST /automations/apply
  (document_ids|run); POST /automations/undo (document_id|run_id).

Design system TRAVADO (06-UI-SPEC): reusar .rule-card/.auto-card/.flow-pill/
  .stack/.badge/.row-action/.select/.search-input/.checkbox/table.docs/.cell-mono/
  StatusPill/ConfidenceBadge. Cores SEMPRE via var(--st-*)/var(--accent); NUNCA hex.
  Mono (--font-mono) p/ caminhos/padrões/nome. Sem dependência npm nova.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos + cliente API + hooks de automações (S1 dados)</name>
  <read_first>
    - frontend/src/types.ts (LER: estilo das interfaces; o mock Automation a substituir; AttentionList)
    - frontend/src/lib/api.ts (LER: request<T>, funções de templates como molde exato)
    - frontend/src/hooks/useTemplates.ts + useDocuments.ts (molde de query + mutations + polling)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-04-PLAN.md §interfaces (shapes dos endpoints)
  </read_first>
  <files>frontend/src/types.ts, frontend/src/lib/api.ts, frontend/src/hooks/useAutomations.ts</files>
  <action>
    Em `types.ts`, SUBSTITUIR a interface mock `Automation` por tipos reais: `RuleCondition` (`field_name: string`, `operator: 'eq'|'gt'|'lt'|'contains'`, `value: string`, `position: number`), `AutomationRule` (`id`, `name`, `priority`, `conjunction: 'and'|'or'`, `name_pattern: string|null`, `folder_pattern: string|null`, `active: boolean`, `conditions: RuleCondition[]`), `RuleCreate`/`RulePatch` (sem id), e `DryRunRow` (`document_id`, `original_filename`, `source_path: string`, `dest_path: string|null`, `collision: 'none'|'suffix'|'duplicate'|'blocked'`, `blocked_field: string|null`). Em `api.ts`, adicionar `getAutomationRules`/`createAutomationRule`/`updateAutomationRule`/`deleteAutomationRule`/`postDryRun`/`postApply`/`postUndo` reusando `request<T>` (paths `/automations*`). Criar `useAutomations.ts` com `AUTOMATIONS_KEY = ['automations']`: `useAutomations()` (useQuery), `useCreateRule`/`useUpdateRule`/`useDeleteRule` (mutations invalidando a key), `useDryRun`/`useApply`/`useUndo` (mutations que invalidam `['automations']` + `['documents']` + `['attention']`). Renderizar nomes/paths como texto puro (sem dangerouslySetInnerHTML).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sem erros de tipo.
    - `grep -c "useAutomations\|useDryRun\|useApply\|useUndo" frontend/src/hooks/useAutomations.ts` retorna ≥4.
    - `grep -c "/automations" frontend/src/lib/api.ts` retorna ≥4 (CRUD + ações).
    - `grep -c "trigger\|runs" frontend/src/types.ts` no contexto de `Automation` retorna 0 (mock substituído).
  </acceptance_criteria>
  <done>Tipos reais de regra/dry-run; cliente /automations; hooks query+mutations; build TS verde.</done>
</task>

<task type="auto">
  <name>Task 2: AutomationsPage (S1/S2/S3) reescrita + DryRunPage (S4) + ícones + navegação</name>
  <read_first>
    - frontend/src/pages/AutomationsPage.tsx (LER: o mock atual a substituir)
    - frontend/src/pages/TemplatesPage.tsx (molde S1/S2/S3: form inline, loading/erro/vazio, confirmRemove, linhas de condição)
    - frontend/src/pages/DocumentsPage.tsx (molde S4: table.docs, checkbox lote allIds/allSel, estados de tela)
    - frontend/src/components/Icon.tsx (estilo de stroke p/ adicionar undo/alert/arrowUp/arrowDown)
    - frontend/src/components/StatusPill.tsx + ConfidenceBadge.tsx (reuso de estado/cor)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-UI-SPEC.md (Copywriting + Color + Visuals — TODO o contrato)
    - frontend/src/App.tsx (registro de rota/navegação)
  </read_first>
  <files>frontend/src/pages/AutomationsPage.tsx, frontend/src/pages/DryRunPage.tsx, frontend/src/components/Icon.tsx, frontend/src/App.tsx</files>
  <action>
    Adicionar a `Icon.tsx` os ícones que faltarem no MESMO estilo de stroke: `undo`, `alert` (colisão/bloqueio), `arrowUp`/`arrowDown` (reordenação de prioridade). REESCREVER `AutomationsPage.tsx` removendo o import de `data/mock`: S1 = lista de `.rule-card` ordenadas por priority com estados loading/erro/vazio (empty state com a copy exata do UI-SPEC: "Nenhuma regra de automação ainda" + corpo + CTA "Nova regra"); cada card mostra a posição/prioridade, o resumo condição→ação, e ações reordenar ↑/↓ (`.row-action` com `aria-label`+`title` "Aumentar/Diminuir prioridade") + editar/remover (confirmRemove molde). S2 = editor inline (form): linhas de condição (`select` campo + `select` operador `= > < contém` + input valor) + combinador E/OU + a AÇÃO (input padrão de nome `{campo}_...` + input padrão de pasta) — CTA "Salvar regra"/"Nova regra", sem "Cancelar" supérfluo. S3 = sub-bloco do padrão: lista de tokens `{campo}` inseríveis por clique + pré-visualização ao vivo do nome/caminho resolvido com dados de exemplo (hint exato: "Pré-visualização com dados de exemplo. Caracteres inválidos no Windows são removidos automaticamente."). Criar `DryRunPage.tsx` (S4) molde `DocumentsPage`: `table.docs` com colunas origem→destino em `var(--font-mono)`, coluna de colisão sinalizada (badge âmbar `--st-leitura` sufixo D-09 / azul `--st-encontrado` duplicata D-10 / vermelho `--st-erro` bloqueio D-07 com a copy exata do UI-SPEC), seleção por linha (`.checkbox`) e por lote (allIds/allSel), contagem no topo, CTA accent "Aplicar automações"/"Aplicar selecionados" DESABILITADA até o preview carregar. Ação "Desfazer aplicação" (`.row-action` neutra, nunca accent/vermelho) + diálogo S6 que comunica reversibilidade (copy de resultado undo/undone_from_cas exata do UI-SPEC). Registrar a rota de Dry-run na navegação em `App.tsx`. SEM visualizador de documento (restrição absoluta). Cores SEMPRE via token; mono nos caminhos/padrões.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5 && ! grep -q "data/mock" src/pages/AutomationsPage.tsx && grep -q "DryRunPage" src/App.tsx && echo verify-ok</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` conclui sem erro.
    - `! grep -q "data/mock" frontend/src/pages/AutomationsPage.tsx` (mock removido; exit 0).
    - `grep -c "DryRunPage" frontend/src/App.tsx` retorna ≥1 (rota/navegação da tela de dry-run registrada).
    - `grep -c "dangerouslySetInnerHTML" frontend/src/pages/AutomationsPage.tsx frontend/src/pages/DryRunPage.tsx` retorna 0 (texto puro).
    - `grep -c "aria-label" frontend/src/pages/AutomationsPage.tsx` retorna ≥2 (reordenação ↑/↓ acessível, Visuals).
    - `grep -c "var(--st-\|var(--accent" frontend/src/pages/DryRunPage.tsx` retorna ≥1 e `grep -c "#" ...` não introduz hex de cor hardcoded.
    - `grep -c "Nenhuma regra de automação ainda" frontend/src/pages/AutomationsPage.tsx` retorna 1 (copy exata do UI-SPEC).
  </acceptance_criteria>
  <done>AutomationsPage real (S1/S2/S3) sem mock; DryRunPage (S4) com preview origem→destino + colisão + Aplicar gated; Desfazer não-destrutivo (S5/S6); ícones e navegação; build verde; design system honrado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Frontend da Fase 6: aba Automações real (lista de regras ordenadas por prioridade, editor de regra com condições {campo}[op]valor → padrão de nome/pasta, pré-visualização ao vivo com sanitização) e tela de Dry-run/Preview (pares origem→destino, colisões sinalizadas, Aplicar gated, Desfazer). Backend (Plans 01-04) já provê /automations + apply + undo.
  </what-built>
  <how-to-verify>
    1. Subir backend (`cd backend && . .venv/bin/activate && uvicorn app.main:app --reload --workers 1`) e frontend (`cd frontend && npm run dev`).
    2. Abrir a aba "Automações": confirmar que NÃO é mais o mock (lista vazia → empty state "Nenhuma regra de automação ainda" com CTA "Nova regra").
    3. Criar uma regra: condição (ex.: `tipo` contém `holerite` E `valor` > `3000`) → padrão de pasta `Documentos/{cliente}/{data:aaaa-mm}/` e nome `{cliente}_{numero}.pdf`. Verificar a pré-visualização ao vivo resolvendo com dados de exemplo e mostrando a sanitização.
    4. Reordenar prioridade com ↑/↓ e confirmar a ordem refletida (D-05).
    5. Abrir o Dry-run/Preview: confirmar pares origem→destino em fonte mono, sinalização de colisão (âmbar sufixo / azul duplicata / vermelho bloqueio-por-campo-faltante) e que "Aplicar" só habilita após o preview carregar.
    6. Aplicar em um documento de teste e confirmar a ação "Desfazer aplicação"; abrir o diálogo de undo e conferir que a linguagem é de reversibilidade (nunca exclusão permanente).
    7. Conferir tema claro E escuro (cores via token).
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva os ajustes visuais/de cópia necessários</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → render React | nomes/paths vindos do backend renderizados na UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Info disclosure / XSS | render de paths/valores da API | mitigate | texto puro (React escapa); grep gate dangerouslySetInnerHTML == 0 |
| T-06-18 | Tampering | confiar em otimismo de UI que mascare falha de operação | mitigate | mutations invalidam queries; fonte de verdade é a API (molde useTemplates); estado de operação vem do backend |
| T-06-SC | Tampering | instalações npm | accept | nenhuma dependência npm nova (code-and-config, 06-UI-SPEC §Registry Safety) |
</threat_model>

<verification>
- `npm run build` + `npx tsc --noEmit` verdes.
- Mock removido (grep data/mock == 0); sem dangerouslySetInnerHTML; cores por token.
- Checkpoint humano aprovado (S1-S6 conforme UI-SPEC).
</verification>

<success_criteria>
- Aba Automações real (regras ordenadas + editor + preview); Dry-run com preview origem→destino + colisão + Aplicar gated; Desfazer não-destrutivo.
- Design system travado honrado; sem dependência npm nova; sem visualizador de documento.
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-05-SUMMARY.md` when done
</output>
