---
phase: 06-automa-es-de-arquivo-renomear-mover
plan: 07
type: execute
wave: 2
depends_on: ["06-06"]
files_modified:
  - backend/app/automation/pipeline.py
  - backend/app/automation/rules.py
  - backend/app/automation/stage.py
  - backend/app/api/automations.py
  - backend/app/queue/worker.py
  - backend/tests/automation/test_pipeline.py
  - backend/tests/automation/test_stage.py
  - backend/tests/test_api_automations.py
autonomous: true
requirements: [AUT-01, AUT-02, AUT-03, AUT-04, AUT-05, AUT-06, TPL-02]
must_haves:
  truths:
    - "Um documento percorre TODAS as etapas do pipeline cujo filtro de entrada casa, na ordem definida pelo usuário (D-12)"
    - "Renomear e Mover são transformações puras do plano-alvo (folder, name); o disco é tocado UMA vez ao final (materialização única, Open Q1)"
    - "[Move,Rename] e [Rename,Move] produzem o MESMO destino final (Pitfall 8)"
    - "Uma etapa Rotear interrompe o pipeline e o documento NÃO é materializado (Pitfall 9)"
    - "Um documento que não casa NENHUMA etapa tem comportamento explícito (no-op, mantido na origem — Pitfall 10), nunca materializado silenciosamente para a raiz"
    - "O gate Identificar tipo reusa a classificação existente e NÃO re-cobra a IA quando já há ClassificationResult (D-15)"
    - "O dry-run simula o pipeline inteiro por documento sem tocar o disco e sem escrever AuditLog (AUT-03)"
    - "O write-ahead (AuditLog intent) é persistido ANTES de materializar; o undo cobre a operação do pipeline por-doc e por-run (AUT-04/AUT-05)"
  artifacts:
    - path: "backend/app/automation/pipeline.py"
      provides: "Executor PURO do pipeline (run_pipeline) + filtros de entrada (filter_matches)"
      contains: "def run_pipeline"
    - path: "backend/app/automation/stage.py"
      provides: "apply_stage reescrito: executa o pipeline + materializa 1x"
      contains: "def apply_stage"
    - path: "backend/app/api/automations.py"
      provides: "CRUD de pipeline/steps/filtros + dry-run/apply/undo"
      exports: ["router"]
  key_links:
    - from: "backend/app/automation/stage.py"
      to: "backend/app/automation/pipeline.py"
      via: "run_pipeline chamado por apply_stage/dry_run"
      pattern: "run_pipeline"
    - from: "backend/app/automation/stage.py"
      to: "backend/app/automation/fileops.py"
      via: "materialize_to_dest + remove_original (UMA vez ao final)"
      pattern: "materialize_to_dest"
    - from: "backend/app/queue/worker.py"
      to: "apply_stage"
      via: "dispatch APPLY_STEP + enqueue_pending_applications"
      pattern: "apply_stage"
---

<objective>
Reescrever a camada de ORQUESTRAÇÃO das automações para o modelo de pipeline: (1) um executor PURO `pipeline.py` que itera as etapas ordenadas, aplica filtros de entrada (D-14), resolve o plano-alvo `(folder, name)` por transformações puras (Rename/Move), trata o gate Identificar tipo (D-15) e a etapa Rotear (Pitfall 9); (2) `apply_stage` reescrito para executar o pipeline e MATERIALIZAR DO CAS UMA ÚNICA VEZ ao final (Open Q1 — recomendação HIGH do research); (3) `api/automations.py` reescrito para CRUD aninhado de pipeline/steps/filtros + dry-run/apply/undo; (4) ajuste do worker para o novo apply_stage.

Purpose: É o coração funcional do REDESIGN. Os tijolos físicos (naming/fileops/undo) NÃO mudam — são reusados como ações atômicas. O valor é montar o pipeline declarativo por cima deles com materialização única e write-ahead/undo agrupados por execução.
Output: pipeline.py novo, rules.py estendido (filtros), stage.py/api reescritos, worker ajustado, testes RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-PATTERNS.md
@.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-06-SUMMARY.md

<interfaces>
<!-- TIJOLOS REUSÁVEIS — NÃO reescrever. Assinaturas reais extraídas do código: -->

backend/app/automation/naming.py:
  resolve_pattern(pattern: str, fields: dict[str, str]) -> str | None   # None = campo faltante (D-07)
  resolve_dest_folder(pattern: str, fields: dict[str, str], *, base_root: Path) -> Path | None  # None = D-07/V4
  sanitize_component(value: str, max_len: int | None = None) -> str

backend/app/automation/fileops.py:
  materialize_to_dest(content_hash: str, dst: Path) -> Path   # escreve do CAS + verifica hash (D-11)
  resolve_collision(dst: Path, src: Path) -> Path | None      # None = D-10 idêntico; != dst = D-09 sufixo
  remove_original(source_path: Path) -> None                  # AUT-06 crit 5 (só após verificar)
  hash_file(path: Path) -> str ; class IntegrityError

backend/app/automation/undo.py:
  undo_document(session, document_id: int) -> list[str]   # reabre CONCLUIDO→PROCESSANDO
  undo_run(session, run_id: str) -> int

backend/app/automation/rules.py (ESTENDER, não substituir):
  @dataclass Condition(field_name, operator, value)
  evaluate_condition(cond: Condition, fields: dict[str, str]) -> bool   # reusar p/ filter_type="field"
  _as_decimal(text) -> Decimal | None  # coerção numérica (reusar p/ filtro size, Pitfall 2)
  _OPERATORS = {"eq","gt","lt","contains"}

backend/app/classification/stage.py:
  async classify_stage(session, *, content_hash: str, forced_template_id: int | None = None) -> ClassifyStageResult
  # ClassifyStageResult(matched, template_id, called_ai). IDEMPOTENTE: ClassificationResult existente → no-op (called_ai=False). NÃO re-cobra IA.

backend/app/pipeline/state_machine.py:
  transition(session, document, to_state, completed_step=None) -> Document  # comita TUDO junto; NUNCA commit manual antes
  # allowlist: PROCESSANDO → {EM_REVISAO, CONCLUIDO, QUARENTENA, FALHA}; CONCLUIDO → {PROCESSANDO} (aresta da Fase 6, undo)

backend/app/models/audit_log.py: AuditLog(document_id, action, status, source_path, dest_path, run_id, content_hash, created_at) — JÁ tem write-ahead.

backend/app/queue/worker.py — dispatch atual já tem `elif step == APPLY_STEP: run_id = json.loads(payload).get("run_id"); await apply_stage(session, content_hash=original_hash, run_id=run_id)` E `enqueue_pending_applications` (auto-aplica D-01 por confidence_score >= review_confidence_threshold, exclui docs com AuditLog status="done"). Verificar se permanecem válidos com o apply_stage reescrito.
</interfaces>

<!-- stage.py ATUAL (a reescrever) já tem: dry_run(session, *, content_hash), apply_stage(session, *, content_hash, run_id, dry_run), reconcile_orphans, ApplyStageResult, APPLY_STEP="apply", APPLIED_STEP="aplicado", CLASSIFIED_STEP="classificado", _fields_map, _source_path, _base_root, _has_done. Reusar _fields_map/_source_path/_base_root/_has_done/reconcile_orphans; SUBSTITUIR _resolve_plan/_to_pure_rules (eram baseados em AutomationRule) pela chamada ao run_pipeline. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Executor PURO do pipeline + filtros de entrada (pipeline.py, rules.py)</name>
  <read_first>
    - backend/app/automation/rules.py (avaliador a estender — evaluate_condition/_as_decimal)
    - backend/app/automation/naming.py (resolve_pattern/resolve_dest_folder — ações Rename/Move)
    - backend/tests/automation/test_pipeline.py (RED — fonte de verdade da API pública; ler ANTES de codar)
    - backend/tests/automation/conftest.py (fixtures pipeline_factory/classified_doc_attrs do 06-06)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md §"Pattern 1/2/3" (executor puro, filtros, gate)
  </read_first>
  <behavior>
    - filter_matches: step sem filtros → casa sempre (aplica a todos os docs, P10); conjunction "or" → any, "and" → all; filter_type desconhecido → não casa (falha fechada, V5).
    - filtro "field" reusa evaluate_condition; "extension" case-insensitive sobre file_attrs["ext"]; "filename" eq/contains sobre original_filename; "size" coerção numérica (Decimal) gt/lt (Pitfall 2); "source_folder" eq sobre source_folder_id; "template" eq sobre identified_template_id (porteiro do gate).
    - run_pipeline ordering (D-12): itera steps por position; só executa os que o filtro casa.
    - action rename → muta SÓ target_name (resolve_pattern; None → blocked=True, D-07).
    - action move → muta SÓ target_folder (resolve_dest_folder confinado; None → blocked=True, D-07/V4).
    - Pitfall 8: pipeline [move,rename] e [rename,move] → MESMO PipelinePlan final (folder/name independentes).
    - action route → retorna imediatamente PipelinePlan(route_to=...) e NÃO materializa (Pitfall 9).
    - action identify_type → chama classify_fn(template_id), seta identified_template_id; consumível por filtro "template" seguinte (D-15). NÃO re-cobra IA (classify_fn idempotente).
    - P10: nenhum step casa → PipelinePlan com plano-alvo = default (folder=base_root, name=original) E uma flag matched_any=False, para o caller decidir no-op explícito (não materializar p/ raiz silenciosamente).
  </behavior>
  <action>
    (a) ESTENDER backend/app/automation/rules.py com o avaliador de FILTROS de entrada (D-14), SEM remover a API existente (evaluate_condition/rule_matches continuam — viram o ramo "field"):
    - `evaluate_filter(filter, fields, file_attrs, identified_template_id) -> bool`: dispatch por filter_type. "field" → reusa evaluate_condition (montando Condition de field_name/operator/value). "source_folder" → eq sobre str(file_attrs.get("source_folder_id")). "extension" → casefold eq sobre file_attrs.get("ext"). "filename" → eq/contains sobre file_attrs.get("original_filename"). "size" → coerção via _as_decimal sobre file_attrs.get("size") e value (gt/lt; Pitfall 2). "template" → eq sobre str(identified_template_id). filter_type desconhecido → False (V5 falha fechada).
    - `filter_matches(filters, conjunction, fields, file_attrs, identified_template_id) -> bool`: sem filtros → True (P10); senão any/all por conjunction.
    Os filtros são objetos PUROS (dataclass ou duck-typed com .filter_type/.operator/.value/.field_name) — o caller (pipeline.py) mapeia StepFilter ORM → essa forma pura, igual stage._to_pure_rules fazia.

    (b) Criar backend/app/automation/pipeline.py (PURO — sem disco, sem ORM direto), conforme 06-RESEARCH §"Pattern 1":
    - `@dataclass(frozen=True) PipelinePlan`: target_folder: Path|None, target_name: str|None, blocked: bool=False, route_to: str|None=None, identified_template_id: int|None=None, matched_any: bool=False.
    - `@dataclass PipelineStepSpec`: position, action_type, conjunction, params (dict), filters (list de filtros puros) — a forma pura que o caller monta a partir do ORM.
    - `run_pipeline(steps: list[PipelineStepSpec], fields: dict[str,str], file_attrs: dict, *, base_root: Path, classify_fn) -> PipelinePlan`: folder=base_root.resolve(); name=file_attrs["original_filename"]; identified=None; matched_any=False. Para cada step ordenado por position: se não filter_matches → continue; senão matched_any=True e dispatch por action_type (rename/move/identify_type/route) conforme <behavior>. route → return PipelinePlan(route_to=..., matched_any=True). rename/move com None → return PipelinePlan(blocked=True, matched_any=True). identify_type → identified=classify_fn(params["template_id"]); file_attrs/identified atualizados. Ao fim → PipelinePlan(target_folder=folder, target_name=name, identified_template_id=identified, matched_any=matched_any).
    NUNCA eval. NUNCA logar valores de campo (V7/V9).

    (c) Criar backend/tests/automation/test_pipeline.py se ainda RED-stub do 06-06 não cobrir tudo — cobrir os marcadores do Test Map (06-RESEARCH §"Phase Requirements → Test Map"): -k filter (D-14), -k ordering (D-12), -k actions (D-13), -k order_independent (Pitfall 8), -k route_stops (Pitfall 9), -k no_match (Pitfall 10), -k gate (D-15, classify_fn mockado que conta chamadas → assert não re-chama se já classificado). Usar pipeline_factory/classified_doc_attrs do conftest.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_pipeline.py tests/automation/test_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def run_pipeline" backend/app/automation/pipeline.py` == 1
    - `grep -v '^#' backend/app/automation/pipeline.py | grep -c "eval("` == 0 (V5)
    - `grep -c "def filter_matches\|def evaluate_filter" backend/app/automation/rules.py` == 2
    - `grep -c "def evaluate_condition" backend/app/automation/rules.py` == 1 (API existente preservada)
    - test_pipeline.py cobre order_independent (Pitfall 8), route_stops (Pitfall 9), no_match (Pitfall 10), gate (D-15 não re-cobra IA)
    - Suite alvo verde; `ruff check backend/app/automation/pipeline.py backend/app/automation/rules.py` limpo
  </acceptance_criteria>
  <done>run_pipeline resolve o plano-alvo por etapas puras com materialização única conceitual; filtros D-14 reusam o avaliador; Pitfalls 8/9/10 e gate D-15 provados em teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply_stage/dry_run reescritos sobre o pipeline + ajuste do worker</name>
  <read_first>
    - backend/app/automation/stage.py (a reescrever — reusar _fields_map/_source_path/_base_root/_has_done/reconcile_orphans; substituir _resolve_plan/_to_pure_rules)
    - backend/app/automation/pipeline.py (run_pipeline — saída desta task anterior)
    - backend/app/queue/worker.py §_dispatch APPLY_STEP + enqueue_pending_applications (verificar compatibilidade)
    - backend/tests/automation/test_stage.py (RED — dry-run/write-ahead/idempotência/reconcile)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-RESEARCH.md §"System Architecture Diagram" (fluxo apply_stage)
  </read_first>
  <behavior>
    - dry_run simula o pipeline inteiro por doc sem tocar disco e sem AuditLog (AUT-03); sinaliza blocked/route/skipped_identical/collision/no_match.
    - apply_stage idempotente: AuditLog(status="done") existente → no-op (não re-materializa).
    - route (Pitfall 9): PipelinePlan.route_to "em_revisao" → transition(EM_REVISAO); "nao_tratar"/"ignorar" → marcador interno (NÃO novo DocState, A4/Open Q3) — conclui logicamente sem materializar. NUNCA materializa.
    - no_match (Pitfall 10): matched_any=False → no-op explícito (mantido na origem; conclui logicamente OU permanece — escolher: NÃO materializar para a raiz silenciosamente). Documentar a escolha.
    - blocked (D-07): transition(EM_REVISAO) sem tocar disco, sem AuditLog de operação.
    - materialização ÚNICA (Open Q1): plano-alvo final → resolve_collision (D-09 sufixo / D-10 skip) → AuditLog(intent) + commit ANTES de materialize_to_dest → remove_original só após verificar → audit.status="done" + transition(CONCLUIDO) num commit único.
    - gate identify_type via classify_fn: passar uma closure que chama LER ClassificationResult existente (custo 0) e só usa classify_stage/forced_template_id quando o step trava um template não confirmado (D-15/A3). NÃO re-cobrar IA.
  </behavior>
  <action>
    (a) Reescrever backend/app/automation/stage.py:
    - Remover imports e helpers de AutomationRule/_to_pure_rules (modelo dropado). Importar AutomationPipeline/PipelineStep/StepFilter e pipeline.run_pipeline.
    - Novo helper `_load_pipeline_specs(session) -> list[PipelineStepSpec]`: carrega o(s) AutomationPipeline ativo(s) com steps ativos ordenados por position, mapeia params_json (json.loads) e filtros ORM → a forma pura de pipeline.py. (Decisão de produto: v1 assume UM pipeline ativo; se múltiplos, concatenar por (pipeline, position) ou exigir 1 — escolher e documentar; recomendação: 1 pipeline ativo, steps ordenados.)
    - Novo helper `_file_attrs(session, doc) -> dict`: ext (Path(original_filename).suffix), size (os.stat da origem OU len do blob do CAS — ler uma vez, A6), source_folder_id (via IngestedOriginal), original_filename, template_id (ClassificationResult existente).
    - Novo `_classify_fn` closure: dado template_id alvo, LÊ o ClassificationResult existente do doc (custo 0) e devolve seu template_id; se ausente/forçado, chama classify_stage(forced_template_id) — mas como apply_stage é coroutine, o classify_fn pode ser síncrono lendo o CR existente (caminho comum) e o caso de re-classificar fica como await fora do run_pipeline OU documentado como fora do v1 do gate. Recomendação: gate v1 LÊ o CR existente (síncrono, custo 0); re-classificação forçada no meio do pipeline fica anotada como evolução (não bloquear o plano). Documentar.
    - Reescrever `_resolve_plan` (ou substituí-lo por `_run`): monta fields (_fields_map) + file_attrs + specs e chama run_pipeline → PipelinePlan. dry_run e apply_stage consomem o PipelinePlan: blocked → revisão; route_to → transição/marcador; matched_any False → no-op explícito; senão dest = target_folder/target_name (preservar extensão do original se o name não trouxe, igual ao código atual).
    - Manter ApplyStageResult mas ADICIONAR campos: routed: bool, route_target: str|None, no_match: bool (para o dry-run sinalizar P9/P10 na UI). Atualizar dry_run/apply_stage para preenchê-los.
    - Manter o write-ahead/materialização única/idempotência/reconcile_orphans EXATAMENTE como hoje (são corretos — só o "como resolve o dest" muda). NUNCA session.commit() manual antes de transition.

    (b) Ajustar backend/app/queue/worker.py: o dispatch de APPLY_STEP e enqueue_pending_applications JÁ existem e referenciam apply_stage/AuditLog status="done" — verificar que continuam válidos (a assinatura apply_stage(session, *, content_hash, run_id) é mantida). Se a assinatura mudar (ex.: dry_run param), ajustar a chamada. NÃO mudar a lógica de sweep/confidence (D-01) — só compatibilidade.

    (c) Atualizar backend/tests/automation/test_stage.py para o novo modelo: dry_run do pipeline inteiro; test_intent_before_materialize (AuditLog intent ANTES de materialize_to_dest — materialização única); idempotência por done; reconcile; NOVOS: route interrompe e não materializa; no_match não materializa p/ raiz. Usar pipeline_factory.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/automation/test_stage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "run_pipeline" backend/app/automation/stage.py` >= 1
    - `grep -c "AutomationRule\|_to_pure_rules\|first_matching_rule" backend/app/automation/stage.py` == 0 (modelo antigo removido)
    - `grep -c "materialize_to_dest" backend/app/automation/stage.py` >= 1 (materialização única reusa o tijolo)
    - `grep -c "session.commit()" backend/app/automation/stage.py` — write-ahead intent commit presente, MAS nenhum commit imediatamente antes de um transition (revisão manual do diff)
    - test_stage.py: assert intent persistido ANTES de materialize_to_dest; assert route não materializa; assert no_match não materializa p/ raiz
    - Suite alvo verde; ruff limpo
  </acceptance_criteria>
  <done>apply_stage executa o pipeline ordenado, materializa do CAS uma única vez com write-ahead intacto; worker compatível; route/no-match/blocked tratados sem tocar disco.</done>
</task>

<task type="auto">
  <name>Task 3: API reescrita — CRUD de pipeline/steps/filtros + dry-run/apply/undo</name>
  <read_first>
    - backend/app/api/automations.py (a reescrever — reusar a forma In/Patch/Out + ações dry-run/apply/undo)
    - backend/app/api/templates.py (molde 1:N aninhado: Template→fields, 409/422/404/204, PATCH substitui coleção)
    - backend/tests/test_api_automations.py (RED — fonte de verdade dos endpoints/contratos)
    - .planning/phases/06-automa-es-de-arquivo-renomear-mover/06-UI-SPEC.md §"Vocabulário de UI do pipeline" (action_type/filter_type/operadores — os conjuntos que a API valida)
  </read_first>
  <action>
    Reescrever backend/app/api/automations.py para CRUD ANINHADO de pipeline (pipeline → steps → filtros), espelhando templates.py:
    - Schemas Pydantic: `StepFilterIn`(filter_type, operator, value, field_name|None) com field_validator validando filter_type ∈ {field,source_folder,extension,filename,size,template} e operator ∈ {eq,gt,lt,contains} (422 fora — V5); `StepIn`(action_type ∈ {move,rename,identify_type,route} validado, conjunction ∈ {and,or}, params: dict, active, filters: list[StepFilterIn]); `PipelineIn`(name, active, steps: list[StepIn]); `PipelinePatch` (todos opcionais; steps informado SUBSTITUI a coleção inteira — delete-orphan); `*Out` correspondentes (from_attributes/serialização explícita; steps ordenados por position, filtros por position).
    - Endpoints CRUD: GET "" (lista pipelines), GET "/{id}", POST "" (201), PATCH "/{id}", DELETE "/{id}" (204). Padrão request.app.state.engine + with get_session; IntegrityError → 409; ausente → 404. params da ação validados conforme action_type (ex.: move exige folder_pattern; rename exige name_pattern; identify_type exige template_id; route exige target ∈ {em_revisao,nao_tratar,ignorar}) — 422 se faltar o param obrigatório do tipo.
    - Ações PRESERVADAS (espelham documents.py): POST "/dry-run" (ActionIn document_ids; chama stage.dry_run por doc; DryRunRow ganha campos routed/route_target/no_match além de blocked/collision/skipped_identical para a UI sinalizar P9/P10); POST "/apply" (ApplyIn document_id|document_ids; gera run_id; _requeue (content_hash, APPLY_STEP) com run_id no payload; 404 se nenhum id; D-03 por-doc e por-lote); POST "/undo" (UndoIn document_id|run_id; undo.undo_document/undo_run; 422 se nenhum). Reusar `_requeue` de api/documents.py e `_reopen_if_concluded`.
    - NÃO logar valores de campo (V7/V9) — só ids/paths/run_id/status no corpo da resposta.
    - main.py já inclui automations_api.router — manter (verificar o prefixo /automations).

    Atualizar backend/tests/test_api_automations.py para o novo modelo: CRUD de pipeline com steps/filtros aninhados (criar/ler/editar substituindo coleção/deletar com cascade); 409 nome duplicado se aplicável; 422 action_type/filter_type/operator inválido; 422 param obrigatório faltante por tipo; 404 pipeline ausente; dry-run/apply/undo (mockando o stage onde necessário para não tocar disco). Espelhar test_api_templates.py.
  </action>
  <verify>
    <automated>cd backend && . .venv/bin/activate && pytest tests/test_api_automations.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class PipelineIn\|class StepIn\|class StepFilterIn" backend/app/api/automations.py` == 3
    - `grep -c "AutomationRule\|RuleCondition\|ConditionIn\|RuleIn" backend/app/api/automations.py` == 0 (modelo antigo removido)
    - `grep -c "identify_type\|route\|move\|rename" backend/app/api/automations.py` >= 4 (action_types validados)
    - `grep -v '^#' backend/app/api/automations.py | grep -c "eval("` == 0
    - Endpoints dry-run/apply/undo presentes; 422 para action_type/filter_type/operator inválido testado
    - Suite alvo verde; ruff limpo; app importa (uvicorn não quebra: `python -c "from app.main import app"`)
  </acceptance_criteria>
  <done>API expõe CRUD aninhado de pipeline/steps/filtros + dry-run/apply/undo; valida conjuntos de action_type/filter_type/operator (V5); ações preservadas e fiadas ao apply_stage do pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campo extraído (IA/documento) → caminho de destino | valor não-confiável vira pasta/nome (path traversal) |
| params/filtros do step (operador) → API | input de configuração do operador (injeção/eval) |
| CAS → filesystem do cliente (NTFS) | materialização física sob falha parcial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Elevation/Tampering | naming.resolve_dest_folder (ação Move) | mitigate | confinamento is_relative_to(base_root) já implementado no tijolo; reusado tal-qual — V4 |
| T-06-05 | Tampering | run_pipeline / evaluate_filter | mitigate | dispatch EXPLÍCITO por action_type/filter_type/operator; NUNCA eval (gate `eval(` == 0); filter_type/operator desconhecido → falha fechada (não casa) — V5 |
| T-06-09 | Tampering/Denial | materialização (apply_stage) | mitigate | resolve_collision a montante (D-09 sufixo / D-10 skip); os.replace nunca sobrescreve cego; CAS como rede — já no tijolo fileops |
| T-06-12 | Denial/data loss | write-ahead apply_stage | mitigate | AuditLog(intent) + commit ANTES de materialize_to_dest (provado em teste test_intent_before_materialize); reconcile_orphans no startup |
| T-06-14 | Information disclosure | logs / API responses | mitigate | logar/retornar só ids/paths/run_id/status; NUNCA valores de campo ou full_text (V7/V9) |
| T-06-SC | Tampering | npm/pip installs | accept | NENHUMA dependência nova (stdlib + libs do projeto) — 06-RESEARCH §Standard Stack |
</threat_model>

<verification>
- `cd backend && . .venv/bin/activate && pytest tests/automation -x -q` verde (test_pipeline + test_stage + tijolos intactos)
- `pytest tests/test_api_automations.py -x -q` verde
- `python -c "from app.main import app"` sem erro (router registrado, imports sãos)
- `pytest -q` (suite completa) verde no merge da wave
</verification>

<success_criteria>
- Documento percorre TODAS as etapas cujo filtro casa, em ordem (D-12)
- Materialização ÚNICA ao final (Open Q1); [Move,Rename]==[Rename,Move] (Pitfall 8)
- Route interrompe sem materializar (Pitfall 9); no-match = no-op explícito (Pitfall 10)
- Gate identify_type reusa classificação sem re-cobrar IA (D-15)
- Dry-run simula pipeline inteiro sem disco (AUT-03); write-ahead + undo por-doc/run intactos (AUT-04/05); cross-device via CAS (AUT-06)
- API CRUD aninhado de pipeline + ações; valida conjuntos (V5)
</success_criteria>

<output>
Create `.planning/phases/06-automa-es-de-arquivo-renomear-mover/06-07-SUMMARY.md` when done
</output>
