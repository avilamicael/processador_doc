---
phase: 02-ingest-o-e-fila-ass-ncrona
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/models/enums.py
  - backend/app/models/watched_folder.py
  - backend/app/models/ingested_original.py
  - backend/app/models/job.py
  - backend/app/models/document.py
  - backend/app/models/__init__.py
  - backend/alembic/versions/0002_ingestion.py
  - backend/tests/test_migrations.py
  - backend/tests/test_stabilizer.py
  - backend/tests/test_splitter.py
  - backend/tests/test_dedup_gate.py
  - backend/tests/test_queue.py
  - backend/tests/test_ingest_stage.py
  - backend/tests/test_watcher.py
  - backend/tests/conftest.py
autonomous: true
requirements: [ING-05, ING-06, PROC-02, PROC-03]
user_setup: []

must_haves:
  truths:
    - "As três novas tabelas (watched_folders, ingested_originals, jobs) existem no schema versionado via Alembic"
    - "documents.origin_original_id existe como FK nullable para ingested_originals"
    - "watchfiles e pikepdf estão instalados e importáveis no venv"
    - "Os arquivos de teste Wave 0 existem e são coletáveis pelo pytest"
  artifacts:
    - path: "backend/app/models/job.py"
      provides: "Modelo Job (fila durável) com UNIQUE(original_hash, step)"
      contains: "class Job"
    - path: "backend/app/models/ingested_original.py"
      provides: "Gate de dedup pré-split com original_hash unique"
      contains: "class IngestedOriginal"
    - path: "backend/app/models/watched_folder.py"
      provides: "Config de pasta monitorada (path + pages_per_block + active)"
      contains: "class WatchedFolder"
    - path: "backend/alembic/versions/0002_ingestion.py"
      provides: "Migração das novas tabelas + coluna origin_original_id"
      contains: "down_revision"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "Job, IngestedOriginal, WatchedFolder"
      via: "imports + __all__ (registro em Base.metadata para Alembic)"
      pattern: "from app.models.job import Job"
    - from: "backend/alembic/versions/0002_ingestion.py"
      to: "0001"
      via: "down_revision = '0001'"
      pattern: "down_revision.*0001"
---

<objective>
Estabelecer o substrato de schema da Fase 2: três novas tabelas (`watched_folders`, `ingested_originals`, `jobs`), o enum `JobStatus`, a coluna de vínculo `documents.origin_original_id`, e a migração Alembic `0002` que as cria sobre a revisão `0001`. Instalar as duas dependências novas (`watchfiles`, `pikepdf`) e criar os esqueletos de teste Wave 0 que as ondas seguintes preencherão.

Purpose: Toda a costura da Fase 2 (fila, dedup gate, separação, watcher, UI) depende destas tabelas e dependências existirem primeiro. Schema só evolui por Alembic (D-10 da Fase 1) — nada de `create_all` em produção. Sem isto, nenhum plano posterior pode persistir estado.

Output: Modelos SQLAlchemy 2.0 registrados, migração `0002` reversível, venv com watchfiles+pikepdf, e arquivos de teste Wave 0 coletáveis (vazios/skeleton) para satisfazer a Nyquist Rule das ondas seguintes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-CONTEXT.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-RESEARCH.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-PATTERNS.md

<interfaces>
<!-- Contratos da Fase 1 que estes modelos reusam/estendem. Extraídos do código. -->

De backend/app/models/enums.py — JobStatus será adicionado neste mesmo arquivo, ao lado de:
  class DocState(str, Enum): RECEBIDO="recebido" PROCESSANDO="processando" EM_REVISAO="em_revisao" CONCLUIDO="concluido" QUARENTENA="quarentena" FALHA="falha"

De backend/app/models/document.py — padrão SAEnum a copiar para Job.status (linhas 44-56):
  SAEnum(DocState, name="ck_documents_doc_state", native_enum=False, create_constraint=True, values_callable=lambda enum: [member.value for member in enum], length=20)
  content_hash: Mapped[str] = mapped_column(String(64), index=True, unique=True, nullable=False)
  timestamps created_at/updated_at via server_default=func.now() (+onupdate=func.now() em updated_at)

De backend/app/models/audit_log.py — padrão FK nullable a copiar:
  document_id: Mapped[int | None] = mapped_column(ForeignKey("documents.id", ondelete="SET NULL"), index=True, nullable=True)

De backend/app/storage/db.py — Base é a base declarativa para todos os modelos:
  from app.storage.db import Base

De backend/alembic/versions/0001_initial.py — estrutura da migração a copiar (header revision/down_revision; op.create_table + with op.batch_alter_table(...) as batch_op: batch_op.create_index(...); trigger trg_documents_updated_at criado nas linhas 84-90).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências + esqueletos de teste Wave 0</name>
  <read_first>
    - backend/pyproject.toml (dependências e [tool.pytest.ini_options])
    - backend/tests/conftest.py (fixtures `sqlite_url`/`engine` existentes)
    - backend/tests/test_state_machine.py (padrão de fixture `schema_engine` com create_all permitido só em teste)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-RESEARCH.md §"Validation Architecture" e §"Wave 0 Gaps"
  </read_first>
  <action>
    Instalar as dependências novas no venv do backend com `cd backend && uv add watchfiles==1.2.0 pikepdf==10.8.0` (versões VERIFIED na pesquisa; MIT e MPL-2.0 — não usar watchdog nem PyMuPDF). Confirmar que entram em `pyproject.toml` `[project].dependencies` e no lockfile.
    Criar os arquivos de teste Wave 0 como skeletons coletáveis (cada um com pelo menos uma função de teste que pode ser `pytest.mark.skip(reason="Wave 0 stub — implementado no plano NN")` ou um `assert True` placeholder, NUNCA falhando): `tests/test_stabilizer.py`, `tests/test_splitter.py`, `tests/test_dedup_gate.py`, `tests/test_queue.py`, `tests/test_ingest_stage.py`, `tests/test_watcher.py`. Cada skeleton importa do alvo futuro só dentro da função de teste (lazy) para não quebrar a coleta antes do módulo existir. Confirmar `asyncio_mode="auto"` já no pyproject — nenhum decorator de asyncio necessário.
    Adicionar à `tests/conftest.py` uma fixture `schema_engine` (se ainda não existir) que faz `Base.metadata.create_all(engine)` / `drop_all` no teardown, no estilo de `test_state_machine.py` linhas 29-36 — create_all SÓ em teste; produção é Alembic.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -c "import watchfiles, pikepdf; print(watchfiles.__version__, pikepdf.__version__)" && .venv/bin/python -m pytest tests/ -q --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -c "import watchfiles, pikepdf"` sai com código 0
    - `grep -q "watchfiles" backend/pyproject.toml && grep -q "pikepdf" backend/pyproject.toml` verdadeiro
    - `pytest --collect-only` coleta os 6 novos arquivos de teste sem ImportError
    - Suite existente continua verde: `cd backend && .venv/bin/python -m pytest tests/ -q` exit 0
  </acceptance_criteria>
  <done>watchfiles+pikepdf instaláveis e importáveis; 6 skeletons de teste coletáveis; fixture schema_engine disponível; suite Fase 1 intacta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Definir JobStatus + três modelos novos + coluna de vínculo</name>
  <read_first>
    - backend/app/models/enums.py (DocState — adicionar JobStatus no mesmo arquivo/estilo)
    - backend/app/models/document.py (SAEnum, content_hash unique, timestamps, __init__ default)
    - backend/app/models/usage.py (tabela simples — analog de watched_folder)
    - backend/app/models/audit_log.py (FK nullable ondelete SET NULL)
    - backend/app/models/__init__.py (registro de modelos)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-PATTERNS.md §"app/models/*" (atribuições exatas)
  </read_first>
  <behavior>
    - JobStatus(str, Enum) tem membros pending/running/done/failed com valores string "pending"|"running"|"done"|"failed"
    - WatchedFolder instanciável com path, pages_per_block=None, active default True; created_at/updated_at presentes
    - IngestedOriginal rejeita dois registros com o mesmo original_hash (IntegrityError) — prova o gate D-09
    - Job rejeita dois registros com o mesmo par (original_hash, step) — prova a idempotência PROC-03
    - Document aceita atribuir origin_original_id e ele persiste como nullable
  </behavior>
  <action>
    Em `enums.py`, adicionar `class JobStatus(str, Enum)` com membros `PENDING="pending"`, `RUNNING="running"`, `DONE="done"`, `FAILED="failed"` (mesmo estilo `(str, Enum)` e docstring citando PROC-02/PROC-03).
    Criar `app/models/watched_folder.py` (D-02): tabela `watched_folders` com `id` PK; `path: Mapped[str] = mapped_column(String, unique=True, nullable=False, index=True)`; `pages_per_block: Mapped[int | None] = mapped_column(Integer, nullable=True)` (None = "não separar", default da UI); `active: Mapped[bool] = mapped_column(default=True, server_default=...)`; timestamps `created_at`/`updated_at` copiados verbatim de document.py. Docstring cita D-02/D-05.
    Criar `app/models/ingested_original.py` (D-09): tabela `ingested_originals` com `id` PK; `original_hash: Mapped[str] = mapped_column(String(64), unique=True, index=True, nullable=False)` (o gate); `original_filename: Mapped[str]`; `source_folder_id: Mapped[int | None] = mapped_column(ForeignKey("watched_folders.id", ondelete="SET NULL"), index=True, nullable=True)`; `block_count: Mapped[int] = mapped_column(Integer, default=0, server_default="0")`; `duplicate_hits: Mapped[int] = mapped_column(Integer, default=0, server_default="0")` (alimenta o contador D-10); `created_at`. Docstring cita D-08/D-09/D-10.
    Criar `app/models/job.py` (PROC-02/PROC-03): tabela `jobs` com `id` PK; `original_hash: Mapped[str] = mapped_column(String(64), index=True, nullable=False)`; `step: Mapped[str] = mapped_column(String, default="ingest", server_default="ingest", nullable=False)`; `payload: Mapped[str] = mapped_column(Text, nullable=False)` (JSON com source_path/folder_id/pages_per_block); `status: Mapped[JobStatus]` via SAEnum (`name="ck_jobs_status"`, native_enum=False, create_constraint=True, values_callable, length=10) default `JobStatus.PENDING` + server_default `"pending"`; `attempts: Mapped[int] default 0`; `max_attempts: Mapped[int] default 5`; `next_run_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), index=True, nullable=False)`; `last_error: Mapped[str | None] = mapped_column(Text, nullable=True)`; timestamps created_at/updated_at. Adicionar `__table_args__ = (UniqueConstraint("original_hash", "step", name="uq_jobs_hash_step"),)`. Docstring cita PROC-02/PROC-03.
    Em `app/models/document.py`, adicionar coluna `origin_original_id: Mapped[int | None] = mapped_column(ForeignKey("ingested_originals.id", ondelete="SET NULL"), index=True, nullable=True)` (vínculo bloco→original, RESEARCH Open Question 1). Docstring atualizada citando D-09.
    Em `app/models/__init__.py`, adicionar imports e entradas em `__all__` para `IngestedOriginal`, `Job`, `JobStatus`, `WatchedFolder` (ordem alfabética) — sem isto o Alembic autogenerate não vê as tabelas (env.py importa app.models).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_models.py -q && .venv/bin/python -c "from app.models import Job, IngestedOriginal, WatchedFolder, JobStatus; from app.models.document import Document; assert hasattr(Document, 'origin_original_id')"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.models import Job, IngestedOriginal, WatchedFolder, JobStatus` importa sem erro
    - `Job.__table_args__` contém UniqueConstraint nomeado `uq_jobs_hash_step` (grep `uq_jobs_hash_step` em job.py)
    - `IngestedOriginal.original_hash` é unique=True (grep `unique=True` em ingested_original.py)
    - `Document` tem atributo `origin_original_id`
    - Os 4 nomes novos aparecem em `app/models/__init__.py` `__all__`
  </acceptance_criteria>
  <done>JobStatus + 3 modelos definidos com constraints corretas, coluna de vínculo em Document, registro completo em __init__.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migração Alembic 0002 + teste de round-trip</name>
  <read_first>
    - backend/alembic/versions/0001_initial.py (estrutura exata a copiar; trigger trg_documents_updated_at)
    - backend/alembic/env.py (render_as_batch=True; import app.models)
    - backend/tests/test_migrations.py (conjunto ESPERADAS + padrão de round-trip up/down/up)
    - backend/app/models/job.py, ingested_original.py, watched_folder.py, document.py (do Task 2 — fonte do schema)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-PATTERNS.md §"alembic/versions/0002_*.py"
  </read_first>
  <behavior>
    - upgrade head cria watched_folders, ingested_originals, jobs (além das 4 da Fase 1)
    - documents passa a ter coluna origin_original_id após upgrade
    - jobs tem índice único uq_jobs_hash_step
    - downgrade base remove as 3 novas tabelas e a coluna origin_original_id, deixando o schema da Fase 1
    - round-trip up→down→up é idempotente
    - o trigger trg_documents_updated_at sobrevive ao batch-recreate de documents (UPDATE em documents ainda carimba updated_at)
  </behavior>
  <action>
    Gerar a migração com `cd backend && .venv/bin/python -m alembic revision --autogenerate -m "ingestion: jobs, watched_folders, ingested_originals + documents.origin_original_id"` e renomear/ajustar o arquivo para `0002_ingestion.py` com `revision = "0002"` e `down_revision = "0001"`.
    Revisar o autogenerate à mão (CLAUDE.md / D-10): cada `create_table` usa `op.create_table(...)` e os índices via `with op.batch_alter_table(name) as batch_op: batch_op.create_index(...)` (modo batch obrigatório no SQLite — env.py linha render_as_batch). O enum `jobs.status` renderiza inline como CHECK (igual a 0001 linha 32). O UniqueConstraint `uq_jobs_hash_step` deve constar no create_table de jobs.
    Para adicionar `documents.origin_original_id` (FK nova): usar `with op.batch_alter_table("documents") as batch_op: batch_op.add_column(sa.Column("origin_original_id", sa.Integer(), nullable=True)); batch_op.create_foreign_key(...); batch_op.create_index(...)`. ATENÇÃO ao caveat do trigger: o batch recreate de `documents` pode dropar `trg_documents_updated_at` (criado em 0001 linhas 84-90). No `upgrade()`, após o batch, recriar o trigger com o mesmo DDL de 0001 se `op.get_bind().dialect.name == "sqlite"` (drop IF EXISTS + create), e no `downgrade()` reverter (remover a coluna/FK via batch e recriar/garantir o trigger conforme estado 0001). Ordem de criação respeita FKs: `watched_folders` antes de `ingested_originals` (FK), `ingested_originals` antes da coluna em `documents`.
    Estender `tests/test_migrations.py`: adicionar `"watched_folders", "ingested_originals", "jobs"` ao set `ESPERADAS`; adicionar um teste que após `upgrade head` confirma `origin_original_id` em `inspect(engine).get_columns("documents")` e que o índice `uq_jobs_hash_step` existe; garantir que o teste existente `test_round_trip_up_down_up` e `test_updated_at_avanca_em_update_via_sql_cru` continuam passando (prova o trigger preservado).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "down_revision.*['\"]0001['\"]" backend/alembic/versions/0002_ingestion.py` casa
    - `pytest tests/test_migrations.py -q` exit 0 (inclui round-trip up/down/up e trigger updated_at)
    - `ESPERADAS` em test_migrations.py inclui as 3 novas tabelas (grep)
    - Após upgrade, `inspect.get_columns("documents")` inclui `origin_original_id`
  </acceptance_criteria>
  <done>Migração 0002 reversível criando as 3 tabelas + coluna de vínculo, preservando o trigger; test_migrations verde com as novas tabelas no round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → DB | DDL gerado por autogenerate pode divergir do modelo; revisão manual obrigatória (D-10) |
| Dependency install → venv | Pacotes de terceiros (watchfiles, pikepdf) entram no runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Migração 0002 corrompe schema/perde trigger updated_at | mitigate | Revisão manual do autogenerate; teste de round-trip up/down/up + teste do trigger updated_at via SQL cru (test_migrations.py) |
| T-02-02 | Tampering | Modelo não registrado em __init__.py → tabela ausente no schema versionado | mitigate | Task 2 registra os 3 modelos em imports + __all__; test_migrations assert ESPERADAS.issubset |
| T-02-SC | Tampering | Supply chain dos pacotes instalados (watchfiles, pikepdf) | accept | Ambos `[OK]` no slopcheck e Approved no Package Legitimacy Audit (RESEARCH); versões pinadas VERIFIED em PyPI (MIT / MPL-2.0); sem `[ASSUMED]`/`[SUS]` — checkpoint não requerido |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/ -q` — suite inteira verde (Fase 1 + migrações novas)
- `cd backend && .venv/bin/python -m alembic upgrade head && .venv/bin/python -m alembic downgrade -1` — migração aplica e reverte
- Confirmar SQLite ≥ 3.35 no venv (A1): `cd backend && .venv/bin/python -c "import sqlite3; assert sqlite3.sqlite_version_info >= (3,35), sqlite3.sqlite_version"` (já confirmado 3.50.4 no ambiente alvo)
</verification>

<success_criteria>
- 3 tabelas novas + coluna `documents.origin_original_id` no schema versionado via Alembic 0002 (não create_all)
- JobStatus + 3 modelos importáveis e registrados em Base.metadata
- watchfiles==1.2.0 e pikepdf==10.8.0 instalados
- 6 arquivos de teste Wave 0 coletáveis
- Round-trip de migração idempotente e trigger updated_at preservado
</success_criteria>

<output>
Create `.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-01-SUMMARY.md` when done
</output>
