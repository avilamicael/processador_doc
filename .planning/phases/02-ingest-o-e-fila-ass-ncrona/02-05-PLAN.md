---
phase: 02-ingest-o-e-fila-ass-ncrona
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useDocuments.ts
  - frontend/src/hooks/useWatchedFolders.ts
  - frontend/src/types.ts
  - frontend/src/components/StatusPill.tsx
  - frontend/src/pages/DocumentsPage.tsx
  - frontend/src/pages/ConfigPage.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [ING-02, ING-06]

must_haves:
  truths:
    - "A tela Documentos mostra documentos reais com seu estado de domínio, atualizando por polling 3-5s sem flicker"
    - "A tela mostra o contador de 'duplicados ignorados' de forma neutra (não alerta)"
    - "Configurações → Pastas monitoradas permite adicionar/editar/remover/ativar pastas reais (path + páginas/bloco), persistindo no backend"
    - "Estados são rotulados conforme o domínio real; nada exibe 'Tratado/Concluído' nesta fase; o terminal aparece como 'Aguardando extração'"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "cliente fetch tipado para a API do backend"
      contains: "fetch"
    - path: "frontend/src/hooks/useDocuments.ts"
      provides: "hook TanStack Query com polling para documentos + duplicados"
      contains: "useQuery"
    - path: "frontend/src/components/StatusPill.tsx"
      provides: "pílula mapeando estados de domínio reais → label → token --st-*"
      contains: "aguardando"
  key_links:
    - from: "frontend/src/pages/DocumentsPage.tsx"
      to: "useDocuments (GET /documents)"
      via: "hook de polling TanStack Query"
      pattern: "useDocuments"
    - from: "frontend/src/pages/ConfigPage.tsx"
      to: "useWatchedFolders (CRUD /watched-folders)"
      via: "hook TanStack Query + mutations"
      pattern: "useWatchedFolders|useMutation"
    - from: "frontend/src/main.tsx"
      to: "QueryClientProvider"
      via: "wrap do App"
      pattern: "QueryClientProvider"
---

<objective>
Conectar as duas telas em escopo (Documentos e Configurações → Pastas monitoradas) à API real da Fase 2, instalando e configurando a camada de dados net-new (TanStack Query 5.101 + cliente fetch tipado) e reusando o design system travado. Documentos atualiza por polling sem flicker e mostra o contador de duplicados; Pastas é um CRUD real persistido no backend. Cobre a parte de UI de ING-02 (ver documentos entrando na fila + configurar pastas) e ING-06 (visibilidade de duplicados).

Purpose: É a face do usuário para o critério de sucesso "configura pastas pela interface e vê os documentos entrando na fila com seu estado". O design já está aprovado e implementado (UI-SPEC LOCKED); este plano apenas troca os mocks por dados reais sem inventar visual novo.

Output: camada `src/lib/api.ts` + `src/hooks/*`, `QueryClientProvider` em `main.tsx`, tipos de domínio reais, `StatusPill`/`DocumentsPage`/`ConfigPage` fiados à API, e um checkpoint de verificação visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-UI-SPEC.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-PATTERNS.md

<interfaces>
<!-- Contratos a reusar. Frontend existente + API do backend (Plano 04). -->

Endpoints do backend (Plano 04):
  GET /watched-folders -> [{id, path, pages_per_block, active, created_at}]
  POST /watched-folders {path, pages_per_block, active}; PATCH /watched-folders/{id}; DELETE /watched-folders/{id}
  GET /documents -> {items: [{id, original_filename, state, last_completed_step, source_folder_path, created_at, size?}], counts: {recebido, processando, em_revisao, concluido, quarentena, falha, total}}
  GET /documents/duplicates-count -> {count}
  POST /rescan -> {enqueued}

De frontend/src/main.tsx — root atual a estender:
  createRoot(...).render(<StrictMode><App /></StrictMode>)  // envolver App com <QueryClientProvider>

De frontend/src/components/StatusPill.tsx — atual (mock): usa DocStatus + STATUS_LABELS de data/mock; estilo token-driven `var(--st-${status})`/`var(--st-${status}-bg)`. Estender para estados de domínio reais.

De frontend/src/types.ts — substituir o DocStatus mock ('encontrado'|'leitura'|'tratado'|'erro') pela união de domínio real.

De frontend/src/pages/DocumentsPage.tsx — manter estrutura visual (stat-grid, table-toolbar/chips, table.docs, table-foot); trocar import de DOCS (mock) por hook; props atuais: {search, status, onStatus, selected, onToggleSel, onToggleAll}.

De frontend/src/pages/ConfigPage.tsx — PastasTab (linhas 48-94): reusar .folder-row/.folder-icon/Switch; trocar FOLDERS (mock) por hook; NÃO fiar RegrasTab (fora de escopo v2).

De frontend/src/App.tsx — estado local de filtros/seleção/folderState; ajustar para passar dados reais aos componentes.

UI-SPEC mapeamento de estados (autoritativo):
  RECEBIDO→"Na fila"/--st-encontrado; PROCESSANDO→"Processando"/--st-leitura;
  terminal (processando + last_completed_step=="aguardando_extracao")→"Aguardando extração"/--st-encontrado (azul, muted, nunca verde);
  FALHA→"Falha"/--st-erro; CONCLUIDO→"Concluído"/--st-tratado (NÃO alcançável nesta fase — reservar token);
  EM_REVISAO/QUARENTENA→--st-leitura (forward-compat, não produzidos na Fase 2).
  Copy pt-BR, contador de duplicados neutro (--text-3), polling 3-5s com keepPreviousData/placeholderData (sem flicker), refetchIntervalInBackground:false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar TanStack Query + cliente API + hooks + tipos de domínio</name>
  <read_first>
    - frontend/package.json (dependências atuais: só react/react-dom)
    - frontend/src/main.tsx (root StrictMode a envolver)
    - frontend/src/types.ts (tipos mock a substituir)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-PATTERNS.md §"Frontend" e §"src/lib/api.ts + src/hooks/*"
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-UI-SPEC.md (mapeamento de estados, polling, copy)
  </read_first>
  <files>frontend/package.json, frontend/src/main.tsx, frontend/src/lib/api.ts, frontend/src/hooks/useDocuments.ts, frontend/src/hooks/useWatchedFolders.ts, frontend/src/types.ts</files>
  <action>
    Instalar a dependência: `cd frontend && npm i @tanstack/react-query` (versão 5.101 da stack CLAUDE.md). Confirmar entrada em `package.json` dependencies.
    Em `src/main.tsx`, criar um `QueryClient` e envolver `<App />` com `<QueryClientProvider client={queryClient}>` dentro do `StrictMode` existente (analog PATTERNS).
    Criar `src/lib/api.ts`: cliente fetch tipado com base URL relativa (single-origin — FastAPI serve o front em prod); funções `getDocuments()`, `getDuplicatesCount()`, `getWatchedFolders()`, `createWatchedFolder(body)`, `updateWatchedFolder(id, body)`, `deleteWatchedFolder(id)`, `postRescan()`. Cada uma faz fetch, checa `res.ok` (lança em erro para o TanStack Query tratar) e retorna JSON tipado. (openapi-fetch é opcional — fetch tipado manual é aceitável.)
    Em `src/types.ts`: substituir `DocStatus` mock pela união de domínio real `DocState = 'recebido' | 'processando' | 'em_revisao' | 'concluido' | 'quarentena' | 'falha'`; ajustar/`Doc` e `Folder` para as formas da API (Doc: id, original_filename/name, source_folder_path, state, last_completed_step, size?, created_at; Folder: id, path, pages_per_block, active). Remover campos mock-only (type/tmpl/who/rec/freq/last). Manter o estilo `export interface`/`export type`.
    Criar `src/hooks/useDocuments.ts` (TanStack Query): `useDocuments()` com `useQuery({queryKey:['documents'], queryFn: getDocuments, refetchInterval: 4000, refetchIntervalInBackground: false, placeholderData: (prev)=>prev})` (sem flicker) e `useDuplicatesCount()` análogo; `useRescan()` como `useMutation` que invalida `['documents']`. Criar `src/hooks/useWatchedFolders.ts`: `useWatchedFolders()` (query) + mutations `useCreateFolder`/`useUpdateFolder`/`useDeleteFolder` que invalidam `['watched-folders']`.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@tanstack/react-query" frontend/package.json` verdadeiro
    - `grep -q "QueryClientProvider" frontend/src/main.tsx` verdadeiro
    - `grep -q "refetchInterval" frontend/src/hooks/useDocuments.ts` verdadeiro
    - `cd frontend && npm run build` exit 0 (tsc + vite build sem erro de tipos)
    - types.ts não contém mais a união mock 'encontrado'|'leitura'|'tratado'|'erro'
  </acceptance_criteria>
  <done>TanStack Query instalado e provido; cliente fetch tipado + hooks de documentos/pastas com polling sem flicker; tipos de domínio reais; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Fiar StatusPill + DocumentsPage + ConfigPage(PastasTab) aos dados reais</name>
  <read_first>
    - frontend/src/components/StatusPill.tsx (token-driven; estender mapeamento)
    - frontend/src/pages/DocumentsPage.tsx (estrutura a preservar; trocar DOCS por hook)
    - frontend/src/pages/ConfigPage.tsx (PastasTab; trocar FOLDERS por hook; não fiar RegrasTab)
    - frontend/src/App.tsx (estado de filtros/seleção a ajustar)
    - frontend/src/index.css (tokens travados — usar var(--token), nunca hex)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-UI-SPEC.md (copy pt-BR, colunas, empty/loading/error, confirmação destrutiva, contador neutro)
  </read_first>
  <files>frontend/src/components/StatusPill.tsx, frontend/src/pages/DocumentsPage.tsx, frontend/src/pages/ConfigPage.tsx, frontend/src/App.tsx</files>
  <action>
    `StatusPill.tsx`: estender para aceitar o estado de domínio real + (opcionalmente) `last_completed_step`. Mapear estado→label→token conforme a tabela do UI-SPEC: recebido→"Na fila"/--st-encontrado; processando→"Processando"/--st-leitura; processando+last_completed_step=="aguardando_extracao"→"Aguardando extração"/--st-encontrado (azul muted, nunca verde); falha→"Falha"/--st-erro; em_revisao/quarentena→--st-leitura; concluido→"Concluído"/--st-tratado (reservar, não setado nesta fase). Manter o estilo `var(--st-${token})`/`var(--st-${token}-bg)`.
    `DocumentsPage.tsx`: substituir o import de `DOCS` (mock) por `useDocuments()`/`useDuplicatesCount()`/`useRescan()`. Preservar a estrutura visual (stat-grid, toolbar/chips, table.docs, table-foot). Colunas em escopo v1: Arquivo (file-name mono + docMini), Pasta de origem (mono), Status (StatusPill com estado real), Tamanho (mono), Data. REMOVER/ocultar colunas Tipo/Template/Responsável. Chips/stat-cards usam os `counts` da API rotulados como Na fila/Processando/Aguardando extração/Falha. Botão "Forçar varredura" (.btn-primary + ícone refresh) chama `useRescan()`. Estados: loading inicial = linhas skeleton dentro da table.docs (não spinner de tela); refetch de background silencioso (opcional "Atualizando…" em .table-foot com --text-3, nunca limpar a tabela); empty = copy "Nenhum documento ainda" centrado no corpo da tabela; error = copy do UI-SPEC com botão "Tentar novamente" (refetch), dentro do card. Contador de duplicados no .table-foot com --text-3 (neutro, não alerta), texto "{n} duplicado(s) ignorado(s)" + title do UI-SPEC. Escapar `original_filename` ao renderizar (T-02-11). Ações de linha sem função nesta fase: ocultar (não exibir desabilitadas sem propósito).
    `ConfigPage.tsx` (só PastasTab): substituir `FOLDERS` (mock) por `useWatchedFolders()`. Reusar .folder-row/.folder-icon/Switch. Meta por linha: regra de separação ("Separar a cada N páginas" ou "Não separar") + ativa (Switch → updateFolder). "Adicionar pasta" (.btn-primary) abre form (modal/inline reusando .card/.search-input/.select/.btn-primary/.btn-ghost) com campos Caminho da pasta (input mono, placeholder `C:\Documentos\Entrada`) e "Separar a cada N páginas" (stepper/select; "Não separar" = default/zero) → createFolder. Remover usa a confirmação destrutiva do UI-SPEC (botões "Manter pasta"/"Remover" com --st-erro) → deleteFolder. NÃO fiar RegrasTab/LeituraTab/IntegracoesTab (fora de escopo). Remover do mock os valores fabricados (rec/types/freq/last) — não exibir placeholders. Watcher status box: manter visual; só refletir estado real se a API expuser, senão estático (não fabricar "última varredura há 2 min").
    Ajustar `App.tsx` conforme necessário para passar/ler os dados reais (ex.: filtro de status sobre os estados de domínio reais; remover dependência do folderState mock se o Switch agora persiste via mutation). Não redesenhar o shell.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useDocuments" frontend/src/pages/DocumentsPage.tsx` verdadeiro e `grep -q "DOCS" frontend/src/pages/DocumentsPage.tsx` FALSO (mock removido)
    - `grep -qi "aguardando" frontend/src/components/StatusPill.tsx` verdadeiro
    - `grep -q "useWatchedFolders" frontend/src/pages/ConfigPage.tsx` verdadeiro
    - nenhum documento é renderizado com label "Tratado" (estado concluido não é setado nesta fase)
    - `cd frontend && npm run build` exit 0
  </acceptance_criteria>
  <done>StatusPill mapeia estados reais (inclui "Aguardando extração", nunca verde nesta fase); Documentos lê dados reais por polling com empty/loading/error e contador de duplicados neutro; PastasTab é CRUD real; build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual end-to-end (Documentos + Pastas)</name>
  <files>(verificação manual — nenhum arquivo modificado)</files>
  <action>
    Esta é uma verificação humana APÓS a automação dos Tasks 1-2 e do backend (Planos 01-04). O executor não escreve código nesta task; apenas sobe os serviços, prepara as instruções abaixo para o usuário e PAUSA até o sinal de retomada. Toda a implementação que habilita esta verificação já foi feita autonomamente.
  </action>
  <what-built>
    Backend da Fase 2 (watcher+fila+split+dedup, Planos 01-04) e o frontend fiado (Plano 05 Tasks 1-2). O sistema agora monitora pastas reais, ingere/separa documentos e os mostra na UI por polling, com CRUD de pastas e contador de duplicados.
  </what-built>
  <how-to-verify>
    1. Subir o backend: `cd backend && .venv/bin/python -m uvicorn app.main:app --workers 1` (1 worker — Pitfall 5). Subir o frontend: `cd frontend && npm run dev`. Abrir a URL do Vite no navegador.
    2. Ir em Configurações → Pastas monitoradas. Clicar "Adicionar pasta", informar um caminho real existente (ex.: uma pasta de teste local com PDFs/imagens) e "Não separar"; salvar. Confirmar que a pasta aparece na lista (persistida).
    3. Copiar um PDF multi-página para a pasta. Editar a pasta para "Separar a cada 1 página" e copiar outro PDF. Voltar para a tela Documentos.
    4. Confirmar (sem recarregar a página) que os documentos aparecem entrando na fila e mudando de estado por polling (Na fila → Processando → Aguardando extração), SEM flicker/piscar da tabela. Nenhum documento deve aparecer como "Tratado"/"Concluído" (verde).
    5. Confirmar que o PDF separado virou N documentos independentes (1 por página).
    6. Copiar o MESMO PDF de novo (ou aguardar um rescan / clicar "Forçar varredura"). Confirmar que NÃO surgem documentos duplicados na lista e que o contador "{n} duplicados ignorados" (neutro, no rodapé da tabela) incrementa.
    7. Remover uma pasta: confirmar o diálogo destrutivo ("Manter pasta"/"Remover"); confirmar que os documentos já ingeridos PERMANECEM na lista (D-03).
    8. Verificar estados vazio/erro: parar o backend e confirmar a mensagem de erro com "Tentar novamente" dentro do card; reiniciar e confirmar recuperação.
  </how-to-verify>
  <verify>
    <human-check>Usuário confirma os 8 passos de verificação visual acima (estados por polling sem flicker, separação, dedup com contador, remoção preserva documentos, empty/erro).</human-check>
  </verify>
  <done>Usuário aprovou a verificação visual end-to-end das telas Documentos e Pastas monitoradas.</done>
  <resume-signal>Digite "approved" se tudo acima funciona como descrito, ou descreva os problemas observados (estado errado, flicker, duplicata aparecendo, perda de documento, visual divergente do design).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → UI | dados do backend renderizados; original_filename pode conter chars de controle |
| UI → API de pastas | path digitado pelo usuário enviado ao backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure / XSS | original_filename / path renderizados na UI | mitigate | React escapa por padrão ao interpolar texto (não usar dangerouslySetInnerHTML); nomes exibidos como texto puro |
| T-02-13 | Spoofing (UX) | UI exibe estado falso/otimista que diverge do backend | mitigate | fonte de verdade é a API por polling; mutations invalidam queries; sem otimismo que mascare falha (PROC-02) |
| T-02-SC | Tampering | supply chain do pacote frontend (@tanstack/react-query) | accept | pacote maduro e amplamente usado (stack travada CLAUDE.md); instalado por npm com lockfile |
</threat_model>

<verification>
- `cd frontend && npm run build` — build de produção verde (tsc + vite)
- Checkpoint humano (Task 3) aprovado
- Nenhum documento renderizado como "Tratado/Concluído" nesta fase (UI-SPEC)
</verification>

<success_criteria>
- TanStack Query instalado e provido; camada api/hooks net-new tipada
- Documentos: dados reais por polling 3-5s sem flicker, counts, empty/loading/error no card, contador de duplicados neutro
- Pastas: CRUD real persistido (path validado no backend), confirmação destrutiva, default "Não separar"
- Estados de domínio reais; "Aguardando extração" para o terminal; nunca "Tratado" nesta fase
- Checkpoint visual end-to-end aprovado
</success_criteria>

<output>
Create `.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-05-SUMMARY.md` when done
</output>
