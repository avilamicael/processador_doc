---
phase: 02-ingest-o-e-fila-ass-ncrona
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - backend/app/ingest/watcher.py
  - backend/app/api/__init__.py
  - backend/app/api/watched_folders.py
  - backend/app/api/documents.py
  - backend/app/main.py
  - backend/tests/test_watcher.py
  - backend/tests/test_api_watched_folders.py
  - backend/tests/test_api_documents.py
autonomous: true
requirements: [ING-02, ING-06, PROC-02]

must_haves:
  truths:
    - "O watcher detecta arquivos novos nas pastas ativas do DB, estabiliza, passa pelo dedup gate e enfileira um job"
    - "Watcher e worker sobem como asyncio.Task no lifespan do FastAPI e encerram limpo no shutdown"
    - "A UI pode criar/editar/remover/listar pastas monitoradas (path + páginas/bloco + ativa) via API, com path validado"
    - "A UI pode listar documentos com seu estado e ler o contador de duplicados ignorados"
    - "POST /rescan reescaneia as pastas ativas e re-emite candidatos idempotentemente"
  artifacts:
    - path: "backend/app/ingest/watcher.py"
      provides: "run_watcher: awatch sobre paths do DB → estabiliza → dedup gate → enqueue"
      contains: "async def run_watcher"
    - path: "backend/app/api/watched_folders.py"
      provides: "CRUD de pastas monitoradas com validação de path"
      contains: "router"
    - path: "backend/app/api/documents.py"
      provides: "GET /documents, GET /documents/duplicates-count, POST /rescan"
      contains: "duplicates-count"
  key_links:
    - from: "backend/app/main.py"
      to: "run_watcher + run_worker"
      via: "asyncio.create_task no lifespan + stop Event"
      pattern: "create_task"
    - from: "backend/app/ingest/watcher.py"
      to: "app.queue.repo.enqueue / app.pipeline.ingest_stage"
      via: "candidato estável → dedup gate → enqueue"
      pattern: "enqueue"
    - from: "backend/app/api/watched_folders.py"
      to: "watched_folders (DB)"
      via: "Path.resolve() valida + persiste"
      pattern: "resolve\\(\\)"
---

<objective>
Costurar a ingestão de ponta a ponta no backend: o **watcher** (watchfiles.awatch sobre as pastas ativas do DB → estabiliza → calcula hash do original → dedup gate → enqueue), o **wiring do lifespan** que sobe watcher+worker como asyncio.Task e os encerra limpo, e a **API fina** (CRUD de pastas, lista de documentos, contador de duplicados, rescan) que a UI consome. Cobre ING-02 (pastas configuráveis + processamento automático após estável), e expõe ING-06/PROC-02 à UI.

Purpose: Fecha o backend da Fase 2 — depois deste plano o sistema, rodando, monitora pastas reais e move arquivos pelo pipeline sem intervenção. A API é a fronteira que o Plano 05 (frontend) consome.

Output: `app/ingest/watcher.py`, `app/api/watched_folders.py`, `app/api/documents.py`, lifespan estendido em `main.py`, e testes de watcher (integração) + API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-CONTEXT.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-RESEARCH.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-PATTERNS.md
@.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-UI-SPEC.md

<interfaces>
<!-- Contratos a reusar/consumir. Extraídos do código (Fase 1 + Planos 01-03). -->

De backend/app/main.py — lifespan atual (estender, preservar bloco de verificação WAL linhas 26-29):
  @asynccontextmanager async def lifespan(app): settings=get_settings(); ensure_data_dir(settings);
    engine=create_db_engine(settings.effective_database_url); [verifica WAL]; app.state.engine=engine; yield; finally engine.dispose()
  app = FastAPI(title=..., version=__version__, lifespan=lifespan)
  @app.get("/health") usa app.state.engine + get_session

De backend/app/storage/db.py: get_session(engine) contextmanager; create_db_engine.
De backend/app/queue/repo.py (Plano 03): enqueue(session, *, original_hash, step="ingest", payload, max_attempts=None); requeue_running.
De backend/app/queue/worker.py (Plano 03): run_worker(engine, stop: asyncio.Event).
De backend/app/pipeline/ingest_stage.py (Plano 03): process_ingest(...); AWAITING_EXTRACTION_STEP.
De backend/app/ingest (Plano 02): wait_stable(path, window_s=None, poll_s=1.0) -> bool; is_supported_ext; SUPPORTED_EXTENSIONS.
De backend/app/models (Plano 01): WatchedFolder(path unique, pages_per_block, active); Document(state, last_completed_step, original_filename, content_hash, origin_original_id); IngestedOriginal(duplicate_hits); DocState.

watchfiles (RESEARCH Pattern 6 — VERIFIED): `async for changes in awatch(*paths, stop_event=stop): ...` — múltiplos paths + stop_event para shutdown.

UI-SPEC contratos relevantes:
  - estados de domínio expostos: recebido|processando|em_revisao|concluido|quarentena|falha (string do enum)
  - terminal Fase 2 = processando + last_completed_step "aguardando_extracao" → UI rotula "Aguardando extração"
  - endpoints: GET /watched-folders CRUD; GET /documents (lista blocos + counts por estado); GET /documents/duplicates-count; POST /rescan ("Forçar varredura")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watcher (awatch → estabiliza → hash → dedup gate → enqueue) + lifespan</name>
  <read_first>
    - backend/app/main.py (lifespan; padrão asyncio + dispose; bloco WAL a preservar)
    - backend/app/queue/repo.py, worker.py (Plano 03 — enqueue, run_worker, requeue_running)
    - backend/app/ingest/stabilizer.py (Plano 02 — wait_stable), splitter.py (is_supported_ext)
    - backend/app/models/watched_folder.py (Plano 01 — pastas ativas)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-RESEARCH.md §"Pattern 6" (lifespan + awatch), §"Pitfall 5" (1 worker uvicorn), §"Open Question 3" (rescan), §A5 (reconfig de pastas)
  </read_first>
  <action>
    Criar `app/ingest/watcher.py` com `async def run_watcher(engine, stop: asyncio.Event) -> None`. Ler as pastas ativas (`WatchedFolder.active == True`) do DB via `get_session`. Rodar `async for changes in awatch(*paths, stop_event=stop)`: para cada caminho adicionado/modificado que `is_supported_ext`, criar uma task/await que (1) `await wait_stable(path)` — se False, descartar (parcial/removido, Pitfall 1); (2) calcular o SHA-256 do original (mesma função stdlib hashlib usada pelo CAS — hash do ORIGINAL pré-split, D-09) via `await asyncio.to_thread(...)`; (3) checar o dedup gate: se `ingested_originals` já tem o hash → incrementar duplicate_hits (chamar o caminho "duplicate" do ingest_stage ou um helper) e NÃO enfileirar; senão `enqueue(session, original_hash=h, step="ingest", payload=json com source_path/folder_id/pages_per_block da pasta)`. O processamento pesado (store/split/Documents) é do worker (Plano 03) — o watcher só estabiliza, faz hash e enfileira. Para reconfiguração de pastas (D-02/A5): adotar a estratégia de **supervisor que relê a lista de pastas do DB periodicamente** (ex.: a cada N s) e reinicia o `awatch` quando o conjunto de paths muda — documentar a escolha na docstring; alternativa (reiniciar a task na mudança via API) é aceitável se mais simples. Tratar paths inexistentes sem derrubar o watcher (log + pular).
    Expor um helper reutilizável `async def scan_and_enqueue(engine, paths: list[Path]) -> int` que percorre os arquivos das pastas e passa cada um pelo mesmo caminho estabiliza→hash→gate→enqueue — usado tanto no startup quanto pelo endpoint `/rescan` (Plano Task 3, idempotente por dedup).
    Estender o `lifespan` de `main.py` (RESEARCH Pattern 6): após setar `app.state.engine`, criar `stop = asyncio.Event()`, `watcher_task = asyncio.create_task(run_watcher(engine, stop))`, `worker_task = asyncio.create_task(run_worker(engine, stop))`; guardar `stop`/tasks em `app.state`. No `finally`: `stop.set()`; `for t in (watcher_task, worker_task): t.cancel()`; `await asyncio.gather(watcher_task, worker_task, return_exceptions=True)`; então `engine.dispose()`. PRESERVAR o bloco de verificação WAL existente (linhas 26-29). Documentar o requisito `uvicorn --workers 1` (Pitfall 5) num comentário/docstring no main.py.
    Preencher `tests/test_watcher.py` (integração; pode ser marcado e usar tmp_path): criar uma pasta temporária, registrar uma WatchedFolder ativa apontando para ela, escrever um PDF/arquivo, rodar `scan_and_enqueue` (caminho determinístico em vez de depender do timing do awatch) e assertar que um Job pending foi criado; um segundo `scan_and_enqueue` do mesmo arquivo NÃO cria um segundo Job (dedup gate). Encurtar a janela de estabilização via env/monkeypatch para o teste rodar <60s.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_watcher.py -q && .venv/bin/python -c "from app.ingest.watcher import run_watcher, scan_and_enqueue; from app.main import app"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def run_watcher" backend/app/ingest/watcher.py` verdadeiro
    - `grep -q "create_task" backend/app/main.py` verdadeiro e o bloco WAL (PRAGMA journal_mode) permanece em main.py
    - `pytest tests/test_watcher.py -q` exit 0: 1º scan cria Job pending, 2º scan do mesmo arquivo não duplica (gate)
    - `from app.main import app` importa sem efeitos colaterais de rede
  </acceptance_criteria>
  <done>Watcher estabiliza, faz hash do original, aplica dedup gate e enfileira; lifespan sobe/derruba watcher+worker limpo preservando o check WAL; teste de scan idempotente verde.</done>
</task>

<task type="auto">
  <name>Task 2: API CRUD de pastas monitoradas (com validação de path)</name>
  <read_first>
    - backend/app/main.py (padrão de rota + app.state.engine + get_session; include_router a adicionar)
    - backend/app/models/watched_folder.py (Plano 01 — colunas)
    - backend/app/config.py (Settings; Pydantic para schemas de request/response)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-UI-SPEC.md §"Configurações → Pastas monitoradas" (campos, copy, default "Não separar")
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-RESEARCH.md §"Security Domain" (V5/V12 path traversal)
  </read_first>
  <action>
    Criar `app/api/__init__.py` (vazio) e `app/api/watched_folders.py` com um `APIRouter` (prefixo `/watched-folders`). Endpoints (API fina — só validar/ler/configurar, lógica fica fora): `GET /watched-folders` (lista), `POST /watched-folders` (cria: body Pydantic `{path: str, pages_per_block: int | None, active: bool=True}`), `PATCH /watched-folders/{id}` (edita path/pages_per_block/active), `DELETE /watched-folders/{id}` (remove monitoramento — NÃO apaga Documents, D-03). Usar `app.state.engine` + `get_session` (padrão de main.py). Definir schemas Pydantic de request/response (`WatchedFolderIn`/`WatchedFolderOut`).
    VALIDAÇÃO DE PATH (Security V5/V12 — T-02-10): no create/edit, normalizar com `Path(path).resolve()`; rejeitar (HTTP 422/400) caminhos vazios ou claramente inválidos; armazenar a forma resolvida; a unicidade de `path` (constraint do modelo) evita duplicatas — capturar IntegrityError → 409. Documentar que, no v1 single-tenant local, a pasta é escolha do operador, mas o formato/normalização é validado para barrar path traversal acidental e quebra de parsing.
    Registrar o router em `main.py` via `app.include_router(...)`.
    Criar `tests/test_api_watched_folders.py` usando `fastapi.testclient.TestClient` (httpx já é dependência dev) sobre o app com um engine de teste (sobrescrever `app.state.engine` ou usar a fixture de schema): testar POST cria + GET lista + PATCH edita + DELETE remove; POST com path duplicado → 409; POST com path inválido/vazio → 4xx. Garantir que os Documents não são apagados no DELETE (assert).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_api_watched_folders.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "resolve()" backend/app/api/watched_folders.py` verdadeiro (validação de path)
    - `grep -q "include_router" backend/app/main.py` verdadeiro
    - `pytest tests/test_api_watched_folders.py -q` exit 0 cobrindo CRUD + 409 duplicado + 4xx path inválido
    - DELETE de pasta não remove Documents (assert no teste)
  </acceptance_criteria>
  <done>CRUD de pastas funcional com path validado/normalizado e unicidade; router incluído; testes verdes; remover pasta preserva documentos.</done>
</task>

<task type="auto">
  <name>Task 3: API documentos — lista + contador de duplicados + rescan</name>
  <read_first>
    - backend/app/main.py (padrão de rota read-only /health)
    - backend/app/models/document.py, ingested_original.py (Plano 01 — estado, duplicate_hits)
    - backend/app/ingest/watcher.py (Task 1 — scan_and_enqueue para o rescan)
    - .planning/phases/02-ingest-o-e-fila-ass-ncrona/02-UI-SPEC.md §"Documentos" (colunas, counts por estado, duplicados, rescan)
  </read_first>
  <action>
    Criar `app/api/documents.py` com um `APIRouter`. Endpoints: `GET /documents` — retorna a lista de Documents (os blocos, D-06) com campos para a UI: `id`, `original_filename`, estado (`state` string do enum), `last_completed_step` (para a UI rotular "Aguardando extração" quando == AWAITING_EXTRACTION_STEP), pasta de origem (derivar via origin_original_id → ingested_originals.source_folder_id → watched_folders.path; pode ser nullable), `created_at`; e um bloco de contagens por estado (`counts`: recebido/processando/em_revisao/concluido/quarentena/falha + total) para os stat-cards/chips. Duplicatas NUNCA aparecem como linhas (D-10). Suportar paginação simples opcional (limit/offset) — a UI usa polling. `GET /documents/duplicates-count` — retorna `{count: SUM(ingested_originals.duplicate_hits)}` (D-10). `POST /rescan` — chama `scan_and_enqueue(engine, paths das pastas ativas)` e retorna quantos candidatos foram enfileirados; idempotente por dedup ("Forçar varredura" do UI-SPEC).
    Schemas Pydantic de response (`DocumentOut`, `DocumentListOut` com counts, `DuplicatesCountOut`). Manter rotas finas. Registrar o router em `main.py` (`include_router`).
    Criar `tests/test_api_documents.py` com TestClient: semear alguns Documents em estados variados + um IngestedOriginal com duplicate_hits>0; assertar `GET /documents` retorna as linhas (sem duplicatas) e counts corretos por estado; `GET /documents/duplicates-count` retorna a soma; `POST /rescan` retorna 200 e um inteiro de enfileirados (com uma pasta de teste vazia → 0).
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/test_api_documents.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "duplicates-count" backend/app/api/documents.py` verdadeiro
    - `grep -q "rescan" backend/app/api/documents.py` verdadeiro
    - `pytest tests/test_api_documents.py -q` exit 0: lista sem duplicatas, counts por estado, soma de duplicados, rescan idempotente
    - GET /documents inclui last_completed_step para a UI distinguir "Aguardando extração"
  </acceptance_criteria>
  <done>API expõe lista de documentos com counts, contador de duplicados e rescan; duplicatas fora da lista; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/HTTP → API de pastas | path da pasta é entrada não-confiável (path traversal / leitura arbitrária) |
| Pasta do cliente → watcher | arquivos do FS local; só PDF/JPG/PNG entram no pipeline |
| Lifespan → tasks | watcher/worker como asyncio.Task; shutdown deve ser limpo; 1 worker uvicorn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Elevation / Information Disclosure | path traversal no caminho da pasta (UI grava `..\..\Windows`) | mitigate | `Path.resolve()` + rejeição de caminho inválido/vazio na API (V5/V12); allowlist de extensão limita o que é lido; teste de path inválido → 4xx |
| T-02-11 | Information Disclosure | nome de arquivo com chars de controle exibido na UI | mitigate | CAS endereça por hash (nome nunca compõe o caminho de storage — Fase 1); `original_filename` é só metadado; a UI escapa ao renderizar (Plano 05) |
| T-02-12 | Denial of Service | múltiplos workers uvicorn duplicam watcher/worker | mitigate | documentar/forçar `uvicorn --workers 1` (Pitfall 5) em comentário no main.py; lifespan roda por processo |
| T-02-SC | Tampering | supply chain (watchfiles/pikepdf) | accept | Aprovados no Package Legitimacy Audit (slopcheck [OK]); pinados; sem [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/ -q` — suite inteira verde
- `cd backend && .venv/bin/python -c "from app.main import app; print([r.path for r in app.routes])"` — rotas /watched-folders, /documents, /documents/duplicates-count, /rescan presentes
- App sobe sem erro (lifespan): `cd backend && .venv/bin/python -c "import asyncio; from app.main import app"`
</verification>

<success_criteria>
- Watcher detecta→estabiliza→hash→dedup gate→enqueue; supervisor relê pastas do DB
- Lifespan sobe/derruba watcher+worker limpo, preservando o check WAL; 1 worker documentado
- CRUD de pastas com path validado (V5/V12) e unicidade; DELETE preserva Documents
- GET /documents (lista + counts), GET /documents/duplicates-count, POST /rescan idempotente
- Testes de watcher (scan idempotente) + API (pastas e documentos) verdes
</success_criteria>

<output>
Create `.planning/phases/02-ingest-o-e-fila-ass-ncrona/02-04-SUMMARY.md` when done
</output>
