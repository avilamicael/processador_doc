---
phase: 06.2-acao-de-automacao-copiar
verified: 2026-06-18T23:30:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Verificação visual ao vivo do fluxo completo copiar→dry-run→aplicar→desfazer"
    expected: "Cópia materializada no destino, original permanece na pasta de origem, badge 'Copiado — original mantido' no dry-run, undo apaga a cópia sem tocar o original"
    result: passed
    evidence: "Executado pelo orquestrador via Playwright contra o app rodando (uvicorn :8000 + vite :5173) com documento e blob de CAS reais. Editor mostra a opção Copiar + campo destino + nota 'O original permanece'; dry-run renderizou o badge 'Copiado — original mantido' (action_kind=copy, removes_original=false); aplicar materializou a cópia (855b, idêntica ao blob do CAS) com o original intacto + audit_log copy/done; desfazer apagou a cópia com o original nunca tocado + audit_log copy/undone. Screenshot: .playwright-mcp/dryrun-copiar-badge.png"
post_review_gap_closure:
  - finding: "CR-01 (BLOCKER, code review): rename+copy sem move removia o original via heurística has_effective_move — violava D-01 (perda de arquivo)"
    fix: "AutomationPlan.has_explicit_move (executor liga só no branch move); stage.py usa has_effective_move = (not copies) or has_explicit_move. Commit 81b3f80 + teste test_rename_copy_no_move_keeps_original."
  - finding: "WR-01..03 (frontend multi-saída): chave React duplicada + IDs duplicados em aplicar/seleção para copy+move"
    fix: "chave única por (document_id, action_kind, idx) + dedupe via Set. Commit abe119c."
  - note: "Suíte completa pós-fix: 401 passed. Frontend build limpo."
---

# Phase 6.2: Ação de Automação Copiar — Relatório de Verificação

**Phase Goal:** Adicionar a ação Copiar ao modelo Condições→Ações da Fase 6, mantendo Mover e Renomear sem regressão. Copiar materializa o arquivo no destino e deixa o original na origem (não remove). Reusa a infraestrutura da Fase 6 (fileops.materialize_to_dest, anti-colisão, audit write-ahead, undo, dry-run).
**Verified:** 2026-06-18T23:30:00Z
**Status:** passed (após gap-closure pós-revisão — ver frontmatter `post_review_gap_closure`)
**Re-verification:** Sim — checkpoint visual aprovado via Playwright + BLOCKER CR-01 corrigido.

---

## Goal Achievement

### Observable Truths (D-01..D-07)

| # | Truth (Decision) | Status | Evidence |
|---|-----------------|--------|----------|
| 1 | D-01: Copiar materializa no destino via `materialize_to_dest` SEM chamar `remove_original`; original permanece intacto | VERIFIED | `stage.py` L663: `copied = _materialize(cdst)  # D-01: NUNCA chama remove_original p/ cópia.`; `remove_original` chamado apenas no bloco de move (L755), após todas as cópias |
| 2 | D-02: Parâmetros da ação Copiar usam `dest_folder` com suporte a tokens de campo — igual ao Mover | VERIFIED | `automations.py` L127-128: `if self.action_type == "copy" and not p.get("dest_folder"): raise ValueError("ação 'copy' exige params.dest_folder")`; UI `AutomationsPage.tsx` mapeia `copy → {dest_folder: stripQuotes(a.pattern)}` |
| 3 | D-03: Cópias executam PRIMEIRO; Mover é o ÚLTIMO e só remove o original após todas as cópias materializadas e verificadas | VERIFIED | `stage.py` L624-L755: loop `for planned_copy in plan.copies` (cópias) completo antes do bloco do move; `remove_original` L755 só executado após `_materialize(dest)` no bloco move; comentário explícito "CÓPIAS PRIMEIRO (D-03)" |
| 4 | D-04: Mover e Renomear sem regressão — suíte completa passa | VERIFIED | `pytest -q` resultado: **399 passed, 24 warnings in 49.81s**; `executor.py` preserva `target_folder`/`target_name`/`blocked`/`matched`/`automation_id` intactos com `copies=()` default |
| 5 | D-05: Após copy-only, original permanece na pasta monitorada; documento marcado CONCLUIDO normalmente | VERIFIED | `stage.py` L685-706: bloco `if not has_effective_move` faz `transition(CONCLUIDO, completed_step=APPLIED_STEP)` SEM tocar o original; distinção move-efetivo vs default confirma que copy-only não materializa para a raiz |
| 6 | D-06: Undo de Copiar APAGA a(s) cópia(s); NUNCA toca o original; undo por-run reverte execução inteira (cópias + move juntos) | VERIFIED | `undo.py` L112-117: `if audit.action == "copy": ... dest.unlink(missing_ok=True)` antes do fluxo move-back; `undo_run` e `undo_document` já iteram todos os `done` do run/doc e chamam `undo_operation` por registro; 3 testes copy-specific em `test_undo.py` passam |
| 7 | D-07: Audit write-ahead registra intenção de CADA saída de cópia ANTES de materializar; anti-colisão (`resolve_collision`) por destino; Copiar NUNCA sobrescreve silenciosamente; dry-run mostra cada saída; `action_type='copy'` com `dest_folder` faltante bloqueia (D-07) | VERIFIED | `stage.py` L649-661: `AuditLog(action=_COPY_ACTION, status="intent")` + `session.commit()` antes de `_materialize(cdst)`; `_resolve_for_write` chama `fileops.resolve_collision` por destino (L628); `automations.py` L127-128 rejeita copy sem `dest_folder`; `executor.py` L153: `return None, None, True, ()` quando `resolve_dest_folder` retorna None |

**Score:** 7/7 truths verificadas

### Scope Fence: Cortar/split-por-template

| Item | Status | Evidence |
|------|--------|----------|
| Cortar NÃO implementado no backend | VERIFIED | Busca por `cortar\|cut\|split_by_template` em `backend/app/` retornou zero ocorrências em arquivos de produção |
| Cortar NÃO implementado no frontend | VERIFIED | Busca em `frontend/src/` retornou zero ocorrências |
| Alembic permanece em 0008 (sem 0009) | VERIFIED | `ls backend/alembic/versions/` lista exatamente 8 arquivos (`0001` a `0008`); nenhum `0009_*` |

---

### Required Artifacts (3 planos, 6 arquivos de produção + 3 de teste)

| Artifact | Status | Evidence |
|----------|--------|----------|
| `backend/app/automation/executor.py` | VERIFIED | `PlannedCopy` dataclass (frozen, `folder`+`name`); `AutomationPlan.copies: tuple[PlannedCopy, ...] = ()`; ramo `elif action.action_type == "copy":` em `_apply_actions`; 8 referências a `PlannedCopy` |
| `backend/app/models/automation.py` | VERIFIED | L140-145: docstring inclui `"rename" \| "move" \| "copy"` e nota "String LIVRE, sem migração" |
| `backend/tests/automation/test_executor.py` | VERIFIED | 19 referências a `copy`; testes cobre copy-only, rename→copy, copy+copy, copy+move, bloqueio, confinamento V4, action_type desconhecido inerte, não-regressão |
| `backend/app/automation/stage.py` | VERIFIED | `StageOutput(kind, dest_path, collision, skipped_identical)` com `removes_original` derivado; `ApplyStageResult.outputs: tuple[StageOutput, ...]`; `_COPY_ACTION = "copy"`; `_copy_dest`; loop de cópias com write-ahead antes do move |
| `backend/app/automation/undo.py` | VERIFIED | Ramo `if audit.action == "copy":` (L112) ANTES do fluxo move-back; `dest.unlink(missing_ok=True)` sem tocar `source_path` |
| `backend/app/api/automations.py` | VERIFIED | `_ACTION_TYPES = frozenset({"rename", "move", "copy"})` L62; validador copy exige `dest_folder` L127-128; `DryRunRow` com `action_kind`/`removes_original` L387-388; endpoint emite 1 linha por saída L483-498 |
| `backend/tests/automation/test_stage.py` | VERIFIED | 38 referências a `copy`; cobre copy-only mantém original, write-ahead por cópia, N cópias→N audit, cópias-antes-do-move, anti-colisão, idempotência, dry-run multi-saída |
| `backend/tests/automation/test_undo.py` | VERIFIED | 14 referências a `copy`; cobre undo apaga cópia/mantém original, undo por-run copy+move, dest ausente no-op |
| `backend/tests/test_api_automations.py` | VERIFIED | 13 referências a `copy`; cobre copy sem dest→422, copy com dest→201, dry-run copy+move 1 linha por saída |
| `frontend/src/types.ts` | VERIFIED | `ActionType = 'rename' \| 'move' \| 'copy'` L212; `DryRunRow` com `action_kind?: ActionType \| null` L297 e `removes_original?: boolean` L299 |
| `frontend/src/pages/AutomationsPage.tsx` | VERIFIED | `ACTION_META.copy` L79; barra de botões inclui `'copy'` L1164; `toCreateBody` mapeia copy→`dest_folder` L442; `validate` exige `dest_folder` para copy L425-426; nota "O original permanece" L854-856 |
| `frontend/src/pages/DryRunPage.tsx` | VERIFIED | Ramo `if (row.action_kind === 'copy')` L62 com badge "Copiado — original mantido" L69; posicionado APÓS `blocked`/`no_match`/`skipped_identical`/`collision` (precedência preservada) e ANTES do default "Pronto" L79 |

---

### Key Link Verification

| De | Para | Via | Status |
|----|------|-----|--------|
| `executor._apply_actions` | `AutomationPlan.copies` | Acumula `PlannedCopy` no ramo `copy` e repassa em `evaluate_automations` | WIRED |
| `executor._apply_actions` | `naming.resolve_dest_folder` | Chama `resolve_dest_folder` no ramo `copy` com `base_root=base_root` (confinamento V4) | WIRED |
| `stage.apply_stage` | `fileops.materialize_to_dest` | `_materialize(cdst)` chamado por cópia SEM `remove_original`; `remove_original` só no bloco de move por último | WIRED |
| `stage.apply_stage` | `AuditLog(action='copy', status='intent')` | Write-ahead por cópia (`_COPY_ACTION`) persistido com `session.commit()` ANTES de `_materialize` | WIRED |
| `undo.undo_operation` | `audit.action == "copy"` discriminator | Ramo antes do fluxo move-back; `dest.unlink(missing_ok=True)`, NUNCA escreve em `source` | WIRED |
| `automations.py::DryRunRow.action_kind/removes_original` | `stage.dry_run.outputs[].kind` | Endpoint itera `plan.outputs` e preenche `action_kind=out.kind`, `removes_original=out.removes_original` | WIRED |
| `AutomationsPage.tsx::toCreateBody` | `API POST /automations action_type='copy'` | Mapeia `a.action_type === 'copy'` → `{action_type:'copy', params:{dest_folder}}` | WIRED |
| `DryRunPage.tsx::SituationBadge` | `DryRunRow.action_kind` | `row.action_kind === 'copy'` renderiza badge "Copiado — original mantido" | WIRED |

---

### Data-Flow Trace (Level 4)

| Camada | Dado | Fonte | Produz dado real | Status |
|--------|------|-------|-----------------|--------|
| `executor._apply_actions` ramo copy | `resolved_copy_folder` | `naming.resolve_dest_folder(params.dest_folder, fields, base_root=base_root)` | Sim — resolve tokens de campo contra `fields` reais do documento | FLOWING |
| `stage.apply_stage` cópias | `plan.copies` (PlannedCopy) | `evaluate_automations` → `_apply_actions` acumula copies | Sim — tuple de PlannedCopy preenchida com folder/name reais | FLOWING |
| `undo.undo_operation` ramo copy | `dest_path` | `AuditLog.dest_path` gravado no write-ahead | Sim — caminho real da cópia gravado antes de materializar | FLOWING |
| `DryRunRow.action_kind` | `out.kind` | `StageOutput.kind` definido como `"copy"` no loop de cópias | Sim — discriminador do backend, não inferência no frontend | FLOWING |

---

### Behavioral Spot-Checks

| Comportamento | Comando | Resultado | Status |
|--------------|---------|-----------|--------|
| 399 testes backend passam (não-regressão D-04) | `pytest -q --tb=no` | 399 passed, 24 warnings in 49.81s | PASS |
| 19 testes copy-específicos passam | `pytest -k "copy" -q --tb=no` em test_executor+test_stage+test_undo+test_api_automations | 19 passed, 49 deselected in 3.42s | PASS |
| Frontend compila sem erros de tipos (TypeScript + Vite build) | `npm run build` (frontend/) | vite v8.0.16, 82 modules, ✓ built in 205ms, zero erros | PASS |
| Alembic sem migração nova (schema inalterado) | `ls backend/alembic/versions/` | 8 arquivos (`0001`–`0008`), nenhum `0009_*` | PASS |
| `_ACTION_TYPES` inclui `"copy"` na API | `grep '"copy"' backend/app/api/automations.py` | 6 ocorrências; `_ACTION_TYPES = frozenset({"rename", "move", "copy"})` L62 | PASS |
| Cortar/Cortar ausente do codebase | `grep -rn "cortar\|Cortar" backend/app/ frontend/src/` | Zero ocorrências em arquivos de produção | PASS |

### Probe Execution

Não aplicável — esta fase não declara probes (`scripts/*/tests/probe-*.sh`). Verificação funcional coberta pelos 399 testes do pytest e pelo checkpoint human-verify do Plano 03.

---

### Requirements Coverage

| Requisito | Plano | Descrição | Status |
|-----------|-------|-----------|--------|
| AUT-01 | 01, 02, 03 | Automação cria, edita, lista, remove com condições e ações | SATISFIED — copy é nova action_type aceita pelo CRUD existente |
| AUT-02 | 01, 02, 03 | Executor avalia condições E, primeira-que-casa-vence | SATISFIED — copy é ação aditiva no executor; condições e semântica first-match intactas |
| AUT-03 | 02, 03 | Dry-run sem tocar disco nem AuditLog; preview por documento | SATISFIED — `stage.dry_run` emite N linhas (cópias + move) sem disco/AuditLog; API + UI expõem `action_kind`/`removes_original` por linha |
| AUT-04 | 02 | Audit write-ahead (intent→done) antes de qualquer operação de disco | SATISFIED — cada cópia tem seu próprio `AuditLog(action='copy', status='intent')` persistido antes de `_materialize` |
| AUT-05 | 02, 03 | Undo por-doc e por-run revertendo a execução inteira | SATISFIED — `undo_operation` discrimina `action='copy'` (apaga a cópia); `undo_run`/`undo_document` iteram todos os `done` → revertem cópias + move juntos |
| AUT-06 | 02 | Materialização do CAS + verificação de hash antes de remover a origem | SATISFIED — `_materialize` chama `fileops.materialize_to_dest` (verifica hash); `remove_original` só para o move, após verificação |

---

### Anti-Patterns Found

Nenhum anti-pattern encontrado nos arquivos modificados por esta fase:
- Zero ocorrências de `TBD`, `FIXME`, `XXX` nos 8 arquivos de produção modificados.
- Zero `TODO`/`HACK`/`PLACEHOLDER` introduzidos.
- Nenhum `return null` / `return {}` / `return []` em caminho de renderização de dados (os defaults de `copies=()` e `outputs=()` são valores base legítimos que se tornam não-vazios quando há cópias reais).

---

### Human Verification Required

#### 1. Fluxo copiar→dry-run→aplicar→desfazer ao vivo

**Test:** Subir o backend e o frontend com um documento classificado e pronto. Criar uma automação com ação "Copiar" (pasta de destino com token). Executar dry-run, aplicar e desfazer.
**Expected:**
- Editor mostra botão "Copiar" com campo "Pasta de destino" e nota "O original permanece onde está".
- Dry-run exibe badge "Copiado — original mantido" com `action_kind=copy, removes_original=false`.
- Após aplicar: arquivo aparece no destino E original permanece na pasta monitorada.
- Após desfazer: cópia apagada, original intacto, documento reaberto como PROCESSANDO.
**Why human:** O fluxo renderizado com arquivos reais no disco (CAS → destino) e a confirmação de não-perda de arquivo só são observáveis rodando o app ao vivo. Os testes cobrem cada peça isoladamente; este checkpoint confirma a integração ponta a ponta.

**Nota:** O checkpoint human-verify do Plano 03 já foi executado ao vivo via Playwright e reportado como APROVADO no 06.2-03-SUMMARY.md (veja a seção "Human Verification"). Esta entrada é registrada como `human_needed` por protocol — a evidência de aprovação ao vivo já existe na fase.

---

### Gaps Summary

Nenhum gap. Todos os 7 critérios de decisão (D-01..D-07) são verificáveis no código e estão implementados. O único item pendente é o checkpoint human-verify do Plano 03, que já foi executado e aprovado durante a execução da fase (relatado em `06.2-03-SUMMARY.md`), mas requer confirmação formal do usuário para fechar o `status: human_needed`.

---

_Verificado: 2026-06-18T23:30:00Z_
_Verificador: Claude (gsd-verifier)_
