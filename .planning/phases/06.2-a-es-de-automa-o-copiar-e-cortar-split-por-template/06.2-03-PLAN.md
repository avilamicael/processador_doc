---
phase: 06.2-acao-de-automacao-copiar
plan: 03
type: execute
wave: 3
depends_on: ["06.2-02"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/pages/AutomationsPage.tsx
  - frontend/src/pages/DryRunPage.tsx
autonomous: false
requirements: [AUT-01, AUT-02, AUT-03, AUT-05]
must_haves:
  truths:
    - "No editor de automação (Condições→Ações), o usuário adiciona a ação 'Copiar' ao lado de 'Mover' e 'Renomear', informando a pasta de destino com tokens (D-02)"
    - "A ação Copiar mostra a nota 'O original permanece onde está' diferenciando-a de Mover (D-05)"
    - "O dry-run renderiza uma linha origem→destino por cópia (+ a do move), com badge 'Copiado — original mantido' deixando claro que o original permanece (D-07)"
    - "Mover e Renomear na UI continuam idênticos — Copiar é estritamente aditivo; npm run build verde (D-04)"
    - "Os tipos TS espelham o backend: ActionType inclui 'copy' e DryRunRow ganha o discriminador (action_kind/removes_original) do Plano 02"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "ActionType com 'copy' + discriminador em DryRunRow"
      contains: "copy"
    - path: "frontend/src/pages/AutomationsPage.tsx"
      provides: "opção 'Copiar' no ACTION_META + botão + mapeamento draft↔body + validação"
      contains: "copy"
    - path: "frontend/src/pages/DryRunPage.tsx"
      provides: "badge 'Copiado — original mantido' por saída de cópia"
      contains: "copy"
  key_links:
    - from: "frontend/src/pages/AutomationsPage.tsx::toCreateBody"
      to: "API POST /automations action_type='copy'"
      via: "mapeia a ação copy → params.dest_folder (igual ao move)"
      pattern: "copy"
    - from: "frontend/src/pages/DryRunPage.tsx::SituationBadge"
      to: "DryRunRow.action_kind/removes_original"
      via: "renderiza o badge de cópia (não remove original)"
      pattern: "copy"
---

<objective>
Expor a ação **Copiar** na interface do construtor de automações e no dry-run,
espelhando exatamente o padrão visual já estabelecido de **Mover** (modelo
Condições→Ações de 06-12), com a diferença comunicada ao usuário: "O original
permanece onde está".

Purpose: fechar a fatia vertical da feature — o usuário cria uma automação Copiar pela
UI, vê no dry-run que a cópia vai para o destino e o original fica, aplica e desfaz —
reusando o vocabulário visual (tokens `--st-*`), o draft local genérico (`pattern`
serve tanto name_pattern quanto dest_folder) e o fluxo apply/undo já existentes.
Output: tipos TS atualizados, botão/editor "Copiar", badge de cópia no dry-run,
verificação visual ao vivo (autonomous=false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-a-es-de-automa-o-copiar-e-cortar-split-por-template/06.2-CONTEXT.md
@.planning/phases/06.2-a-es-de-automa-o-copiar-e-cortar-split-por-template/06.2-PATTERNS.md

<interfaces>
<!-- Pontos de extensão exatos. Extraídos do código. NÃO re-explorar. -->

frontend/src/types.ts:
- L211: export type ActionType = 'rename' | 'move'      → adicionar | 'copy'
- DryRunRow (L282-292): {document_id, original_filename, source_path, dest_path, blocked,
  collision, skipped_identical, no_match, automation_id} → adicionar discriminador
  (action_kind?: 'copy'|'move'|null e/ou removes_original?: boolean) conforme Plano 02

frontend/src/pages/AutomationsPage.tsx:
- L71-77 ACTION_META: Record<ActionType, {label, icon:'docMini'|'folder', dot}> — rename/move; +copy
- L91-96 interface ActDraft { key, action_type, pattern }   # pattern = name_pattern OU dest_folder (genérico)
- L112-114 newAct(action) ; L116-142 automationToDraft (lê params.dest_folder p/ não-rename)
- L416-420 validate(): rename exige pattern; move exige stripQuotes(pattern)
- L433-438 toCreateBody(): rename→{name_pattern}; else→{action_type:'move', params:{dest_folder:stripQuotes(pattern)}}
- L570-573 (preview inline): isRn = action_type==='rename'; cor move = --st-encontrado
- L719-849 renderAct(): isRn caso especial; else = move (input "Pasta de destino"); L816-832
- L1151 barra de botões: (['rename','move'] as ActionType[]).map(...) ; usa ACTION_META[act]

frontend/src/pages/DryRunPage.tsx:
- L17-70 SituationBadge({row}): ramos blocked/no_match/skipped_identical/collision/default("Pronto"),
  cada um usando tokens --st-*. Adicionar ramo de cópia ("Copiado — original mantido").
- L300-328 tabela: rows.map(r => <tr> source_path | dest_path | <SituationBadge row={r}/> </tr>)
  já itera linha-a-linha — N linhas por doc (cópia(s)+move) aparecem naturalmente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos TS + editor "Copiar" no construtor de automações</name>
  <files>frontend/src/types.ts, frontend/src/pages/AutomationsPage.tsx</files>
  <read_first>
    - frontend/src/types.ts (ActionType L211, DryRunRow L282-292 — está em interfaces)
    - frontend/src/pages/AutomationsPage.tsx (ACTION_META, ActDraft, automationToDraft, validate, toCreateBody, renderAct, barra de botões — linhas em interfaces)
    - .planning/phases/06.2-a-es-de-automa-o-copiar-e-cortar-split-por-template/06.2-PATTERNS.md §"types.ts" e §"AutomationsPage.tsx" (espelha move; copy reusa `pattern` como dest_folder)
    - .planning/phases/06.2-a-es-de-automa-o-copiar-e-cortar-split-por-template/06.2-02-SUMMARY.md (nome exato do discriminador adicionado ao DryRunRow do backend)
  </read_first>
  <action>
    types.ts: estender `ActionType` para `'rename' | 'move' | 'copy'` (L211). Estender
    `DryRunRow` com o discriminador que o Plano 02 adicionou ao backend
    (`action_kind?: 'copy' | 'move' | null` e/ou `removes_original?: boolean`) —
    confirme o nome exato no 06.2-02-SUMMARY.md/no schema do endpoint e espelhe-o
    1:1. Atualize o comentário do DryRunRow para descrever as N linhas por doc (cópias
    + move).

    AutomationsPage.tsx (espelhar `move`, copy é aditivo):
    - ACTION_META (L71-77): adicionar `copy: { label: 'Copiar', icon: 'folder', dot:
      <token de cor distinto, ex. var(--st-encontrado)> }`. Escolha uma cor que
      diferencie de move/rename usando tokens existentes (sem cor hard-coded nova).
    - Barra de botões (L1151): incluir `'copy'` → `(['rename','move','copy'] as
      ActionType[])`.
    - `toCreateBody` (L433-438): mapear copy IGUAL ao move →
      `{ action_type: 'copy', params: { dest_folder: stripQuotes(a.pattern) } }`.
      Generalize o `else` (hoje assume move) para distinguir move vs copy pelo
      `a.action_type`.
    - `automationToDraft` (L116-142): copy lê `params.dest_folder` no `pattern`
      (mesma leitura do move — generalizar a condição não-rename).
    - `validate` (L416-420): copy exige `stripQuotes(a.pattern).trim()` (mesma regra do
      move), com mensagem "Em \"Copiar\", defina a pasta de destino.".
    - `renderAct` (L719-849): copy usa o MESMO input de "Pasta de destino" do move
      (`isRn` permanece o único caso especial; onde hoje `else` = move, trate
      `move|copy` igualmente para o input/placeholder/onBlur stripQuotes). Adicionar,
      no caso copy, um texto de ajuda curto "O original permanece onde está." (D-05),
      diferenciando-o do move.
    - Preview inline (L570-573): copy pode reusar a cor/comportamento do move (não-rename);
      garantir que `isRn` continue sendo o único ramo especial.

    NÃO altere o comportamento de rename/move (D-04). NÃO instale pacote novo
    (code-and-config, padrão das fases anteriores). NÃO use `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - types.ts: ActionType inclui 'copy' (grep -c "'copy'" frontend/src/types.ts ≥ 1) e DryRunRow tem o discriminador do backend
    - AutomationsPage.tsx: ACTION_META tem chave copy; a barra de botões inclui 'copy'; toCreateBody mapeia copy→dest_folder
      (grep -c "copy" frontend/src/pages/AutomationsPage.tsx ≥ 4)
    - renderAct mostra "O original permanece onde está" no caso copy (D-05)
    - validate exige dest_folder para copy
    - rename/move inalterados na UI (D-04)
    - `npm run build` (tsc -b && vite build) verde — typecheck + build limpos, zero erros
  </acceptance_criteria>
  <done>
    O usuário consegue, pela UI, adicionar a ação Copiar (com pasta de destino + tokens)
    a uma automação, com a nota de que o original permanece; tipos TS sincronizados com
    o backend; build verde.
  </done>
</task>

<task type="auto">
  <name>Task 2: Badge "Copiado — original mantido" no dry-run</name>
  <files>frontend/src/pages/DryRunPage.tsx</files>
  <read_first>
    - frontend/src/pages/DryRunPage.tsx (SituationBadge L17-70; tabela L300-328 — está em interfaces)
    - .planning/phases/06.2-a-es-de-automa-o-copiar-e-cortar-split-por-template/06.2-PATTERNS.md §"DryRunPage.tsx" (ramo de cópia espelhando os badges existentes, mesmos tokens --st-*)
  </read_first>
  <action>
    Em `SituationBadge` (L17-70), adicionar um ramo para a saída de cópia ANTES do
    return default ("Pronto"): quando `row.action_kind === 'copy'` (ou
    `row.removes_original === false`, conforme o discriminador do Plano 02), renderizar
    um `<span className="badge">` com tokens existentes (ex.: `--st-encontrado` /
    `--st-encontrado-bg`) e o texto "Copiado — original mantido", com `title` "O arquivo
    será copiado para o destino. O original permanece onde está." Mantenha os ramos
    blocked/no_match/skipped_identical/collision/default intactos (ordem preservada —
    blocked/no_match continuam tendo precedência).

    A tabela (L300-328) já itera `rows.map` linha-a-linha, então as N linhas por
    documento (cópias + move) aparecem como linhas separadas sem mudança estrutural —
    confirme que source_path/dest_path por linha renderizam corretamente para cada
    saída. Opcional: ajustar a mensagem de confirmação de undo (se houver texto fixo)
    para mencionar que cópias são apagadas e o original nunca é tocado (D-06).

    NÃO altere `isApplicable`/`useApply`/`useUndo`/`confirmUndo` (undo por run já reverte
    cópias+move juntos, D-06). NÃO instale pacote novo. NÃO use dangerouslySetInnerHTML.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DryRunPage.tsx: SituationBadge tem um ramo de cópia ("Copiado — original mantido")
      (grep -c "Copiado" frontend/src/pages/DryRunPage.tsx ≥ 1)
    - O ramo usa tokens --st-* existentes (sem cor hard-coded fora do design system)
    - Os ramos blocked/no_match/skipped_identical/collision permanecem e mantêm precedência
    - `npm run build` verde
  </acceptance_criteria>
  <done>
    O dry-run mostra cada saída de cópia com um badge explícito de que o original é
    mantido, usando o vocabulário visual existente; build verde.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual ao vivo — criar/copiar/dry-run/aplicar/desfazer</name>
  <files>(nenhum — checkpoint de verificação humana; não edita arquivos)</files>
  <action>
    PAUSAR para verificação humana — NÃO edite arquivos nesta task. O trabalho
    automatizado das Tasks 1-2 (e dos Planos 01/02) já está completo e provado por
    testes/build. Apresente ao usuário os passos de <how-to-verify> e aguarde o
    <resume-signal>. Este checkpoint existe porque o fluxo renderizado com arquivos
    reais no disco (copiar→dry-run→aplicar→desfazer) só é observável rodando o app ao
    vivo — os testes cobrem cada peça isoladamente, não a UI ao vivo.
  </action>
  <what-built>
    Ação Copiar de ponta a ponta: editor (botão "Copiar" + pasta de destino + nota "o
    original permanece"), dry-run (linha de cópia com badge "Copiado — original
    mantido"), aplicar (materializa no destino, original fica) e desfazer (apaga a
    cópia, original intacto). Backend (Planos 01/02) já provado por testes; este
    checkpoint confirma o fluxo renderizado com arquivos reais no disco.
  </what-built>
  <how-to-verify>
    1. Subir o backend (uvicorn, 1 worker) e o frontend (npm run dev); ter ao menos um
       documento classificado e pronto (PROCESSANDO + "classificado").
    2. Em Automações, criar uma automação com uma condição que case o documento e uma
       ação "Copiar" com uma pasta de destino usando um token (ex.: `Arquivo/{cliente}/`).
       Confirmar que aparece a nota "O original permanece onde está".
    3. (Opcional D-03) Adicionar TAMBÉM uma ação "Mover" para outra pasta na MESMA
       automação e confirmar que ambas convivem.
    4. Rodar o Dry-run: confirmar uma linha origem→destino para a cópia com o badge
       "Copiado — original mantido" (e, se houver move, a linha do move separada).
    5. Aplicar: confirmar no Windows Explorer/sistema de arquivos que o arquivo APARECE
       no destino da cópia E o ORIGINAL CONTINUA na pasta de origem. Se houver move, o
       original foi movido para a pasta do move APÓS a cópia existir.
    6. Desfazer (por documento ou por execução): confirmar que a(s) cópia(s) são
       APAGADAS do destino e o original permanece (ou, com move, é restaurado) — nenhum
       arquivo some.
  </how-to-verify>
  <verify>
    <human-check>Humano confirma o fluxo copiar→dry-run→aplicar→desfazer ao vivo: cópia no destino, original mantido, undo apaga a cópia, nada se perde.</human-check>
  </verify>
  <done>
    O usuário aprovou o fluxo ao vivo: Copiar materializa no destino deixando o original,
    o dry-run comunica isso, e o undo apaga a cópia sem perder nenhum arquivo (D-01/D-05/D-06).
  </done>
  <resume-signal>Digite "approved" se o fluxo copiar/dry-run/aplicar/desfazer funciona e nenhum arquivo se perde, ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input do usuário (pasta de destino) → corpo POST /automations | dest_folder digitado no editor enviado ao backend |
| resposta de dry-run/apply → render na UI | paths exibidos na tabela; sem valores de campo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.2-09 | Tampering / Injection | dest_folder digitado no editor | mitigate | Validação/confinamento é autoritativa no backend (Plano 01 naming + Plano 02 API exige dest_folder, V4/V5). A UI só envia a string; o servidor valida e confina. Frontend não decide caminho final. |
| T-06.2-10 | Information Disclosure (XSS) | render de paths/badges na tabela | mitigate | React escapa por padrão; NÃO usar dangerouslySetInnerHTML; paths renderizados como texto. Valores de campo nunca chegam ao frontend (V7/V9). |
| T-06.2-SC | Tampering | npm installs | mitigate | NENHUM pacote npm novo (code-and-config, padrão 04-06/05-04/06-08/06.1-03). Sem checkpoint de legitimidade necessário. |
</threat_model>

<verification>
- `cd frontend && npm run build` → typecheck (tsc -b) + vite build limpos, zero erros (gate de drift de tipos com o backend).
- Checkpoint humano: fluxo copiar→dry-run→aplicar→desfazer com arquivo real (original permanece; nada se perde).
</verification>

<success_criteria>
- O usuário adiciona a ação Copiar pela UI com pasta de destino + tokens e vê que o
  original permanece (D-02/D-05).
- O dry-run mostra cada saída de cópia com badge "Copiado — original mantido" (D-07/AUT-03).
- Aplicar copia ao destino deixando o original; desfazer apaga a cópia sem perder nada
  (D-01/D-06) — confirmado ao vivo.
- Mover/Renomear sem regressão na UI; npm run build verde (D-04).
</success_criteria>

<output>
Create `.planning/phases/06.2-a-es-de-automa-o-copiar-e-cortar-split-por-template/06.2-03-SUMMARY.md` when done
</output>
