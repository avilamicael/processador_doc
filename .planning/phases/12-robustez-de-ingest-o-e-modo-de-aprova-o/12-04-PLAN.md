---
phase: 12-robustez-de-ingest-o-e-modo-de-aprova-o
plan: 04
type: execute
wave: 2
depends_on: ["12-03"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useAttention.ts
  - frontend/src/pages/ConfigPage.tsx
  - frontend/src/pages/DryRunPage.tsx
autonomous: false
requirements: [BL-12]
must_haves:
  truths:
    - "O usuário liga/desliga 'automações aguardam aprovação' na ConfigPage (espelha o toggle de IA-fallback)"
    - "Com o toggle LIGADO, a Pré-visualização é a fila de aprovação: aprovar (aplicar) ou negar/pular por linha"
    - "Negar/pular NÃO chama o backend e NÃO move o arquivo — só tira o doc da rodada atual (fica pronto)"
    - "Aprovar reusa o apply existente (aprovar = apply); com o toggle DESLIGADO a página segue como hoje"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "interface ApprovalMode { enabled: boolean }"
      contains: "ApprovalMode"
    - path: "frontend/src/lib/api.ts"
      provides: "get/putApprovalMode contra /config/approval-mode"
      contains: "approval-mode"
    - path: "frontend/src/hooks/useAttention.ts"
      provides: "useApprovalMode / useSaveApprovalMode"
      contains: "useApprovalMode"
    - path: "frontend/src/pages/ConfigPage.tsx"
      provides: "card toggle de modo de aprovação"
      contains: "approval"
    - path: "frontend/src/pages/DryRunPage.tsx"
      provides: "fila de aprovação com negar/pular por linha quando ligado"
      contains: "useApprovalMode"
  key_links:
    - from: "frontend/src/pages/ConfigPage.tsx"
      to: "/config/approval-mode"
      via: "useApprovalMode/useSaveApprovalMode (TanStack Query)"
      pattern: "useSaveApprovalMode"
    - from: "frontend/src/pages/DryRunPage.tsx"
      to: "estado local de linhas (setRows)"
      via: "negar/pular remove as linhas do doc sem chamar backend"
      pattern: "setRows"
---

<objective>
Item 12 frontend (D-03/D-06): toggle global "automações aguardam aprovação" na ConfigPage (espelha o par ai-fallback ponta-a-ponta: types → api → hook → field) e a DryRunPage (Pré-visualização) vira a fila de aprovação. Com o toggle LIGADO, os docs de alta confiança NÃO foram auto-aplicados pelo worker (gate do 12-03) e aparecem aqui como "Pronto"; o usuário aprova (= aplicar existente) ou nega/pula por linha. Negar = não aplica naquele doc nesta rodada (arquivo intocado, doc segue pronto — o move só no aprovar). Com o toggle DESLIGADO, a página se comporta exatamente como hoje.

Purpose: entregar o "modo de teste" visível ao usuário, reusando dry-run/apply existentes e o molde do toggle de IA-fallback. Zero npm novo, valores texto puro.

Output: toggle na ConfigPage + fila de aprovação na DryRunPage + verificação visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-CONTEXT.md
@.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-PATTERNS.md
@.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-03-SUMMARY.md

<interfaces>
<!-- Molde EXATO do par ai-fallback (Fase 10) ponta-a-ponta. Endpoint do 12-03:
     GET/PUT /config/approval-mode com shape {enabled: boolean}. -->

frontend/src/types.ts (analog, linhas 407-409):
- `export interface AiFallback { enabled: boolean }` → criar `export interface ApprovalMode { enabled: boolean }`.

frontend/src/lib/api.ts (analog, linhas 177-186):
- `getAiFallback()` → `request<AiFallback>('/config/ai-fallback')`
- `putAiFallback(enabled)` → `request(..., { method:'PUT', body: JSON.stringify({enabled}) })`
- (request é o cliente fetch tipado base — NÃO usar frontend/src/api/client.ts, que não existe.)

frontend/src/hooks/useAttention.ts (analog, linhas 27 + 134-148):
- `AI_FALLBACK_KEY = ['ai-fallback']`; `useAiFallback()` (useQuery); `useSaveAiFallback()`
  (useMutation com `qc.invalidateQueries({ queryKey: AI_FALLBACK_KEY })` no onSuccess).

frontend/src/pages/ConfigPage.tsx (analog, linhas 523-589 + render 512-515):
- `AiFallbackField` — card com `<Switch on={enabled} onToggle={toggle}>`, título, descrição,
  estados loading/error/saveError; renderizado dentro de `LeituraTab` ao lado de `ReviewThresholdField`.

frontend/src/pages/DryRunPage.tsx (a própria página vira fila de aprovação, D-06):
- `useDryRun`/`useApply`/`useUndo` de '../hooks/useAutomations' (já importados, linha 4).
- estado local `rows`/`setRows` (89), `selected`/`toggleSel` (90,121), `doApply(ids)` (139)
  que chama `useApply` → apply_stage. `isApplicable` (113), `applicableIds` (118),
  `SituationBadge` (18). Aprovar = `doApply`; negar = remover as linhas do `document_id`
  de `rows` via `setRows` SEM backend (arquivo intocado).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle approval-mode ponta-a-ponta (types + api + hook + ConfigPage)</name>
  <read_first>
    - frontend/src/types.ts (interface AiFallback 407-409)
    - frontend/src/lib/api.ts (get/putAiFallback 177-186)
    - frontend/src/hooks/useAttention.ts (AI_FALLBACK_KEY 27; useAiFallback/useSaveAiFallback 134-148)
    - frontend/src/pages/ConfigPage.tsx (AiFallbackField 523-589; render em LeituraTab 512-515)
  </read_first>
  <action>
    Replicar o par ai-fallback 1:1 trocando o nome, contra o endpoint /config/approval-mode do 12-03:
    (1) types.ts: `export interface ApprovalMode { enabled: boolean }`.
    (2) lib/api.ts: `getApprovalMode(): Promise<ApprovalMode>` → `request('/config/approval-mode')`; `putApprovalMode(enabled: boolean)` → PUT com `{ enabled }`.
    (3) useAttention.ts: `APPROVAL_MODE_KEY = ['approval-mode']`; `useApprovalMode()` (useQuery) e `useSaveApprovalMode()` (useMutation invalidando APPROVAL_MODE_KEY no onSuccess).
    (4) ConfigPage.tsx: criar `ApprovalModeField` espelhando `AiFallbackField` (card + título "Automações aguardam minha aprovação" + descrição curta: LIGADO = automações ficam pendentes na Pré-visualização para você aprovar/negar; DESLIGADO = alta confiança aplica sozinha mantendo a trava de confiança; usa `<Switch on={enabled} onToggle={toggle}>` + estados loading/error/saveError). Renderizar dentro de `LeituraTab` ao lado de `ReviewThresholdField`/`AiFallbackField`. Valores texto puro, zero npm novo.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>build verde; o toggle aparece na ConfigPage, lê o GET e salva via PUT (invalidando a query); shape {enabled} alinhado ao 12-03.</done>
</task>

<task type="auto">
  <name>Task 2: DryRunPage como fila de aprovação (negar/pular por linha quando ligado)</name>
  <read_first>
    - frontend/src/pages/DryRunPage.tsx (página inteira — rows/setRows, doApply, SituationBadge, isApplicable)
    - frontend/src/hooks/useAttention.ts (useApprovalMode da Task 1)
  </read_first>
  <action>
    Em DryRunPage.tsx, ler `useApprovalMode()` e renderizar a página como fila de aprovação quando `enabled === true`:
    (1) Banner/explicação no topo (apenas quando ligado): "Modo de aprovação ligado — as automações aguardam você. Aprovar aplica; Negar deixa o documento pronto sem mover (o arquivo não é tocado)." Texto puro.
    (2) Ação por-linha "Negar / Pular" (apenas quando ligado, para linhas `isApplicable`): remove TODAS as linhas daquele `document_id` de `rows` via `setRows` (filtro local) e tira o id de `selected` — NÃO chama backend, NÃO move arquivo (D-06: o move só no aprovar). Reusar `row-action`/Icon do padrão existente da página.
    (3) Aprovar = o `doApply` existente (por linha via `doApply([id])` e/ou em lote via `doApply(selected)`/`applicableIds`) — sem mudança de semântica; aprovar = apply_stage.
    (4) Com o toggle DESLIGADO, a página fica idêntica ao comportamento atual (sem banner, sem botão Negar) — gate só na renderização condicional.
    Não recriar hooks de apply/dry-run (reusar useAutomations). Não adicionar dependência.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>build verde; com approval-mode ON a página mostra banner + Negar/Pular por linha (negar é local, sem backend) e Aprovar aplica; com OFF a página é idêntica ao comportamento atual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Toggle "automações aguardam minha aprovação" na ConfigPage (espelha IA-fallback) e a Pré-visualização (DryRunPage) repurposada como fila de aprovação: aprovar (aplicar) / negar (local, sem mover) por linha quando ligado; comportamento atual quando desligado. Backend do 12-03 já gateia o auto-apply.</what-built>
  <how-to-verify>
    1. Suba backend + frontend (modo dev habitual).
    2. Em Configurações → aba Leitura: confirme o novo card "Automações aguardam minha aprovação", default DESLIGADO. Ligue-o (deve persistir após reload).
    3. Tenha documentos de ALTA confiança prontos (classificados, score ≥ limiar). Com o toggle LIGADO, eles NÃO devem ser auto-aplicados pelo worker — abra a Pré-visualização: devem aparecer como "Pronto" com o banner de modo de aprovação.
    4. Numa linha, clique "Negar / Pular": a linha some da lista, NENHUM arquivo é movido (verifique no Windows Explorer/pasta), e o documento continua pronto.
    5. Noutra linha, clique aprovar/aplicar: o arquivo é movido/renomeado conforme a automação (origem→destino) e pode ser desfeito.
    6. DESLIGUE o toggle: novos docs de alta confiança voltam a ser auto-aplicados; a Pré-visualização volta ao comportamento atual (sem banner/Negar).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os ajustes necessários</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → API | toggle do modo de aprovação (PUT) e apply (aprovar) |
| ação "Negar" → disco | negar NÃO pode tocar arquivo — é só estado local de UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-11 | Tampering / data loss | "Negar/Pular" move ou apaga arquivo | mitigate | negar só filtra `rows` via `setRows` (estado local) e não chama backend; nenhum apply/undo/delete é disparado — arquivo intocado (D-06) |
| T-12-12 | Spoofing/XSS | render de caminhos origem→destino | mitigate | valores como texto puro (cell-mono), zero `dangerouslySetInnerHTML` — padrão estabelecido da página |
| T-12-13 | Tampering | UI permite auto-apply quando ligado | accept | a UI não decide auto-apply — o gate autoritativo está no worker (12-03); a UI só lista/aprova |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | zero npm novo (CONTEXT/CLAUDE.md) → N/A |
</threat_model>

<verification>
- `cd frontend && npm run build` verde (tsc -b + vite build) após cada task.
- Checkpoint visual humano (Task 3) cobre o fluxo ligado/desligado e a garantia "negar não move arquivo".
</verification>

<success_criteria>
Usuário liga/desliga o modo de aprovação na ConfigPage; ligado, a Pré-visualização é a fila de aprovação (aprovar=aplicar; negar=local, sem mover); desligado, o sistema roda como hoje. Zero npm novo, valores texto puro.
</success_criteria>

<output>
Create `.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-04-SUMMARY.md` when done
</output>
