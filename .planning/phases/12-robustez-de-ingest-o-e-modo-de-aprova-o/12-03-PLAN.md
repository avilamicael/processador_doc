---
phase: 12-robustez-de-ingest-o-e-modo-de-aprova-o
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/config.py
  - backend/app/api/config.py
  - backend/app/queue/worker.py
  - backend/tests/test_api_config.py
  - backend/tests/queue/test_approval_gate.py
autonomous: true
requirements: [BL-12]
must_haves:
  truths:
    - "Existe um setting global approval_mode (default OFF) lido do .env, exposto por GET/PUT /config/approval-mode"
    - "Com approval_mode OFF, enqueue_pending_applications auto-aplica alta confiança como hoje (trava de confiança mantida)"
    - "Com approval_mode ON, enqueue_pending_applications NÃO auto-aplica nada — os docs ficam pendentes aguardando aprovação"
    - "apply_stage NÃO é gateado — a aprovação manual (aprovar = apply) continua funcionando em ambos os modos"
  artifacts:
    - path: "backend/app/config.py"
      provides: "campo Settings approval_mode_enabled (default False)"
      contains: "approval_mode_enabled"
    - path: "backend/app/api/config.py"
      provides: "par GET/PUT /config/approval-mode (espelha ai-fallback)"
      contains: "approval-mode"
    - path: "backend/app/queue/worker.py"
      provides: "gate do toggle em enqueue_pending_applications"
      contains: "approval_mode_enabled"
    - path: "backend/tests/queue/test_approval_gate.py"
      provides: "teste do gate ON/OFF"
      contains: "def test_"
  key_links:
    - from: "backend/app/queue/worker.py:enqueue_pending_applications"
      to: "get_settings().approval_mode_enabled"
      via: "curto-circuito (return 0) no topo quando ON"
      pattern: "approval_mode_enabled"
    - from: "backend/app/api/config.py:put_approval_mode"
      to: ".env + get_settings.cache_clear"
      via: "persist_env_setting + cache_clear (sem reiniciar)"
      pattern: "persist_env_setting"
---

<objective>
Item 12 backend (D-03/D-04/D-05): setting global novo "automações aguardam aprovação" (default OFF), cópia literal do par `ai-fallback` (Fase 10) — Settings field → GET/PUT em api/config.py. O GATE do toggle entra em `enqueue_pending_applications` (queue/worker.py), o ponto ÚNICO de auto-apply de alta confiança (decisão 06-04): OFF auto-aplica alta confiança como hoje (trava de confiança mantida); ON curto-circuita (não enfileira nada) e os docs ficam pendentes aguardando aprovação humana via DryRunPage.

Purpose: dar ao usuário um "modo de teste" — durante um período as automações esperam aprovação; depois ele desliga e o sistema roda sozinho mantendo a rede de segurança por confiança.

CRÍTICO (D-04/D-05, ver Discrepâncias do PATTERNS): o gate vai SÓ no sweep automático (`enqueue_pending_applications` em `queue/worker.py`), NUNCA em `apply_stage` (`automation/stage.py`) — `apply_stage` é o executor real chamado tanto pela aprovação manual quanto pelo auto-apply; gateá-lo quebraria a aprovação manual (D-06: aprovar = apply). A trava de confiança/limiar segue intacta no `classify_stage` em ambos os modos.

Output: campo de settings + par de endpoints + gate no worker + testes ON/OFF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-CONTEXT.md
@.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-PATTERNS.md

<interfaces>
<!-- Molde EXATO do par ai-fallback (Fase 10) — copiar 1:1 trocando o nome. -->

backend/app/config.py (campo analog, linhas 169-174):
- `classify_ai_fallback_enabled: bool = Field(default=False, validation_alias=AliasChoices("CLASSIFY_AI_FALLBACK_ENABLED", "classify_ai_fallback_enabled"))`
- `Field`, `AliasChoices` já importados; `persist_env_setting` já existe (config.py:271).

backend/app/api/config.py (par analog, linhas 70-100):
- `_AI_FALLBACK_ENV_KEY = "CLASSIFY_AI_FALLBACK_ENABLED"`
- `class AiFallbackOut(BaseModel): enabled: bool`
- `class AiFallbackIn(BaseModel): enabled: bool`
- `@router.get("/ai-fallback", ...)` retorna `get_settings().classify_ai_fallback_enabled`.
- `@router.put("/ai-fallback", ...)`: `persist_env_setting(_AI_FALLBACK_ENV_KEY, str(body.enabled)); get_settings.cache_clear()`.
- `router = APIRouter(prefix="/config", ...)`; `get_settings`, `persist_env_setting` já importados.

backend/app/queue/worker.py (gate, enqueue_pending_applications linhas 364-408):
- Varre Documents `state == DocState.PROCESSANDO`, `last_completed_step == CLASSIFIED_STEP`,
  `ClassificationResult.confidence_score >= threshold`, sem `AuditLog(status="done")`, e
  enfileira `(content_hash, APPLY_STEP)`. `threshold = get_settings().review_confidence_threshold`
  (linha 383) — `get_settings` já importado. Retorna nº de jobs novos.

NÃO mexer em backend/app/automation/stage.py (apply_stage). O gate é só no enqueue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setting approval_mode_enabled + par GET/PUT /config/approval-mode</name>
  <read_first>
    - backend/app/config.py (campo classify_ai_fallback_enabled 160-174; persist_env_setting ~271)
    - backend/app/api/config.py (par ai-fallback 70-100; par review-threshold 30-67 como referência de docstring/validação)
    - backend/tests/test_api_config.py (testes do par ai-fallback/review-threshold como analog)
  </read_first>
  <action>
    (1) Em backend/app/config.py, adicionar o campo `approval_mode_enabled: bool = Field(default=False, validation_alias=AliasChoices("APPROVAL_MODE_ENABLED", "approval_mode_enabled"))` ao lado de `classify_ai_fallback_enabled`, com comentário explicando: default OFF preserva o auto-apply atual (D-04); ON faz as automações de alta confiança aguardarem aprovação (D-05); o gate vive no enqueue do worker, nunca em apply_stage.
    (2) Em backend/app/api/config.py, replicar o bloco ai-fallback: constante `_APPROVAL_MODE_ENV_KEY = "APPROVAL_MODE_ENABLED"`; models `ApprovalModeOut(enabled: bool)` e `ApprovalModeIn(enabled: bool)`; `@router.get("/approval-mode", response_model=ApprovalModeOut)` retornando `get_settings().approval_mode_enabled`; `@router.put("/approval-mode", response_model=ApprovalModeOut)` chamando `persist_env_setting(_APPROVAL_MODE_ENV_KEY, str(body.enabled)); get_settings.cache_clear()` e retornando o valor relido. Sem DB/subprocess; o Pydantic garante o tipo bool (fora dele → 422).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_api_config.py tests/test_config.py -x</automated>
  </verify>
  <done>GET /config/approval-mode retorna `{enabled:false}` por padrão; PUT persiste no .env + cache_clear e o GET reflete; body não-bool → 422.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate do toggle em enqueue_pending_applications (D-05)</name>
  <read_first>
    - backend/app/queue/worker.py (enqueue_pending_applications 364-408; docstring explica auto-apply 06-04)
    - backend/tests/queue/test_dispatch.py / test_chaining.py (analogs de fixture de fila/sessão)
    - backend/app/api/config.py (par approval-mode da Task 1, para flipar o toggle no teste)
  </read_first>
  <behavior>
    - Test 1 (OFF, default): com docs de alta confiança prontos (PROCESSANDO + classificado + score >= threshold, sem AuditLog done), `enqueue_pending_applications` retorna > 0 e enfileira (content_hash, apply) — comportamento atual mantido.
    - Test 2 (ON): com o mesmo cenário e `APPROVAL_MODE_ENABLED=True` (via PUT /config/approval-mode ou persist_env_setting + cache_clear), `enqueue_pending_applications` retorna 0 e NÃO enfileira nenhum job de apply (docs ficam pendentes).
    - Test 3 (trava de confiança intacta): docs de baixa confiança continuam roteados a EM_REVISAO pelo classify_stage independente do toggle — o gate só afeta o sweep de auto-apply, não o roteamento de confiança.
  </behavior>
  <action>
    Em `enqueue_pending_applications` (backend/app/queue/worker.py), inserir no TOPO da função o gate D-05: `if get_settings().approval_mode_enabled: return 0` (curto-circuito antes da query/loop — não auto-aplica nada). `get_settings` já é importado. Comentar referenciando D-05: com o toggle ON, os docs de alta confiança NÃO são auto-aplicados e ficam pendentes para aprovação humana via DryRunPage; a trava de confiança (D-04) segue no classify_stage, fora deste sweep. NÃO tocar `apply_stage`/`automation/stage.py` (executor compartilhado com aprovação manual — gateá-lo quebraria D-06). Criar backend/tests/queue/test_approval_gate.py com os testes do bloco de comportamento (flipar o toggle via persist_env_setting + get_settings.cache_clear ou via PUT no client).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/queue/test_approval_gate.py -x</automated>
  </verify>
  <done>OFF: auto-apply de alta confiança enfileira como hoje; ON: enqueue_pending_applications retorna 0 e não enfileira; apply_stage não é gateado; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → API config | valor bool do toggle (PUT /config/approval-mode) |
| setting global → worker | o toggle gateia o auto-apply automático (efeito em operações de arquivo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | gate do toggle permite auto-apply quando ON | mitigate | curto-circuito `if get_settings().approval_mode_enabled: return 0` no TOPO de enqueue_pending_applications, antes de qualquer enqueue — ON nunca auto-aplica; coberto por Test 2 |
| T-12-08 | Elevation of Privilege | gateamento errado em apply_stage quebraria aprovação manual | mitigate | gate vive SÓ no enqueue (worker); apply_stage intocado → aprovar=apply continua em ambos os modos (D-06) |
| T-12-09 | Tampering | input não-bool no PUT | mitigate | Pydantic valida `enabled: bool` → 422 antes de escrever; valor gravado como `APPROVAL_MODE_ENABLED=<True|False>`, nunca interpolado em SQL/shell |
| T-12-10 | Information Disclosure | GET expõe config | accept | só retorna um bool de modo de operação; sem segredo (segue padrão review-threshold/ai-fallback) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo nesta fase → N/A |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/test_api_config.py tests/test_config.py tests/queue/test_approval_gate.py -x` verde.
- Confirmar manualmente o contrato do endpoint para o frontend (12-04): GET/PUT /config/approval-mode com shape `{enabled: bool}`.
</verification>

<success_criteria>
Toggle global approval_mode (default OFF) lido do .env, exposto por GET/PUT /config/approval-mode; OFF auto-aplica alta confiança (trava de confiança mantida); ON deixa todos os docs pendentes sem auto-aplicar; aprovação manual (apply_stage) intacta.
</success_criteria>

<output>
Create `.planning/phases/12-robustez-de-ingest-o-e-modo-de-aprova-o/12-03-SUMMARY.md` when done
</output>
