---
phase: quick-260626-g4g
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/classification/stage.py
  - backend/app/classification/matcher.py
  - backend/tests/classification/test_stage_ai_fallback.py
  - backend/tests/classification/test_matcher_norm.py
autonomous: true
requirements: [WR-01, WR-03, WR-04]
must_haves:
  truths:
    - "Doc ambíguo cuja IA de desempate recusa (null) com fallback ON paga IA exatamente 1x e vai para quarentena"
    - "Sem templates cadastrados + fallback ON: nenhuma chamada paga de IA; doc vai direto para quarentena"
    - "Needle simbólico curto ('R$'/'Nº') não casa o haystack só por uma letra solta; needle normal ('DANFE'/'NOTA FISCAL') ainda casa com tolerância de caixa/acento/espaço; needle/value vazio não casa"
  artifacts:
    - path: "backend/app/classification/stage.py"
      provides: "Gate do IA-fallback com guardas `not called_ai` e `templates`"
      contains: "not called_ai"
    - path: "backend/app/classification/matcher.py"
      provides: "_condition_matches com fallback raw casefolded para needle normalizado curto"
      contains: "casefold"
  key_links:
    - from: "backend/app/classification/stage.py gate do fallback (~264)"
      to: "openai_client.disambiguate"
      via: "condição estendida com `and not called_ai and templates`"
      pattern: "not called_ai"
    - from: "backend/app/classification/matcher.py _condition_matches"
      to: "haystack_lower (param já recebido)"
      via: "value.casefold() in haystack_lower quando len(needle_norm) < 2"
      pattern: "haystack_lower"
---

<objective>
Corrigir 3 achados do code-review da Phase 10 no motor de classificação (backend), cada um com teste de regressão TDD (RED→GREEN):

- WR-01: doc ambíguo cuja IA de desempate recusa não pode pagar IA 2x (dupla chamada paga).
- WR-03: sem templates cadastrados o fallback não pode disparar uma chamada paga de IA que só retorna null.
- WR-04: needle normalizado curto/simbólico ("R$", "Nº") não pode over-matchar haystacks por uma única letra solta.

Purpose: eliminar custo de IA desnecessário (WR-01/WR-03) e misclassificação silenciosa (WR-04), preservando a tolerância de acento/caixa/quebra/pontuação da Phase 10.
Output: 2 fixes em `stage.py` e `matcher.py` + testes de regressão verdes; commits atômicos por task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contratos já existentes no codebase — usar diretamente, sem exploração. -->

backend/app/classification/stage.py:
- Gate do IA-fallback (~264): `if (matched_template_id is None and settings.classify_ai_fallback_enabled and forced_template_id is None):` → chama `await openai_client.disambiguate(_candidates_summary(templates), extraction.full_text)`.
- Variável `called_ai` inicializada `False` (~185); o ramo "ambiguous" (~221-247) JÁ chama `disambiguate` e seta `called_ai = True`.
- `templates` é a lista `list(session.scalars(select(Template)).all())` (~205); vazia quando nenhum template cadastrado.

backend/app/classification/matcher.py:
- `_condition_matches(cond, haystack_norm, haystack_lower) -> bool` (~159). Ramo "texto" (~191-195): `needle = _normalize_text(value); if not needle: return False; return needle in haystack_norm`.
- `_normalize_text(s)` colapsa pontuação→espaço: "R$" → "r", "Nº" → "no".
- `haystack_lower` (param já recebido) = `full_text.lower()` (NÃO normalizado); ramo regex já o usa.
- `_prepare_haystacks(full_text) -> (haystack_lower, haystack_norm)`.

backend/tests/classification/test_stage_ai_fallback.py — helpers reusáveis: `_set_fallback(monkeypatch, enabled=...)`, `_seed_doc(...)`, `_template(...)`, `_pairs_json(...)`, `_envelope(structured, resp_id)`, fixtures `schema_engine`/`_openai_key`; mock OpenAI via `respx.mock(base_url="https://api.openai.com/v1")` em `POST /responses`; asserção de custo via `route.call_count`.

backend/tests/classification/test_matcher_norm.py — helpers `_tpl(id, signals=...)` e `_confidence(tpl, full_text)`; testa via `matcher.match_templates`/`evaluate_groups` (confiança boolean 1.0/0.0).

Decisão de threshold: confiança é BOOLEANA (1.0/0.0). "ambiguous" exige 2+ templates ambos em 1.0 (best - second = 0 < margem, ambos ≥ threshold). Default `classify_match_threshold` em config.py.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Gate do IA-fallback — não pagar 2x (WR-01) nem sem templates (WR-03)</name>
  <files>backend/app/classification/stage.py, backend/tests/classification/test_stage_ai_fallback.py</files>
  <behavior>
    Adicionar 2 testes a test_stage_ai_fallback.py (reusando _set_fallback/_seed_doc/_template/_envelope/respx):
    - WR-01 (dupla chamada): 2 templates cujos sinais AMBOS casam o mesmo full_text (→ decide "ambiguous"); fallback ON; mock `POST /responses` retorna SEMPRE `matched_template_id=None` (IA recusa). Asserir: `route.call_count == 1` (desempate pago 1x, fallback NÃO re-dispara), doc em DocState.QUARENTENA, ClassificationResult(template_id=None), called_ai=True. (RED hoje: count==2.)
    - WR-03 (sem templates): NENHUM template cadastrado; fallback ON; doc qualquer. Asserir: `route.call_count == 0` (nada paga), doc em QUARENTENA, ClassificationResult(template_id=None), n_usage==0. (RED hoje: count==1.)
    Não regredir os 5 testes existentes do arquivo.
  </behavior>
  <action>Escrever os 2 testes RED acima. Depois estender o gate do IA-fallback em stage.py (~264) de `if (matched_template_id is None and settings.classify_ai_fallback_enabled and forced_template_id is None):` para incluir `and not called_ai and templates` — `not called_ai` impede re-disparo após desempate ambíguo recusado (WR-01); `templates` (lista não-vazia, truthy) impede chamada paga quando nada pode casar (WR-03). Ajustar o comentário do bloco (~253-263) para refletir as duas novas guardas. Não tocar no ramo "ambiguous" nem na persistência de Usage/quarentena.</action>
  <verify>
    <automated>backend/.venv/bin/python -m pytest backend/tests/classification/test_stage_ai_fallback.py -x -q</automated>
  </verify>
  <done>Os 2 novos testes (WR-01 call_count==1, WR-03 call_count==0) passam e os 5 testes existentes continuam verdes.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Matcher — needle normalizado curto casa RAW casefolded (WR-04)</name>
  <files>backend/app/classification/matcher.py, backend/tests/classification/test_matcher_norm.py</files>
  <behavior>
    Adicionar testes a test_matcher_norm.py (via _tpl/_confidence):
    - "R$" NÃO casa haystack que contém só a letra solta: `_confidence(_tpl(1, signals=[[{"mode":"texto","value":"R$"}]]), "... carro rural ...") == 0.0` (hoje "r" in haystack_norm → 1.0, RED).
    - "Nº" NÃO casa por causa do "n"/"no" solto: `_confidence(_tpl(2, signals=[[{"mode":"texto","value":"Nº"}]]), "documento no campo norte") == 0.0` (hoje "no" in haystack_norm → 1.0, RED).
    - "R$" AINDA casa quando o símbolo raw está presente: haystack "valor R$ 1.234,00" → 1.0 (prova que o fallback raw funciona, não é só negar).
    - Needle normal preserva tolerância: "nota fiscal" casa "ISTO E UMA NOTÁ FISCÁL" → 1.0 (caixa/acento); "DANFE" casa "documento danfe emitido" → 1.0.
    - value vazio: `_tpl(signals=[[{"mode":"texto","value":""}]])` não casa nada → 0.0 (fail-closed).
  </behavior>
  <action>Escrever os testes RED acima. Depois alterar o ramo "texto" de `_condition_matches` (matcher.py ~191-195): manter `needle = _normalize_text(value)`, mas quando o needle normalizado for curto demais para ser significativo (`len(needle) < 2`) NÃO usar o normalizado — casar o valor RAW casefolded contra o `haystack_lower` já recebido: `return bool(value.casefold().strip()) and value.casefold().strip() in haystack_lower`. Para needle normal (`len(needle) >= 2`) manter o comportamento atual (`return needle in haystack_norm`, com toda a tolerância de acento/caixa/quebra/pontuação). value vazio → `value.casefold().strip()` é "" → fail-closed (não casa). Não tocar no ramo regex (D-03) nem em `_normalize_text`/`_prepare_haystacks`.</action>
  <verify>
    <automated>backend/.venv/bin/python -m pytest backend/tests/classification/test_matcher_norm.py backend/tests/classification/test_matcher.py backend/tests/classification/test_matcher_groups.py -x -q</automated>
  </verify>
  <done>Símbolos curtos ("R$"/"Nº") deixam de over-matchar por letra solta mas casam quando o raw está presente; needles normais e a tolerância de acento/caixa/quebra continuam verdes; value vazio não casa; testes do matcher (norm/base/groups) todos passam.</done>
</task>

</tasks>

<verification>
Suite de classificação completa verde após os dois fixes:

```bash
backend/.venv/bin/python -m pytest backend/tests/classification -q
```

Nenhuma regressão nos testes de stage/matcher/filler/forced existentes.
</verification>

<success_criteria>
- WR-01: doc ambíguo com IA recusando + fallback ON → `disambiguate` chamado exatamente 1x (não 2x); quarentena.
- WR-03: zero templates + fallback ON → `disambiguate` chamado 0x; quarentena; sem Usage.
- WR-04: needle simbólico curto não over-matcha por letra solta; casa quando o raw está presente; needle normal preserva tolerância; value vazio fail-closed.
- Todos os testes de backend/tests/classification verdes (venv).
- 2 commits atômicos (Task 1 = stage.py; Task 2 = matcher.py).
</success_criteria>

<output>
Create `.planning/quick/260626-g4g-fix-classificacao-fase-10-custo-ia-e-nor/260626-g4g-SUMMARY.md` when done
</output>
