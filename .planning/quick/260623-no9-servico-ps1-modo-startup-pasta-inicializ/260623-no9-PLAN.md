---
phase: quick-260623-no9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/iniciar-servidor.py
  - servico.ps1
  - INSTALL-WINDOWS.md
autonomous: true
requirements: [QUICK-260623-no9]
must_haves:
  truths:
    - "O launcher nao sobe um segundo uvicorn se a porta 8000 ja estiver escutando (sai com codigo 0 e loga)"
    - "servico.ps1 instalar (sem -Modo) instala o modo startup: escreve ProcessadorDocumentos.vbs na pasta Inicializar do usuario e sobe o servidor JA, sem janela/terminal"
    - "Os subcomandos iniciar/parar/reiniciar/status/remover/logs funcionam no modo startup (parar encerra o pythonw que serve a 8000)"
    - "Resolve-ModoInstalado detecta o .vbs como modo startup e startup e o padrao quando nada esta instalado"
    - "O guia INSTALL-WINDOWS.md descreve startup (invisivel, sem admin, sem janela) como o padrao e mantem NSSM como avancado 24/7"
  artifacts:
    - path: "tools/iniciar-servidor.py"
      provides: "Guarda de instancia unica antes de subir o uvicorn"
      contains: "create_connection"
    - path: "servico.ps1"
      provides: "Modo startup (novo padrao) + subcomandos de controle + deteccao"
      contains: "Startup"
    - path: "INSTALL-WINDOWS.md"
      provides: "Secao de background com startup como padrao"
      contains: "Inicializar"
  key_links:
    - from: "servico.ps1 (Invoke-InstalarStartup)"
      to: "%APPDATA%\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\ProcessadorDocumentos.vbs"
      via: "escrita do .vbs com caminhos resolvidos de $PSScriptRoot + wscript.exe"
      pattern: "Startup"
    - from: "ProcessadorDocumentos.vbs"
      to: "tools/iniciar-servidor.py"
      via: "pythonw.exe (sem console)"
      pattern: "pythonw"
---

<objective>
Tornar o modo **startup** (`.vbs` na pasta Inicializar do Windows que sobe o servidor com `pythonw.exe` + `tools\iniciar-servidor.py`, completamente invisivel) o **PADRAO** do `servico.ps1`, validado ao vivo na maquina piloto. A Tarefa Agendada se mostrou fragil para o usuario (exige sessao interativa; clicar no script nao registra); o `.vbs` na Startup roda no logon, sem admin, sem a API de tarefas, e foi a solucao que FUNCIONOU.

Purpose: dar ao usuario o comportamento que ele pediu — "rodar sem o usuario saber; ele acessa o localhost quando quiser" — de forma confiavel e sem terminal aberto.

Output: launcher com guarda de instancia unica; `servico.ps1` com modo `startup` como padrao (instalar/iniciar/parar/reiniciar/status/remover/logs/diagnostico) + deteccao do modo; guia reescrito. NSSM permanece como avancado 24/7; o modo `tarefa` permanece selecionavel mas deixa de ser o padrao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@tools/iniciar-servidor.py
@servico.ps1
@empacotar.ps1
@INSTALL-WINDOWS.md

<notas_criticas>
- **Host de execucao NAO tem Windows nem pwsh.** Toda verificacao e ESTATICA (grep dos cmdlets, presenca do novo modo no ValidateSet/switch, parse AST do .py para a guarda) + roteiro de teste manual no SUMMARY. O orquestrador tem acesso a maquina piloto via WSL e valida ao vivo depois.
- **ENCODING: os .ps1 DEVEM permanecer em UTF-8 COM BOM.** O PS 5.1 quebra o parse sem BOM (bug ja corrigido, v0.1.5). NAO introduzir regressao: preservar o BOM ao editar `servico.ps1`. Verificar com `head -c 3 servico.ps1 | xxd` esperando `efbbbf`.
- **SEGREDO: a chave da IA NUNCA em log / .vbs / diagnostico.** O `.vbs` so contem caminhos (CurrentDirectory de backend\, pythonw.exe, launcher). A chave continua lida pelo app de `backend\.env` via CWD.
- **NAO commitar** o `.vbs` (gerado no cliente) nem `nssm.exe`/zip. O `.vbs` NAO vai no pacote — e escrito pelo `servico.ps1` na maquina do cliente.
- **Bump de versao (0.1.5 -> 0.1.6) e do orquestrador no empacotamento** — fora deste plano.
- O `.vbs` valido (caminhos resolvidos de `$PSScriptRoot`, NUNCA hardcoded):
  ```
  Set sh = CreateObject("WScript.Shell")
  sh.CurrentDirectory = "<RepoRoot>\backend"
  sh.Run """<RepoRoot>\backend\.venv\Scripts\pythonw.exe"" ""<RepoRoot>\tools\iniciar-servidor.py""", 0, False
  ```
- AVISAR (no script e no guia) para NAO combinar modos (startup + tarefa + servico = multiplas instancias na 8000).
</notas_criticas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guarda de instancia unica no launcher</name>
  <files>tools/iniciar-servidor.py</files>
  <action>
  Em `tools/iniciar-servidor.py`, ANTES de chamar `uvicorn.run(...)` (e idealmente apos abrir o log, para registrar a saida), adicionar uma checagem de porta: tentar abrir um socket TCP para `127.0.0.1:8000` com timeout curto (~0.5s) usando `socket.create_connection(("127.0.0.1", 8000), timeout=0.5)`. Se a conexao SUCEDER, ja ha um servidor escutando — gravar no log a linha `ja ha um servidor escutando em 127.0.0.1:8000 — saindo` (com timestamp ISO, mesmo formato do cabecalho existente), dar `log.flush()`, fechar o log e SAIR com `sys.exit(0)` (codigo 0 — nao e erro; e o comportamento esperado quando o .vbs roda no logon e algo ja estava de pe). Se a conexao FALHAR (`OSError`/`ConnectionRefusedError`/timeout), nada esta escutando: fechar o socket de teste se aberto e seguir o fluxo normal (subir o uvicorn). Encapsular a checagem numa funcao `_porta_em_uso(host: str, porta: int) -> bool` que retorna True se conseguiu conectar. Importar `socket` no topo. Motivo: evita 2 instancias / conflito de porta + escrita concorrente no SQLite single-writer quando o .vbs roda no logon e ja havia um servidor (ex.: instalar.ps1 em 1o plano, ou o proprio instalar do modo startup que ja subiu).
  NAO logar a chave da IA. NAO mudar o comportamento de CWD/sys.path/log existente.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; t=ast.parse(open('tools/iniciar-servidor.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; assert '_porta_em_uso' in names, 'falta _porta_em_uso'; src=open('tools/iniciar-servidor.py').read(); assert 'create_connection' in src, 'falta create_connection'; assert 'import socket' in src, 'falta import socket'; assert src.index('_porta_em_uso(') < src.rindex('uvicorn.run'), 'guarda deve vir ANTES do uvicorn.run'; assert 'sys.exit(0)' in src, 'falta saida limpa codigo 0'; print('OK guarda de instancia')"</automated>
  </verify>
  <done>O launcher define `_porta_em_uso`, checa 127.0.0.1:8000 antes do `uvicorn.run`, e quando ja ha servidor loga e sai com codigo 0; quando nao ha, sobe normalmente. `python3 -m py_compile tools/iniciar-servidor.py` passa.</done>
</task>

<task type="auto">
  <name>Task 2: Modo startup como novo padrao no servico.ps1</name>
  <files>servico.ps1</files>
  <action>
  Editar `servico.ps1` PRESERVANDO o BOM UTF-8 (NAO reescrever o arquivo de forma que remova os 3 bytes `EF BB BF` iniciais; usar Edit cirurgico). Mudancas:

  1. **Param**: trocar `[ValidateSet('tarefa','servico')][string]$Modo = 'tarefa'` por `[ValidateSet('startup','tarefa','servico')][string]$Modo = 'startup'`.

  2. **Constantes do modo startup** (junto das demais, derivadas de `$PSScriptRoot`): `$StartupDir = Join-Path $env:APPDATA 'Microsoft\Windows\Start Menu\Programs\Startup'`; `$VbsFile = Join-Path $StartupDir 'ProcessadorDocumentos.vbs'`. Reusar `$VenvPythonw`, `$Launcher`, `$BackendDir`, `$TaskLog`/`$TaskLogsDir` ja existentes (o launcher loga em %LOCALAPPDATA%\ProcessadorDocumentos\logs\servidor.log — mesmo do modo tarefa).

  3. **`Test-StartupExists`**: retorna `$true` se `Test-Path $VbsFile`.

  4. **`Resolve-ModoInstalado`**: estender a prioridade — se `$script:ModoExplicito` respeita `$Modo`; senao: `Test-StartupExists` -> 'startup'; senao `Test-TaskExists` -> 'tarefa'; senao `Test-ServiceExists` -> 'servico'; senao padrao 'startup'. (Manter o fix `$script:ModoExplicito` ja existente — NAO usar `$PSBoundParameters` dentro da funcao.)

  5. **`Invoke-InstalarStartup`** (SEM admin):
     - `Ensure-Venv` (reuso).
     - Validar `Test-Path $VenvPythonw` e `Test-Path $Launcher` (mensagens falha-fechada como no modo tarefa).
     - ALEMBIC FALHA-FECHADA de dentro de `$BackendDir` (Push-Location + `uv run alembic upgrade head`, abortar se `$LASTEXITCODE -ne 0`) — mesmo padrao das outras instalacoes.
     - Garantir `New-Item -ItemType Directory -Force -Path $TaskLogsDir` (logs) e `-Path $StartupDir`.
     - ESCREVER o `.vbs` com caminhos resolvidos: montar o conteudo de 3 linhas (CreateObject WScript.Shell; `sh.CurrentDirectory = "<$BackendDir>"`; `sh.Run """<$VenvPythonw>"" ""<$Launcher>""", 0, False`) e gravar com `Set-Content -Path $VbsFile -Encoding ASCII` (wscript le ASCII/ANSI bem; NAO precisa de BOM — e o .vbs, nao um .ps1). Usar aspas duplas escapadas corretamente no PowerShell para produzir o `sh.Run "..." "..."` literal.
     - INICIAR agora: `Start-Process -FilePath 'wscript.exe' -ArgumentList ('"' + $VbsFile + '"')` (nao espera o logon). O .vbs sobe pythonw invisivel; a guarda da Task 1 evita 2a instancia.
     - HEALTH-CHECK falha-fechada via `Test-Health` (reuso, ~30s); em falha, mostrar `$TaskLog` e `Get-Content -Tail 30` e `throw`.
     - Mensagem final deixando claro: NAO abre janela/terminal nenhum; o servidor sobe sozinho a cada logon; acesse http://localhost:8000; controle por `.\servico.ps1 status | parar | iniciar | reiniciar | logs | remover`.

  6. **Subcomandos do modo startup** (funcoes dedicadas, espelhando as `*Tarefa`):
     - `Invoke-IniciarStartup`: se nada escutando na 8000 (`Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue`), roda `wscript.exe "<$VbsFile>"`; senao avisa que ja esta no ar.
     - `Get-ServidorPid` (helper): retorna o PID que serve a 8000 — primeiro via `Get-NetTCPConnection -LocalPort 8000 -State Listen` -> `OwningProcess`; fallback: procurar processo `pythonw` cujo `CommandLine`/`Path` aponte para o launcher (via `Get-CimInstance Win32_Process -Filter "Name='pythonw.exe'"` filtrando `CommandLine -like '*iniciar-servidor.py*'`). Robusto a ausencia.
     - `Invoke-PararStartup`: obtem o PID via `Get-ServidorPid`; se achou, `Stop-Process -Id $pid -Force`; senao avisa que nao havia servidor na 8000.
     - `Invoke-ReiniciarStartup`: parar + iniciar.
     - `Invoke-StatusStartup`: mostra existencia do `.vbs` (`Test-StartupExists`), porta 8000 escutando (Get-NetTCPConnection), e `/health` (Invoke-WebRequest 200) de forma clara.
     - `Invoke-RemoverStartup`: `Invoke-PararStartup` e depois `Remove-Item $VbsFile -Force -ErrorAction SilentlyContinue`; confirmar remocao.
     - `Invoke-LogsStartup`: tail de `$TaskLog` (mesmo log do launcher); NUNCA expor a chave.

  7. **Roteador de subcomandos**: no bloco `instalar`, rotear `startup -> Invoke-InstalarStartup`, `servico -> Invoke-Instalar`, `tarefa -> Invoke-InstalarTarefa`. No `switch` de controle (iniciar/parar/reiniciar/status/remover/logs), adicionar o ramo `'startup'` chamando as funcoes `*Startup` (manter os ramos `servico` e `tarefa` intactos).

  8. **`Invoke-Diagnostico`**: na secao 'Persistencia', adicionar o estado do modo startup — existencia de `$VbsFile` e PID na 8000 (reusar `Get-NetTCPConnection`/`Get-ServidorPid`). NUNCA incluir a chave.

  9. **Bloco de ajuda (default)** e o cabecalho de comentario do arquivo: atualizar para refletir que **startup e o PADRAO** (invisivel, sem admin, sem janela), tarefa e opcional (auto-restart, mas exige sessao interativa para registrar) e servico e o avancado 24/7. Incluir o AVISO de nao combinar modos.

  NAO tocar nas funcoes/fluxos do NSSM (`Invoke-Instalar`, ramos `servico`) nem do modo tarefa alem do roteamento. Preservar `Assert-Admin` so para servico.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=servico.ps1; test "$(head -c3 "$f" | od -An -tx1 | tr -d " ")" = "efbbbf" || { echo "FALTA BOM UTF-8"; exit 1; }; b="grep -v ^# $f"; grep -q "ValidateSet(.startup.,.tarefa.,.servico.)" "$f" || { echo "ValidateSet sem startup"; exit 1; }; grep -q "\\$Modo = .startup." "$f" || { echo "default nao e startup"; exit 1; }; for fn in Invoke-InstalarStartup Invoke-IniciarStartup Invoke-PararStartup Invoke-ReiniciarStartup Invoke-StatusStartup Invoke-RemoverStartup Invoke-LogsStartup Test-StartupExists; do grep -q "function $fn" "$f" || { echo "falta funcao $fn"; exit 1; }; done; grep -q "Start Menu.Programs.Startup" "$f" || { echo "falta caminho Startup"; exit 1; }; grep -q "ProcessadorDocumentos.vbs" "$f" || { echo "falta nome do vbs"; exit 1; }; grep -q "wscript" "$f" || { echo "falta wscript"; exit 1; }; grep -q "Get-NetTCPConnection" "$f" || { echo "falta Get-NetTCPConnection"; exit 1; }; grep -q "Stop-Process" "$f" || { echo "falta Stop-Process"; exit 1; }; grep -q "Invoke-InstalarStartup" "$f" && echo "OK servico.ps1 startup"'</automated>
  </verify>
  <done>servico.ps1 tem startup como default no ValidateSet/param; todas as funcoes `*Startup` + `Test-StartupExists` existem; escreve o `.vbs` na pasta Inicializar e inicia via wscript; parar/status usam Get-NetTCPConnection/Stop-Process; Resolve-ModoInstalado detecta startup com prioridade e como padrao; diagnostico inclui o estado startup; BOM UTF-8 preservado; sem regressao no NSSM/tarefa.</done>
</task>

<task type="auto">
  <name>Task 3: Guia (INSTALL-WINDOWS.md) e checagem do empacotamento</name>
  <files>INSTALL-WINDOWS.md</files>
  <action>
  Reescrever a secao de background (item 6 "Rodar sempre em background") do `INSTALL-WINDOWS.md` para refletir os TRES modos com **startup como PADRAO**:
  - **Modo padrao — startup (pasta Inicializar, invisivel, sem admin):** `.\servico.ps1 instalar` (sem `-Modo`). Deixar EXPLICITO que NAO fica nenhuma janela/terminal aberto, que o servidor sobe sozinho a cada logon do usuario, e que o usuario acessa http://localhost:8000 quando quiser. Mencionar que e o recomendado para a maquina de uma pessoa que faz login e usa o sistema. Logs em `%LOCALAPPDATA%\ProcessadorDocumentos\logs\servidor.log`. Limitacao honesta: roda enquanto o usuario esta logado (nao antes do login / nao em headless sem sessao).
  - **Modo tarefa (opcional, auto-restart):** manter como alternativa via `-Modo tarefa`, citando que da reinicio automatico pela Tarefa Agendada, mas EXIGE registrar numa sessao PowerShell interativa (nao funciona por duplo-clique) — por isso deixou de ser o padrao.
  - **Modo servidor 24/7 — Servico Windows (NSSM, avancado):** manter via `-Modo servico` (boot, headless, exige admin + Python all-users / Pegadinha 1).
  - Atualizar a tabela de **comandos de controle** (status/parar/iniciar/reiniciar/logs/remover) deixando claro que detectam automaticamente o modo instalado (agora incluindo startup) e atualizar os caminhos de log por modo na secao de troubleshooting "O servidor nao inicia em background".
  - Manter/atualizar o AVISO de NAO combinar modos (startup + tarefa + servico = 2a instancia na 8000).
  - Em "Onde ficam os dados e os logs" / Troubleshooting, refletir que o modo padrao (startup) usa `servidor.log` em %LOCALAPPDATA% (igual ao tarefa).

  Sobre o EMPACOTAMENTO: o `.vbs` e GERADO no cliente pelo `servico.ps1` — NAO vai no pacote. O `empacotar.ps1` ja inclui `servico.ps1`, `INSTALL-WINDOWS.md` e `tools\iniciar-servidor.py` (verificado nas linhas de staging). Portanto `empacotar.ps1` NAO precisa de alteracao de codigo para esta mudanca. (Se durante a execucao constatar que o launcher ou o servico.ps1 deixaram de ser incluidos, corrija; caso contrario, nao edite o empacotar.ps1.)
  </action>
  <verify>
    <automated>bash -c 'set -e; f=INSTALL-WINDOWS.md; grep -qi "Inicializar" "$f" || { echo "guia nao cita pasta Inicializar"; exit 1; }; grep -qi "startup" "$f" || { echo "guia nao cita startup"; exit 1; }; grep -qi "sem.*janela\|nenhuma janela\|invisivel\|invisível" "$f" || { echo "guia nao diz que e invisivel/sem janela"; exit 1; }; grep -q "servico.ps1 instalar" "$f" || { echo "falta comando instalar"; exit 1; }; grep -qi "NSSM\|24/7" "$f" || { echo "guia perdeu o NSSM avancado"; exit 1; }; e=empacotar.ps1; grep -q "iniciar-servidor.py" "$e" || { echo "empacotar parou de incluir o launcher"; exit 1; }; grep -q "servico.ps1" "$e" || { echo "empacotar parou de incluir servico.ps1"; exit 1; }; echo "OK guia + empacotamento"'</automated>
  </verify>
  <done>INSTALL-WINDOWS.md descreve startup (invisivel, sem admin, sem janela) como o padrao com `.\servico.ps1 instalar`, mantem tarefa como opcional e NSSM como 24/7 avancado, atualiza controle/logs/troubleshooting e o aviso de nao combinar modos; empacotar.ps1 confirmadamente segue incluindo `servico.ps1` e `tools\iniciar-servidor.py` (sem o `.vbs`).</done>
</task>

</tasks>

<verification>
- `python3 -m py_compile tools/iniciar-servidor.py` passa; AST confirma `_porta_em_uso` antes de `uvicorn.run` + `sys.exit(0)`.
- `servico.ps1` mantem BOM UTF-8 (`efbbbf`); ValidateSet/param com `startup` default; todas as funcoes `*Startup` presentes; usa pasta Inicializar, wscript, Get-NetTCPConnection, Stop-Process.
- INSTALL-WINDOWS.md cita Inicializar/startup/invisivel e mantem NSSM; empacotar.ps1 ainda inclui launcher + servico.ps1.
- **Verificacao estatica apenas** (host sem Windows/pwsh). O SUMMARY DEVE conter um roteiro de teste manual para a maquina piloto (WSL): (1) `.\servico.ps1 instalar` sem `-Modo` -> nenhuma janela aparece, /health 200, `.vbs` existe na Startup; (2) fazer logoff/logon -> servidor sobe sozinho invisivel; (3) `.\servico.ps1 status` mostra .vbs+porta+health; (4) `.\servico.ps1 parar` derruba a 8000; (5) rodar o instalar com servidor ja de pe -> a guarda do launcher impede 2a instancia (log "ja ha um servidor escutando"); (6) `.\servico.ps1 remover` apaga o .vbs e encerra o pythonw; (7) confirmar que a chave da IA nao aparece no .vbs nem no diagnostico.
</verification>

<success_criteria>
- O modo startup e o PADRAO do `servico.ps1`: `instalar` sem `-Modo` escreve o `.vbs` na pasta Inicializar e sobe o servidor invisivel (sem janela), com health-check falha-fechada.
- Subcomandos de controle (iniciar/parar/reiniciar/status/remover/logs) operam no modo startup; `parar` encerra o pythonw da 8000.
- `Resolve-ModoInstalado` detecta startup pelo `.vbs` e usa startup como padrao quando nada esta instalado; NSSM e tarefa preservados.
- O launcher tem guarda de instancia unica (porta 8000) que evita 2 servidores.
- Guia atualizado; `.vbs` gerado no cliente (nunca commitado nem no pacote); BOM UTF-8 preservado nos .ps1; chave da IA nunca em log/.vbs/diagnostico.
</success_criteria>

<output>
Create `.planning/quick/260623-no9-servico-ps1-modo-startup-pasta-inicializ/260623-no9-SUMMARY.md` when done, incluindo o roteiro de teste manual para a maquina piloto.
</output>
